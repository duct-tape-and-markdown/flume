{
  "tests/Agent.test.ts::renders the leaf dir, not the full path, for a drive-lettered backslash cwd": "path.basename treats a backslash as a separator only on win32, so the drive-lettered regression is observable only there; the POSIX-style cwd case beside it runs on both lanes.",
  "tests/Baton.test.ts::Baton — an unstattable awake flag is loud": "The fixture is a self-referential symlink and every assertion here is POSIX's ELOOP reading of it; win32 creates it as a file-typed link and answers the directory lookup through it as not found (.claude/rules/platform-facts.md, *win32 reports a path through a non-directory as not found*). The absent arm this block also carries is exercised on every host by \"Baton — wake/sleep/awake roundtrip\".",
  "tests/Dispatcher.test.ts::Dispatcher fanout — createWorktree pins core.longpaths": "core.longpaths is a win32-only git setting; there is no pin to make on a POSIX host.",
  "tests/Dispatcher.test.ts::Dispatcher — loadChainModule/pendingPath win32 total-path limit (DISPATCHER-NAMESPACEDJOIN-WIN32-PATH-TOTAL-LIMIT)": "The ~260-character total-path limit is win32's, and toNamespacedPath is a no-op elsewhere, so the fixture proves nothing on a POSIX host.",
  "tests/Gate.test.ts::shellGate — win32 .cmd shim fallback": ".cmd is an executable form only on win32, so the direct-spawn ENOENT the shared shim retry answers is only reachable there. The retry decision itself is judged on every host in tests/spawnShim.test.ts; this is the real-binary proof.",
  "tests/bin.test.ts::bin/flume symlink walk": "bin/flume is the POSIX shell script kept for direct callers; the published win32 entry is bin/flume.js, and no win32 interpreter execs a shebang script, so the walk's subject does not exist on that host. The bin/flume.js describe below carries the published entry on both lanes.",
  "tests/bin.test.ts::bin/flume.js re-raises the child's terminating signal": "win32 has no signal delivery: a child that self-kills with SIGTERM yields a status there, so there is no signal for the shim to re-raise and the property is POSIX's alone.",
  "tests/builtinGates.test.ts::tscGate — pnpm shim proves the default cmd (win32)": ".cmd is an executable form only on win32, so the direct-spawn ENOENT the shared shim retry answers is only reachable there. The fixture proves the omitted cmd resolves to pnpm by shimming a fake pnpm.cmd ahead on PATH, which has no POSIX spelling.",
  "tests/cli.test.ts::a bare tick whose agent ignores SIGTERM kills it after the declared grace rather than exiting over a live writer": "win32 maps SIGTERM to TerminateProcess, which runs no handler at all, so release-on-signal is a POSIX guarantee; the cross-platform property is stale-reclaim, which spec/loop.md carries and other cases cover.",
  "tests/cli.test.ts::a chain-declared kill grace, not the engine default, bounds a signalled loop's wait on its tick tree": "win32 maps SIGTERM to TerminateProcess, which runs no handler at all, so release-on-signal is a POSIX guarantee; the cross-platform property is stale-reclaim, which spec/loop.md carries and other cases cover.",
  "tests/cli.test.ts::a signalled `flume loop` leaves no tick child alive against the state root whose claim it released": "win32 maps SIGTERM to TerminateProcess, which runs no handler at all, so release-on-signal is a POSIX guarantee; the cross-platform property is stale-reclaim, which spec/loop.md carries and other cases cover.",
  "tests/cli.test.ts::a signalled `flume loop` leaves nothing the tick child spawned alive against the state root it released": "win32 maps SIGTERM to TerminateProcess, which runs no handler at all, so release-on-signal is a POSIX guarantee; the cross-platform property is stale-reclaim, which spec/loop.md carries and other cases cover.",
  "tests/cli.test.ts::a signalled bare `flume tick` exits 143": "win32 maps SIGTERM to TerminateProcess, which runs no handler at all, so release-on-signal is a POSIX guarantee; the cross-platform property is stale-reclaim, which spec/loop.md carries and other cases cover.",
  "tests/cli.test.ts::a signalled bare `flume tick` leaves nothing its agent spawned alive when the tip claim drops": "win32 maps SIGTERM to TerminateProcess, which runs no handler at all, so release-on-signal is a POSIX guarantee; the cross-platform property is stale-reclaim, which spec/loop.md carries and other cases cover.",
  "tests/cli.test.ts::a tick tree that ignores SIGTERM is killed after the declared grace rather than waited on indefinitely": "win32 maps SIGTERM to TerminateProcess, which runs no handler at all, so release-on-signal is a POSIX guarantee; the cross-platform property is stale-reclaim, which spec/loop.md carries and other cases cover.",
  "tests/cli.test.ts::flume friction refuses with EX_IOERR when a listed note cannot be stat'd": "The one friction arm with no structural substitute: the readdir must succeed and the stat of the note it listed must fail, which only a read-without-traverse mode produces — a symlink, a directory or a device in the note's place is filtered out by the dirent's own isFile() before the stat runs. A mode denies nothing on win32 (.claude/rules/platform-facts.md, *chmod denies nothing on win32*), so the case would resolve every call it exists to refuse.",
  "tests/cliHelp.test.ts::flume friction --help names exit 74 for an I/O failure in the channel dir": "The seam's producing half is that same stat refusal, so the agreement gate can only be driven on the host that can produce it; a mode denies nothing on win32 (.claude/rules/platform-facts.md, *chmod denies nothing on win32*) and the help text would be compared against an exit code no refusal produced.",
  "tests/fsProbe.test.ts::existsLoud refuses a path whose ancestor is present and not a directory, rather than reading it as absent": "POSIX answers a lookup that passes through a plain file ENOTDIR; win32 answers it ENOENT, so the refusal asserted here is one that host cannot make (.claude/rules/platform-facts.md, *win32 reports a path through a non-directory as not found*).",
  "tests/fsProbe.test.ts::statLoud refuses that path too, so the probe's Stats face and its boolean face split alike": "POSIX answers a lookup that passes through a plain file ENOTDIR; win32 answers it ENOENT, so the refusal asserted here is one that host cannot make (.claude/rules/platform-facts.md, *win32 reports a path through a non-directory as not found*).",
  "tests/git.test.ts::acquireTipClaim / liveTipClaimPid / release — win32 total-path limit (GITTIPCLAIM-WIN32-PATH-TOTAL-LIMIT)": "The ~260-character total-path limit is win32's, and toNamespacedPath is a no-op elsewhere, so the fixture proves nothing on a POSIX host.",
  "tests/git.test.ts::commitPaths stages only its given path when a sibling name glob-matches it": "A filename may not contain * on win32, so this fixture cannot exist there.",
  "tests/git.test.ts::pinLongPaths": "core.longpaths is a win32-only git setting; there is no pin to make on a POSIX host. The unwritable-config arm denies with a permission bit on purpose: the read-only attribute this sets does refuse git's write on the one host the block runs on.",
  "tests/git.test.ts::readFileAtRef reads a committed path whose name begins with a colon": "A filename may not contain : on win32 — the Win32 path layer reads it as a stream separator — so this fixture cannot exist there.",
  "tests/git.test.ts::removeWorktree — win32 total-path limit (REMOVEWORKTREE-WIN32-PATH-TOTAL-LIMIT)": "The ~260-character total-path limit is win32's, and toNamespacedPath is a no-op elsewhere, so the fixture proves nothing on a POSIX host.",
  "tests/harnessCi.test.ts::the lane block names the tip's branch when the forge filter took git's own directory": "The fixture stages git onto a single-directory PATH through a symlink, and a win32 git reached that way resolves its install layout from the link's directory rather than its own; the fixture refuses out loud on that host.",
  "tests/harnessCi.test.ts::the lane block renders a lane as unread when the forge CLI is absent": "The fixture stages git onto a single-directory PATH through a symlink, and a win32 git reached that way resolves its install layout from the link's directory rather than its own; the fixture refuses out loud on that host.",
  "tests/harnessRunner.test.ts::the vitest runner provisions a base checkout through the declared setup": "The consumer-declared setup command this case drives is an ln -s, which no win32 shell provides; the engine-installer case beside it carries the same runAtBase leg on both lanes.",
  "tests/harnessWindows.test.ts::a window diff narrows by a path whose name carries a glob metacharacter": "A filename may not contain * on win32, so this fixture cannot exist there.",
  "tests/harnessWindows.test.ts::the derive window is live when a commit touches a spec path whose name carries a control character": "A control character is illegal in a path on win32, so this fixture cannot exist there.",
  "tests/job.test.ts::ensureRuntimeIgnores rethrows a non-ENOENT read failure instead of rewriting the .gitignore it could not read": "The denial is a permission bit on the .gitignore the gate reads, and no structural substitute reaches it: a directory in the file's place destroys the template line the case asserts survived. On win32 the bit toggles the read-only attribute, so the read resolves and the rewrite is what fails — the case would go green over a refusal it never made (.claude/rules/platform-facts.md, *chmod denies nothing on win32*).",
  "tests/job.test.ts::job new on a glob-metacharacter job name leaves a dirty sibling job unstaged": "A filename may not contain * on win32, so this fixture cannot exist there.",
  "tests/job.test.ts::job rm on a glob-metacharacter job name leaves a sibling job's files tracked": "A filename may not contain * on win32, so this fixture cannot exist there.",
  "tests/job.test.ts::job.ts existsSync-gated checks — win32 total-path limit (JOB-EXISTSSYNC-WIN32-PATH-TOTAL-LIMIT)": "The ~260-character total-path limit is win32's, and toNamespacedPath is a no-op elsewhere, so the fixture proves nothing on a POSIX host.",
  "tests/job.test.ts::jobStatus frictionCount — win32 total-path limit (FRICTIONCOUNT-WIN32-PATH-TOTAL-LIMIT)": "The ~260-character total-path limit is win32's, and toNamespacedPath is a no-op elsewhere, so the fixture proves nothing on a POSIX host.",
  "tests/job.test.ts::jobStatus reads a job whose own dir is obstructed as an unreadable baton, not as hibernating": "The obstruction is a symlink into a non-directory, the one shape that keeps the job dir listable while its awake dir is not. POSIX answers a lookup that passes through a plain file ENOTDIR; win32 answers it ENOENT, so the refusal asserted here is one that host cannot make (.claude/rules/platform-facts.md, *win32 reports a path through a non-directory as not found*).",
  "tests/job.test.ts::win32 lane — core.longpaths": "core.longpaths is a win32-only git setting; there is no pin to make on a POSIX host.",
  "tests/priorAttempts.test.ts::snapshotReverted preserves a path's trailing space instead of trimming it": "A filename may not end in a space on win32 — the Win32 path layer strips trailing spaces before the create reaches the filesystem — so neither this fixture nor a git checkout of it can exist there.",
  "tests/priorAttempts.test.ts::snapshotReverted writes a colon-leading path's content into the revert snapshot": "A filename may not contain : on win32, so this fixture cannot exist there.",
  "tests/processTree.test.ts::a child that exits on SIGTERM never pays the engine's default grace": "win32 has neither process groups nor signal delivery, so the tree walk and the grace this asserts are POSIX's alone.",
  "tests/processTree.test.ts::a child that ignores SIGTERM is killed once the grace runs out": "win32 has neither process groups nor signal delivery, so the tree walk and the grace this asserts are POSIX's alone.",
  "tests/processTree.test.ts::a grandchild the terminated child spawned is signalled with it, not reparented and left running": "win32 has neither process groups nor signal delivery, so the tree walk and the grace this asserts are POSIX's alone.",
  "tests/processTree.test.ts::terminating a child that has already exited signals nothing": "win32 has neither process groups nor signal delivery, so the tree walk and the grace this asserts are POSIX's alone.",
  "tests/worktrees.test.ts::the worktree registry carries a newline-bearing path as the one path git named": "A control character is illegal in a path on win32, so the registry can never hold one there.",
  "tests/worktrees.test.ts::the worktree registry keeps a worktree path's trailing whitespace": "win32 path normalization strips trailing whitespace before any file is created, so the registry can never hold such a path there."
}
