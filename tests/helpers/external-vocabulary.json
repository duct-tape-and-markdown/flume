{
  ".flume/loop.pid": "a live loop's pidfile, written by a run and not by us",
  ".flume/prompts/build.md": "the prompt path `examples/cascade-chain.ts` tells a copying consumer to point at, in that consumer's own repo",
  ".flume/prompts/notes.md": "`examples/minimal-chain.ts`'s prompt as a copying consumer spells it, under that consumer's `.flume/`",
  "Box.lid": "a member of a fixture this suite authors as source text",
  "Chain.seedDir": "a member `Chain` once declared and this repo retired — cited by the suite that pins its absence, so no declaration answering it is the point",
  "ChildProcess": "`node:child_process`'s process type, imported nowhere here",
  "Container.boxed": "a member of a fixture this suite authors as source text",
  "Guarded.hush": "a member of a fixture this suite authors as source text",
  "SHIPPED.md": "the ledger `examples/backlog-groomer-chain.ts` declares — a page written only in a consumer's repo, never in this one",
  "TerminateProcess": "the Win32 call win32 maps `SIGTERM` to, named in no surface of ours",
  "cmd.exe": "the Windows command interpreter, named as a spawn target",
  "devDependencies": "a field of the consumer's `package.json`, not of ours",
  "dist/harness/cli.js": "the published build's layout, not a checkout's",
  "dist/harness/init.js": "the published build's layout, not a checkout's",
  "dist/harness/prompts.js": "the published build's layout, not a checkout's",
  "dist/src/cli.js": "the published build's layout, not a checkout's",
  "enableGlobalVirtualStore": "a pnpm workspace setting, named in pnpm's own namespace",
  "exactOptionalPropertyTypes": "a tsconfig compiler option",
  "fs.rm": "`node:fs/promises`, under a namespace this tree never imports",
  "gc.autoDetach": "a git config key, spelled in git's own namespace",
  "hookTimeout": "vitest's own option, named in no module of these trees",
  "judgedByEntryTests": "a declaration of `examples/cascade-chain.ts`",
  "merging/throw-m.json": "the state root's layout — `<flumeDir>`-relative",
  "nextPhase": "a declaration of `examples/cascade-chain.ts`",
  "notes/journal.md": "the journal `examples/minimal-chain.ts` appends to — written in a consumer's repo, never in this one",
  "plan-sweep.json": "a record filename a run writes under the state root",
  "prompts/backlog-groomer.md": "a `Phase.promptPath`, resolved against a chain's config dir",
  "prompts/notes.md": "the prompt `minimal-chain.ts` tells a copying consumer to author",
  "prompts/plan.md": "a `Phase.promptPath`, resolved against a chain's config dir",
  "scripts.lint": "a field of the consumer's `package.json`, not of ours",
  "slicePhase": "a declaration of `examples/cascade-chain.ts`",
  "state.md": "the plan artifact `examples/cascade-chain.ts` declares under a consumer's state root",
  "sysexits.h": "the BSD header flume's exit codes are taken from",
  "tsImport": "`tsx/esm/api`'s loader entry — the dependency the chain loads through, not a name the package hands out",
  "util.promisify.custom": "`node:util`, under a namespace this tree never imports",
  "vitestOnTrunk": "a declaration of `examples/cascade-chain.ts`"
}
