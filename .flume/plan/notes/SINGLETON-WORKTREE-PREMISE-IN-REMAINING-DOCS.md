# Two stale claims left beside the four, both needing their own entry

Shipped the four named sites plus three same-premise siblings the sweep found:
the renderer `tag` default (CHAIN-AUTHORING §7), `worktreesBase`'s doc comment
(src/paths.ts), and README's "Singleton ticks commit to HEAD; fanout waves
cherry-pick back onto HEAD" (Trunk contract).

Left for plan — each a rewrite, not a rescope:

1. **README, Trunk contract, the paragraph after the line I fixed**: "Moved —
   a human committed mid-tick, a pull landed ... makes **no commit**" still
   describes the retired refusal. `spec/loop.md`, *Tip verify*: "No leg
   refuses operator activity anymore" — a foreign trunk commit absorbs at the
   merge site; only a live foreign claim refuses. Fixing it means stating the
   absorb/refuse split.

2. **CHAIN-AUTHORING §9, `abortThreshold` bullet**: scopes the backstop to
   "the same provisioning-failure signature". `spec/loop.md`, *Repeated
   identical failures*, keys it on stage-tagged signatures across provision,
   merge, **and** gate. Unrelated to the worktree premise — noted because I
   was in the paragraph above it.
