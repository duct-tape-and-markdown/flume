# The covered clause rides the put-down act, not a second line in the prompt

The entry's second `files` description asked `harness/prompts/plan-sweep.md`
to carry both halves: the budget-line bound *and* "every frontier module the
tick read is recorded covered". Only the first half landed there. The second
already ships from the put-down statement's one home — `PUT_DOWN_ACTS`
(`harness/prompts.ts`), whose `plan-sweep` act reads "commit the
neighborhoods you have swept with every frontier module you read recorded
covered, and leave the rotation open" (fb227202). I wrote the clause into the
prompt's cursor paragraph first and then removed it: the entry's own note
names a second copy spelled there as the drift *Narration is the ladder's
bottom rung* catches, and the page ties the recording to the put-down, so the
act is where it belongs. Acceptance is green through the pointer: line 38 now
makes `{{PUT_DOWN}}` the bound, and that block states the recording.

Observed beside it, filed here rather than as a finding because the sweep
slice is plan's to route: the prompt's cursor paragraph (`:44`) still
restates page mechanics the agent is told two lines earlier to read in full
as they stand — the frontier emptying closes the rotation, the stamp copies
the sha `<sweep-window>` named. Same duplication read as the clause I pulled,
one rung less obvious because the paragraph predates this entry. If it is
drift, the fix is the same shape: the paragraph keeps the plan-state *shape*
(`rotation`, `covered`, `sweptThrough` as fields) and drops the rulings.

`harness/chain.ts:91` dropped the bound from the status line too; nothing
pinned either string, which is why the entry carries no `tests[]` or
`pins[]`.
