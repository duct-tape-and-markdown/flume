# The ship-claim's only engine-side home was the one doc

Swept for siblings restating the retired claim: none. `finalMessage`'s
readers are exactly `classifyNoCommit` (src/tickAttempt.ts) and the retry
render (src/Prompt.ts); the park decision lives entirely in the harness
(`isPark`, harness/chain.ts:298 - path membership, never prose), and
spec/pending.md forbids the inference by name. Nothing needed re-homing,
so the shrink is closed.

The new pin asserts ShipContext's key set by equality, not by absence of
two names. That is what makes it decidable, and it means a legitimately
added ShipContext field reds the test - deliberately. If plan ever queues
a ShipContext widening, the entry should name this test.

Unrelated: src/Dispatcher.ts:944 carries another dated "operator ruling
2026-08-03" cite. Accurate against today's tree, but the date-stamped
form names no actor who retires it - a shape for the narration lens to
look at across the tree, not a finding here.
