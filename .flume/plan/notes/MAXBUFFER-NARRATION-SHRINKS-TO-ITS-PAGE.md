# A fifth narrator, and one cite the pin cannot see

The entry named four sites; a fifth restated the same fact.
`tests/subprocessHelper.test.ts:147` (the header over the two cap cases)
carried "the rejection it builds carries an errno-shaped `code`" as its
reason for driving a real child. Shrunk to the page cite; the
agreement-gate rationale stayed. Other sites may narrate a platform fact
as a *reason* rather than a claim — the grep for "1 MiB" caught this one,
a grep for the ruling's wording would not.

`scripts/build-changelog.mjs`'s cite is prose only. The citation pin
resolves `*.md` names in `src/`, `harness/`, `tests/` comments; `scripts/`
is in the sweep domain and in `platform-facts.md`'s frontmatter paths, but
not in the pin's tree list. A rename of the page leaves that cite standing
while the other four go red.

Unrelated: `tests/build-changelog.test.ts` and `tests/subprocessHelper.test.ts`
are prettier-unclean at HEAD (long `it` titles). Pre-existing, no gate
reads it, untouched here.
