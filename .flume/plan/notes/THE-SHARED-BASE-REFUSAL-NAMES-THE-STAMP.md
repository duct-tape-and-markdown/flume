# The 0.17 note has no section of its own for the worktree stamp

Shipped as filed: the shared-base paragraph now names the stamp check as the
arm that refuses a sibling's live tree (the registry leg passes it), and a
new paragraph names the hand-clearance that arm costs.

Two things for plan, both outside the entry:

1. I also widened the next paragraph ("a 0.16 run's worktrees are residue
   the 0.17 sweep will not reach"). It stated the sweep's rule as the
   registry alone, which 619805b4 also changed: an unstamped tree is left
   standing even at the top level of the base, so a non-namespaced 0.16 run
   leaves residue too. Without that, the page's own hand-clearance advice
   read as namespace-only.

2. The stamp (619805b4, 4ea5039d) is a 0.17-line behavior change with no
   section of its own. It surfaces only inside 9, whose "Affects" is the
   namespace removal; "Which sections apply to you" routes a residue reader
   there by one clause. A reader who never set FLUME_JOB and has pre-0.17
   worktrees on disk is affected and is not addressed by name. Whether that
   earns its own section is plan's call, not this tick's.
