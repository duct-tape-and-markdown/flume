# Three more unread cites rode in the same four comments

The four named sites are closed, and each reds when misdirected (probed by
running the repo scanner with each pair pointed at a file that does not
declare the name).

Beyond the four, the same three comments each carried `namespacedJoin
(src/paths.ts)` with *both* halves unfenced -- the same defect a step
further along: not "half unfenced, so the pin reads prose", but "wholly
unfenced, so the pin reads nothing". I closed all three here, since the
comment was already being rewritten and leaving an unread cite in a line I
just touched is the residue the section names. Not scope plan asked for; say
so if the preference is to file such neighbours rather than fold them in.

Possible sibling, unfixed, outside this entry's four:
`src/priorAttempts.ts:533` says "writeRevertNote/harvestFriction guard use"
-- two names slash-joined and unfenced, so neither resolves. No pair is
drawn there, so this entry's acceptance does not reach it, and whether a
slash-joined pair of names is a spelling the pin should read is plan's call.
