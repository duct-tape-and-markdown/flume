# The blockquote arm outruns the rule page's enumeration

Shipped: `sectionTitles` (`tests/helpers/docSections.ts`) mints a bolded lead
opening a blockquote line, the quote marker folded out of the wrap, so the
pair cite naming `docs/CLI.md`'s *Reading the exit codes* resolves.
`NAMED_EXIT_CODE` (`tests/cliHelp.test.ts`) now carries that pair instead of
a prose pointer.

Three things for the next plan tick.

**The rule page is behind the suite.** `.claude/rules/engineering.md`,
*Narration is the ladder's bottom rung* enumerates the section arm as "that
page's headings and bolded bullet leads". The suite now resolves a third
class the same phrase's own predicate admits, so the enumeration understates
what ships. Human's surface; a build tick cannot touch it. That wording is
also quoted by the commentCitations case title, which I left alone rather
than widen a claim plan did not file.

**The arm widens the citable set repo-wide**, all new titles, none retired:
every `> **Dated record.**` banner under `docs/MIGRATING-*.md`, the
`> **SN verdict: ...**` leads under `docs/surveys/consumer-chains/`, and the
`Note` / `Drift` / `Gap` leads in `spec/loop.md`, `spec/worktrees.md`,
`spec/pending.md`, `spec/prompt.md`. The spec ones mint one-word titles that
several pages now carry — resolution is per-page, so no collision, but a
cite reading *Note* says very little.

**Debt, not filed.** `FENCE` (`tests/helpers/docSections.ts`) matches a fence
at line start only, so a fenced sample *inside* a blockquote is not skipped
and its bolded bullet leads would mint titles. No page in the sweep domain
writes that shape today, so nothing is wrong on disk; it becomes real the
first time a banner quotes a sample. Not correctness-adjacent by the routing
bar as the tree stands.
