# The symbol half landed; spec/worktrees.md cites a test file

Shipped as measured: 44 in-subject cites, the only unresolved three are the
absence sentences the operator declared (`Chain.harvest`,
`Chain.worktreesDir`, `DispatcherOptions.trunkBranch`), each now carrying its
reason. The prefix set is derived, not restated: the eight come off
`spec-writing.md`'s *Public surface* bullet and the rest off `src/index.ts`'s
export inventory, so a ninth type ratified on the page widens the scan in
that commit.

**Observed, out of scope here.** `spec/worktrees.md` (*Integration lane*)
names `Dispatcher.test.ts` — a test file. `spec-writing.md` ("A claim names
behavior, never location") refuses "a test title or a fixture" outright, and
neither half of the spec-lint pin reaches it: the path half matches only
`src/…`, the symbol half excludes filename tails on purpose. The sentence is
making a real point about the lane boundary's cost, so it wants a directed
rewrite naming the behavior rather than the file — a human edit, not a build
fix. Or the path half's grammar widens past `src/` and takes it.
