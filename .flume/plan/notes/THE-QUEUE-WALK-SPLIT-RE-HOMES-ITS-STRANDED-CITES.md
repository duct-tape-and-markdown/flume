# The stranded cites moved; the doc block moved with the job

Shipped: the three cites now name `pathsUnderStateRoot`, and the absence /
refusal / descent block moved from `listUnderStateRoot`'s doc onto
`pathsUnderStateRoot`, which is the function that holds those rules. A fourth
site went with them — `fileUnderStateRoot`'s doc named `listUnderStateRoot`
as the listing that hands back its spelling; the listing that goes through
the composer is `pathsUnderStateRoot`. The `.gitkeep`-is-not-a-record
paragraph moved to `filesCarrying`, which owns the extension filter.

Observed, for the sweep rather than the queue: this family is the same one
6bc45949 opened, and the pin cannot see it. The citation pin resolves the
token, so `listUnderStateRoot` stayed green at every door it was pointing at
wrongly. What is uncheckable is the *sentence* around the token — "the
descent X runs for the record queue" — and nothing mechanical reads that.
The one decidable proxy available is caller count: a cite naming a function
as some caller's walk, where that caller does not reach the function, is a
graph question the export scan's program could answer. Not filed — no second
measured drift of that shape yet, and precision is not itself a finding
(`.claude/rules/engineering.md`, *Narration is the ladder's bottom rung*).

No behavior changed; no `tests[]`, no `pins[]`, as the entry declared. Full
suite green (72 files, 1923 passed), `tsc --noEmit` clean.
