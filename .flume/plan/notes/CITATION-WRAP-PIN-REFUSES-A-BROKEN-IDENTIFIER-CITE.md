# The wrap pin carries no exclusion arm

Ran the widened reading over `src/` + `harness/`: 31 wrapped spans, and
closing the break finds exactly one subject — `src/cliVerdict.ts:122`
(`TickVerdictMergeOutcome.` + `tag`), now rewrapped. The other 30 close to
prose that keeps a space of its own. No narrower rule and no exclusion were
needed, as the entry's fork expected.

Forward-looking: the break-closed reading can in principle report a wrap that
was never a citation — two prose halves whose join spells a camel hump
(`really` + `Fast` -> `reallyFast`). None today. Unlike the dangling pin, the
wrap pin has no `EXTERNAL_VOCABULARY` arm, so the fix for such a report is to
rewrap or reword the comment, never to widen the scan. If it recurs, the
ruling to make is whether a wrap report is a finding or an advisory.
