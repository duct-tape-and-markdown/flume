# The adoption verb is now prose in three docs with nothing reading it

Two adjacent staleness fixes taken alongside the scoped work, both the same
pre-package claim inside the assigned files: CHAIN-AUTHORING's intro sold
`.flume/chain.ts` as "the living reference" for hand-authored chain shape (it
is now the factory hop), and INTENT's Provenance bullet attributed the `per
cites resolve` gate to `.flume/chain.ts` rather than to the package plus a
declared `specLocus`.

Debt, for the ladder: `flume-harness init` and `@dtmd/flume/harness` are prose
in README, CHAIN-AUTHORING and INTENT, checked by nothing — a verb rename
strands all three. Sibling entry README-QUICKSTART-LEADS-WITH-THE-ADOPTION-VERB
ships a README-against-`harness/cli.ts` verb scan in
`tests/harnessPackaging.test.ts`; widening its file set to these two docs is
one glob.

INTENT's chain-package surface table was deleted, not updated — it restated
`spec/harness.md` *What the package owns* / *What a consumer declares*
(engineering.md, *Derived state is computed*).
