# The loader fact's ledger rows were four, not two

`tests/helpers/host-declarations.json` carried the `.cmd`-shim cite over a
shebang plant in **four** git-floor rows, not the two the entry named (at or
above the floor / version unreadable / relocated state root / below the
floor). All four now cite *win32 spawns no shebang script* for the plant, and
the `.cmd` section only for why a `.cmd` plant is no substitute either —
`src/git.ts` spawns git with no shell.

Left as they read, deliberately: the setup-restore row (the entry's model),
and the script-gate row, which points at the command-gate row rather than
restating the fact.

Incidental, fixed in passing: the `recordingShell` prose said "the two cases
that declare this path as their shell" over three (`posixOnly`,
`tests/harnessChain.test.ts`). The cardinal is dropped, not corrected — no
pin holds it.
