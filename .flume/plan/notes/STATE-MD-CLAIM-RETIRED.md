# Two neighbours of the retired claim, left unfiled

Both sites shipped. Two adjacent observations, neither in scope:

1. `README.md` *This repo's own chain writes* enumerates the chain-placed
   artifacts and omits `.flume/plan/notes/<TAG>.md` — build's only channel to
   plan, written every build tick. The omission predates this entry; one
   bullet if plan wants the enumeration true.

2. `tests/Dispatcher.test.ts` §8 (prose-durability describe, ~:10212) has a
   lead comment and a test title that both name `state.md / open-questions.md`
   and "plan". Its chain is a synthetic fixture, so the filename is that
   fixture's — out of scope by the entry's own carve-out. But the comment
   reasons about *the plan phase's* prose dying to a reset, which is the
   retired claim's shape wearing a fixture's clothes. Filing it needs a
   judgement on whether engine test prose may name a chain's conventions.
