# Three note homes shipped; the predicate reading the third is a wave behind

`notes/continuing/<TAG>.md` is now a note home the build fence admits, the
records gate judges under the tick's own tag, and the build prompt names.

**The window this opens.** The prompt now tells a build agent to commit a
green segment and write the continuing note, but `shipped` still reads only
the parked path (`isPark`, `harness/chain.ts`) — that is
A-CONTINUATION-KEEPS-ITS-ENTRY-IN-THE-QUEUE, blocked on this entry. Until it
lands, a tick that takes the prompt at its word ships its segment to trunk
and its entry leaves the queue with the rest of the work dropped, silently.
Pick that entry in the very next wave, alone if need be.

**The thresholds are my call, not the spec's.** *A tick puts work down* says
the prompt names them and no more; *The agent seam* mentions eighty percent
in passing. I wrote 70% of the context window for "open no new ground" and
80% for "commit and write the note", with a clause for a chain that declared
no window. Worth a human read — that number is the experiment's dial, and
nothing mechanical holds it.

**Two layout facts split apart.** `RECORD_DIR_NAMES` is now "what plan
drains"; `NOTE_DIR_RELS` is "where a build note lives". They were one
derivation, and a continuation is a note no drain takes. The records gate
reads the union (`recordOrNoteGlobs`, `harness/layout.ts`).

**A stale page outside every fence.** `harness/templates/PROTOCOL.md` gained
a "Build continuations" bullet. This repo's own `.flume/PROTOCOL.md` still
lists two note homes, and no phase can write it.
