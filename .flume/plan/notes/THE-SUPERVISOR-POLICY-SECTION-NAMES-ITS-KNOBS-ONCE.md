# The three CHAIN-AUTHORING walks are now one sequence spelled three times

Shipped arm (a): binding time moved onto each knob's bullet, the closing
paragraph explains the two classes without naming members, and the
supervisor-policy walk gained the `restatementsOf` arm its siblings had.

Observed while there: the three describes in `tests/examples.test.ts` that
walk this page are now near-identical - a checker-over-one-module resolver, a
`declaredX()` vacuity wrapper, a `sectionOf` anchor, an equality arm, a
restatement arm; ~90 lines each, differing only in module, interface name and
anchor phrase. Third spelling of one sequence (`engineering.md`, *A module is
one job*: "a second copy of a sequence ... is one function with two
callers"). A fourth walk will copy whichever is nearest, exactly as the eight
`sectionOf` copies did before `docSections.ts`. Target shape: one helper in
`tests/helpers/` taking module, interface, heading and vacuity member,
yielding the declared set plus the anchored section, with the three describes
as callers. Not filed here - build writes code, plan files entries.
