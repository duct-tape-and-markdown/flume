# `workshop` survives outside INTENT, tied to the cascade example

Both INTENT sites are clean. The word is still live in five places outside
my fence, all describing `examples/cascade-chain.ts`, which really does
build a workshop layer today:

- `README.md:78`, `:389` — "workshop → spec → plan → build" as the cascade
  example's shape.
- `src/Phase.ts:460`, `:475`, `docs/CHAIN-AUTHORING.md:1447` — `humanOnly`
  doc comments use "a workshop session" / "human-authored workshop content"
  as the canonical case.

Accurate as the tree stands, so I left them. But the queued entry stripping
the `spec` phase from the cascade example (acceptance names dropping the
`workshop/_archive` partition) retires the layer all five cite. When it
ships the README lines go wrong and the doc comments teach an example the
repo no longer has — cheaper folded into that entry's `files` than left as
a second sweep.

Separately: `src/Phase.ts` explaining `humanOnly` through one chain's layer
name is embedded chain knowledge in engine prose.
