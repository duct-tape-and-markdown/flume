[
  {
    "tag": "SETUP-WORKTREE-HELPER",
    "summary": "Ship a pnpm/npm lockfile-aware setupWorktree helper; migrate the dogfood chain onto it, per spec/RELEASE-v0.7.md §11",
    "per": {
      "path": "spec/RELEASE-v0.7.md",
      "section": "11. `setupWorktree` package-manager-aware helper"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "src/setupWorktree.ts",
          "description": "New exported helper, sibling to builtinGates.ts's shellGate/tscGate precedent: pnpm-lock.yaml → pnpm install --frozen-lockfile; package-lock.json → npm ci; neither → refuse cleanly, per §11."
        },
        {
          "path": "tests/setupWorktree.test.ts",
          "description": "New test file asserting lockfile-detection behavior per §11."
        }
      ],
      "edit": [
        {
          "path": "src/index.ts",
          "description": "Re-export the new helper alongside the existing builtinGates re-exports (~L30-37)."
        },
        {
          "path": ".flume/chain.ts",
          "description": "Replace buildSetupWorktree (~L95-103) with the new helper — dogfood discipline."
        },
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "Rewrite the setupWorktree worked example (~L418-458), currently teaching hardcoded pnpm install, to show the new helper as the recommended default."
        },
        {
          "path": "CHANGELOG.md",
          "description": "Add bullet under Added per §14."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/setupWorktree.test.ts",
        "asserts": "pointed at a pnpm-lock.yaml fixture dir runs pnpm install --frozen-lockfile; pointed at a package-lock.json fixture dir runs npm ci; pointed at neither refuses cleanly."
      }
    ],
    "acceptance": "The helper runs pnpm install --frozen-lockfile against a pnpm-lock.yaml fixture, npm ci against a package-lock.json fixture, and refuses cleanly against neither.",
    "notes": "Non-goals per spec: yarn/bun lockfile support; migrating connect's own chain.ts (that repo's own follow-up, outside this entry's blast radius)."
  }
]
