[
  {
    "tag": "BUILTINGATES-BARE-GATE-TITLES-NAME-THEIR-GATE",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/builtinGates.test.ts",
          "description": "reorder the it.each table so the title's second %s takes the gate name, not the gate function"
        }
      ],
      "retire": []
    },
    "summary": "Substitute the gate's name into the bare-Gate it.each title instead of the gate function's source text",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A green verdict is proven non-vacuous"
    },
    "acceptance": "The three bare-Gate cases render as `<gate> stays a bare Gate (name=<name>, when=afterCommit) whether used directly or called with no override`, with no source text in any title.",
    "pins": [
      "tscGate stays a bare Gate (name=tsc, when=afterCommit) whether used directly or called with no override",
      "vitestGate stays a bare Gate (name=vitest, when=afterCommit) whether used directly or called with no override",
      "eslintGate stays a bare Gate (name=eslint, when=afterCommit) whether used directly or called with no override"
    ],
    "notes": "tests/builtinGates.test.ts:615-620: the table is [label, gate, name] but the title carries two %s, so the second takes the gate function. Measured titles end mid-expression at `name=(override) => build(`, dropping the when=afterCommit claim the body does assert."
  }
]
