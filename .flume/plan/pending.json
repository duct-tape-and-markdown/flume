[
  {
    "tag": "RELEASE-CITES-REACH-DOCS",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "docs/CLI.md",
          "description": "the eight dead cites go"
        },
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "the four dead cites go"
        },
        {
          "path": "src/Agent.ts",
          "description": "the bare `§6` at :501 the grammar cannot see"
        },
        {
          "path": "tests/retired-narration.test.ts",
          "description": "the scan domain partitions docs/"
        }
      ],
      "retire": []
    },
    "summary": "docs/CLI.md and docs/CHAIN-AUTHORING.md ship twelve cites into the deleted RELEASE corpus, and the scan that would refuse them stops at src/ and examples/",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [
      "no current-reference docs page carries a release-numbered spec cite",
      "every docs page is either scanned for release cites or declared a dated record"
    ],
    "pins": [],
    "acceptance": "The two current-reference pages carry no cite into the deleted corpus, the scan reaches docs/, and an unclassified new docs page fails loud.",
    "notes": "From RELEASE-CITES-PINNED's note. 16 needle hits in docs/; three pages are dated records legitimately naming the world they left — CASCADE-DRY-RUN.md, MIGRATING-0.10.md:20, PRD-dock-collapse.md. Partition, never exclude: scanned ∪ dated = readdir(docs), so a new page forces a classification. src/Agent.ts:501 rides here — same dead corpus, invisible to the grammar; its cut is unpinnable, name that in the body."
  },
  {
    "tag": "CASCADE-LADDER-DRIVEN-BY-A-REAL-TICKRESULT",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/examples.test.ts",
          "description": "the ladder's agreement claim drives a real tick"
        }
      ],
      "retire": []
    },
    "summary": "cascade's ladder tests decode a TickResult the tester hand-built, so an engine-side rename of the handoff vocabulary ships green",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A seam gate reads what the real writer wrote"
    },
    "tests": [
      "cascade's plan ladder routes the baton from a TickResult the dispatcher produced"
    ],
    "pins": [],
    "acceptance": "At least one ladder rung transition is read off a real `Dispatcher.tick()` outcome instead of the `after()` fixture; the hand-authored cases that remain are the refusal shapes the section's carve-out allows.",
    "notes": "From CASCADE-PLAN-SLICES-ORDERED-HANDOFF's note. tests/examples.test.ts:322 `after()` hand-authors pickableAfter/shippedTags/noCommit/mergeOutcome. Cascade declares no phase agent, so `DispatcherOptions.agent` reaches it (src/Dispatcher.ts:1844) and the backlog-groomer drive's fixture shape applies. Fast lane: no Node startup, no real agent (spec/worktrees.md, the lane split). Build-wave refusal legs spawn children — out of scope."
  },
  {
    "tag": "DISPATCHER-EXTRACT-PRIOR-ATTEMPTS",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "src/priorAttempts.ts",
          "description": "the prior-attempt read/write/clear surface and the record builders"
        }
      ],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "the prior-attempt section leaves; the class calls the module"
        },
        {
          "path": "src/index.ts",
          "description": "re-export path for slugify / priorAttemptPath / priorAttemptsDir"
        }
      ],
      "retire": []
    },
    "summary": "Prior-attempt persistence — key, read, write, clear, revert snapshots, record builders — is a separate home inside Dispatcher.ts",
    "per": {
      "path": ".claude/rules/posture-sweep.md",
      "section": "A violation counts only when verified on disk this tick"
    },
    "tests": [
      "the prior-attempt record builders are exported from src/priorAttempts.ts"
    ],
    "pins": [],
    "acceptance": "A pure move: no behavior change, the package's public surface through `src/index.ts` unchanged, tests move with their subjects and differ only in import path.",
    "notes": "Sections at src/Dispatcher.ts:4106-:4460 plus the builders at :5267-:5359; `slugify`, `priorAttemptPath` and `priorAttemptsDir` are already public and must resolve identically from `flume`."
  },
  {
    "tag": "DISPATCHER-EXTRACT-FRICTION",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "src/friction.ts",
          "description": "harvest, count line, and the friction declaration check"
        }
      ],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "the friction helpers leave"
        },
        {
          "path": "src/cli.ts",
          "description": "import path for frictionCountLine"
        }
      ],
      "retire": []
    },
    "summary": "Friction harvesting, the count line and the friction declaration validator are one job spread through Dispatcher.ts",
    "per": {
      "path": ".claude/rules/posture-sweep.md",
      "section": "A violation counts only when verified on disk this tick"
    },
    "tests": [
      "frictionCountLine is exported from src/friction.ts"
    ],
    "pins": [],
    "acceptance": "A pure move: no behavior change, `src/index.ts` unchanged, tests move with their subjects and differ only in import path.",
    "notes": "`frictionCountLine` (src/Dispatcher.ts:1032), `validateFrictionDeclaration` (:935), `harvestFriction` (:3738). src/job.ts:444 already exports a helper for `frictionCountLine` to share — the new module keeps that direction."
  },
  {
    "tag": "DISPATCHER-EXTRACT-WORKTREES",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "src/worktrees.ts",
          "description": "create, teardown and the stale sweep"
        }
      ],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "the worktree helpers leave; sweepStaleWorktrees stays as the method cli.ts calls"
        }
      ],
      "retire": []
    },
    "summary": "Worktree provisioning, teardown and the stale sweep are a separate home inside Dispatcher.ts, beside src/setupWorktree.ts which already owns the hook half",
    "per": {
      "path": ".claude/rules/posture-sweep.md",
      "section": "A violation counts only when verified on disk this tick"
    },
    "tests": [
      "worktree provisioning and teardown are exported from src/worktrees.ts"
    ],
    "pins": [],
    "acceptance": "A pure move: no behavior change, `src/index.ts` unchanged, `dispatcher.sweepStaleWorktrees()` stays callable from cli.ts, tests move with their subjects and differ only in import path.",
    "notes": "`createWorktree` (src/Dispatcher.ts:4050), `teardownWorktreeInstance` (:3847), `sweepStaleWorktrees` (:3931), `worktreeDirName` (:793). Take last of the four: it is the group most entangled with the class's own state."
  }
]
