[
  {
    "tag": "CLI-RENDER-REMOVAL-SMOKE-DOC-SITES",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": ".github/workflows/ci.yml",
          "description": "swap the `flume render notes` install-smoke invocation for `flume status` — read-only, loads a real chain, still exists"
        },
        {
          "path": "scripts/smoke-install.mjs",
          "description": "swap the Windows shim's `render notes` smoke invocation for `status`, same rationale"
        },
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "remove (or replace) the ### Dry-run section describing the now-removed `flume render` command"
        }
      ],
      "retire": []
    },
    "summary": "CLI-RENDER-REMOVAL took the `render` subcommand out, but two install-smoke steps and one doc section still invoke or document it.",
    "per": {
      "path": "spec/cli.md",
      "section": "Subcommand surface"
    },
    "tests": [
      "the POSIX and Windows install-smoke steps pass end-to-end against the packed tarball using a subcommand that still exists"
    ],
    "acceptance": "ci.yml and scripts/smoke-install.mjs no longer invoke `render`; docs/CHAIN-AUTHORING.md's Dry-run section is gone or names a command that exists.",
    "notes": "Already scouted in CLI-RENDER-REMOVAL's own commit body and open-questions.md; filing now per that note's recommendation. Real CI break on the next run touching ci.yml."
  },
  {
    "tag": "CLI-CHANGELOG-SILENT-TAG-DROP",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "scripts/build-changelog.mjs",
          "description": "parseBuildSubject silently `continue`s past a build: subject matching none of the 3 declared tag shapes; its bracket matching also accepts mismatched pairs like `(TAG]`. Make both loud per engineering.md."
        },
        {
          "path": "tests/build-changelog.test.ts",
          "description": "cover the two cases"
        }
      ],
      "retire": []
    },
    "summary": "The changelog miner silently drops a build: commit whose subject matches no declared tag shape, and accepts mismatched bracket pairs as a valid tag.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Loud or nothing"
    },
    "tests": [
      "a build: commit subject matching none of the 3 declared tag shapes surfaces loud (refusal, or a listed warning) rather than silently vanishing from the draft",
      "a mismatched bracket pair such as `(TAG]` is rejected, not accepted as tag `TAG`"
    ],
    "acceptance": "the miner never silently omits an entry for a build: commit; a malformed bracket pair is refused rather than parsed as a valid tag.",
    "notes": "Found auditing CLI-CHANGELOG-FROM-GIT (58e075f). This repo's own history uses all-caps single-bracket tags exclusively, so it hasn't bitten in practice yet."
  },
  {
    "tag": "LOOP-WAVE-VERDICT-MULTIENTRY-COVERAGE",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "cover a multi-entry wave (one ships, one declines/reverts) hitting the ledger-rewrite PendingParseFailure path, not just the single-entry case already there"
        },
        {
          "path": "tests/cli.test.ts",
          "description": "assert tick-verdict.json's on-disk content after a ledger-rewrite failure, not just the in-memory outcome.verdict"
        }
      ],
      "retire": []
    },
    "summary": "LOOP-WAVE-VERDICT-LOST-ON-LEDGER-PARSEFAILURE's own test only exercises a single-entry wave and never asserts the on-disk tick-verdict.json content.",
    "per": {
      "path": "spec/loop.md",
      "section": "The tick verdict — one facts artifact"
    },
    "tests": [
      "a multi-entry wave with mixed outcomes (one shipped, one declined/reverted) folds the correct shippedTags/gateResults into the verdict when the ledger rewrite fails",
      "tick-verdict.json on disk reflects the wave's verdict after a ledger-rewrite PendingParseFailure, not just the in-memory outcome"
    ],
    "acceptance": "both new tests pass against the current implementation (coverage-only, no known defect) and would catch a regression in either the multi-entry or the on-disk-write path.",
    "notes": "Found auditing 952106904ab3. Plumbing looks correct by inspection; unverified beyond the trivial single-entry case."
  }
]
