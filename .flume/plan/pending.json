[
  {
    "tag": "INIT-MANIFEST-NONOBJECT-PIN",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/harnessInit.test.ts",
          "description": "the non-object branch of readConsumerManifest's refusal, beside the two unparseable-JSON cases"
        }
      ],
      "retire": []
    },
    "summary": "pin the second shape harnessInit's consumer-manifest refusal covers — a package.json that parses to a non-object",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A fix ships the test that would have caught it"
    },
    "acceptance": "the non-object arm of readConsumerManifest's refusal has a case of its own; removing that arm turns the suite red",
    "tests": [],
    "pins": [
      "flume-harness init refuses a consumer package.json that parses to a non-object before writing anything"
    ],
    "notes": "INIT-MANIFEST-REFUSAL-BEFORE-WRITE shipped one refusal over two shapes (harness/init.ts:430): unparseable, and parsed-non-object. Only the first has tests. Pre-fix, null reached pkg[\"dependencies\"] as a bare TypeError after the whole adoption landed — same defect, different exception. Property holds today, so pins[], not tests[]."
  }
]
