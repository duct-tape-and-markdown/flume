[
  {
    "tag": "RENDER-CORE-HINTS-CHAIN-VOCABULARY",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/PendingSchema.ts",
          "description": "renderSchemaForPrompt's dependsOnForks/files core hint lines, and the files doc comment carrying the same vocabulary"
        },
        {
          "path": "tests/PendingSchema.test.ts",
          "description": "inline snapshot, and the workshop pin widened from one literal to the class"
        }
      ],
      "retire": []
    },
    "summary": "Strip this repo's phase names, plan artifacts and JS-stack nouns from renderSchemaForPrompt's core hints; widen the workshop pin from one literal to the class",
    "per": {
      "path": ".claude/rules/engine-boundary.md",
      "section": "Capability vs convention"
    },
    "tests": [
      "the rendered pending schema names no phase from this repo's chain",
      "the rendered `files` hint names no language-specific incidental",
      "the rendered `dependsOnForks` hint names no open-questions artifact"
    ],
    "pins": [],
    "acceptance": "renderSchemaForPrompt's core hints carry no phase name, plan-lane artifact or JS-stack noun from this repo; snapshot green and all three pins red on the pre-fix tree.",
    "notes": "src/PendingSchema.ts:512-513 render `the build tick`, `a plan defect`, `lockfile, barrel export`, `open-question-slug`, `RESOLVED` into every downstream chain's plan prompt. 6a6e446 removed `workshop` from the same block but pinned the literal, not the class, so the siblings shipped green (engineering.md, The fix lands at the mechanism). Carry the existing pin's vacuity guard: assert the hint line is present before asserting the absence. The workshop case folds into pin 1."
  }
]
