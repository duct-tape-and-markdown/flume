[
  {
    "tag": "PRIOR-ATTEMPT-MODE-ROSTER",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Prompt.ts",
          "description": "one roster value beside NO_COMMIT_MODES; PriorAttempt's discriminant derived from it"
        },
        {
          "path": "src/priorAttempts.ts",
          "description": "the reader's mode acceptance reads the roster instead of spelling it"
        },
        { "path": "src/index.ts", "description": "export the roster" },
        {
          "path": "tests/priorAttempts.test.ts",
          "description": "ALL_MODES retires; the round trip reads the roster"
        }
      ],
      "retire": []
    },
    "summary": "Give the six PriorAttempt modes one engine-held roster; the record reader and the round-trip gate read it instead of each copying it",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Derived state is computed, never restated beside its source"
    },
    "acceptance": "One roster names the six modes; src/priorAttempts.ts:300 and tests/priorAttempts.test.ts:54 read it, and a seventh union member cannot land without the roster growing.",
    "tests": [
      "the engine's prior-attempt mode roster names every mode a record builder mints"
    ],
    "pins": [
      "PriorAttemptStore.read accepts a record for every mode the engine's prior-attempt mode roster names"
    ],
    "notes": "Three copies today: src/Prompt.ts:392 (the union), src/priorAttempts.ts:300-305 (the reader's disjunction), tests/priorAttempts.test.ts:54 (ALL_MODES, whose doc claims an exhaustiveness nothing holds). Only the renderer's switch is tsc-pinned: a seventh variant would read back as no-prior silently and leave its write/read seam unjudged. NO_COMMIT_MODES (src/Prompt.ts:79) is the shape already shipped beside it."
  }
]
