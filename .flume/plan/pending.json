[
  {
    "tag": "CORE-DOC-COMMENTS-CHAIN-VOCABULARY",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "`forkResolver` doc: opaque chain-resolved slug, no plan-lane gloss"
        },
        {
          "path": "src/Phase.ts",
          "description": "`entryChannelPaths` doc: generic cross-tick-artifact example"
        }
      ],
      "retire": []
    },
    "summary": "Strip this repo's plan-lane vocabulary from the shipped doc comments for `forkResolver` and `entryChannelPaths`",
    "per": {
      "path": ".claude/rules/engine-boundary.md",
      "section": "Capability vs convention"
    },
    "tests": [
      "the shipped doc comments for `forkResolver` and `entryChannelPaths` name no plan-lane artifact from this repo's chain"
    ],
    "pins": [],
    "acceptance": "Both doc comments describe only what the engine's mechanics consume; the scan naming the class is red on the pre-fix tree.",
    "notes": "Remainder of RENDER-CORE-HINTS-CHAIN-VOCABULARY, which fixed the rendered hints only. Declarations ship (tsconfig.build.json), so these are the hover text every consumer reads. Dispatcher.ts:1163 glosses the slug as \"is this open-question fork resolved?\" five lines above \"The runtime stays format-agnostic\"; Phase.ts:342 illustrates entryChannelPaths with `.flume/plan/open-questions.md` as its only example. Name the class, not a literal — the shipped pins' lesson."
  }
]
