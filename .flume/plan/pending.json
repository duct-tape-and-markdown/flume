[
  {
    "tag": "SPEC-PATH-LOCATORS-PINNED",
    "gate": {
      "kind": "open"
    },
    "files": {
      "edit": [
        {
          "path": "tests/retired-narration.test.ts",
          "description": "spec/ locator scan, beside the src/+examples/ cite scan"
        }
      ]
    },
    "summary": "Pin `spec/` against path locators: no `path:NN` line cite, no `src/` file path outside a declared claim-subject allowlist",
    "per": {
      "path": ".claude/rules/spec-writing.md",
      "section": "What holds this page above prose"
    },
    "tests": [],
    "pins": [
      "every `src/` file path in a spec page is one the locator allowlist declares",
      "no spec page carries a `path:NN` line locator",
      "the spec locator scan reads every page of spec/",
      "the locator grammar flags an injected `src/` path and an injected line locator"
    ],
    "acceptance": "`pnpm test` green with the scan present; each allowlist entry carries its claim-subject reason at the site.",
    "notes": "Re-measured at eb69567: zero `:NN` locators; the only `src/` file paths are `src/index.ts` (export inventory, 4 sites), `src/flumeApi.ts` (pending.md export-map sentence) and `src/Dispatcher.ts` (chain.md deep-import example). Bare `src/` and `examples/` directory mentions are not paths. The rule sentence's symbol half is red on the base — see open-questions, spec residue."
  },
  {
    "tag": "FRICTION-LINE-ONE-RENDERER",
    "gate": {
      "kind": "open"
    },
    "files": {
      "edit": [
        {
          "path": "src/friction.ts",
          "description": "split the count->line render out of frictionCountLine; export it"
        },
        {
          "path": "src/cliJobVerbs.ts",
          "description": "job status calls the shared renderer on JobStatus.frictionCount"
        },
        {
          "path": "tests/friction.test.ts",
          "description": "agreement pin across the two real surfaces"
        }
      ]
    },
    "summary": "Render `flume job status`'s friction line from the shared renderer in `src/friction.ts`, not a second hand-kept wording",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "The fix lands at the mechanism"
    },
    "tests": [],
    "pins": [
      "`flume job status` and `flume status` render the same friction wording for the same count, unreadable included"
    ],
    "acceptance": "`pnpm test` green; `src/cliJobVerbs.ts` composes no friction wording of its own — both surfaces render through the one function.",
    "notes": "Measured at ed9b3bd: `src/cliJobVerbs.ts` inlines both strings `frictionCountLine` (`src/friction.ts`) owns for `flume status` and the loop-end summary; the two-space prefix is the row separator, not part of the line. `job status` already holds the count (`JobStatus.frictionCount`, via the shared `countFrictionFiles`), so the unshared piece is count→line alone: `frictionCountLine` becomes count + render. Neither symbol is in `src/index.ts`."
  }
]
