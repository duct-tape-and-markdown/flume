[
  {
    "tag": "EXAMPLES-INTEGRATION-API-PATHS-ARE-THE-TICKS",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "edit": [
        {
          "path": "tests/examples.integration.test.ts",
          "description": "build the chain per-test from the fixture repo's roots; drop the api.paths disclaimer"
        },
        {
          "path": "tests/examples.test.ts",
          "description": "same for the ladder drive's cascadeChain (module scope, l.61) — it ticks at fx.repo (l.556); carries the pin"
        }
      ]
    },
    "summary": "Build the example chains under test from the roots their Dispatcher ticks against, not from this repo's",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A seam gate reads what the real writer wrote"
    },
    "tests": [],
    "pins": [
      "the cascade chain the ladder drive ticks is built from the fixture repo's roots"
    ],
    "acceptance": "The integration file builds its chain per-test from the fixture repo's roots and its 'does not read api.paths yet' disclaimer is gone; the ladder drive in tests/examples.test.ts ticks a cascadeChain built from the same roots it hands its Dispatcher. Pure shape reads keep module-scope EXAMPLE_PATHS — they tick nothing.",
    "notes": "Re-scoped from the 2026-09-14 park. The fix was verified (5/5 integration tests pass) but its tests[] line was unjudgeable twice over: the gate reads the fast lane only, and a tests/-only entry has no pre-fix tree (red-on-base lays the merged bytes of the files holding the named tests over the base). So the line moves to pins[] and homes in the fast lane, where the same defect sits."
  }
]
