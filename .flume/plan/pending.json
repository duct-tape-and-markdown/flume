[
  {
    "tag": "PRIOR-ATTEMPTS-DIR-EXPORT",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "Export priorAttemptsDir(flumeDir) beside priorAttemptPath, deriving from the same private PRIOR_ATTEMPTS_SUBDIR."
        },
        {
          "path": "src/index.ts",
          "description": "Re-export priorAttemptsDir alongside priorAttemptPath/slugify."
        },
        {
          "path": "src/flumeApi.ts",
          "description": "Add priorAttemptsDir to the FlumeApi interface and buildFlumeApi, mirroring priorAttemptPath's entry."
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "Agreement pin beside the existing slugify/priorAttemptPath re-export test."
        }
      ],
      "retire": []
    },
    "summary": "Export priorAttemptsDir so a chain can derive the prior-attempts scan dir instead of hardcoding it",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A fact the engine holds is reported, never rediscovered"
    },
    "tests": [
      "priorAttemptsDir(flumeDir) agrees with the directory component of priorAttemptPath(flumeDir, tag) for an arbitrary tag"
    ],
    "acceptance": "priorAttemptsDir is exported from Dispatcher.ts/index.ts/flumeApi.ts; new agreement test is green.",
    "notes": "Scope corrected this tick: dropped .flume/chain.ts from files (outside every phase's lane, spec-plan-build.md) — the prior tick's declaration there caused the recorded merge-failure footprint. chain.ts's own consumption of the export is now a parked open question."
  }
]
