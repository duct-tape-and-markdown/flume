[
  {
    "tag": "STATE-ROOT-LAYOUT-ONE-HOME",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/paths.ts",
          "description": "Add the state-root layout home: one accessor per runtime path, beside `namespacedJoin`/`entryWriteScopeUnion`."
        },
        {
          "path": "src/cli.ts",
          "description": "Route the seven literal sites (`stop` :316/:433/:775, `loop.pid` :303/:799, queue default :360/:499) through it."
        },
        {
          "path": "src/job.ts",
          "description": "Same for `RUNTIME_IGNORES` (:45), `liveLoopPid` (:309), `jobStatus`'s awake probe (:510) and queue default (:520)."
        },
        { "path": "src/Baton.ts", "description": "`awake` dir (:31) from the accessor." },
        {
          "path": "src/Dispatcher.ts",
          "description": "`stop` check (:4900), `PRIOR_ATTEMPTS_SUBDIR` (:115), both queue-default resolutions (:1523, :1590)."
        },
        { "path": "tests/cli.test.ts", "description": "Agreement pins across the seams below." }
      ],
      "retire": []
    },
    "summary": "State-root layout (`stop`, `loop.pid`, `awake`, `prior-attempts`, the default queue path) is restated as literals at 17 sites across 4 modules",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Derived state is computed, never restated beside its source"
    },
    "tests": [
      "the stop flag `flume stop` writes is the one the supervisor's per-iteration check honors — driven through both real sites, not a shared fixture",
      "the `loop.pid` `flume loop` writes is the one `liveLoopPid` reads back",
      "an undeclared `Chain.pendingPath` resolves to the same absolute file for the dispatcher and for `flume check`"
    ],
    "acceptance": "No runtime state-root filename or queue-path default appears as a literal outside its accessor in `src/paths.ts`.",
    "notes": "Writer and reader of each fact sit in different modules, so a rename is a silent bypass: `stop` (cli refuses, Dispatcher honors), `loop.pid` (cli writes, job.ts reads — the one-supervisor lock). Home is `src/paths.ts`: all four consumers already import it and it imports only `node:path`. Precedent: `priorAttemptsDir`. `worktrees` (Dispatcher.ts:3741/:3874) is excluded — the parked `Chain.worktreesDir` fork owns it."
  },
  {
    "tag": "DOC-CLAIMS-CONTRADICT-CODE",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Agent.ts",
          "description": "Retarget `AgentInvocation.extraEnv`'s doc (:43-48) to the tick's `setupWorktree` return value, both concurrencies."
        },
        {
          "path": "src/Gate.ts",
          "description": "Name `configDir`'s escape branch (:70-79), pointing at `computeStateRootRel` as its owner — the shape `stateRootRel`'s doc two fields up already uses."
        },
        { "path": "tests/Dispatcher.test.ts", "description": "Pin the singleton extraEnv path." }
      ],
      "retire": []
    },
    "summary": "`Agent.ts:47` says singleton phases never carry extraEnv (`runSingleton` passes it, unpinned); `Gate.ts:70-79` states the configDir rebase without its escape branch",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [
      "a singleton phase's `setupWorktree` extraEnv reaches the agent invocation"
    ],
    "acceptance": "Both docs state what the code does, and `runSingleton`'s extraEnv pass-through is pinned.",
    "notes": "Both drained from open-questions.md, where build parked them as out-of-fence prose lags; neither carries a design fork. Verified on disk: `runSingleton` collects extraEnv (Dispatcher.ts:1826) and passes it (:1911), and the only extraEnv test is fanout (Dispatcher.test.ts:2726) — so the doc endorses deleting an unpinned path. Gate.ts's behavior half is already pinned (\"GateContext.configDir rebase\"); only its prose lags."
  }
]
