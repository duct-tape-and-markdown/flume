[
  {
    "tag": "TEST-SUITE-LANES",
    "summary": "Split vitest into fast (default) + integration lanes per §17; real-subprocess tests off the in-worktree gate, run at host via test:integration",
    "per": {
      "path": "spec/RELEASE-v0.3.md",
      "section": "17. Test-suite lanes — integration tests off the in-worktree gate"
    },
    "gate": { "kind": "open" },
    "files": {
      "new": [
        { "path": "tests/loop-process-boundary.integration.test.ts", "description": "Relocated real-subprocess suite (renamed from .test.ts) per §17" }
      ],
      "edit": [
        { "path": "vitest.config.ts", "description": "Exclude **/*.integration.test.ts from the default run per §17" },
        { "path": "package.json", "description": "Add test:integration running only the integration lane per §17" },
        { "path": "docs/CHAIN-AUTHORING.md", "description": "Document the lane convention per §17 (don't gate the in-worktree build on host-level integration tests)" }
      ],
      "retire": [ "tests/loop-process-boundary.test.ts" ]
    },
    "schemaDelta": "none",
    "tests": [
      { "path": "tests/loop-process-boundary.integration.test.ts", "asserts": "Existing §2 chain-reload process-boundary cases preserved, now in the integration lane" }
    ],
    "acceptance": "pnpm test (vitest run) excludes the integration lane and stays fast/green in-worktree; pnpm test:integration runs only **/*.integration.test.ts",
    "notes": "§17a: exclude not timeout-bump; filename-convention+config-exclude (build may use vitest projects). No src/ change. Resolves OQ#5 (option B). Exclude lands in same commit whose afterMerge gate benefits — gate runs the narrowed vitest run."
  }
]
