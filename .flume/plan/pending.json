[
  {
    "tag": "HARNESS-INIT-LEAVES-THE-QUEUE-UNCREATED-SO-ADOPTION-WALLS-ITS-FIRST-PLAN-TICK",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "harness/init.ts",
          "description": "write an empty queue into the state root beside the three skeletons, and report it in `written`"
        },
        {
          "path": "tests/harnessInit.test.ts",
          "description": "the seeded queue's presence, and the real writer's bytes through the engine's parse"
        }
      ],
      "retire": []
    },
    "summary": "init writes the state root with no queue in it, so a fresh adoption's every plan tick refuses over an absent queue and the first wave never runs",
    "per": {
      "path": "spec/harness.md",
      "section": "Adoption and upgrade"
    },
    "acceptance": "init writes an empty queue at the state root's resolved pending path, names it in `written`, and a repo it just adopted renders a plan slice's queue span instead of refusing",
    "tests": [
      "flume-harness init seeds an empty queue in the state root",
      "the queue flume-harness init writes parses as an empty pending queue"
    ],
    "pins": [],
    "notes": "Gap at HEAD: init writes declaration.ts, chain.ts, PROTOCOL.md and matches nothing for the queue, while the span that reads it is unguarded (harness/prompts/plan-derive.md:8) — the readings half of the ruling is already shipped. Path from resolvePendingPath(stateRootAbs) undeclared, never a second spelling; it composes with node:path, so the `written` line takes gitPath as harness/chain.ts:202 does. The queue sits a directory down from the skeletons."
  },
  {
    "tag": "QUESTIONS-DIRECTORY-TITLE-CLAIMS-A-REFUSAL-THE-HELPER-NEVER-ENTAILS",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/harnessPrompts.test.ts",
          "description": "retitle the QUESTIONS_PATH wrong-kind case to the claim its body asserts, as its PLAN_STATE_PATH sibling now reads"
        }
      ],
      "retire": []
    },
    "summary": "the open-questions-directory title names an unconditional refusal over a helper that is conditional by construction, true today only by accident",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A green verdict is proven non-vacuous"
    },
    "acceptance": "the title names the two-branch claim the helper actually asserts, so a slice losing its QUESTIONS_PATH span moves assertion and title together",
    "tests": [],
    "pins": [
      "each plan slice prompt's verdict on an open-questions directory in place follows whether its spans read that artifact"
    ],
    "notes": "Site: tests/harnessPrompts.test.ts:502 over everySliceOverWrongKindAt (:453), whose per-slice verdict is refusal or clean render depending on spanSubstitutes. All three plan prompts substitute QUESTIONS_PATH today, so the title reads true; the moment one stops it claims a refusal the body asserts the absence of. Sibling shipped at d8ea99c; same helper, one `it` apart."
  }
]
