[
  {
    "tag": "cli-tip-claim-tests-integration-lane",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "tests/tip-claim.integration.test.ts",
          "description": "Relocated real-subprocess tests from cli.test.ts's 'tip claim wiring (v0.11 §4)' describe block (~1013-1521)."
        }
      ],
      "edit": [
        {
          "path": "tests/cli.test.ts",
          "description": "Remove the 'tip claim wiring (v0.11 §4)' describe block (~1013-1521), relocated to the integration lane."
        }
      ],
      "retire": []
    },
    "summary": "cli.test.ts's tip-claim-wiring tests spawn real flume tick/loop subprocesses and are load-flaky in the default lane; move to the integration lane.",
    "per": {
      "path": "spec/worktrees.md",
      "section": "The default test lane must stay fast"
    },
    "tests": [
      "the relocated suite is excluded from the default `vitest run`/`pnpm test` and included in `pnpm test:integration`"
    ],
    "acceptance": "The 12 real-subprocess tests under 'tip claim wiring (v0.11 §4)' live in a *.integration.test.ts file excluded from the default vitest run; cli.test.ts retains no real-subprocess flume tick/loop claim tests from this block.",
    "notes": "Inbox finding (752346f): an afterMerge gate-revert on friction-nonenoent-swallowed's first attempt was caused by this suite flaking under full-suite CPU contention — confirmed independently this tick, the same two tests ('a bare flume tick ... acquires and releases a claim') fail even in isolated re-runs, not just under load. Precedent: spec/worktrees.md's 'Rejected' list already directs relocation over deletion."
  }
]
