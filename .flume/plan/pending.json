[
  {
    "tag": "ESCAPESROOT-DOC-STATES-THE-RULE-NOT-ITS-ASKERS",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "src/paths.ts",
          "description": "escapesRoot's doc drops the enumeration of its askers, keeping the one-spelling rule and the prefix-test warning"
        },
        {
          "path": "harness/scriptRunner.ts",
          "description": "takes the escapesRoot cite if the trim strands readLine's only one"
        }
      ]
    },
    "summary": "escapesRoot's doc states the one-spelling rule, not a hand-kept list of the sites that ask it",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Derived state is computed, never restated beside its source"
    },
    "acceptance": "escapesRoot's doc (`src/paths.ts`) enumerates no caller; the rule and the prefix-test warning stay, every cite the trim would have stranded sits at the site that decides on the fact, and the citation and doc-comment suites are green.",
    "notes": "From the BASE-TREE note: the list read 'three', was stale on arrival, and shipping baseTree made it five. Who calls escapesRoot is the program's answer. No pins[] — escapesRoot is not on the exports map, so the doc-comment carve-out in *Narration is the ladder's bottom rung* does not license pinning the list; that section's shrink clause bounds the fix instead. `readLine` (`harness/scriptRunner.ts`) is cited nowhere else."
  }
]
