[
  {
    "tag": "JOB-NEW-HELP-INTERPOLATES-RUNTIME-IGNORES",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "src/cliHelp.ts",
          "description": "HELP_JOB's `job new` verb: interpolate RUNTIME_IGNORES (src/job.ts) in place of the hand-spelled roster"
        },
        {
          "path": "tests/cliHelp.test.ts",
          "description": "help text carries every RUNTIME_IGNORES entry"
        }
      ]
    },
    "summary": "Interpolate RUNTIME_IGNORES into HELP_JOB's `job new` line — the hand-spelled roster names 5 of 10 and is already stale",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Derived state is computed, never restated beside its source"
    },
    "acceptance": "`flume job new --help` names the ignore set read from the constant; no respelling left in HELP_JOB",
    "tests": ["job new help names every RUNTIME_IGNORES entry"],
    "notes": "Note's fork resolved by the cited section: regenerating is mechanical, so the copy should not exist — interpolate, do not pin prose. cliHelp.ts already interpolates DEFAULT_ABORT_THRESHOLD the same way; src/job.ts does not import cliHelp, so no cycle. Wrapping the longer line is build's call."
  }
]
