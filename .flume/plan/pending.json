[
  {
    "tag": "AGENT-SPAWN-WIN32-SHIM",
    "summary": "claudeCode spawns bare `claude` (src/Agent.ts:133) — ENOENT on win32 when claude is an npm .cmd shim. Retry once with shell:true on win32 ENOENT, mirroring execGate (e360352).",
    "per": {
      "path": "spec/RELEASE-v0.1.md",
      "section": "2. Public API surface"
    },
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Agent.ts",
          "description": "claudeCode invoke: on win32 spawn 'error' ENOENT before close, re-spawn once with shell:true and rewire stdio + stdin prompt write; POSIX path unchanged."
        },
        {
          "path": "tests/Agent.test.ts",
          "description": "spawn already vi.mocked — stub process.platform; assert win32 ENOENT triggers exactly one shell:true retry, POSIX ENOENT still rejects."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/Agent.test.ts",
        "asserts": "win32 ENOENT → single shell:true re-spawn resolves with the retried proc's output; non-win32 ENOENT rejects unchanged"
      }
    ],
    "acceptance": "pnpm test green; claudeCode invoke survives a .cmd-shim-only claude on win32 (mocked spawn + stubbed platform)",
    "notes": "Audit echo of e360352 (same .cmd-shim class, fixed there for pnpm gate/worktree spawns; this is the claude spawn). Latent-fatal for npm-installed claude on win32; native-installer hosts unaffected — why the dogfood loop survives. Defect repair on shipped v0.1 §2 surface, same posture as AGENT-TAG-WIN32-BASENAME. shell:true arg-quoting caveat is the tradeoff e360352 already accepted (argv is fixed flags + chain-authored extraArgs)."
  },
  {
    "tag": "GATE-EXECGATE-FALLBACK-TEST",
    "summary": "e360352's execGate win32 ENOENT→shell fallback (src/builtinGates.ts:31-45) landed untested. Add shellGate coverage per v0.1 §5.",
    "per": {
      "path": "spec/RELEASE-v0.1.md",
      "section": "5. Tests"
    },
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/Gate.test.ts",
          "description": "runIf(win32): temp-dir .cmd fixture ENOENTs the direct spawn, goes green via the shell retry; existing missing-binary fail test stays green on all platforms."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/Gate.test.ts",
        "asserts": "win32: a .cmd-only command turns shellGate green through the fallback; a truly-missing binary is still ok=false"
      }
    ],
    "acceptance": "pnpm test green; fallback path exercised on win32 hosts",
    "notes": "Backfill for interactive fix e360352, which shipped untested (the loop could not self-heal a gate reverting its own repair; test lane was red until d4d2317). Fallback is only observable where .cmd shims exist → runIf(win32), same precedent as abc6368's drive-letter test. No src/ change expected."
  }
]
