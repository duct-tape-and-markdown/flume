[
  {
    "tag": "CHANGELOG-DRAFT-BREAKING-SECTION-BOUNDED",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "scripts/build-changelog.mjs",
          "description": "renderSection: keep the non-breaking list out of the ### Breaking subsection"
        },
        {
          "path": "tests/build-changelog.test.ts",
          "description": "assert the boundary the Breaking test's comment already names"
        }
      ],
      "retire": []
    },
    "summary": "renderSection nests every non-breaking entry under `### Breaking`; the test naming that boundary asserts only presence — fix the render and its assertion",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A fix ships the test that would have caught it"
    },
    "tests": [
      "a non-breaking entry renders outside the ### Breaking subsection when a breaking entry is present"
    ],
    "pins": [
      "a draft whose entries are all breaking still renders the ### Breaking subheading"
    ],
    "acceptance": "A draft mined from a range holding one breaking and one non-breaking build: commit places the non-breaking entry outside the `### Breaking` subsection, and the tests[] line is red on the pre-fix tree.",
    "notes": "Verified on disk: renderSection appends the non-breaking block after the breaking one with no closing heading, so a non-breaking entry sits inside `### Breaking` in the rendered markdown. The test's comment states the boundary; its assertions check only presence and a single heading. spec/cli.md *Versioning policy* names `### Breaking` and no sibling heading — hold the boundary without inventing one."
  }
]
