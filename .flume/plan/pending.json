[
  {
    "tag": "BARE-TICK-CLAIM-RELEASES-ON-SIGNAL",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/cli.ts",
          "description": "bare-tick tip claim: install exit/SIGINT/SIGTERM release, gated by a held flag, as the loop branch does"
        },
        {
          "path": "tests/tip-claim.integration.test.ts",
          "description": "bare-tick signal case beside the loop's SIGTERM case"
        }
      ],
      "retire": []
    },
    "summary": "Release the bare `flume tick` tip claim on exit/SIGINT/SIGTERM, not in a `finally` alone",
    "per": {
      "path": "spec/loop.md",
      "section": "The loop lock and the tip claim"
    },
    "acceptance": "A signalled bare `flume tick` leaves no claim file behind; the claim drops through one owner, as the loop's does.",
    "tests": [],
    "pins": [],
    "notes": "Observed next door by LOOP-RELEASE-HANDLERS-PRECEDE-THE-LOCKS; verified at src/cli.ts bareTipClaim. `finally` does not run on SIGINT/SIGTERM, so a signalled bare tick leaves its claim standing \u2014 stale-reclaim bounds it, but a recycled pid probes live and refuses the next tick. Same held-flag-gated drop shape as the loop branch. No tests[]: the decidable case is the integration lane's, which the judge's lane does not run \u2014 name that exception in the commit body."
  }
]
