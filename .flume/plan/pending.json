[
  {
    "tag": "DISPATCHER-EXTRACT-WORKTREES",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "src/worktrees.ts",
          "description": "create, teardown and the stale sweep"
        }
      ],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "the worktree helpers leave; sweepStaleWorktrees stays as the method cli.ts calls"
        }
      ],
      "retire": []
    },
    "summary": "Worktree provisioning, teardown and the stale sweep are a separate home inside Dispatcher.ts, beside src/setupWorktree.ts which already owns the hook half",
    "per": {
      "path": ".claude/rules/posture-sweep.md",
      "section": "A violation counts only when verified on disk this tick"
    },
    "tests": [
      "worktree provisioning and teardown are exported from src/worktrees.ts"
    ],
    "pins": [],
    "acceptance": "A pure move: no behavior change, `src/index.ts` unchanged, `dispatcher.sweepStaleWorktrees()` stays callable from cli.ts, tests move with their subjects and differ only in import path.",
    "notes": "`createWorktree` (src/Dispatcher.ts:4050), `teardownWorktreeInstance` (:3847), `sweepStaleWorktrees` (:3931), `worktreeDirName` (:793). Take last of the four: it is the group most entangled with the class's own state."
  },
  {
    "tag": "CHAINTS-BUILD-HANDOFF-OVER-A-REAL-WAVE",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/chain.test.ts",
          "description": "the build legs leave the hand-built block and join the real-tick ladder"
        }
      ],
      "retire": []
    },
    "summary": "Drive build.handoff off a TickResult the dispatcher produced, so mergeOutcome and shippedTags are read from the real writer",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A seam gate reads what the real writer wrote"
    },
    "tests": [],
    "pins": [
      "a build wave the engine marked cherry-pick-conflict re-wakes build and one it marked not-shipped wakes the inbox slice, off a TickResult the dispatcher produced",
      "a clean build wave with nothing pickable falls through this chain's ladder to the sweep, off a TickResult the dispatcher produced"
    ],
    "acceptance": "The build legs of \"handoff — the legs a hand-built TickResult still owns\" are gone; tickResult() folds only what a real tick cannot produce, and the surviving block's name still holds.",
    "notes": "Fast lane, not integration: api.setupWorktree is already an override seam at chain.test.ts:320, and the package-manager gates come off api too — entries carrying no tests[]/pins[] short-circuit vitestOnCode. Real cherry-pick, real shipped predicate. If a seam turns out to need re-authoring the handoff vocabulary to stub, park with what."
  }
]
