[
  {
    "tag": "A-POSIX-ONLY-CASE-DECLARES-ITS-HOST",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "tests/hostDeclarations.test.ts",
          "description": "the declared-host list with a reason each, and the scan holding the suite to it"
        }
      ],
      "edit": [
        {
          "path": "tests/cli.test.ts",
          "description": "the readdir-succeeds/stat-fails friction arms declare their host"
        },
        {
          "path": "tests/cliHelp.test.ts",
          "description": "the same arm under `flume friction --help`"
        },
        {
          "path": "tests/Baton.test.ts",
          "description": "the symlink-loop ELOOP case declares its host"
        },
        {
          "path": "tests/harnessRunner.test.ts",
          "description": "the declared-setup case drops the symlinked `node_modules` and declares its host"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "the EBUSY-survives-removal case declares its host"
        },
        {
          "path": "tests/job.test.ts",
          "description": "the ensureRuntimeIgnores read-denial arm declares its host"
        },
        {
          "path": "tests/bin.test.ts",
          "description": "the two symlink-walk cases declare theirs"
        }
      ],
      "retire": []
    },
    "observedFiles": [
      ".flume/plan/notes/A-POSIX-ONLY-CASE-DECLARES-ITS-HOST.md",
      "tests/Agent.test.ts",
      "tests/Baton.test.ts",
      "tests/Gate.test.ts",
      "tests/bin.test.ts",
      "tests/builtinGates.test.ts",
      "tests/cli.test.ts",
      "tests/cliHelp.test.ts",
      "tests/fsProbe.test.ts",
      "tests/git.test.ts",
      "tests/harnessCi.test.ts",
      "tests/harnessRunner.test.ts",
      "tests/harnessWindows.test.ts",
      "tests/helpers/hostDeclarations.ts",
      "tests/hostDeclarations.test.ts",
      "tests/job.test.ts",
      "tests/priorAttempts.test.ts",
      "tests/worktrees.test.ts"
    ],
    "summary": "the cases with no structural substitute — a stat that fails after a readdir succeeds, a symlink loop, `ln -s`, a sh shim reached through one — red the lane on their fixture, not a defect",
    "per": {
      "path": "spec/cli.md",
      "section": "win32 is a supported host"
    },
    "acceptance": "every case whose subject is POSIX error semantics names its host and the reason it skips elsewhere, so the win32 lane's remaining reds are engine or fixture defects and nothing skips silently.",
    "tests": [],
    "pins": [
      "no case in tests/ denies with a permission bit outside the declared-host list",
      "every declared-host case carries the reason it cannot run on the other host"
    ],
    "notes": "The declared-host list is an allowlist with a reason each, on the `EXTERNAL_VOCABULARY` idiom (`tests/commentCitations.test.ts`): a site added without one is indistinguishable from residue. `harnessRunner`'s `ln -s` is also what `.claude/rules/platform-facts.md`, *pnpm deletes a symlinked `node_modules` on install* already says not to do. `bin/flume` is the sh shim; the published win32 bin is `bin/flume.js`, so the walk's subject is POSIX-only by construction."
  },
  {
    "tag": "A-SOURCE-SCAN-FOLDS-THE-PATH-IT-FILTERS",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/PendingSchema.test.ts",
          "description": "the call-site scan's exclusion and its job.ts assertion compare a folded leaf, not a slash-spelled suffix"
        }
      ],
      "retire": []
    },
    "summary": "the parsePendingLoose call-site scan filters `filesUnder`'s host-native paths with a slash-spelled suffix, so on win32 it counts its own declaration as a second call site",
    "per": {
      "path": "spec/cli.md",
      "section": "win32 is a supported host"
    },
    "acceptance": "the call-site scan excludes its declaration and names job.ts on both hosts, so the windows lane's red is gone and the pin judges the same set either side.",
    "tests": [],
    "pins": [
      "the parsePendingLoose call-site scan excludes its own declaration from a set that really contained it"
    ],
    "notes": "Windows lane, run 35029787262. `filesUnder` (tests/helpers/repoProgram.ts) hands back `join`-built absolute paths; `relPath` beside it folds, and `basename` is what spec/cli.md prescribes for leaf extraction. Only two sites in the suite carry this shape, both in this one case. No posix-decidable regression — the pin holds on both hosts and bites on win32, the exception RECORDS-READ-DISK-IN-THE-HOSTS-ALPHABET already took."
  },
  {
    "tag": "HARNESS-FS-CALLS-CLEAR-WIN32-MAX-PATH",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "tests/harnessPaths.test.ts",
          "description": "the tree-wide scan holding harness/ to the idiom, and the deep-path round-trips it cannot express"
        }
      ],
      "edit": [
        {
          "path": "harness/planState.ts",
          "description": "the read, the mkdir and the write compose through namespacedJoin"
        },
        {
          "path": "harness/prompts.ts",
          "description": "the in-tree cite reader's join composes through namespacedJoin"
        }
      ],
      "retire": []
    },
    "summary": "four harness fs calls reach disk on bare `join`-built paths under a chain-declared state root — on win32 a deep root reads the plan state as absent, which is a cursor",
    "per": {
      "path": ".claude/rules/platform-facts.md",
      "section": "Windows MAX_PATH (~260 chars) breaks fs calls with no long component"
    },
    "acceptance": "every fs call in harness/ composes its path through namespacedJoin, and a plan state under a state root nesting past win32's ~260-char limit round-trips instead of reading as no cursor.",
    "tests": [
      "every fs call in harness/ is made on a namespacedJoin-built path"
    ],
    "pins": [
      "readPlanState and writePlanState round-trip a plan state under a state root nesting past win32's ~260-char limit"
    ],
    "notes": "From RECORDS-READ-DISK-IN-THE-HOSTS-ALPHABET's note. 7 fs calls in harness/; records.ts, inboxWindow.ts and handoff.ts are already namespaced, planState.ts x3 and prompts.ts x1 are not. planState's is the loud one: absent is a declared state there, so the ENOENT reads as no cursor and re-derives the whole corpus. The scan is tests/Baton.test.ts's per-module detection widened to the tree — share it rather than spell a third. Vacuity pin rides it: modules scanned > 0."
  },
  {
    "tag": "THE-SUPERVISOR-DELEGATES-THE-RELEASE-GRACE",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/loopSupervisor.ts",
          "description": "the default runner signals the tick child's group and waits on its exit unbounded; the run's own grace option goes with the timer"
        },
        {
          "path": "src/processTree.ts",
          "description": "the group signal the runner takes, beside the escalation src/Agent.ts keeps; its two comments stop naming the supervisor as the grace's forwarder"
        },
        {
          "path": "src/cli.ts",
          "description": "the loop branch stops forwarding the grace to superviseLoop"
        },
        {
          "path": "src/Phase.ts",
          "description": "the knob's doc comment stops naming the supervisor as a binder"
        },
        {
          "path": "src/Agent.ts",
          "description": "the killGraceMs doc comment stops cross-referencing the supervisor's retired bound"
        },
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "the three killGraceMs passages name the tick's grace over its agent tree, and the per-tick read scope"
        },
        {
          "path": "tests/cli.test.ts",
          "description": "the loop driver gains the real spawned agent; the two arms pinning the outer grace retire"
        },
        {
          "path": "tests/loopSupervisor.test.ts",
          "description": "whatever the dropped option moves"
        },
        {
          "path": "tests/processTree.test.ts",
          "description": "the signal half, apart from the escalation over it"
        }
      ],
      "retire": []
    },
    "summary": "the supervisor still bounds its tick child by the chain's grace and fires first; spec/loop.md now has it signal the child's group and wait unbounded",
    "per": {
      "path": "spec/loop.md",
      "section": "The loop lock and the tip claim"
    },
    "acceptance": "a signalled loop signals the tick child's group and waits on the child's exit with no bound of its own, so the one timer in the tree is the child's over the agent it can see. No shipped narration — doc comment or authoring page — still names the supervisor as the grace's binder or classes killGraceMs run-scoped.",
    "tests": [
      "an agent that swallows SIGTERM for the whole grace is dead when a signalled loop's run ends",
      "a signalled loop still holds its lock and tip claim past the declared grace while its wedged tick child lives"
    ],
    "pins": [],
    "notes": "Spec at d9e80fe, read scope at ce37152. Live at HEAD: defaultTickRunner calls terminateProcessTree(child, { graceMs }); that timer starts strictly before the child's own, so the agent's group is orphaned at the grace. SuperviseLoopOptions.killGraceMs loses its only consumer. Test 1 wants bareTickAgentChainSrc's spawned agent under the loop driver; test 2 a driver arm that does not await the run's exit. CHAIN-AUTHORING 1700/1732/1771 and processTree's comments carry the retired binder."
  },
  {
    "tag": "A-SIGNALLED-RELEASE-ANNOUNCES-ITS-WAIT",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/cli.ts",
          "description": "both releaseAndExit handlers say what they are waiting on, and under what bound, before they wait"
        },
        {
          "path": "tests/cli.test.ts",
          "description": "the signalled-loop and signalled-bare-tick cases read the line off the run"
        }
      ],
      "retire": []
    },
    "summary": "a signalled loop and a signalled bare tick say nothing until the tree is down; spec/loop.md now has each handler announce its wait at receipt",
    "per": {
      "path": "spec/loop.md",
      "section": "The loop lock and the tip claim"
    },
    "acceptance": "both handlers write the line before they wait, naming the grace the child escalates under, so a Ctrl-C that takes the grace explains itself while it happens rather than after.",
    "tests": [
      "a signalled loop logs the wait it is entering before its tick tree is down, naming the grace",
      "a signalled bare tick logs the wait it is entering before its agent tree is down, naming the grace"
    ],
    "pins": [],
    "notes": "Spec at 4895aed, amended by d9e80fe. --help names no supervisor knob; the operator at Ctrl-C is owed a line then, not a lookup. The loop handler’s wait has no bound of its own under d9e80fe — its line names the grace the tick child escalates under. Both handlers sit in src/cli.ts. The supervisor’s `signalled; stopping` line stays: it reports the run’s end, not the wait’s start. The real signalled-tree cases in tests/cli.test.ts are the fixtures; both declare win32 as a skip."
  },
  {
    "tag": "A-TEST-TITLE-CITES-A-PAGE-THE-TREE-HOLDS",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/helpers/commentCitations.ts",
          "description": "the title arm: a describe/it/test title's `*.md` name resolves against the working tree, identifier arm declared out at the site"
        },
        {
          "path": "tests/commentCitations.test.ts",
          "description": "the arm's own cases and the repo-wide title pin"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "its titles spell the page's directory"
        },
        {
          "path": "tests/PendingSchema.test.ts",
          "description": "its titles spell the page's directory"
        },
        {
          "path": "tests/builtinGates.test.ts",
          "description": "its titles spell the page's directory"
        },
        {
          "path": "tests/chain.test.ts",
          "description": "its titles spell the page's directory"
        },
        {
          "path": "tests/loopSupervisor.test.ts",
          "description": "its titles spell the page's directory"
        },
        {
          "path": "tests/job.test.ts",
          "description": "its titles spell the page's directory"
        },
        {
          "path": "tests/Baton.test.ts",
          "description": "its titles spell the page's directory"
        },
        {
          "path": "tests/cli.test.ts",
          "description": "its titles spell the page's directory"
        },
        {
          "path": "tests/examples.test.ts",
          "description": "a title naming a page no checkout holds"
        },
        {
          "path": "tests/harnessInit.test.ts",
          "description": "a title naming a page no checkout holds"
        }
      ],
      "retire": []
    },
    "summary": "the ratified carve-out reaches a test title's page name, and the scan does not read titles — 17 titles cite a page the working tree cannot answer",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "acceptance": "every `*.md` page name a describe/it/test title carries resolves against the working tree, and a renamed page reds the title citing it as it already reds the comment beside it.",
    "tests": [
      "the citation scan judges an unbackticked *.md page name a describe or it title carries",
      "the citation scan judges a title's page name and not a backticked identifier beside it"
    ],
    "pins": [
      "every *.md page name a src/, harness/ or tests/ title carries names a file the working tree holds"
    ],
    "notes": "Ruled 2026-09-15, rule landed at ea26ffe. 17 unresolved titles, not the record's 14: `engineering.md` x8, `engine-boundary.md` x4, `platform-facts.md` x2, plus `state.md/open-questions.md` (Dispatcher, stale since state.json), `SHIPPED.md` (examples, a runtime artifact no checkout holds), `PROTOCOL.md` (harnessInit). The repo pin is green at base by construction, so it needs its own non-vacuity assertion — titles scanned > 0."
  },
  {
    "tag": "DISPATCHER-HOMES-ITS-FIVE-FRONT-JOBS",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "src/tickVerdict.ts",
          "description": "the verdict types, the write/read/clear I/O and the sync latest-verdict read"
        },
        {
          "path": "src/mergingMarkers.ts",
          "description": "the crash marker's shape and its reader"
        },
        {
          "path": "src/chainLoad.ts",
          "description": "the chain module load, the declaration validations and the CJS-context error"
        },
        {
          "path": "src/log.ts",
          "description": "`Logger` and `consoleLogger` — the file whose absence is the cycle"
        },
        {
          "path": "src/exitCodes.ts",
          "description": "the two process-boundary codes three modules read"
        }
      ],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "keeps the dispatch class and its own helpers; the five jobs leave"
        },
        {
          "path": "src/index.ts",
          "description": "re-exports the same names from their new homes — the surface does not move"
        },
        {
          "path": "src/cliVerdict.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "src/loopSupervisor.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "src/cli.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "src/cliChainLoad.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "src/builtinGates.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "src/job.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "src/flumeApi.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "src/Phase.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "src/worktrees.ts",
          "description": "takes `Logger` from its own file — one leg of the cycle"
        },
        {
          "path": "src/priorAttempts.ts",
          "description": "takes `Logger` from its own file — one leg of the cycle"
        },
        {
          "path": "src/friction.ts",
          "description": "takes `Logger` from its own file — one leg of the cycle"
        },
        {
          "path": "harness/handoff.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/cli.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/cliVerdict.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/cliHelp.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/loopSupervisor.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/job.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/chain.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/paths.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/worktrees.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/helpers/dispatcherFixture.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/loop-process-boundary.integration.test.ts",
          "description": "takes the moved names from their new homes"
        }
      ],
      "retire": []
    },
    "summary": "src/Dispatcher.ts fronts its class with five jobs that have no file — tick-verdict I/O, merging markers, chain loading, the exit codes, and `Logger`, whose homelessness is a type cycle",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A module is one job"
    },
    "acceptance": "each of the five jobs sits in the file its name is, src/Dispatcher.ts holds the dispatch class and its own helpers alone, and no module Dispatcher imports imports a type back out of it. Citations naming an old home for a fact that moved are re-homed in the same commit.",
    "tests": [],
    "pins": [],
    "notes": "Filed at 57bd960, unchanged since. About 1,300 lines out, behavior-free: the module is 4,894 lines, the class 3,070. `worktrees.ts`, `priorAttempts.ts` and `friction.ts` take `Logger` as a type from the module that imports them — a cycle that exists only because `Logger` has no file. `src/index.ts` keeps the same exported names, so the export pin and the citation pins hold the surface."
  },
  {
    "tag": "ONE-ATTEMPT-LEG-AND-ONE-BATCH-SELECTION",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "DISPATCHER-HOMES-ITS-FIVE-FRONT-JOBS"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "one attempt function both concurrencies call, and one batch selection `runFanout` and `render` share"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "whatever the unified legs move in the suite's vocabulary"
        }
      ],
      "retire": []
    },
    "summary": "`runSingleton` and `runFanoutEntry` spell one attempt sequence and differ only in how they return; `render` re-spells `runFanout`'s batch arithmetic instead of taking it",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A module is one job"
    },
    "acceptance": "one attempt function carries render → tip read → invoke → tip verify → afterCommit gates → revert for both concurrencies, one batch selection serves `runFanout` and `render`, and the suite is unchanged.",
    "tests": [],
    "pins": [],
    "notes": "Target: `runAttempt(phase, wt, ref, label, entry?)` returning `{ termination, committed, tipMoved, verdict }`, called by both legs; `selectBatch(chain, pending)` called by `runFanout` and `render`. The `renderPrompt` try/catch is copied at both legs, and `render` repeats `pickableEntries` + `partitionByFileOverlap` with the same two `supervisorPolicy` reads. Blocked on the split so the sequence is unified once, at its final home."
  },
  {
    "tag": "ONE-SYNC-SPAWN-WRAPPER-IN-THE-PACKAGE",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "harness/exec.ts",
          "description": "the sync spawn: the win32 shim retry, the output buffer and the error-detail fold"
        }
      ],
      "edit": [
        {
          "path": "harness/gitRange.ts",
          "description": "`git()` becomes a one-liner over the shared spawn"
        },
        {
          "path": "harness/ci.ts",
          "description": "`branchAt()` and `forge()` become one-liners over it"
        },
        {
          "path": "harness/vitestRunner.ts",
          "description": "takes the buffer from the one home"
        },
        {
          "path": "harness/cli.ts",
          "description": "takes the detail fold from the one home"
        },
        {
          "path": "tests/harnessCi.test.ts",
          "description": "whatever the shared wrapper moves"
        },
        {
          "path": "tests/harnessWindows.test.ts",
          "description": "whatever the shared wrapper moves"
        }
      ],
      "retire": []
    },
    "summary": "three sync spawn wrappers in the package spell one job — the shim retry, the 64 MiB buffer and the error-detail fold are copied across windows.ts, ci.ts and vitestRunner.ts",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A module is one job"
    },
    "acceptance": "one module carries the package's sync spawn, and `git()`, `branchAt()` and `forge()` are each a call into it rather than a third copy of the retry, the buffer and the fold. Citations naming an old home for a fact that moved are re-homed in the same commit.",
    "tests": [],
    "pins": [],
    "notes": "`64 << 20` at ci.ts, gitRange.ts and vitestRunner.ts; `err instanceof Error ? err.message : String(err)` at ci.ts, cursorWindow.ts and cli.ts. `branchAt`'s sync-ness is declared and cited at the site — what files is the triplicated wrapper, not that divergence. Only `forge()` carries the shim retry. `git()` sits in gitRange.ts since the windows split. The sync surfaces upstream are an engine question, not proposed here."
  },
  {
    "tag": "THE-PLAN-ARTIFACT-LAYOUT-HAS-ONE-HOME",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "harness/layout.ts",
          "description": "where each plan artifact sits under a state root, and the fence that is their list"
        }
      ],
      "edit": [
        {
          "path": "harness/chain.ts",
          "description": "assembles the fence from the one home"
        },
        {
          "path": "harness/gates.ts",
          "description": "spells the queue's git path as chain.ts does"
        },
        {
          "path": "harness/init.ts",
          "description": "takes the queue path from the one home"
        },
        {
          "path": "harness/planState.ts",
          "description": "takes its own path from the one home"
        },
        {
          "path": "harness/records.ts",
          "description": "takes the record dirs from the one home"
        },
        {
          "path": "harness/prompts.ts",
          "description": "stops owning the questions path because nothing else did"
        },
        {
          "path": "tests/harnessGates.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/harnessPlanState.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/harnessRecords.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/harnessChain.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/harnessInit.test.ts",
          "description": "takes the moved names from their new homes"
        }
      ],
      "retire": []
    },
    "summary": "the plan-artifact layout has four homes and the queue's path is spelled three ways; chain.ts assembles the plan fence out of all four",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A module is one job"
    },
    "acceptance": "one module states where every plan artifact sits under a state root, chain.ts, init.ts and gates.ts each take theirs from it, and the queue's path is spelled once. Citations naming an old home for a fact that moved are re-homed in the same commit.",
    "tests": [],
    "pins": [],
    "notes": "`pending.json` is the engine's, `state.json` is planState.ts's, `inbox/` and `notes/` are records.ts's, `open-questions.md` is prompts.ts's — 'Spelled here because nothing else owns it' is the smell in prose. gates.ts reaches the queue through `join(ctx.stateRootRel, …)` while chain.ts and init.ts use `gitPath(resolvePendingPath(...))`; `readFileAtRef` folds a native separator today, so that leg is vocabulary rather than a live red."
  },
  {
    "tag": "SUBPROCESS-HELPER-IS-THREE-MODULES",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "tests/helpers/fixtureRoot.ts",
          "description": "`mkTempDir`, `mkFixtureRoot` and the state-root leak guard"
        },
        {
          "path": "tests/helpers/gitEnv.ts",
          "description": "`hermeticEnv`, its strip keys and `pinGitAutoGcOff`"
        }
      ],
      "edit": [
        {
          "path": "tests/helpers/subprocess.ts",
          "description": "keeps the spawn wrappers, the entry points and the spawn budget; the header stops saying 'also home to'"
        },
        {
          "path": "tests/helpers/spawnBudget.ts",
          "description": "its harness-module constant follows the spawn wrappers, not the file"
        },
        {
          "path": "tests/helpers/vitestSetup.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/helpers/dispatcherFixture.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "vitest.config.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/subprocessHelper.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/fixtureRoots.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/cli.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/cliHelp.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/cliJobResolution.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/cliJobVerbs.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/cliRender.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/bin.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/denial.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/fsProbe.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/harnessInit.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/harnessRunner.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/harnessPackaging.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/tip-claim.integration.test.ts",
          "description": "takes the moved names from their new homes"
        },
        {
          "path": "tests/loop-process-boundary.integration.test.ts",
          "description": "takes the moved names from their new homes"
        }
      ],
      "retire": []
    },
    "summary": "tests/helpers/subprocess.ts is four jobs under one name — spawn wrappers, env hygiene, fixture rooting and the state-root leak guard — and its own header lists them as 'also home to'",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A module is one job"
    },
    "acceptance": "subprocess.ts keeps the spawn wrappers, the entry points and the spawn budget; fixture rooting with the leak guard and the git/env hygiene each have their own module, and spawnBudget.ts keys its harness module by the wrappers rather than by this file. Citations naming an old home for a fact that moved are re-homed in the same commit.",
    "tests": [],
    "pins": [],
    "notes": "52% of the file is comment, most of it per-job rationale. `spawnBudget.ts` hardcodes this file as *the* harness module, so the leak guard and the git pin are scanned as spawn wrappers; `dispatcherFixture.ts` imports only `mkTempDir` from it. Ships before EVERY-FIXTURE-ROOT-FOLDS-AT-ITS-MAKER so that pass routes 39 files at the maker's final home."
  },
  {
    "tag": "EVERY-FIXTURE-ROOT-FOLDS-AT-ITS-MAKER",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "SUBPROCESS-HELPER-IS-THREE-MODULES"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/fixtureRoots.test.ts",
          "description": "the scan holding the suite to one maker for its temp roots, beside the case already pinning the property"
        },
        {
          "path": "tests/helpers/fixtureRoot.ts",
          "description": "the fold's one home gains whatever the sync callers need"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/job.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/subprocessHelper.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/examples.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/friction.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/cli.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/harnessPrompts.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/Gate.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/tip-claim.integration.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/loop-process-boundary.integration.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/git.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/cliJobVerbs.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/cliJobResolution.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/builtinGates.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/Agent.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/harnessWindows.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/harnessInit.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/harnessCi.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/harnessChain.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/bin.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/Baton.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/waitForHelper.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/setupWorktree.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/paths.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/job.integration.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/harnessRecords.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/harnessPlanState.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/harnessPackaging.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/harnessHandoff.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/harnessGates.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/harnessBuildArgs.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/exportConsumers.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/examples.integration.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/commentCitations.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/cliVerdict.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/chain.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/build-changelog.test.ts",
          "description": "its bare temp roots come through the folding helper"
        },
        {
          "path": "tests/Prompt.test.ts",
          "description": "its bare temp roots come through the folding helper"
        }
      ],
      "retire": []
    },
    "summary": "163 temp fixture roots in tests/ are named by a bare `mkdtemp(join(tmpdir(), …))`, each correct-by-accident until a comparison against git's output grows beside it",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "acceptance": "every temp fixture root in tests/ is made by the one helper that folds the spelling at creation, and a scan holds the suite to it — so a new bare root reds where it is written rather than on a Windows lane months later.",
    "tests": [
      "every temp fixture root in tests/ is created through the helper that folds it to git's spelling"
    ],
    "pins": [],
    "notes": "From FIXTURE-AND-GIT-SPELL-ONE-PATH-THE-SAME-WAY's note: six lane reds came from this class, three reproducible off-win32 with `TMPDIR` pointed at a symlink. 163 sites over 39 files; 15 are `mkdtempSync` and want a sync sibling beside `mkTempDir`, whose doc comment is the only thing holding the property today. Blocked on the helper split so the 39 files are routed at the maker's final home once, not twice."
  }
]
