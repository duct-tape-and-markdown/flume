[
  {
    "tag": "COMPUTESTATEROOTREL-SECOND-PARAM-NAMES-ONE-CALLER",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/paths.ts",
          "description": "computeStateRootRel's second parameter takes a name for what it is, not for one caller; the doc sentence asserting that generality goes with it"
        }
      ],
      "retire": []
    },
    "summary": "Rename computeStateRootRel's second parameter off one caller's flumeDir; the prose asserting the generality retires into the name",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "acceptance": "computeStateRootRel's second parameter names the root it offsets, not a caller; the doc no longer states in prose that the second root is the caller's to choose; tsc and suite green",
    "notes": "Exposed by the shipped registry trim. Verified this tick: src/paths.ts takes (repoRoot, flumeDir); of three call sites two pass a state root (Dispatcher construction, buildFlumeApi) and attemptCtx (src/Dispatcher.ts) passes configDir. Engine-internal — absent from src/index.ts — so only the parameter and its doc move; callers are positional. No tests[]/pins[]: the diff is a local name and prose no suite reads. The export's own name carries the same bias, left unfiled."
  }
]
