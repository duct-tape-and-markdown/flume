[
  {
    "tag": "STOPFLAG-TIPCLAIM-EXISTS-LOUD",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "src/loopSupervisor.ts",
          "description": "per-iteration stop-flag gate (~:439) adopts existsLoud"
        },
        {
          "path": "src/cli.ts",
          "description": "loop-start stop refusal (~:819) and status's stop-flag (~:350) + tip-claim (~:369) probes adopt existsLoud"
        },
        { "path": "tests/loopSupervisor.test.ts", "description": "supervisor stop-check case" },
        { "path": "tests/cli.test.ts", "description": "loop-start and status cases" }
      ]
    },
    "summary": "Four stop-flag/tip-claim existence gates still read unstattable as absent; adopt existsLoud (src/fsProbe.ts) per the loud/silent split",
    "per": { "path": ".claude/rules/engineering.md", "section": "Loud or nothing" },
    "tests": [
      "`flume loop` refuses naming the error when the stop flag is present but unstattable, instead of starting a run over it",
      "the supervisor's per-iteration stop check throws on a non-ENOENT stop-flag stat instead of ticking on",
      "`flume status` exits EX_IOERR on a non-ENOENT stop-flag stat, instead of printing no stop line",
      "`flume status` exits EX_IOERR on a non-ENOENT tip-claim stat, instead of printing no claim line"
    ],
    "acceptance": "No existsSync remains at these four sites; every other cli/loop output line unchanged.",
    "notes": "Probe shipped by CLI-STATUS-LOOPPID-EXISTS-LOUD. Stakes: spec/loop.md \"Graceful stop — the stop flag\" claims a stale flag can never silently swallow a run, and an unstattable one does. Supervisor disposition: throw, matching baton.hibernating() (readdirSync) one check below. The tip-claim site's comment declares silence for git failures only — not for a stat error; keep that declaration, add the split."
  }
]
