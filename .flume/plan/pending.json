[
  {
    "tag": "CORE-FIELD-NAMES-REPORTED",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/PendingSchema.ts",
          "description": "name the core entry field names as a module export; CORE_FIELDS derives from it, one home"
        },
        {
          "path": "tests/PendingSchema.test.ts",
          "description": "core-field agreement gate reads the export instead of casting; add the shadow-list test"
        }
      ],
      "retire": []
    },
    "summary": "Export the core entry field names so the agreement gate stops casting composePendingList into zod internals",
    "per": {
      "path": "spec/pending.md",
      "section": "The chain-declared extension"
    },
    "acceptance": "The core-field agreement gate sources its names from the engine; no `as unknown as z.ZodArray` cast remains in tests/PendingSchema.test.ts.",
    "tests": [
      "an extension shadowing any exported core entry field name other than tag throws"
    ],
    "notes": "composePendingList returns z.ZodType, so the gate reaches .element.shape through a cast — the same zod internals the per section rules private, and a zod major renaming them reds as a schema defect. Keep the export module-level like declaredPaths: spec/pending.md *What the package exports* enumerates index.ts/FlumeApi and is the human's, and no chain consumer is named yet."
  }
]
