[
  {
    "tag": "QUEUE-FENCE-PRECHECK-ONE-DERIVATION",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/paths.ts",
          "description": "the shared fence-to-violations derivation, beside entryWriteScopeUnion; refresh that comment's consumer list — neither consumer it names calls it now"
        },
        {
          "path": "src/builtinGates.ts",
          "description": "pendingGate takes the shared derivation; fenceWhen stays its own filter"
        },
        {
          "path": "src/cli.ts",
          "description": "flume check takes the shared derivation"
        },
        { "path": "tests/paths.test.ts", "description": "the derivation's own cases" },
        { "path": "tests/cli.test.ts", "description": "the agreement pin" }
      ],
      "retire": []
    },
    "summary": "Give the consumer-phase fence pre-check one derivation: pendingGate and `flume check` share it instead of each spelling the union and the per-entry violation filter",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "The fix lands at the mechanism"
    },
    "tests": [],
    "pins": [
      "flume check and pendingGate name the same offending paths for one queue against one consumer phase"
    ],
    "acceptance": "Both sites read the fence and the violation list from one derivation in src/paths.ts; the pin drives the real `flume check` and the real pendingGate over one queue.",
    "notes": "Drained from ENTRY-WRITE-SCOPE-ONE-DERIVATION's note. src/cli.ts:561 and src/builtinGates.ts:378 each spell `writablePaths ∪ entryChannelPaths` then `declaredPaths(e).filter(p => !matchesAny(p, fence))`. Behavior-preserving: fenceWhen is pendingGate's alone — spec/cli.md scopes `check` to engine mechanics, chain gates do not run there — so it stays a pendingGate-side filter over the shared derivation."
  }
]
