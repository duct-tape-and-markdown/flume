[
  {
    "tag": "BIN-NODE-SHIM-STDIO-ENV-PINNED",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/bin.test.ts",
          "description": "three cases into the existing bin/flume.js describe"
        }
      ],
      "retire": []
    },
    "summary": "Pin bin/flume.js's remaining spec/cli.md Distribution claims: stdio inherited on all three fds, and no environment opinion",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [],
    "pins": [
      "bin/flume.js passes the child's stdout and stderr through unmixed and adds no bytes of its own",
      "bin/flume.js passes stdin through to the child",
      "bin/flume.js adds nothing to the child's environment"
    ],
    "acceptance": "spec/cli.md, Distribution's \"stdio inherited\" and \"holds no environment opinion\" claims each have a holder a rung above prose; the three pins are green.",
    "notes": "From BIN-NODE-SHIM-PINNED's note. Verified gap: the shipped argv case reads child stdout *through* the shim, so the stdout leg and \"parses no options\" already hold (a banner breaks its JSON.parse); stdin, stderr, and env-neutrality have nothing. All three already hold on the base — pins, never tests. No gap on bin/flume: it `exec`s, so stdio and env are the kernel's."
  }
]
