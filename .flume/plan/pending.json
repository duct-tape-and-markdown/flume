[
  {
    "tag": "STATE-ROOT-LAYOUT-COMPOSES-AT-ONE-SPELLING",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/paths.ts",
          "description": "new repo-root layout section: the state-root name plus jobsRoot/jobDir accessors, beside the existing state-root and config-dir sections"
        },
        {
          "path": "src/job.ts",
          "description": "jobNew/jobRm dirs, jobStatus's jobsRoot and jobNew's configDir/flumeDir defaults read the accessors; jobDirPathspec keeps its gitPath fold and composes from them"
        },
        {
          "path": "src/cliJobResolution.ts",
          "description": "resolveStateDirs' three compositions and the repo-root discovery probe read the one name"
        },
        {
          "path": "src/loopSupervisor.ts",
          "description": "flumeDir/configDir defaults read the accessor"
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "flumeDir default reads the accessor"
        },
        {
          "path": "harness/init.ts",
          "description": "DEFAULT_STATE_ROOT takes the engine's name instead of restating it (per the cite's artifact bar)"
        },
        {
          "path": "tests/cliJobResolution.test.ts",
          "description": "agreement pin: real jobNew's dir through real resolveStateDirs"
        },
        {
          "path": "tests/job.test.ts",
          "description": "agreement pin: real jobNew's dir through real jobStatus, whose enumeration cases today read hand-composed fixtures only"
        }
      ],
      "retire": []
    },
    "summary": "One home in paths.ts for the repo-to-state-root layout; the twelve inline `<repoRoot>/.flume[/jobs[/<name>]]` compositions across five src modules and harness/init.ts fold through it",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Derived state is computed, never restated beside its source"
    },
    "acceptance": "No `<repoRoot>/.flume` layout composed outside paths.ts anywhere in src/ or harness/; both pins green.",
    "tests": [],
    "pins": [
      "`--job <name>` resolves the state root `job new <name>` seeded",
      "`job status` lists a job `job new` seeded"
    ],
    "notes": "Drained from JOB-VERB-PATHSPECS-USE-THE-ENGINE-GIT-ALPHABET's note, which reported four sites; verified twelve — src/job.ts 5, src/cliJobResolution.ts 5, src/loopSupervisor.ts 2, src/Dispatcher.ts 1 — plus harness/init.ts's DEFAULT_STATE_ROOT. Same shape STATE_ROOT_NAMES and chainModulePath already have a home for. Both pins are green on the base: no behavior changes, so tests[] is empty by design."
  }
]
