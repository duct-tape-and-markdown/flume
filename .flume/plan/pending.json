[
  {
    "tag": "THE-WORKTREE-STAMP-EARNS-ITS-OWN-MIGRATION-SECTION",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "docs/MIGRATING-0.17.md",
          "description": "the stamp as its own break; § 9 keeps the namespace collision and points at it; the routing block routes to it"
        },
        {
          "path": "tests/worktrees.test.ts",
          "description": "pin the note's stamp filename against the constant the source writes"
        }
      ]
    },
    "summary": "Walk the worktree state-root stamp as its own 0.17 break; it rides inside § 9, whose Affects names namespace readers",
    "per": { "path": "spec/cli.md", "section": "Versioning policy" },
    "acceptance": "The stamp is walked in a section of its own, its Affects naming a reader who never set FLUME_JOB; § 9 points at that section rather than restating it; `Which sections apply to you` routes there.",
    "pins": [
      "the 0.17 migration note names the state-root stamp filename the worktree source writes"
    ],
    "notes": "From build note THE-SHARED-BASE-REFUSAL-NAMES-THE-STAMP. Affected and unnamed today: any consumer with pre-0.17 worktrees on disk, and a provisioning that died between `git worktree add` and the stamp — a 0.17-only case § 9 is the wrong door for. Move § 9's stamp prose rather than copying it. Test file is build's call."
  }
]
