[
  {
    "tag": "HARNESS-STANDIN-SCAN-WHOLE-SUITE",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "tests/stubRunner.test.ts",
          "description": "widen the scanned set past the `harness` prefix; rewrite the scope paragraph, which now includes this file"
        }
      ]
    },
    "summary": "Widen the stand-in scan's domain from `tests/harness*.test.ts` to every `tests/*.test.ts` — the file glob is the last literal left inside type-directed machinery",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "The fix lands at the mechanism"
    },
    "acceptance": "The scan reads every top-level `tests/*.test.ts`, itself included, and the suite is green.",
    "pins": ["no test stands an engine type in through an `unknown` cast"],
    "notes": "Measured at HEAD: 50 top-level `*.test.ts`, 44 importing engine names, 0 stand-in casts — the widening is green as written, so this is a pin, not a test. `readdirSync` is non-recursive and the `.test.ts` filter stays, so `tests/helpers/stubRunner.ts` (quotes `as unknown as Runner` in prose, imports `Runner`) stays out and no comment-stripping is needed. The doc comment's closing paragraph — this file excluded from its own set — inverts."
  }
]
