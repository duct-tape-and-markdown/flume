[
  {
    "tag": "DISPATCHER-ASSISTANTTURNTEXT-EXPORT-SEAM",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "export the private assistantTurnText helper so there is one real implementation, not two"
        },
        {
          "path": "tests/Agent.test.ts",
          "description": "replace the local assistantTurnTextStandIn duplicate with an import of the real exported assistantTurnText"
        }
      ],
      "retire": []
    },
    "summary": "tests/Agent.test.ts's shared-parse agreement test hand-duplicates Dispatcher.ts's private assistantTurnText instead of importing the real export",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A seam gate reads what the real writer wrote"
    },
    "tests": [
      "the withTerminalRenderer/Dispatcher agreement test (tests/Agent.test.ts, 'feeds both withTerminalRenderer's tool_use rendering...') still passes when driven off the real exported assistantTurnText, not a hand-authored stand-in"
    ],
    "acceptance": "assistantTurnText is exported from src/Dispatcher.ts; tests/Agent.test.ts imports it directly in place of assistantTurnTextStandIn; full suite green",
    "notes": "Sibling isResultEvent/isErrorResult test in the same file already drives the real shared functions (Dispatcher.ts imports isResultEvent directly) so it needs no fix; its comment overstates Dispatcher's isErrorResult usage since finalAgentMessage never calls isErrorResult — accepted as narration debt, not filed separately."
  }
]
