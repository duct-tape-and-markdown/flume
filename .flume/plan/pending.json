[
  {
    "tag": "CLI-DOC-STATUS-LOG-EXIT-CODES-PINNED",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "tests/cliHelp.test.ts",
          "description": "two doc-section pins beside CLI-DOC-TICK/LOOP-EXIT-CODES-PINNED, driven from real invocations"
        },
        {
          "path": "docs/CLI.md",
          "description": "phrase each code in the status/log sections as an `exits `N`` clause so `namedExitCodes` resolves it"
        }
      ]
    },
    "summary": "pin docs/CLI.md's status and log exit-code claims against the codes those verbs really return",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A seam gate reads what the real writer wrote"
    },
    "acceptance": "each section's named exit codes equal the set driven from real `flume status` / `flume log` invocations, in both directions",
    "pins": [
      "docs/CLI.md's flume status section names every exit code the real status verb returns",
      "docs/CLI.md's flume log section names every exit code the real log verb returns"
    ],
    "notes": "tick and loop already have this pin; status and log are the two sections left. Driven halves exist: cliHelp.test.ts:793 drives log's 74 via denyFile, and status's 74 arms are the same shape. `namedExitCodes` needs an `exits `N`` clause per code, which neither section currently gives — hence the docs edit."
  }
]
