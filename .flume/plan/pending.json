[
  {
    "tag": "SRC-CITE-MODULE-PATH-PINNED",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/retired-narration.test.ts",
          "description": "the scan, beside the existing doc-to-source describes"
        },
        {
          "path": "src/Phase.ts",
          "description": "repoint :112 — `priorAttemptKey` is `priorAttemptRef`, `src/priorAttempts.ts`"
        },
        {
          "path": "src/job.ts",
          "description": "repoint :73 — `resolveStateDirs` is `src/cliJobResolution.ts`"
        },
        {
          "path": "src/cli.ts",
          "description": "repoint :686 — `defaultTickRunner` is `src/loopSupervisor.ts`, as :746 already says"
        }
      ],
      "retire": []
    },
    "summary": "Pin every module-path cite in a src/ or examples/ comment against the module that declares the symbol; repoint the three the scan turns red",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [
      "every module-path cite in a `src/` or `examples/` comment names a file that declares the symbol"
    ],
    "pins": [
      "the comment module-path scan reads a populated set of cites in both the colon and the parenthesized shape",
      "a cite whose symbol is dotted resolves against its last segment in the named file"
    ],
    "acceptance": "`pnpm test` green with the comment module-path scan in `tests/retired-narration.test.ts`; the three cites below name their declaring module.",
    "notes": "Verified this tick: 43 tight cites across src/+examples/, 5 dotted, 3 red — `src/Phase.ts:112` (`src/Dispatcher.ts:priorAttemptKey`; the symbol is `priorAttemptRef`, `src/priorAttempts.ts`), `src/job.ts:73`, `src/cli.ts:686`. Declares, not references: on `a18b40e^` Dispatcher.ts still imported the symbols the extraction stranded, so the looser reading passes over exactly this class. `spec/`'s own four are human-only — see open-questions."
  }
]
