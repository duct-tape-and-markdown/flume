[
  {
    "tag": "STATUS-PENDING-COUNT-UNSHIPPED",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/cli.ts",
          "description": "status (761-809) prints the pending count; HELP_TOP:227 and HELP_SUB.status (268-278) both describe what it actually emits, capability-skip lines included"
        },
        {
          "path": "src/job.ts",
          "description": "the count detection inline at 485-491 becomes the one probe both status surfaces call"
        },
        {
          "path": "docs/CLI.md",
          "description": "the status entry (21-28) gains the count and drops 'no chain is loaded' - false since the friction and capability reads landed"
        },
        {
          "path": "tests/cli.test.ts",
          "description": "extend the status describes (804, 1047) with the count cases; the job status describes pin the shared probe"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "`flume status` never prints the pending entry count §3 requires, and drops an unparsable pending.json silently - `flume job status` reports both.",
    "per": {
      "path": "spec/RELEASE-v0.1.md",
      "section": "3. CLI surface"
    },
    "tests": [
      {
        "path": "tests/cli.test.ts",
        "asserts": "`flume status` names the entry count for a valid pending.json, 'unparsable' for a corrupt one, and 0 when the file is absent - fails on the pre-fix tree, which prints no count line in any of the three states; `flume job status` output is byte-unchanged by the shared probe"
      }
    ],
    "acceptance": "`flume status` prints a pending count in all three states; one probe serves both status surfaces; help text and docs/CLI.md describe the output the code emits.",
    "notes": "§3 names three things status prints - awake phases, pending entry count, last commit. Only the awake set shipped. The 'last commit' leg is parked, not filed: unspecified format, and it restates git. cli.ts:793 drops a parse failure under a catch (805) whose comment claims only chain-load failures, so today a corrupt file also silences the v0.8 §4 capability lines. job.ts:485-491 already holds the detection - share it, never re-derive."
  },
  {
    "tag": "CHANGELOG-0.9.0-BACKFILL",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "CHANGELOG.md",
          "description": "backfill 5 omitted post-0.8.0 fixes into the published [0.9.0] section"
        }
      ],
      "retire": []
    },
    "summary": "Published [0.9.0] section omits 5 post-0.8.0 fixes that never got a changelog line: 480b81c, 9f4e62f, 1a67d1c, 58be15d, 3170113.",
    "per": {
      "path": "spec/RELEASE-v0.1.md",
      "section": "8. Repository hygiene"
    },
    "tests": [],
    "acceptance": "[0.9.0] section carries one line per commit below; retroactively satisfies the every-ship-gets-a-line convention for the published section.",
    "notes": "480b81c pendingGate lazy targetFence read; 9f4e62f+1a67d1c renderSchemaForPrompt separator-swallowed-by-comment fix; 58be15d win32 inline-exec ENOENT shell-retry fallback; 3170113 shellGate optional env. Pre-1.0: published npm tarball's copy stays stale, acceptable. changelogGate (chain.ts) prevents recurrence going forward; this is the one-time backfill only."
  },
  {
    "tag": "TAG-LENGTH-BOUND-AGREEMENT-PIN",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "extend the §5 revert-note describe (4997) with the agreement case"
        },
        {
          "path": "src/PendingSchema.ts",
          "description": "TAG_MAX_LENGTH doc (56-65) points at the pin instead of restating the writer's arithmetic"
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "slugify doc (368-381) drops 'no runtime check needed' for a pointer to the pin"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "TAG_MAX_LENGTH = 255-39 encodes writeRevertNote's filename overhead; both sides restate it in prose and nothing drives the schema's ceiling through the real writer.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A seam gate reads what the real writer wrote"
    },
    "tests": [
      {
        "path": "tests/Dispatcher.test.ts",
        "asserts": "a gate-revert on an entry whose tag is the longest parsePending accepts writes a revert note that exists on disk with a filename component within 255 chars - the real schema bound through the real writer, so a one-sided change to either breaks it"
      }
    ],
    "acceptance": "The revert-note filename is pinned against the schema's own accepted maximum; neither module's prose restates the other's arithmetic.",
    "notes": "PendingSchema.ts:65 hardcodes 39 = a 24-char stamp + 2 + 2 + 'reverted.md'; Dispatcher.ts:2129 is the writer. Correct today - this pins it, so it does not fail pre-fix (same shape as PARSEPENDINGLOOSE-WRITE-PATH-PIN). A silent break lands as ENAMETOOLONG inside writeRevertNote's best-effort catch (2133): a warn line and a lost operator note."
  },
  {
    "tag": "JOB-BRANCH-GRAMMAR-RETIRED",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/job.ts",
          "description": "jobNew (145-160) drops branch creation/checkout and stays on current HEAD; jobRun (270-282) drops the branchExists assert-or-throw and checkout leg; jobRm (368-397) drops the branch checkout leg and the 'branch survives' framing in its final log (425-426) - per §2/§3"
        },
        {
          "path": "src/cli.ts",
          "description": "delete the wrong-branch guard (745-758); drop the HEAD==job/<name> exit-1 clauses from HELP_SUB.tick (287-288), HELP_SUB.loop (307-308), the --job flag help (260), and the lifecycle-verbs header (357-358); job new/run/rm help text (234-244) drops branch-creation language"
        },
        {
          "path": "src/git.ts",
          "description": "currentBranch (39-42) loses its only caller when the cli.ts guard above goes - LSP-verify zero remaining callers and retire it if so, leave it if not"
        },
        {
          "path": "tests/job.test.ts",
          "description": "update the branch-leg assertions across jobNew (271-506), jobRun preflight (752-876), jobRm (877-1044) to the no-branch-topology behavior per §2/§3"
        },
        {
          "path": "tests/job.integration.test.ts",
          "description": "update the job-new to job-run (143-239) and job-new to run to rm (240-331) integration fixtures off branch-checkout choreography"
        },
        {
          "path": "tests/cli.test.ts",
          "description": "drop or repoint the wrong-branch-guard coverage tied to the removed cli.ts block"
        },
        {
          "path": "CHANGELOG.md",
          "description": "Breaking entry per §7: job/<name> branch convention retired"
        }
      ],
      "retire": []
    },
    "summary": "job/<name> branch grammar retired from job new/run/rm and the tick/loop wrong-branch guard - the engine no longer creates, asserts, or names a job branch.",
    "per": {
      "path": "spec/RELEASE-v0.11.md",
      "section": "2. A job is a state root"
    },
    "tests": [
      {
        "path": "tests/job.test.ts",
        "asserts": "jobNew leaves HEAD on whatever branch it started on with one baseline commit (no branch created); jobRun/jobRm no longer touch branches"
      },
      {
        "path": "tests/cli.test.ts",
        "asserts": "flume tick/loop under --job succeed regardless of current branch; two state roots under one checkout each run a tick sequentially without a branch switch (§2 acceptance fixture)"
      }
    ],
    "acceptance": "job new/run/rm construct, assert, or checkout no job/<name> branch; the cli.ts wrong-branch guard is gone; job new leaves HEAD on whatever branch it started on with one baseline commit; tick/loop run under --job regardless of current branch; tsc/vitest green. (Full §3 grep, including extract's own branch use, lands with JOB-EXTRACT-REMOVED.)",
    "notes": "job.ts's git ops go through its own git() wrapper (61-70), not src/git.ts - no src/git.ts changes needed except currentBranch's possible retirement. branchExists (75-82) may still be needed by jobExtract until JOB-EXTRACT-REMOVED lands - verify before deleting. Blast radius per RELEASE-v0.11.md §1."
  },
  {
    "tag": "JOB-EXTRACT-REMOVED",
    "gate": {
      "kind": "blockedBy",
      "tag": "JOB-BRANCH-GRAMMAR-RETIRED"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/job.ts",
          "description": "delete jobExtract (598-804), JobExtractOptions/JobExtractResult (540-572), gitShowBlob (507-524), worktreeHolding (330-349); delete readFrictionFiles (525-539) only if LSP shows no caller outside extract - v0.6.2 §6 friction reads may still need it"
        },
        {
          "path": "src/cli.ts",
          "description": "delete the extract verb wiring (524-580) and its help text (247-251)"
        },
        {
          "path": "src/Phase.ts",
          "description": "delete Chain.harvest (241-245) - job.ts:637 was its only consumer"
        },
        {
          "path": "tests/job.test.ts",
          "description": "delete the jobExtract describe block and its helper setup (1192-1614) whole, not rewritten, per §3"
        },
        {
          "path": "tests/job.integration.test.ts",
          "description": "delete the job-new to run to extract describe block (332-576) whole, not rewritten, per §3"
        },
        {
          "path": "CHANGELOG.md",
          "description": "Breaking entry per §7: flume job extract and Chain.harvest removed"
        }
      ],
      "retire": []
    },
    "summary": "flume job extract and Chain.harvest removed entirely - the clean-history ending becomes an operator git recipe, not engine machinery (§3).",
    "per": {
      "path": "spec/RELEASE-v0.11.md",
      "section": "3. Job verbs shed navigation; extract is removed"
    },
    "tests": [
      {
        "path": "tests/cli.test.ts",
        "asserts": "flume job extract exits with an unrecognized-verb usage error rather than running"
      }
    ],
    "acceptance": "grep -rn 'harvest' src/ is empty; grep -n 'checkout|cherry-pick|branch' src/job.ts hits no git invocations; extract's test suites are deleted, not rewritten; tsc/vitest green.",
    "notes": "Blocked on JOB-BRANCH-GRAMMAR-RETIRED so the full §3 grep is checkable in one pass - both entries touch job.ts, the engine serializes on the overlap. readFrictionFiles' other-caller status is unverified this tick (research flagged 'possibly used elsewhere') - LSP-verify before deleting."
  },
  {
    "tag": "TIP-CLAIM-ADVISORY",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/git.ts",
          "description": "add a git-common-dir resolver (git rev-parse --git-common-dir) and the tip-claim primitive (exclusive-create wx, pid-liveness reclaim) mirroring liveLoopPid's shape (src/job.ts:312-323) - new code, no existing common-dir helper today"
        },
        {
          "path": "src/cli.ts",
          "description": "loop's start (883-919) acquires the tip claim alongside loop.pid, same exit/SIGINT/SIGTERM release hooks; loop and tick both refuse (exit 1) on detached HEAD before any tick per §4; the status block (769-776) reports the claim's holder pid or staleness alongside supervisor liveness"
        },
        {
          "path": "CHANGELOG.md",
          "description": "Added entry per §7: advisory tip claim"
        }
      ],
      "retire": []
    },
    "summary": "Advisory per-ref tip claim (§4): one flume writer per tip, keyed under <git-common-dir>/flume/tip-claims/<ref path>, mirroring the loop.pid lock's acquire/reclaim shape.",
    "per": {
      "path": "spec/RELEASE-v0.11.md",
      "section": "4. Tip claim - one flume writer per tip, advisory"
    },
    "tests": [
      {
        "path": "tests/git.test.ts",
        "asserts": "acquire creates the claim file under <git-common-dir>/flume/tip-claims/<ref path>; EEXIST + live pid refuses naming the holder; EEXIST + dead pid reclaims silently and retries; release removes the file"
      },
      {
        "path": "tests/cli.test.ts",
        "asserts": "two flume loops against different state roots on one branch - second refuses naming the holder pid; two loops from two worktrees on different branches both run; claim file gone after clean exit and after SIGTERM; flume loop on detached HEAD exits 1 before any tick; flume status reports the claim"
      }
    ],
    "acceptance": "§4's acceptance list verbatim: two loops on one branch serialize with a named-holder refusal; two loops on different branches from different worktrees both run; claim file absent after clean exit/SIGTERM; a dead-pid claim reclaims silently; detached-HEAD loop exits 1 before any tick; tsc/vitest green.",
    "notes": "No git-common-dir resolver exists in src/git.ts today. Mirrors src/job.ts:312-323 (liveLoopPid) and src/cli.ts:883-919 (loop.pid acquire/release) structurally; keying differs (per-ref vs per-state-root) so this is a sibling primitive, not a call-site reuse of liveLoopPid itself."
  },
  {
    "tag": "TIP-VERIFY-COMMIT-ONLY-ONTO-STARTING-TIP",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "record the tip sha at each tick-start read already in place (singleton preHead 1028, fanout-entry preHead ~1139, fanout preCherry 1316, pending-ledger preUpdate ~2295); re-verify immediately before each harness-driven commit call (cherryPick ~1318, commitPaths in commitPendingUpdate ~2303-2310) - moved ref aborts with no commit; for the agent's own direct commit (singleton/fanout-worktree), apply the existing guarded-revert idiom to any commit whose parent isn't the recorded tip, per §5's own cited precedent"
        },
        {
          "path": "src/Prompt.ts",
          "description": "TickOutcome/PriorAttempt gain the tip-moved fact per §5 as a sibling to NoCommitMode (53-57), not a fifth member - see notes"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "cover the §5 acceptance fixture: ref advances between agent completion and commit - tip-moved reported, no commit lands, working tree retains agent output; unmoved ref commits exactly as today"
        },
        {
          "path": "CHANGELOG.md",
          "description": "Added entry per §7: tip-moved verify"
        }
      ],
      "retire": []
    },
    "summary": "Tick commits only land on the tip they started on (§5) - a moved ref refuses the commit and reports tip-moved instead of gate-revert/voluntary-bail/platform-preempt.",
    "per": {
      "path": "spec/RELEASE-v0.11.md",
      "section": "5. Tip verify - commit only onto the tip the tick started on"
    },
    "tests": [
      {
        "path": "tests/Dispatcher.test.ts",
        "asserts": "a fixture that advances the ref between agent completion and commit reports tip-moved, produces no commit, and leaves the agent's output on disk; an unmoved ref's tick commits exactly as before; the tip-moved outcome is distinguishable in the verdict a supervisor reads (superviseLoop's errored-tick classification, ~2497)"
      }
    ],
    "acceptance": "§5's acceptance verbatim: ref-advanced fixture reports tip-moved with no commit and retained agent output; unmoved-ref tick is unchanged; tip-moved is distinguishable in the tick verdict; tsc/vitest green.",
    "notes": "Architecturally split by path: fanout has a real harness-side commit step (cherryPick/commitPaths) to gate cleanly; singleton/fanout-worktree-direct agent commits already exist by the time the harness observes them (the agent runs `git commit` itself), so those need the guarded-revert idiom §5 explicitly cites as its own precedent, not a pre-commit gate. Functionally independent of TIP-CLAIM-ADVISORY - §5's backstop stands alone per spec."
  },
  {
    "tag": "DOCS-JOB-EXTRACT-RETIRED",
    "gate": {
      "kind": "blockedBy",
      "tag": "JOB-EXTRACT-REMOVED"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "docs/MIGRATING-0.11.md",
          "description": "model docs/MIGRATING-0.8.md's shape (audience para, numbered upgrade sections, symptom-to-cause table, Non-goals close); cover integrate-or-abandon for live job/<name> branches via ordinary git then delete the branch, plus the extract-replacement recipe (side branch + operator integration) per §6"
        }
      ],
      "edit": [
        {
          "path": "README.md",
          "description": "rewrite the branch-is-the-job framing (103, 229-276) - jobs as coexisting state roots on whatever branch, no branch guidance beyond 'run on whatever branch you want the record on', worktrees as the parallelism recipe, extract's mention dropped"
        },
        {
          "path": "docs/CLI.md",
          "description": "rewrite job new/run/rm/status (13, 73-126) off branch language; delete the job extract section (134-147) entirely"
        },
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "delete the Chain.harvest field doc (59, 77-80)"
        }
      ],
      "retire": []
    },
    "summary": "README.md/docs/CLI.md job-branch+extract sections rewritten, docs/CHAIN-AUTHORING.md drops harvest, new docs/MIGRATING-0.11.md ships the branch-integration and extract-replacement recipes (§6).",
    "per": {
      "path": "spec/RELEASE-v0.11.md",
      "section": "6. Docs"
    },
    "tests": [],
    "acceptance": "grep -rin 'extract' README.md docs/CLI.md docs/CHAIN-AUTHORING.md is empty; MIGRATING-0.11.md contains the branch-integration and extract-replacement recipes (§6 acceptance verbatim).",
    "notes": "docs/MIGRATING-0.8.md (only existing precedent) is the shape to model - audience para, numbered sections, symptom/cause table, Non-goals. This entry covers only the branch/extract half of §6; tip-claim/verify's doc facts are DOCS-TIP-CLAIM-VERIFY, since README/CLI.md need both edits independently."
  },
  {
    "tag": "DOCS-TIP-CLAIM-VERIFY",
    "gate": {
      "kind": "blockedBy",
      "tag": "TIP-VERIFY-COMMIT-ONLY-ONTO-STARTING-TIP"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "README.md",
          "description": "add the tip claim/verify facts an operator may hit - one advisory claim per ref, worktrees as the way to run jobs hot simultaneously (§4), a tip-moved tick outcome (§5)"
        },
        {
          "path": "docs/CLI.md",
          "description": "document flume status's tip-claim line and the tip-moved exit/verdict behavior"
        }
      ],
      "retire": []
    },
    "summary": "README.md/docs/CLI.md document the tip claim and tip-moved verify as operator-facing facts (§6).",
    "per": {
      "path": "spec/RELEASE-v0.11.md",
      "section": "6. Docs"
    },
    "tests": [],
    "acceptance": "README.md and docs/CLI.md describe the tip claim and tip-verify behavior a real operator invocation now produces.",
    "notes": "Blocked on TIP-VERIFY-COMMIT-ONLY-ONTO-STARTING-TIP rather than TIP-CLAIM-ADVISORY only because gate.blockedBy takes one tag; TIP-CLAIM-ADVISORY should also ship first in practice - both are open today, confirm both landed before writing this."
  },
  {
    "tag": "BUILTINGATES-PNPM-HARDCODED-NO-OVERRIDE",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/builtinGates.ts",
          "description": "give tscGate/vitestGate/eslintGate (122, 135, 148) an optional cmd override, defaulting to 'pnpm' - chain supplies the package manager, engine supplies the enforcement"
        },
        {
          "path": "tests/builtinGates.test.ts",
          "description": "cover the override - a non-default cmd used instead of pnpm; omitted cmd behaves exactly as today"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "tscGate/vitestGate/eslintGate hardcode cmd: 'pnpm' with no override - a non-pnpm chain can't reuse these builtins at all, must hand-roll via shellGate.",
    "per": {
      "path": ".claude/rules/engine-boundary.md",
      "section": "Capability vs convention"
    },
    "tests": [
      {
        "path": "tests/builtinGates.test.ts",
        "asserts": "tscGate/vitestGate/eslintGate accept an optional cmd override that replaces 'pnpm' in the invoked command; omitting it reproduces today's exact shellGate options"
      }
    ],
    "acceptance": "A chain can get tscGate/vitestGate/eslintGate with npm/yarn/bun's invocation instead of pnpm without hand-rolling shellGate from scratch; default (no override) behavior is byte-identical to today; tsc/vitest green.",
    "notes": "Sweep finding, src/builtinGates.ts neighborhood (bootstrap rotation - code delta armed it, no prior stamp covered this file). Same shape as the already-pending PENDING-GATE-HINT-OPTION (missing injection point on a builtin gate) but a different field - the command itself, not the failure hint. All three re-exported via src/index.ts:31-37, presented as reusable builtins in examples/cascade-chain.ts."
  },
  {
    "tag": "SESSION-CAPTURE-DEFAULT-FILENAME-COLLIDES",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Agent.ts",
          "description": "defaultCaptureFilename (262-264) discriminates per invocation, not by clock alone; the SessionCaptureOpts docstring (217-222) claims only what the code holds"
        },
        {
          "path": "tests/Agent.test.ts",
          "description": "first coverage of the default-filename path - both capture tests today supply an explicit filename"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "withSessionCapture's default filename is clock-only, so two fanout captures opening in the same millisecond share one path and one transcript is silently lost.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Loud or nothing"
    },
    "tests": [
      {
        "path": "tests/Agent.test.ts",
        "asserts": "two withSessionCapture invocations with distinct cwds under a frozen clock (vi.setSystemTime) land in two files, each holding its own stream - fails on the pre-fix tree, where the second createWriteStream truncates the first"
      }
    ],
    "acceptance": "Concurrent captures under a stopped clock produce one file per invocation; the docstring's no-collision claim is the one the test pins.",
    "notes": "Untested today - both capture tests pass filename, and this repo's chain does too (.flume/chain.ts:316-320 appends the cwd basename), so the collision only reaches a downstream chain taking the default. The chain's own filename is the shape the default should have had. Chain-overridable default, so no boundary question."
  },
  {
    "tag": "RENDER-RELATIVIZE-SEPARATOR-BLIND",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Agent.ts",
          "description": "relativize (459-463) strips the cwd prefix on either separator instead of hardcoding '/'"
        },
        {
          "path": "tests/Agent.test.ts",
          "description": "file_path rendering has no coverage today; add the backslash-cwd case beside the POSIX one"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "withTerminalRenderer's relativize hardcodes '/', so on win32 every Read/Write/Edit line renders an absolute path while the tag beside it shortens correctly.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "The fix lands at the mechanism"
    },
    "tests": [
      {
        "path": "tests/Agent.test.ts",
        "asserts": "a tool_use whose file_path sits under a backslash cwd renders relative, and the POSIX pair keeps today's output - separator-agnostic, so it fails on the pre-fix tree on every platform"
      }
    ],
    "acceptance": "Read(<file>) renders relative under a backslash cwd and a POSIX cwd alike.",
    "notes": "The sibling detection exists: the default tag (307) uses node:path basename and carries a win32-gated regression test (tests/Agent.test.ts:473). path.relative is platform-bound - it would make the case win32-only again; stripping on either separator keeps it runnable everywhere. Display-only: the rendered line, never the captured transcript."
  },
  {
    "tag": "PENDING-GATE-HINT-OPTION",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/builtinGates.ts",
          "description": "add optional hint?: string to PendingGateOptions, appended to both violation messages"
        },
        {
          "path": "tests/builtinGates.test.ts",
          "description": "cover hint present/absent on both violation paths"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "pendingGate has no injection point for chain-authored operator guidance on a violation - a bay's hand-rolled fork carried this, the builtin drops it.",
    "per": {
      "path": ".claude/rules/engine-boundary.md",
      "section": "Capability vs convention"
    },
    "tests": [
      {
        "path": "tests/builtinGates.test.ts",
        "asserts": "hint text appended verbatim to both schema-violation and fence-violation failure output when supplied; output unchanged from today when omitted"
      }
    ],
    "acceptance": "pendingGate({ ..., hint: 'park it, never re-scope' }) echoes the hint in both failure branches; default behavior unchanged.",
    "notes": "Passes the second-implementation test - chain supplies text, engine supplies enforcement. Keep minimal: one string, no per-violation-class customization; richer needs stay a chain-side run() wrap. CHANGELOG.md added this tick: the entry touched src/ without declaring it, which changelogGate would have reverted."
  },
  {
    "tag": "GIT-DEAD-EXPORTS-RETIRE",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/git.ts",
          "description": "delete softReset (45), commitAll (198), isDirty (226) and the commitPaths comment clause naming commitAll (211); the header's 'nine commands' count (4) goes with them - isDirty is the module's only `git status` consumer, so the count is 8 after this"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "git.ts exports softReset, commitAll, and isDirty with zero consumers - LSP resolves one reference each, its own declaration.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "An export earns its consumer"
    },
    "tests": [],
    "acceptance": "src/git.ts holds only functions with a live caller; tsc and vitest green.",
    "notes": "LSP findReferences this tick: git.ts:45, :198, :226 each resolve to their own declaration alone. index.ts does not re-export git.ts, so none is declared public surface. commitPaths superseded commitAll - the :211 comment naming it goes too. A deletion pins nothing decidably; the green suite is the proof (the named exception). files.retire stays empty: the fence reads every element as a path."
  },
  {
    "tag": "RUNTIME-IGNORES-LOOP-VACUITY-PIN",
    "gate": {
      "kind": "blockedBy",
      "tag": "JOB-EXTRACT-REMOVED"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/job.test.ts",
          "description": "the three `for (const entry of RUNTIME_IGNORES)` verdicts (205-207, 287-289, 552-554) show the judged set populated before reading it"
        }
      ],
      "retire": []
    },
    "summary": "Three .gitignore-merge tests read their verdict off a loop over RUNTIME_IGNORES - an emptied constant leaves each one green over zero assertions.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A green verdict is proven non-vacuous"
    },
    "tests": [
      {
        "path": "tests/job.test.ts",
        "asserts": "each RUNTIME_IGNORES loop is preceded by a populated-set assertion, so an emptied constant fails the merge tests themselves instead of passing over nothing"
      }
    ],
    "acceptance": "No assertion in tests/job.test.ts reads a verdict off a set it has not shown to be populated.",
    "notes": "The pinned behavior (jobNew ignore-merge) survives v0.11 unchanged (job.ts:34/177/219/639), but its three cited lines sit inside tests/job.test.ts's jobNew describe (271-506), which JOB-BRANCH-GRAMMAR-RETIRED rewrites and JOB-EXTRACT-REMOVED further churns - landing this pin before both land would duplicate the work. Blocked on the later of the two."
  },
  {
    "tag": "ERA-SCOPED-NARRATION-PROMPT-NEIGHBORHOOD",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Prompt.ts",
          "description": "lines 47-48, 142, 252 - state the condition or point at the pin"
        },
        {
          "path": "src/builtinGates.ts",
          "description": "lines 77 and 250-251 - same"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "Prompt.ts neighborhood carries era-scoped narration: three 'byte-identical to past release' claims a test already pins, plus a clause citing two shipped-and-gone pending tags.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [],
    "acceptance": "Each cited site states the condition that would change the decision or points at the test holding it; no prose restates a property a test already pins.",
    "notes": "Pinned already, so prose shrinks to a pointer: Prompt.ts:142/:252 by tests/Prompt.test.ts:144; builtinGates.ts:77 by tests/Gate.test.ts:61. builtinGates.ts:250-251 cites GATECONTEXT-REPOROOT-TESTS / CLI-JUNCTION-SAFE-ENTRY-TESTS, both shipped and unresolvable today. Prompt.ts:47-48 is the same shape - and v0.10 §3 rewrites that exact docstring, hence the block; re-read every line number after it lands. Prose-only; no behavior moves."
  },
  {
    "tag": "ERA-SCOPED-NARRATION-DISPATCHER-NEIGHBORHOOD",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "lines 626-632, 2294, 2311, 2392-2393 - state the condition or point at the pin"
        },
        {
          "path": "src/Phase.ts",
          "description": "lines 169-170 and 268-269 - same"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "Dispatcher neighborhood carries era-scoped narration: three 'byte-identical to v0.7 §16' claims tests already pin, plus three sites narrating mechanisms that no longer exist.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [],
    "acceptance": "Each cited site states the condition that would change the decision or points at the test holding it; no prose restates a property a test pins or a mechanism the tree no longer has.",
    "notes": "Pinned already: tests/Dispatcher.test.ts:4202 (undeclared abortThreshold still aborts on the 3rd consecutive tick), :4117 (quarantineScope default). Dispatcher.ts:626-632 narrates a deleted content-hash cache; 2392-2393 compares against the removed in-process loop. Phase.ts:169-170 'existing void-returning implementations are unaffected' is migration-era under the pre-1.0 clean-slate posture. Prose-only; no behavior moves."
  },
  {
    "tag": "DISPATCHER-RENDER-REFUSED-CATCH-UNSHARED",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "extract the render-refused catch bodies (1048-1053 singleton, 1629-1634 fanout) into one shared private method beside classifyNoCommit (2213) that persists the §5 record and logs; each call site keeps constructing its own return shape, matching how classifyNoCommit's two callers already differ"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "add the agreement pin - singleton and fanout render-refused paths persist byte-identical §5 record content and emit the same-shaped log line for equivalent InlineExecRenderError input, driven through the one shared method"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "Dispatcher's render-refused catch (persist §5 record + log) is duplicated inline at both singleton (1038) and fanout (1617) callsites, instead of routing through classifyNoCommit's shared shape.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "The fix lands at the mechanism"
    },
    "tests": [
      {
        "path": "tests/Dispatcher.test.ts",
        "asserts": "singleton and fanout render-refused catches persist byte-identical mode+failures text and the same-shaped log line for equivalent input - a one-sided edit to either site's persisted record or log text breaks the pin"
      }
    ],
    "acceptance": "One shared private method persists the render-refused record and logs it; both singleton and fanout callsites call it instead of re-deriving the write+log inline; tsc/vitest green.",
    "notes": "Verified this tick: classifyNoCommit (2213) is the shared method both runSingleton (1112) and the fanout path (1654) call for voluntary-bail/platform-preempt persist+log. render-refused (9dffc51) skips this precedent - duplicated inline at 1038-1065 (singleton) and 1617-1635 (fanout), each re-deriving writePriorAttempt+log.warn+return. Identical today, so nothing fails pre-fix (the named exception); the pin catches a future one-sided drift."
  },
  {
    "tag": "LOOP-LOCK-SHARES-LIVELOOPPID",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/cli.ts",
          "description": "the loop lock (885-902) takes liveness from liveLoopPid — already imported at :34 — instead of re-deriving the probe inline"
        },
        {
          "path": "src/job.ts",
          "description": "liveLoopPid docstring (306-311) names the lock among its consumers; its reuse claim only becomes true with this entry"
        },
        {
          "path": "tests/cli.test.ts",
          "description": "add the agreement pin to the §2a loop-lock describe (651); the :800 status-liveness describe already narrates the sharing"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "flume loop's lock re-derives liveLoopPid's pid probe inline — the helper's own docstring says it was exported to prevent exactly this second implementation.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "The fix lands at the mechanism"
    },
    "tests": [
      {
        "path": "tests/cli.test.ts",
        "asserts": "the loop lock and `flume status` return the same liveness verdict over one live and one stale pidfile — one probe, so a future one-sided change breaks it"
      }
    ],
    "acceptance": "Liveness reaches the loop lock through liveLoopPid; no second process.kill pid probe remains in src/cli.ts; the loop-lock and status-liveness suites stay green.",
    "notes": "Verified this tick: cli.ts:886-895 repeats liveLoopPid's read/finite/positive/kill(pid,0) sequence; job.ts:306-311 claims the opposite. Drop-in — the lock wants the pid only for its refusal message, which liveLoopPid returns, and the block is already async. Behavior-identical today, so nothing fails pre-fix (the named exception); the agreement pin is what makes a later divergence fail."
  },
  {
    "tag": "PARSEPENDING-DUP-ERROR-MAPPING",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/PendingSchema.ts",
          "description": "extract the JSON.parse-into-ParseError wrap (288-303, 331-346) and the zod-issues-to-ParseError[] map (310-318, 359-367) into one shared helper; parsePending and parsePendingLoose both call it instead of each re-deriving the same two blocks"
        },
        {
          "path": "tests/PendingSchema.test.ts",
          "description": "mechanism pin beside the existing single-call-site test (451-463) — same source-text-scan idiom"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "parsePending and parsePendingLoose duplicate the invalid-JSON wrap and the zod-issues-to-ParseError[] mapping verbatim instead of sharing one implementation.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "The fix lands at the mechanism"
    },
    "tests": [
      {
        "path": "tests/PendingSchema.test.ts",
        "asserts": "the literal `invalid JSON: ` error-message construction occurs exactly once in the module source after the refactor, not twice — a future fix to one path can no longer silently diverge from the other"
      }
    ],
    "acceptance": "One helper builds the invalid-JSON ParseError and one helper maps zod issues to ParseError[]; both parsePending and parsePendingLoose call them; both functions' error output is byte-unchanged; tsc/vitest green.",
    "notes": "Sweep finding, tests/PendingSchema.test.ts neighborhood (frontier: 7e71fcc touched this file; import src/PendingSchema.ts read with it). Verified on disk this tick: PendingSchema.ts:288-303 and :331-346 are identical JSON.parse try/catch blocks; :310-318 and :359-367 are identical issue-mapping loops. Behavior-identical today, so nothing fails pre-fix (the named exception) — the pin catches a future one-sided drift, same shape as DISPATCHER-RENDER-REFUSED-CATCH-UNSHARED."
  },
  {
    "tag": "ERA-SCOPED-NARRATION-JOB-NEIGHBORHOOD",
    "gate": {
      "kind": "blockedBy",
      "tag": "JOB-EXTRACT-REMOVED"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/job.ts",
          "description": "lines 48, 417, 570, 793 - state the condition or drop the retired framing"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "job.ts carries era-scoped narration: a cite to the shipped-and-gone JOB-RESOLUTION tag, two pre-0.9 migration clauses, and a superseded harvest convention.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [],
    "acceptance": "No site in src/job.ts cites an unresolvable pending tag or scopes itself to a closed migration window; no behavior moves.",
    "notes": ":570/:793 sit inside extract, which JOB-EXTRACT-REMOVED deletes outright; :417's pre-0.9 clause is in jobRm, which JOB-BRANCH-GRAMMAR-RETIRED rewrites. Only :48 (JOB-RESOLUTION cite) survives both untouched. Re-verify line numbers and re-scope to :48 alone (or drop) once JOB-EXTRACT-REMOVED lands."
  },
  {
    "tag": "CLI-RESOLVESTATEDIRS-DOCSTRING-ORPHANED",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/cli.ts",
          "description": "move the 77-105 block down onto resolveStateDirs (126); resolveRepoRoot keeps its own 107-114 block. A move, not a rewrite - the text is accurate for its subject"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "resolveStateDirs' 29-line docstring is detached - a second docstring intervenes, so it documents nothing and the v0.6 §3 resolution authority is invisible to hover.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [],
    "acceptance": "Hover on resolveStateDirs shows the job-resolution semantics; hover on resolveRepoRoot is unchanged.",
    "notes": "LSP hover this tick: resolveStateDirs (126) returns the signature alone, no docs; resolveRepoRoot (115) returns only its own block. The 77-105 text sits between JobResolutionConflictError (75) and resolveRepoRoot's docstring, separated by a blank line, so TS attaches it to nothing - the conflict rule, the write-back rationale, and the FLUME_JOB composition case all read as current while reaching no consumer. Prose-only."
  },
  {
    "tag": "GATECONTEXT-REPOROOT-REQUIRED",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Gate.ts",
          "description": "repoRoot (52) becomes required; the 45-51 docstring drops the fixture-compat clause and states the field's meaning alone"
        },
        {
          "path": "tests/Gate.test.ts",
          "description": "the ctx helper (23-31) sets repoRoot; delete the 294-304 describe, which asserts only that object spread works"
        },
        {
          "path": "tests/builtinGates.test.ts",
          "description": "same one-line ctx helper change (21)"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit; Breaking - a hand-built GateContext must now set repoRoot"
        }
      ],
      "retire": []
    },
    "summary": "GateContext.repoRoot is optional only so pre-v0.7 fixtures compile - the engine always sets it, so the type advertises a state no producer creates.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [
      {
        "path": "tests/Gate.test.ts",
        "asserts": "the existing gate suites keep passing over a repoRoot-bearing ctx; no assertion remains whose only subject is the local fixture builder"
      }
    ],
    "acceptance": "GateContext.repoRoot is non-optional, tsc and vitest are green, and no docstring scopes the field to a closed fixture-migration window.",
    "notes": "Verified this tick: every dispatcher-constructed context sets repoRoot (Dispatcher.ts:1219, 1324, 1358, 1752, 1900); the only undefined producers are the two test ctx helpers. v0.7 §6 mandates the field, not its optionality. tsc's required-field check is the pin - no runtime test fails pre-fix (the named exception); the real §6 behavior stays pinned at tests/Dispatcher.test.ts:4459. Chain gates currently branch on a state the engine never emits."
  },
  {
    "tag": "ERA-SCOPED-NARRATION-BARREL-NEIGHBORHOOD",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/index.ts",
          "description": "line 3 - the exports map exists and does enforce; state the condition or drop the clause"
        },
        {
          "path": "src/setupWorktree.ts",
          "description": "lines 4-10 - drop the pre-adoption 'has been hand-rolling' rationale for what the helper does"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "index.ts and setupWorktree.ts carry expired narration: a 'no exports map yet' claim package.json contradicts, and a hand-rolling rationale the dogfood chain retired.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [],
    "acceptance": "Neither site claims a condition the tree contradicts; no behavior moves.",
    "notes": "Fourth sibling of the ERA-SCOPED-NARRATION-* family. Verified this tick: package.json:26-31 declares an exports map exposing '.' alone, so index.ts:3's 'not enforced - there's no exports map yet' fired its own trigger. setupWorktree.ts:5-7 names flume's dogfood buildSetupWorktree as a hand-rolled hardcode; chain.ts:50 imports the engine helper, :197-201 delegates to it, and :192-193 says so outright. Prose-only."
  },
  {
    "tag": "CLI-TEST-DOCBLOCKS-ORPHANED",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/cli.test.ts",
          "description": "move the 402-412 block onto the describe it heads (651), and the 585-600 block onto its own describe holding the two v0.8 §8 tests (716-793) that today sit under a title naming the loop lock; the four src/cli.ts cites (425, 512, 937, 1083) name symbols, not lines"
        }
      ],
      "retire": []
    },
    "summary": "Two suite doc blocks sit ~200 lines above their subjects, one claiming what is false of the tests it now heads; all four src/cli.ts line cites in the file point at unrelated lines.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [],
    "acceptance": "Each doc block sits on the describe it describes; no cite in the file names a line number; the suite is green and no test moves between files.",
    "notes": "Verified on disk this tick: isInvokedDirectly is cli.ts:1041 not :854; the render CjsContextLoadError catch is :966 not :872-881; runJobVerb is :449 not :356-387; frictionCountLine is Dispatcher.ts:509, not cli.ts at all. The 402-412 block's 'no chain.ts, no git repo, no child processes' is false of the two supervisorPolicy tests it now heads. Prose + placement only; no assertion changes."
  },
  {
    "tag": "DISPATCHER-STREAMJSON-PARSE-DUP",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Agent.ts",
          "description": "factor renderStreamJsonLine's line-parse + assistant-content-block extraction (346-380) into two shared helpers - parse-one-NDJSON-event, and content-blocks-of-type from an assistant event's message.content - exported for Dispatcher.ts"
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "finalAgentMessage/assistantTurnText (2735-2789) call the shared Agent.ts helpers instead of re-deriving the same JSON.parse+type-dispatch+content-block-walk"
        },
        {
          "path": "tests/Agent.test.ts",
          "description": "mechanism pin for the extracted helpers, beside the existing withTerminalRenderer describe (368)"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "the existing stream-json voluntary-bail test (2985-3132) rides the refactor unchanged - proves it stays byte-identical"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "Dispatcher's finalAgentMessage/assistantTurnText re-derive the same NDJSON line-parse and message.content block-walk that Agent.ts's renderStreamJsonLine already implements for terminal rendering.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "The fix lands at the mechanism"
    },
    "tests": [
      {
        "path": "tests/Agent.test.ts",
        "asserts": "the extracted NDJSON-event-parse and content-blocks-of-type helpers are used by both renderStreamJsonLine (tool_use blocks) and a stand-in caller shaped like Dispatcher's text-block extraction - a one-sided change to either consumer's block-type filter doesn't touch the shared parse"
      }
    ],
    "acceptance": "One shared pair of helpers in src/Agent.ts parses an NDJSON line and extracts message.content blocks by type; both withTerminalRenderer's renderStreamJsonLine and Dispatcher's finalAgentMessage/assistantTurnText call them instead of re-deriving the parse; tests/Dispatcher.test.ts:2985-3132 stays green unchanged; tsc/vitest green.",
    "notes": "Sweep finding, tests/Dispatcher.test.ts neighborhood (Agent.ts read as its import). Verified on disk: Agent.ts:353-372 and Dispatcher.ts:2740-2789 both JSON.parse a line, check evt is an object, read .type, and for \"assistant\" walk message.content[] by block .type - one keeps tool_use (rendering), the other keeps text (bail message). Agent.ts already owns stream-json awareness via the decorator; Dispatcher.ts re-deriving the parse is the duplication, not the awareness itself."
  },
  {
    "tag": "DISPATCHER-COMMIT-MESSAGE-PREFIX-HARDCODED",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "commitPendingUpdate's git.commitPaths call (2303-2310) takes its message from a new optional DispatcherOptions field (e.g. commitMessage), defaulting to today's exact 'chore(flume): ship ...' / 'chore(flume): record merge-failure footprints for ...' text"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "cover the override producing a custom commit message; the omitted-option case stays byte-identical to today"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "commitPendingUpdate hardcodes the \"chore(flume): ...\" commit-message text with no chain override point - a non-Flume-convention chain can't choose its own ship-commit message.",
    "per": {
      "path": ".claude/rules/engine-boundary.md",
      "section": "Capability vs convention"
    },
    "tests": [
      {
        "path": "tests/Dispatcher.test.ts",
        "asserts": "a DispatcherOptions-supplied commit-message override lands verbatim in the pending.json ship commit; omitting the option reproduces today's exact 'chore(flume): ship ...' / 'chore(flume): record merge-failure footprints for ...' text"
      }
    ],
    "acceptance": "A chain can override the pending-ledger ship-commit message text; default (no override) behavior is byte-identical to today; tsc/vitest green.",
    "notes": "Verified on disk this tick: Dispatcher.ts:2303-2310 hardcodes \"chore(flume): ship ...\" / \"chore(flume): record merge-failure footprints...\" with no override. job.ts:221/407 carry the identical literal but sit outside this neighborhood and are due for rewrite under JOB-BRANCH-GRAMMAR-RETIRED/JOB-EXTRACT-REMOVED - leave those for a later sweep once that churn lands, to avoid rework. DispatcherOptions already carries chain-overridable knobs of this shape (namespace, chainLoader)."
  }
]
