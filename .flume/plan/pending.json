[
  {
    "tag": "EXAMPLE-PLAN-PROMPT-DROPS-THE-RETIRED-SPEC-PARTITION",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        { "path": "examples/prompts/plan.md", "description": "collapse the two partition spans onto the corpus root cascade's `per` hint names" },
        { "path": "tests/examples.test.ts", "description": "absence pin beside the existing cascade phase-list and span-rooting blocks" }
      ],
      "retire": []
    },
    "summary": "Retire the cut spec phase's specs/active + specs/_aligned partition from the shipped plan template",
    "per": { "path": ".claude/rules/engineering.md", "section": "An export earns its consumer" },
    "acceptance": "No shipped example prompt names a partition segment the cut spec phase administered; the template's corpus span agrees with cascade's `per` hint root.",
    "tests": ["no shipped example prompt spans the retired specs/active or specs/_aligned partition"],
    "notes": "Residue of 58092d0 (EXAMPLES-SPEC-PHASE-CUT): the fence and phase list lost `specs/**`, the prompt did not. Both spans render empty and silently — `|| echo` never fires behind `head` (spec/prompt.md, *An unresolved inline-exec span fails the tick*: exit status decides). Cascade still declares a corpus (header doc, `per` hint `specs/.../foo.md`), so collapse to one span over it rather than deleting the material."
  }
]
