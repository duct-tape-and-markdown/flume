[
  {
    "tag": "LOOPSUPERVISOR-DEFAULTS-CASE-COVERS-BOTH-KNOBS",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/loopSupervisor.test.ts",
          "description": "the neither-knob defaults case exercises the quarantine default it is titled for"
        },
        {
          "path": "src/loopSupervisor.ts",
          "description": "repoint both option docs' quoted-title cite at the case that pins them"
        }
      ],
      "retire": []
    },
    "summary": "make the supervisor's neither-knob defaults case assert the quarantine default its title claims, and repoint the two option docs at it",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A green verdict is proven non-vacuous"
    },
    "acceptance": "the case titled for both supervisor-policy defaults drives a tagged failure through an undeclared quarantineScope and asserts the hold; src/loopSupervisor.ts's quarantineScope and abortThreshold docs both cite that title verbatim",
    "pins": [
      "a chain declaring neither supervisor knob gets both defaults: a run-scoped quarantine and a three-tick abort"
    ],
    "notes": "tests/loopSupervisor.test.ts:1368 titles both knobs but its runTick takes no quarantinedSlugs and its provisionFailures carry no tag, so src/loopSupervisor.ts:364-366 does nothing; src/loopSupervisor.ts:76-78 and :89 nonetheless cite that title as the quarantine default's pin. The real default-quarantine pin is the unrelated case at :682."
  }
]
