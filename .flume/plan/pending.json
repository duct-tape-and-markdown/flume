[
  {
    "tag": "TOLERANT-READ-WARNS-NAME-THE-LEDGER-PATH",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "src/pendingLedger.ts",
          "description": "the three readPendingTolerant degrade warns take reportedPendingPath(ctx) instead of a hardcoded basename"
        },
        {
          "path": "tests/pendingLedger.test.ts",
          "description": "one arm per degrade, over a ctx whose pendingPath is not the default"
        }
      ]
    },
    "summary": "readPendingTolerant's stat, read and parse warns name the chain's declared pending path, not a literal `pending.json`",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A fact the engine holds is reported, never rediscovered"
    },
    "acceptance": "No warn site in src/pendingLedger.ts spells the ledger file by hand; each degrade announcement spells the reported path. Suite green.",
    "tests": [
      "a tolerant read's stat, read and parse warns each name the chain's declared pending path"
    ],
    "notes": "Observed beside LEDGER-REFUSAL-NAMES-THE-UNCOMMITTED-QUEUE-REWRITE, which introduced the reporter one module over. A chain declaring Chain.pendingPath elsewhere is warned about a file it does not have."
  },
  {
    "tag": "CLI-DOC-STATES-ITS-EXIT-CODE-VERB-CONVENTION",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "docs/CLI.md",
          "description": "the page intro states the introducing-verb convention its exit-code readers depend on, in one line"
        },
        {
          "path": "tests/cliHelp.test.ts",
          "description": "the every-verb EX_IOERR membership assertion's message names what the page owes"
        }
      ]
    },
    "summary": "State docs/CLI.md's introducing-verb convention on the page, and name it in the every-verb exit-74 assertion's failure message",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "acceptance": "docs/CLI.md's intro states the convention in one line; the every-verb 74 membership assertion fails with a message naming it. Suite green, and the reader's value-vs-code discrimination is unchanged.",
    "notes": "Operator ruling (e1a82004, inbox 2026-09-22-two-questions-ruled): options 3 and 2 together, list continuation declined. The convention lives today only in NAMED_EXIT_CODE's doc comment, where the page's author never reads it. Failure-path prose plus a page line, so no tests[] line is judgeable."
  },
  {
    "tag": "MIGRATING-0.18-OPENS-WITH-THE-SUSPECTFLAKE-BREAK",
    "gate": { "kind": "open" },
    "files": {
      "new": [
        {
          "path": "docs/MIGRATING-0.18.md",
          "description": "the series' next note, opening on what it does not cover and walking the GateRevertAttempt.suspectFlake removal with before/after"
        }
      ],
      "edit": [
        {
          "path": "README.md",
          "description": "the note joins the upgrade-note listing"
        }
      ]
    },
    "summary": "Open docs/MIGRATING-0.18.md, walking GateRevertAttempt.suspectFlake's removal with before and after",
    "per": { "path": "spec/cli.md", "section": "Versioning policy" },
    "acceptance": "docs/MIGRATING-0.18.md exists, names the note before it in the series, and walks the suspectFlake removal with before/after for a chain whose shouldRun read it. Suite green.",
    "pins": [
      "every docs/MIGRATING page is linked from README.md or CHANGELOG.md"
    ],
    "notes": "The break shipped at d2a33ed3 with no note: its entry's acceptance demanded the name appear nowhere in docs/, which the note contradicts. Breaks landing on 0.18 after this one join the page as they ship; the full census is the cut's curation."
  },
  {
    "tag": "BARE-FS-CALL-REPORTS-ITS-UNMASKED-ARGUMENT",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "tests/helpers/namespacedFsScan.ts",
          "description": "splitArguments answers spans; a bare call's reported argument is sliced from the unmasked source"
        },
        {
          "path": "tests/namespacedFsPaths.test.ts",
          "description": "the rendering fixture takes a path whose segments are string literals"
        }
      ]
    },
    "summary": "A bare fs call's reported argument is sliced from the unmasked source, so a path composed of string literals prints instead of blanks",
    "per": { "path": ".claude/rules/engineering.md", "section": "Loud or nothing" },
    "acceptance": "describeBareCall over a join carrying string literals renders those literals; every live-tree verdict is unchanged.",
    "tests": [
      "a bare fs call's reported argument prints the string literals its path composes"
    ],
    "notes": "Found by FOUR-MORE-SCANS-ADOPT-THE-FINDINGS-RENDERING's fixture, which sidesteps it with identifier segments and says so at the site. The masking is the scan's own, so the real span is gone before the report is built; this changes the reporting, not any verdict."
  }
]
