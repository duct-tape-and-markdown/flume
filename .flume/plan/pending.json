[
  {
    "tag": "INTEGRATION-LANE-WAITS-ARE-EVENT-BASED",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [
        {
          "path": "tests/helpers/waitFor.ts",
          "description": "one event-based wait every integration pre-assertion sync point uses, per spec/worktrees.md *The default test lane must stay fast*"
        },
        {
          "path": "tests/waitForHelper.test.ts",
          "description": "default-lane cover for the helper — the named tests[] lines"
        }
      ],
      "edit": [
        {
          "path": "tests/loop-process-boundary.integration.test.ts",
          "description": "the 800ms pre-assertion sleep becomes the event wait"
        },
        {
          "path": "tests/tip-claim.integration.test.ts",
          "description": "same two sites, green only by luck today"
        }
      ],
      "retire": []
    },
    "summary": "Replace the integration lane's fixed pre-assertion sleeps with an event-based wait; the 800ms one reds the tip-claim case on every full-lane run",
    "per": {
      "path": "spec/worktrees.md",
      "section": "The default test lane must stay fast"
    },
    "acceptance": "A whole-lane `pnpm test:integration` passes, not only per-file; no fixed sleep stands between a spawn and an assertion on its effect.",
    "tests": [
      "the wait helper resolves as soon as its predicate holds rather than waiting out its deadline",
      "the wait helper rejects naming what it waited for when its deadline passes"
    ],
    "notes": "Reproduced this tick both ways: whole lane reds at loop-process-boundary.integration.test.ts:496 (`expected false to be true`, 853ms) — the claim file has not appeared 800ms after the spawn under four-file contention; the file alone passes (that case 3355ms). The probe agents' own in-invoke sleeps are the window, not a sync point, and stay. The failure message is the second defect: a bare boolean says nothing about what was awaited."
  },
  {
    "tag": "ERA-SCOPED-NARRATION-CUT",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "six sites: three `to today`, `the plain v0.2`, `identical to v0.2` (describe + comment)"
        },
        {
          "path": "tests/Gate.test.ts",
          "description": "`byte-identical to today`, `matching current behavior`"
        },
        {
          "path": "tests/job.test.ts",
          "description": "`identically to today's plan/pending.json`"
        },
        {
          "path": "tests/partition.test.ts",
          "description": "`byte-identical to today`"
        },
        {
          "path": "tests/PendingSchema.test.ts",
          "description": "`preserves v0.2 pickability`"
        },
        {
          "path": "src/builtinGates.ts",
          "description": "doc comment quotes the Gate.test.ts title verbatim — moves in the same commit"
        },
        {
          "path": "src/git.ts",
          "description": "`the v0.6.1 dogfood symptom` — the symptom stays, the release label goes"
        },
        {
          "path": "README.md",
          "description": "`the only isolation primitive in v0.1`"
        },
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "`v0.1 ships one implementation`"
        }
      ],
      "retire": []
    },
    "summary": "Restate the fifteen era-scoped narration sites as the condition that would change the decision; third debt acceptance of this family in the rotation",
    "per": {
      "path": ".claude/rules/posture-sweep.md",
      "section": "Routing"
    },
    "acceptance": "No named site scopes its claim to a release line or to `today`; every test body and every stated fact is unchanged, and `src/builtinGates.ts`'s quoted title still matches the title it quotes.",
    "notes": "Claims no property by the filing clause. Count read off `git log` from the rotation cursor b7972ec: accepted as debt in 05b9ae1, 98dd180, 23ca8d6. Sites re-verified on this tree, not carried from those bodies — the release-cite cut moved every line number. Excluded deliberately: README.md's `## Status` `**v0.1**` label (the section's subject, and a factual-staleness question against 0.15.0, not narration drift); docs/ numbered §-cites, ruled archival at 1a8bb8e."
  }
]
