[
  {
    "tag": "VERDICT-MERGEOUTCOMES-DOC-NOT-FANOUT-ONLY",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/tickVerdict.ts",
          "description": "the mergeOutcomes member doc points at TickVerdictMergeOutcome instead of restating a presence rule"
        }
      ],
      "retire": []
    },
    "summary": "TickVerdict.mergeOutcomes' shipped doc says 'Fanout only; empty for a singleton phase', which a pinned singleton span row contradicts",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "acceptance": "The member doc no longer states a per-concurrency presence rule; TickVerdictMergeOutcome's own doc is the single home and the member points at it.",
    "tests": [],
    "pins": [],
    "notes": "src/tickVerdict.ts:585. Contradicted in the same file by TickVerdictMergeOutcome's own doc ('per entry under fanout, the phase's own single span under singleton'), by src/singletonTick.ts:245,535, by src/Dispatcher.ts:900, and pinned false by tests/Dispatcher.test.ts:16434 'a singleton tick reverted by an afterCommit gate leaves a span row naming both shas'. Barrel-exported (src/index.ts:149), so it is hover text a chain author reads. No tests[]: the behavior is pinned; only the prose is wrong."
  }
]
