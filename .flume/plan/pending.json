[
  {
    "tag": "CLIHELP-TICK-EXITCODES-HAND-COPIED",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/cliHelp.test.ts",
          "description": "derive the range from tickExitCode; correct the describe's doc block"
        }
      ],
      "retire": []
    },
    "summary": "tests/cliHelp.test.ts's `tick --help` exit-code agreement runs off a hand-copied list, never tickExitCode's own output",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A seam gate reads what the real writer wrote"
    },
    "tests": [],
    "pins": [
      "flume tick --help documents every exit code tickExitCode returns, beside a named process-level set"
    ],
    "acceptance": "the pin drives src/cliVerdict.ts's tickExitCode over the TickOutcome space instead of the literal list, and the describe's doc block states no range tickExitCode cannot return",
    "notes": "Verified on disk: tickExitCode (src/cliVerdict.ts:25) returns 78/2/69/0 only — never the 1 the block calls part of its range; 1 is cli.ts's tick branch (detached HEAD, held tip claim). A code added to tickExitCode never enters the tester's list, so the one-sided change ships green. The block's \"(tested above)\" also dangles — the CJS case did not follow the cli.test.ts split."
  }
]
