[
  {
    "tag": "AFTERMERGE-REVERT-TIP-CHECK",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "Before resetKeepTo(repoRoot, preCherry) on an afterMerge gate failure (singleton leg and fanout per-entry leg), revParse trunk and refuse — naming both shas, routed through the existing revert-refused reporting (log line + gateFailures/prior-attempt) — when it isn't mergedSha, per spec/loop.md Tip verify."
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "Cover the new refusal for both concurrencies."
        }
      ],
      "retire": []
    },
    "observedFiles": [
      "src/Dispatcher.ts",
      "tests/Dispatcher.test.ts"
    ],
    "summary": "afterMerge revert refuses instead of resetting over a foreign trunk commit, singleton and fanout alike",
    "per": {
      "path": "spec/loop.md",
      "section": "Tip verify — one writer per branch, absorption at the merge"
    },
    "tests": [
      "afterMerge gate failure with a foreign commit landed atop the cherry-pick refuses the revert (both shas named) and leaves the foreign commit on trunk, fanout leg",
      "same refusal on the singleton leg"
    ],
    "acceptance": "resetKeepTo is only attempted when current trunk tip == mergedSha; a mismatch refuses loudly via the existing revert-refused path instead of resetting",
    "notes": "Field-traced at temper 0.13.0 (entry a385660a cherry-picked, operator commit 3da55a88 landed on top, afterMerge gate failed, trunk rewound to pre-cherry-pick). Recovered manually. Inbox 2026-09-06 (temper-main)."
  },
  {
    "tag": "AGENTRESULT-FINALMESSAGE",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Agent.ts",
          "description": "claudeCode's invoke() extracts finalMessage from the full captured stdout (same three cases as Dispatcher's finalAgentMessage: stream-json result event text, last assistant turn text, raw stdout for plain-text) and sets it on AgentResult, unbound; export the extraction helper."
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "Delete the finalAgentMessage free function; carry finalMessage on AgentTermination's clean variant from AgentResult; buildVoluntaryBail reads it and still applies tailBound(_, MAX_PRIOR_NOCOMMIT) itself at record-build time."
        },
        {
          "path": "tests/Agent.test.ts",
          "description": "Cover the three finalMessage extraction cases moved from Dispatcher."
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "Update fixtures feeding termination.stdout to instead set termination.finalMessage; voluntary-bail record text stays byte-identical."
        }
      ],
      "retire": []
    },
    "observedFiles": [
      "src/Agent.ts",
      "src/Dispatcher.ts",
      "tests/Agent.test.ts",
      "tests/Dispatcher.test.ts"
    ],
    "summary": "Move stdout finalMessage extraction from Dispatcher into claudeCode's AgentResult.finalMessage",
    "per": {
      "path": "spec/chain.md",
      "section": "The agent seam"
    },
    "tests": [
      "AgentResult.finalMessage carries the stream-json result event's text, or the last assistant turn's text, or raw stdout for a plain-text agent",
      "voluntary-bail record's constraint text is unchanged for existing fixtures after the move"
    ],
    "acceptance": "src/Dispatcher.ts holds no NDJSON re-parse of stdout for this purpose; the adapter is the one place provider transcript shape is read",
    "notes": "Bounding (tailBound/MAX_PRIOR_NOCOMMIT) stays Dispatcher-side — record-size policy, not provider shape."
  },
  {
    "tag": "TICKCONTEXT-PICKABLE-PRIORATTEMPTS",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Phase.ts",
          "description": "Add TickContext.pickable (readonly PendingEntry[]) and TickContext.priorAttempts (map keyed as prior-attempt files are), per spec/chain.md What a hook receives."
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "Populate both at every TickContext construction site (singleton shouldRun/promptArgs, fanout per-entry), reusing the same isPickable + quarantine-drop computation fanout selection uses, and a full read of prior-attempts/ via the engine's own reader/tolerance."
        },
        {
          "path": "src/index.ts",
          "description": "Re-export type PriorAttempt from ./Prompt.js — not yet on the public surface."
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "TickContext.pickable matches fanout selection's own verdict; priorAttempts reflects persisted records, tolerating a corrupt one as absent."
        }
      ],
      "retire": []
    },
    "observedFiles": [
      "src/Dispatcher.ts",
      "src/Phase.ts",
      "src/index.ts",
      "tests/Dispatcher.test.ts"
    ],
    "summary": "TickContext gains pickable (dispatcher's own selection verdict) and priorAttempts (persisted records), so a hook stops re-deriving either",
    "per": {
      "path": "spec/chain.md",
      "section": "What a hook receives"
    },
    "tests": [
      "singleton shouldRun's TickContext.pickable agrees with the next fanout tick's own selection",
      "TickContext.priorAttempts is keyed as the on-disk files are (tag slug / phase name) and a corrupt record reads as absent"
    ],
    "acceptance": "a shouldRun/promptArgs hook can read ctx.pickable and ctx.priorAttempts instead of calling isPickableNow or scanning prior-attempts/ itself",
    "notes": ".flume/chain.ts's isPickableNow(shouldRun) call and hand-rolled anyVoluntaryBailRecord scan are out of build's writablePaths (harness surface) — adoption is a separate chore(flume) commit."
  },
  {
    "tag": "TICKRESULT-PICKABLEAFTER-ROOTS-ENTRIES",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Phase.ts",
          "description": "Add TickResult.pickableAfter, flumeDir, configDir, and entries (fanout only: {tag, committed, shipped, reverted, declined?, noCommit?} per provisioned entry, before the wave's fold), per spec/chain.md What a hook receives."
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "Populate all four at TickResult construction (singleton tick and fanout wave-end); entries is absent on singleton and on a wave that provisioned nothing; top-level shippedTags/revertedTags/noCommit/declined stay the byte-identical fold."
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "pickableAfter matches TickContext.pickable's verdict taken post-tick; a wave with one shipped and one bailed sibling reports the bailed entry's noCommit mode in entries even though the top-level noCommit is absent."
        }
      ],
      "retire": []
    },
    "observedFiles": [
      "src/Dispatcher.ts",
      "src/Phase.ts",
      "tests/Dispatcher.test.ts",
      "tests/chain.test.ts"
    ],
    "summary": "TickResult gains pickableAfter, flumeDir, configDir, and per-entry fanout outcomes (entries), so a bail isn't lost behind a sibling's ship",
    "per": {
      "path": "spec/chain.md",
      "section": "What a hook receives"
    },
    "tests": [
      "TickResult.pickableAfter is pendingAfter filtered by the same verdict as TickContext.pickable",
      "a fanout wave with a shipped entry and a voluntarily-bailed sibling reports the bail's mode in entries[] while shippedTags/noCommit stay the existing wave-summary shape"
    ],
    "acceptance": "handoff can read result.pickableAfter/flumeDir/configDir/entries without process.env or a re-picked isPickableNow call",
    "notes": "May collide with TICKCONTEXT-PICKABLE-PRIORATTEMPTS on src/Phase.ts and src/Dispatcher.ts (expected — sequential ship, not a design coupling)."
  },
  {
    "tag": "FLUMEAPI-PATHS",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/flumeApi.ts",
          "description": "Add FlumeApi.paths: {repoRoot, configDir, flumeDir}; buildFlumeApi(paths) takes them as its argument instead of zero args."
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "Thread {repoRoot, configDir, flumeDir} into every buildFlumeApi() call site (chain load), so the handed api.paths are the identity-same resolved roots the dispatcher already holds."
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "A chain factory sees api.paths matching the Dispatcher's own resolved roots."
        }
      ],
      "retire": []
    },
    "observedFiles": [
      ".flume/plan/open-questions.md",
      "src/Dispatcher.ts",
      "src/flumeApi.ts",
      "tests/Dispatcher.test.ts",
      "tests/chain.test.ts",
      "tests/examples.integration.test.ts"
    ],
    "summary": "FlumeApi carries paths {repoRoot, configDir, flumeDir}, the dispatcher's own resolved roots, by reference",
    "per": {
      "path": "spec/chain.md",
      "section": "Per-run artifacts belong under `FLUME_DIR`"
    },
    "tests": [
      "api.paths.flumeDir/configDir/repoRoot equal the Dispatcher's own resolved values at chain-load time"
    ],
    "acceptance": "a chain can resolve a per-run artifact against api.paths.flumeDir instead of process.env.FLUME_DIR with a `?? CHAIN_DIR` fallback",
    "notes": "cli.ts already resolves state dirs ahead of every chain-loading verb including the job verbs (e814195); that half of the spec's drift note is stale. Only the FlumeApi.paths gap itself remains live. `.flume/chain.ts` adoption is a separate chore(flume) commit (harness surface)."
  }
]
