[
  {
    "tag": "WORKTREE-RACE-SERIALIZE",
    "summary": "Serialize .git/worktrees/-mutating ops: setup createWorktree Promise.all + teardown removeWorktree Promise.all → sequential for…await; keep the per-entry agent fanout Promise.all parallel",
    "per": {
      "path": "spec/RELEASE-v0.2.md",
      "section": "4. Worktree create/teardown race serialization"
    },
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "Convert the setup worktree-create Promise.all and the teardown worktree-remove Promise.all to sequential for…await (mirror the already-serialized pre-wave pruneWorktrees); the per-entry agent-invocation Promise.all stays parallel."
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "Stale-slug-seeded N≥2 wave: all worktrees created + entries shipped; teardown leaves git worktree list clean; the §7b test's maxInFlight===2 agent-fanout parallelism canary stays green (it is the only such assertion — bd5e6f4 folded it there; do not re-derive a standalone one)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/Dispatcher.test.ts",
        "asserts": "stale-slug N≥2 wave completes (no add fails on a sibling remove); teardown leaves clean git worktree list; the §7b test's maxInFlight===2 parallelism canary stays green (§4 acceptance)"
      }
    ],
    "acceptance": "Stale-slug N≥2 wave completes with all worktrees created + entries shipped; teardown leaves git worktree list clean; the §7b test's maxInFlight===2 agent-fanout parallelism canary stays green; pnpm test green",
    "notes": "§4 substance unchanged. Spec §4's :257/:284/:353 are pre-reload — locate the setup/teardown worktree Promise.alls + the keep-parallel agent-fanout one by shape, not number. Cross-tick: §4's 'existing fanout-parallelism assertion' is NOT standalone — bd5e6f4 folded it into the §7b test (Dispatcher.test.ts, maxInFlight===2 over the agent-fanout Promise.all); assert against that canary, don't re-derive a phantom. Internal git.ts/Dispatcher.ts (not v0.1 §2) → CHANGELOG ### Fixed."
  },
  {
    "tag": "BAIL-CONSTRAINT-LEGIBILITY",
    "summary": "voluntary-bail constraint capture degrades to NDJSON noise under the dogfood stream-json agent; extract the agent's final message from the transcript before bounding, not the raw stdout tail",
    "per": {
      "path": "spec/RELEASE-v0.2.md",
      "section": "5. Prior-outcome feedback to the retrying tick"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "WORKTREE-RACE-SERIALIZE"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "buildVoluntaryBail must capture the agent's final message, not tailBound(raw result.stdout). Extract the final assistant/result text from the stream-json transcript shape when stdout is NDJSON; fall back to the raw tail for plain-text agents. Keep the existing bound + empty-final-message placeholder."
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "Add a voluntary-bail case whose fake agent emits stream-json NDJSON stdout (assistant text + result event + cost/usage) → the next tick's §5 block names the refused constraint cleanly, free of escaped-JSON/cost-metadata; the existing plain-text voluntary-bail test stays green."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/Dispatcher.test.ts",
        "asserts": "stream-json-stdout voluntary-bail → §5 <prior-attempt> names the refused constraint legibly (no NDJSON/cost noise); plain-text voluntary-bail path unchanged (§5 voluntary-bail variant / §6 'names the prior bail and its constraint')"
      }
    ],
    "acceptance": "A voluntary-bail under a stream-json agent forwards a legible refused-constraint in the next tick's §5 block (not raw NDJSON/cost metadata); the plain-text path stays green; pnpm test green",
    "notes": "Audit 58d8491: buildVoluntaryBail = tailBound(result.stdout). OK for claudeCode({text}); dogfood .flume/chain.ts uses withTerminalRenderer(withSessionCapture(claudeCode({stream-json}))) — decorators pass stdout raw, so the tail is escaped-JSON result/assistant + cost/usage, the loop §6 targets. Agent.ts already parses stream-json (renderStreamJsonLine). Alt finalMessage-on-AgentResult is an unspecced v0.1-§2 add (cf. teardownWorktree OQ); prefer in-lane extraction."
  },
  {
    "tag": "CHAIN-AUTHORING-GATE-GUIDANCE",
    "summary": "CHAIN-AUTHORING.md: add gate-placement default (expensive correctness→afterMerge, cheap structural→afterCommit) + contention rationale, and the byte-equality anti-pattern section",
    "per": {
      "path": "spec/RELEASE-v0.2.md",
      "section": "7. Fanout revert isolation + gate-placement defaults"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "BAIL-CONSTRAINT-LEGIBILITY"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "Add (a) gate-placement default guidance — expensive correctness gates at afterMerge, cheap structural (tscGate, bundle self-containment) at afterCommit — with the fanout-contention rationale; (c) a 'gate on the safety property, never on byte-equality of generated artifacts' anti-pattern section using the bundleFreshnessGate worked example."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "CHAIN-AUTHORING.md documents the expensive→afterMerge / structural→afterCommit default with rationale and contains the byte-equality anti-pattern section with the bundleFreshnessGate example",
    "notes": "§7 (a) doc + (c) doc only — both docs/, build-writable. §7a's actual dogfood .flume/chain.ts gate-move (vitestGate→afterMerge) is OFF build's writablePaths → parked OQ (chore(flume)/human). §7c (v) is docs-only per §12 — no runtime entry. blockedBy BAIL-CONSTRAINT linearizes Dispatcher.ts-touch order; semantically wants §7b landed so the afterMerge guidance isn't a pre-isolation footgun."
  },
  {
    "tag": "RELEASE-0.2.0",
    "summary": "Cut 0.2.0: package.json version → 0.2.0; CHANGELOG ## [0.2.0] with ### Breaking / ### Added / ### Fixed / ### Changed exactly per §9",
    "per": {
      "path": "spec/RELEASE-v0.2.md",
      "section": "9. Versioning & distribution"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "CHAIN-AUTHORING-GATE-GUIDANCE"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "package.json",
          "description": "version → 0.2.0."
        },
        {
          "path": "CHANGELOG.md",
          "description": "Add `## [0.2.0]` transcribing §9's four subsections verbatim — ### Breaking / ### Added / ### Fixed / ### Changed. §9 holds the exact text (### Added is now the reconciled prior-outcome wording: gate-revert|voluntary-bail|platform-preempt, §5/§6). Single consolidated section, no per-entry cherry-picks."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "package.json is 0.2.0; CHANGELOG has ## [0.2.0] with all four §9 subsections; pnpm build clean; v0.1 §8 consumer-install smoke + attw esm-only green in CI",
    "notes": "Single consolidated [0.2.0] section, blockedBy the last functional entry, avoids CHANGELOG cherry-pick conflicts. npm publish + git tag are human release-ceremony (OQ#3 lane), out of build scope. OQ#1 resolved by 4187f44 → the prior CONTINGENT wording note is dropped. CONTINGENT: the ### Changed dogfood-gate-placement line depends on the §7a chain.ts OQ (chore(flume) move shipped) — else omit/soften, do not over-claim."
  }
]
