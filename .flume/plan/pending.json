[
  {
    "tag": "GATE-BUILTIN-WHEN-ROSTER-DERIVED",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/Gate.test.ts",
          "description": "widen the when=afterCommit claim to the exported roster, per the cited section"
        }
      ],
      "retire": []
    },
    "summary": "Derive the builtin-gate when=afterCommit roster from src/builtinGates.ts's exports instead of a hand-restated four",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A green verdict is proven non-vacuous"
    },
    "acceptance": "The roster claim reads its subjects off the module's exports, and pendingGate's placement is among them.",
    "pins": [
      "every builtin gate src/builtinGates.ts exports declares when=afterCommit by default"
    ],
    "notes": "tests/Gate.test.ts:363 titles a universal over the builtins; the body enumerates tsc/vitest/eslint/writablePaths. chainLoadGate is asserted separately at :384; pendingGate(...).when (src/builtinGates.ts:329) is asserted nowhere under tests/. Derive the roster so a new builtin joins for free (engineering.md, The fix lands at the mechanism)."
  }
]
