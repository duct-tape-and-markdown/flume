[
  {
    "tag": "EXAMPLE-PLAN-PROMPT-SPANS-READ-THE-INJECTED-ROOT",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "examples/prompts/plan.md",
          "description": "root the four state-root spans at the reserved token per the cited section, quoted"
        },
        {
          "path": "tests/examples.test.ts",
          "description": "odd-root render pin over examples/prompts/, shaped after tests/harnessPrompts.test.ts's cases"
        }
      ]
    },
    "summary": "examples/prompts/plan.md hardcodes .flume/ in four spans — the footgun the cited section names, in the file docs/CHAIN-AUTHORING.md quotes",
    "per": {
      "path": "spec/prompt.md",
      "section": "The reserved `{{FLUME_DIR}}` prompt arg"
    },
    "acceptance": "no span under examples/prompts/ names a literal .flume/; both odd-root cases green",
    "tests": [
      "every example prompt's spans read their artifacts under a state root path carrying a space",
      "every example prompt's spans read their artifacts under a state root path carrying a backslash"
    ],
    "notes": "Two of the four spans carry an `|| echo` fallback, so a relocated root is a silent blind tick, not a refusal. The odd root carries the quoting half too (7b76805 quoted examples/prompts/build.md with nothing pinning it). cascade-chain.ts's writablePaths hardcode the same literal; that half is parked in open-questions and is out of scope here."
  }
]
