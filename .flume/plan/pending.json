[
  {
    "tag": "verdict-anchor-head-sha-at",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "TickVerdict: add headSha: string, at: string (ISO timestamp), populated by writeTickVerdict. Add readLatestVerdictsSync(flumeDir): most-recent verdict per phaseName, synchronous, reading the same verdict log readTickVerdicts does."
        },
        {
          "path": "src/index.ts",
          "description": "export readLatestVerdictsSync."
        },
        {
          "path": "src/flumeApi.ts",
          "description": "export readLatestVerdictsSync."
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "headSha/at correctness; readLatestVerdictsSync per-phase latest reduction."
        }
      ],
      "retire": []
    },
    "summary": "Every TickVerdict carries headSha/at; readLatestVerdictsSync gives shouldRun/handoff a synchronous per-phase anchor read.",
    "per": {
      "path": "spec/loop.md",
      "section": "The tick verdict — one facts artifact"
    },
    "tests": [
      "a written verdict's headSha matches the trunk tip and at is a valid ISO timestamp",
      "readLatestVerdictsSync returns the most recent verdict per phase name from the verdict log",
      "headSha/at are written on a quiet (no-commit) tick too, not only a committed one"
    ],
    "acceptance": "A chain compares a verdict's headSha to the live tip without inferring it from touched paths, and reads it from shouldRun/handoff without await.",
    "notes": "readTickVerdicts stays async; readLatestVerdictsSync is the new sync sibling for the two synchronous chain hooks."
  },
  {
    "tag": "verdict-invocations-usage",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Agent.ts",
          "description": "AgentResult gains per-invocation usage: model id, turn count, duration, and token counts (input/output/cacheCreation/cacheRead), parsed from the same stream-json result event withTerminalRenderer's formatResult already reads — one source, not a second parser."
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "TickVerdict: add invocations[] — one row per agent run this tick (tag present for a fanout entry, absent for a singleton), populated from each AgentResult."
        },
        {
          "path": "tests/Agent.test.ts",
          "description": "usage extraction from a result event, including a field the agent didn't report."
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "singleton vs fanout invocations[] shape."
        }
      ],
      "retire": []
    },
    "summary": "TickVerdict.invocations[] carries one usage row per agent run (model, turns, duration, token counts incl. cache split); absent fields the agent didn't report.",
    "per": {
      "path": "spec/loop.md",
      "section": "The tick verdict — one facts artifact"
    },
    "tests": [
      "a singleton tick's verdict carries one invocations[] row with no tag",
      "a fanout tick's verdict carries one invocations[] row per provisioned entry, each tagged",
      "a field the agent's result didn't report is absent on the row, not zero"
    ],
    "acceptance": "Cost/telemetry for a tick is readable from the verdict alone, without re-parsing the agent's stdout stream in a decorator.",
    "notes": "Collides with verdict-anchor-head-sha-at on TickVerdict/Dispatcher.ts — expected, both edit the same type."
  },
  {
    "tag": "prior-attempt-anchor-export",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "slugify and priorAttemptPath become exported free functions: priorAttemptPath(flumeDir, tag) (internally slugifies). Update priorAttemptKey/readPriorAttempt/writePriorAttempt call sites to the new signature."
        },
        {
          "path": "src/Prompt.ts",
          "description": "all 5 PriorAttempt variants gain headSha: string and at: string."
        },
        {
          "path": "src/index.ts",
          "description": "export slugify, priorAttemptPath."
        },
        {
          "path": "src/flumeApi.ts",
          "description": "export slugify, priorAttemptPath."
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "priorAttemptPath(flumeDir, tag) matches the dispatcher's own read/write path; headSha/at on each variant."
        },
        {
          "path": "tests/Prompt.test.ts",
          "description": "render coverage carrying the two new fields through the five variants."
        }
      ],
      "retire": []
    },
    "summary": "priorAttemptPath/slugify become exported so a chain reads the engine's own rule; every prior-attempt record anchors on headSha/at.",
    "per": {
      "path": "spec/loop.md",
      "section": "Prior-outcome feedback to the retrying tick"
    },
    "tests": [
      "priorAttemptPath(flumeDir, tag) matches the path the dispatcher itself reads/writes for that tag",
      "every one of the 5 prior-attempt record variants carries headSha and at when written",
      "a chain's shouldRun can compare a stale record's headSha to the live tip instead of the file's mtime"
    ],
    "acceptance": "A chain-authored shouldRun locates and interprets a prior-attempt record using only exported engine rules.",
    "notes": "Export is also spec'd at spec/pending.md What the package exports. Collides with gate-failing-files-suspect-flake on Prompt.ts's GateRevertAttempt — expected."
  }
]
