[
  {
    "tag": "GITTEST-CHERRYPICKABORT-ZERO-ABORT-UNPINNED",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "tests/git.test.ts",
          "description": "population pin on the no-sequencer leg's judged call slice; title matched to what the leg asserts"
        }
      ]
    },
    "summary": "cherryPickAbort's no-sequencer leg asserts zero --abort calls over a slice nothing pins non-empty — green survives a dead call log",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A green verdict is proven non-vacuous"
    },
    "pins": [
      "cherryPickAbort probes for sequencer state and issues no --abort when none is present"
    ],
    "acceptance": "the no-sequencer leg pins its logged slice populated — cherryPickAbort's two rev-parse --git-path probes — before asserting zero --abort calls",
    "notes": "tests/git.test.ts:641-643. Judged set is execArgsLogSince(since); nothing asserts it was populated, and the file header calls the promisify.custom interception fragile. The title over-claims with it: src/git.ts:426-435 issues two rev-parse --git-path probes, so \"issues no git command\" is false — the population pin and the corrected title are one edit."
  }
]
