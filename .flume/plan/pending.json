[
  {
    "tag": "THE-DOC-BULLET-READ-HAS-ONE-HOME",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/helpers/docSections.ts",
          "description": "`bulletOf` beside `sectionOf`/`walkOf` — one bullet, folded, through the next lead"
        },
        {
          "path": "tests/examples.test.ts",
          "description": "three callers fold onto it: the two `bulletFor` locals and the shellGate inline slice"
        }
      ],
      "retire": []
    },
    "summary": "Fold the three spellings of the per-bullet read in tests/examples.test.ts onto one `bulletOf` in docSections",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A module is one job"
    },
    "acceptance": "One reader answers 'this bullet, wrapping folded'; the lead spelling is its parameter.",
    "tests": [],
    "pins": [],
    "notes": "Note THE-CHAIN-AUTHORING-WALKS-SHARE-ONE-READER, verified wider on disk: three copies, not two — the supervisor-policy local (`- **`x`** —`), the gate-field local (`- `x` —`), and the shellGate bullet sliced inline. Behavior-free; the typecheck and the existing cases hold it, so it claims no property."
  }
]
