[
  {
    "tag": "bin-flume-symlink-walk-test",
    "gate": { "kind": "open" },
    "files": {
      "new": [
        {
          "path": "tests/bin.test.ts",
          "description": "exercise bin/flume's symlink-chain walk directly (single-hop and multi-hop, matching npm/pnpm's real bin-linking shapes), asserting it execs the resolved dist/cli.js with argv/cwd intact"
        }
      ],
      "edit": [],
      "retire": []
    },
    "summary": "bin/flume's symlink-walk logic (fixed once for a real bug, c336ead) has zero test coverage since package.json's bin field moved to bin/flume.js",
    "per": { "path": "spec/cli.md", "section": "Distribution" },
    "tests": [
      "bin/flume resolves through a single-hop symlink (npm-style) to the real script and execs dist/cli.js relative to it",
      "bin/flume resolves through a multi-hop symlink chain (pnpm .pnpm/<hash>/-style) to the real script and execs dist/cli.js relative to it"
    ],
    "acceptance": "new test fails if bin/flume's symlink walk regresses to the pre-c336ead dirname-only bug; passes on current tree",
    "notes": "spec/cli.md Distribution declares bin/flume as shipped surface 'for direct callers'. scripts/smoke-install.mjs only exercises npm's generated shim, which wraps bin/flume.js per package.json's bin field — bin/flume itself is untested."
  }
]
