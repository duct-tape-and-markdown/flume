[
  {
    "tag": "README-QUICKSTART-LEADS-WITH-THE-ADOPTION-VERB",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        { "path": "README.md", "description": "Quickstart opens on the harness verb per the cited section; hand-written chain demoted below it" },
        { "path": "tests/harnessPackaging.test.ts", "description": "the README-against-bin pin joins the existing README prerequisite scans" }
      ]
    },
    "summary": "README Quickstart leads with `npx flume-harness init` and the `./harness` subpath; the hand-written chain stays below as the engine-level path",
    "per": { "path": "spec/harness.md", "section": "Adoption and upgrade" },
    "acceptance": "README's Quickstart opens on the adoption verb and names what it writes; the hand-written `.flume/chain.ts` remains, labelled as the engine-level path; the new test is green.",
    "tests": [
      "the README quickstart names the adoption verb the flume-harness bin dispatches"
    ],
    "notes": "Measured on this tree: the README's only `harness package` mention is under *Where state lives*; the verb, the subpath and the declaration appear nowhere. The pin reads the README against `harness/cli.ts`'s verb set, not a literal — the bin dispatches exactly one verb."
  },
  {
    "tag": "MIGRATING-0.16-CARRIES-THE-UNRELEASED-BREAKS",
    "gate": { "kind": "open" },
    "files": {
      "new": [
        { "path": "docs/MIGRATING-0.16.md", "description": "the release's migration note per the cited section, one section per break plus the adoption path" }
      ]
    },
    "summary": "Write the 0.16 migration note: the four breaks in the unreleased range, plus the path for a hand-written chain moving onto the harness package",
    "per": { "path": "spec/harness.md", "section": "Adoption and upgrade" },
    "acceptance": "`docs/MIGRATING-0.16.md` carries one section per breaking change in `v0.15.0..HEAD` with the before and after call shape, and one on adopting the package from a hand-written chain.",
    "notes": "The four: `WorktreeSetupContext.worktreeKey`; `entryTag` on both verdict rows; `priorAttemptPath(flumeDir, ref)` with keyspace-scoped map keys; `--strict-mcp-config` by default, `inheritUserMcp` the opt-out. The last is 66781ef, whose body carries no `BREAKING:` line, so `git log --grep` and the mined draft both miss it. CHANGELOG curation stays the cut's (`spec/cli.md`, *Versioning policy*)."
  },
  {
    "tag": "CONSUMER-DOCS-NAME-THE-SHIPPED-HARNESS-PACKAGE",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        { "path": "docs/CHAIN-AUTHORING.md", "description": "a section on adopting the package per the cited section" },
        { "path": "docs/INTENT.md", "description": "the consumable-chain heading and its tense state what shipped" }
      ]
    },
    "summary": "CHAIN-AUTHORING gains the harness-package adoption section; INTENT's consumable-chain heading moves from decided to shipped",
    "per": { "path": "spec/harness.md", "section": "Adoption and upgrade" },
    "acceptance": "`docs/CHAIN-AUTHORING.md` carries a section on the package — the verb, the `./harness` subpath, and the declaration as distinct from the engine-level `Chain` field it currently means by `declaration`; `docs/INTENT.md`'s *Decided, not yet executed — the consumable chain* heading no longer claims unexecuted.",
    "notes": "Measured: every `declaration` hit in CHAIN-AUTHORING is the engine-level `Chain` field; no section names the package. INTENT's heading is contradicted by `harness/` and by this repo's own `.flume/chain.ts`."
  }
]
