[
  {
    "tag": "PRIOR-ATTEMPTS-DIR-EXPORT",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "Export priorAttemptsDir(flumeDir) beside priorAttemptPath, deriving from the same private PRIOR_ATTEMPTS_SUBDIR."
        },
        {
          "path": "src/index.ts",
          "description": "Re-export priorAttemptsDir alongside priorAttemptPath/slugify."
        },
        {
          "path": "src/flumeApi.ts",
          "description": "Add priorAttemptsDir to the FlumeApi interface and buildFlumeApi, mirroring priorAttemptPath's entry."
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "Agreement pin beside the existing slugify/priorAttemptPath re-export test."
        }
      ],
      "retire": []
    },
    "summary": "Export priorAttemptsDir so a chain can derive the prior-attempts scan dir instead of hardcoding it",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A fact the engine holds is reported, never rediscovered"
    },
    "tests": [
      "priorAttemptsDir(flumeDir) agrees with the directory component of priorAttemptPath(flumeDir, tag) for an arbitrary tag"
    ],
    "acceptance": "priorAttemptsDir is exported from Dispatcher.ts/index.ts/flumeApi.ts; new agreement test is green.",
    "notes": "Scope corrected this tick: dropped .flume/chain.ts from files (outside every phase's lane, spec-plan-build.md) — the prior tick's declaration there caused the recorded merge-failure footprint. chain.ts's own consumption of the export is now a parked open question."
  },
  {
    "tag": "PENDING-GATE-STALE-TIP-READ",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/builtinGates.ts",
          "description": "pendingGate: require ctx.commitSha (mirror writablePathsGate/chainLoadGate's early check); read the queue via git.readFileAtRef(ctx.repoRoot, ctx.commitSha, relPath) instead of readFile(join(ctx.flumeDir, pendingPath)), falling back to the flumeDir disk read only when pendingPath resolves outside repoRoot (relocated state root — mirror Dispatcher.ts's isPendingRelocated check)."
        },
        {
          "path": "tests/builtinGates.test.ts",
          "description": "Pin: a commit adding an off-fence entry to an in-repo pending.json is reverted by that same commit's pendingGate run, not a later one; a commit removing the violation passes on its own commit."
        }
      ],
      "retire": []
    },
    "summary": "pendingGate reads pending.json from flumeDir disk (stale tip), not the commit it is gating",
    "per": {
      "path": "spec/pending.md",
      "section": "`pendingGate` — validation and fence pre-check as an opt-in builtin"
    },
    "tests": [
      "pendingGate reverts the commit that introduces an off-fence declaration, not the next one",
      "pendingGate passes a commit that removes a prior off-fence declaration",
      "relocated flumeDir (pendingPath outside repoRoot) still reads from disk, unchanged"
    ],
    "acceptance": "pendingGate resolves the queue from ctx.commitSha via git.readFileAtRef for an in-repo pendingPath; new tests fail on the pre-fix tree and pass after.",
    "notes": "Inbox 2026-09-03. src/builtinGates.ts:347 reads join(ctx.flumeDir, pendingPath) — the working-tree/trunk file, not the gated commit — so a fix-the-prior-violation commit is judged against the state it fixes and reverts identically (observed: 70f4632 -> c168d3b). git.readFileAtRef already exists and is used the same way by Dispatcher.ts:3986; no new engine surface."
  }
]
