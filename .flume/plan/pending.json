[
  {
    "tag": "INIT-MANIFEST-REFUSAL-BEFORE-WRITE",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "harness/init.ts",
          "description": "read+parse the consumer manifest in the pre-write phase beside readSelfPackage; refuse in flume's voice naming the manifest"
        },
        {
          "path": "tests/harnessInit.test.ts",
          "description": "the malformed-manifest refusal, beside the existing state-root one"
        }
      ],
      "retire": []
    },
    "summary": "harnessInit takes the consumer-manifest refusal before the first byte, not after the whole adoption has landed",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Loud or nothing"
    },
    "acceptance": "an unparseable consumer package.json throws a flume-voiced error naming the manifest, with no state root, no skeleton files and no .gitignore change on disk",
    "tests": [
      "flume-harness init refuses an unparseable consumer package.json naming the manifest",
      "flume-harness init leaves the repository untouched when the consumer manifest cannot be parsed"
    ],
    "notes": "addDependency (harness/init.ts:390) runs at :351 — after mkdir, three writeFiles and the .gitignore merge. Its JSON.parse (:399) escapes as a raw SyntaxError; the re-run then refuses at :309. Module header :17-23 declares the opposite invariant, and tests/harnessInit.test.ts:215 already pins it for the state-root refusal."
  }
]
