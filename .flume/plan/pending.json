[
  {
    "tag": "PARSE-REFUSAL-CARRIES-THE-LEDGER-PATH-AS-A-FIELD",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "src/PendingSchema.ts",
          "description": "`readonly path` on `PendingParseFailure` beside `errors`; its doc states the alphabet `reportedPendingPath` returns"
        },
        {
          "path": "tests/pendingLedger.test.ts",
          "description": "the field read off a refusal the real strict read threw over a declared `queue/ledger.json`"
        }
      ]
    },
    "summary": "Carry the ledger path on `PendingParseFailure` as a field, not in the message alone — the twin `QueueParseFailure` already reports it",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A fact the engine holds is reported, never rediscovered"
    },
    "acceptance": "A chain catching a `PendingParseFailure` reads the ledger path off the error rather than regexing the message; both throw sites otherwise unchanged.",
    "tests": [
      "a strict pending read's parse refusal carries the chain's declared ledger path on the error"
    ],
    "notes": "Build left the field out per *An export earns its consumer* (no consumer yet). This section overrides for an engine fact: a chain catching the throw can only regex the message for the path, the shape `engine-boundary.md`, *Told, not inferred* refuses. Sibling asymmetry is the cite in one class: `errors` is a field, the path is prose. From the note on PARSE-REFUSAL-NAMES-THE-DECLARED-LEDGER-PATH."
  }
]
