[
  {
    "tag": "PENDING-SCHEMA-CORE-EXTENSION-SPLIT",
    "summary": "Shrink core PendingEntry schema to tag/files/gate/dependsOnForks; add chain-declared extension mechanism composing validator + renderSchemaForPrompt, per v0.8 §2.",
    "per": {
      "path": "spec/RELEASE-v0.8.md",
      "section": "2. Entry schema splits: engine core + chain-declared extension"
    },
    "gate": {
      "kind": "parked",
      "reason": "Core-shrink invalidates today's extension-shaped pending.json at pendingParseGate unless chain.ts declares its extension in the same landing (CLAUDE.md atomicity) — but chain.ts sits outside build's fence. Confirmed live: 17-tick wave bailed voluntarily on exactly this gap (plan: commit 2026-07-30 drains the inbox report). Needs operator to land chain.ts's extension declaration in lockstep with this entry's build commit, not as an independent trailing leg."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/PendingSchema.ts",
          "description": "shrink PendingEntry core to tag/files/gate/dependsOnForks; add extension-declaration composition (merged zod validator + renderSchemaForPrompt renders core+extension), per §2"
        },
        {
          "path": "src/Phase.ts",
          "description": "Chain interface gains an optional entry-extension declaration field (per-field zod schema + prompt hint)"
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "loadChainModule reads the chain's extension declaration; internal parsePending call (~1825) composes core+extension so today's dogfood-shaped entries still parse"
        },
        {
          "path": "src/cli.ts",
          "description": "parsePending call (~1029) composes core+extension from the loaded chain"
        },
        {
          "path": "src/job.ts",
          "description": "parsePending call (~530) composes core+extension from the loaded chain"
        },
        {
          "path": "src/index.ts",
          "description": "export the new extension-declaration type alongside existing PendingSchema/Phase exports"
        },
        {
          "path": "tests/PendingSchema.test.ts",
          "description": "core-only validation rejects unknown fields; extension composition accepts declared fields, rejects undeclared; renderSchemaForPrompt renders core+extension"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "fanout/pending-read paths still parse dogfood-shaped entries once the chain's extension is threaded through"
        },
        {
          "path": "tests/cli.test.ts",
          "description": "cli's parsePending callsite still parses extension-shaped entries"
        },
        {
          "path": "tests/job.test.ts",
          "description": "job's parsePending callsite still parses extension-shaped entries"
        },
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "new section: declaring an entry extension"
        },
        {
          "path": "CHANGELOG.md",
          "description": "Unreleased/Changed bullet per §2"
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/PendingSchema.test.ts",
        "asserts": "a chain with no extension validates and renders the bare core; a chain's declared extension composes into the merged validator and the rendered prompt schema with no drift between them"
      }
    ],
    "acceptance": "dogfood plan/build ticks run green with the dogfood extension declared in chain.ts and zero dogfood-specific fields in src/; a chain with no extension validates and renders the bare core; grep -ri 'prisma\\|schemaDelta' src/ is empty.",
    "notes": "Dogfood .flume/chain.ts's own extension declaration (summary/per/tests/acceptance/notes/schemaDelta fields) is operator-applied (§2/§3/§6 dogfood legs, chain.ts sits outside every phase's fence) — not part of this entry. This entry ships the engine mechanism plus internal call-site threading (Dispatcher/cli/job) so pre-adoption pending.json keeps parsing."
  },
  {
    "tag": "REQUIRES-CAPABILITY-GENERALIZATION",
    "summary": "Generalize gate kind requiresDockerHost to requiresCapability(capability); chain declares capabilities[]; status names the missing capability on skip, per v0.8 §4.",
    "per": {
      "path": "spec/RELEASE-v0.8.md",
      "section": "4. `requiresDockerHost` generalizes to `requiresCapability`"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/PendingSchema.ts",
          "description": "Gate union: replace requiresDockerHost variant with { kind: \"requiresCapability\", capability: string }"
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "isPickable switch (~2255-2274) matches requiresCapability against chain-asserted capabilities; unasserted → skipped, never silent"
        },
        {
          "path": "src/Phase.ts",
          "description": "Chain interface gains optional capabilities?: string[] (chain may probe environment at load time)"
        },
        {
          "path": "src/cli.ts",
          "description": "status names the missing capability for entries skipped on an unasserted requiresCapability gate"
        },
        {
          "path": "docs/CASCADE-DRY-RUN.md",
          "description": "update requiresDockerHost mentions (~lines 129, 132) to the requiresCapability(\"docker-host\") worked example"
        },
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "update if it references requiresDockerHost anywhere; add if it doesn't cover capability gating yet"
        },
        {
          "path": "tests/PendingSchema.test.ts",
          "description": "requiresCapability gate parses; requiresDockerHost no longer a valid variant"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "entry gated on an asserted capability is picked; on an unasserted capability is skipped"
        },
        {
          "path": "tests/cli.test.ts",
          "description": "status names the missing capability for a skipped entry"
        },
        {
          "path": "CHANGELOG.md",
          "description": "Unreleased/Changed bullet per §4"
        }
      ],
      "retire": [
        "requiresDockerHost gate variant"
      ]
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/Dispatcher.test.ts",
        "asserts": "an entry gated on an asserted capability is picked; on an unasserted one it is skipped and status names it"
      }
    ],
    "acceptance": "an entry gated on an asserted capability is picked; on an unasserted one it is skipped and status names it; grep -ri docker src/ is empty.",
    "notes": "Clean-slate delete of requiresDockerHost (pre-1.0, no compat shim, per spec-plan-build.md). Independent of PENDING-SCHEMA-CORE-EXTENSION-SPLIT — touches the Gate union, not the extension mechanism."
  },
  {
    "tag": "TICK-VERDICT-ARTIFACT",
    "summary": "One engine-written, chain-readable tick-verdict artifact (phase, tags, commit class, gate results, shipped tags, merge outcome), superseding last-tick.json, per v0.8 §5.",
    "per": {
      "path": "spec/RELEASE-v0.8.md",
      "section": "5. Tick verdict: one facts artifact, engine-written, chain-read"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "new unified verdict-artifact write per tick at a stable state-dir path, superseding TickCountsRecord/writeTickCounts (lines 80-143); commitPendingUpdate's footprint generation (~1835-1885) sources from the same verdict record"
        },
        {
          "path": "src/cli.ts",
          "description": "loop/superviseLoop reads the verdict artifact instead of last-tick.json for counts/exit-code"
        },
        {
          "path": "src/index.ts",
          "description": "export a read accessor (last N verdicts) for chain use"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "a committed tick, a gate-reverted tick, and a voluntary-bail tick each leave a verdict artifact carrying the facts; supervisor exit-code/count behavior is byte-identical to v0.7 §4"
        },
        {
          "path": "CHANGELOG.md",
          "description": "Unreleased/Changed bullet per §5"
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/Dispatcher.test.ts",
        "asserts": "a chain reading last-N verdicts sees a reverted tick's violating paths; the artifact never carries interpretation fields, only facts"
      }
    ],
    "acceptance": "a committed tick, a gate-reverted tick, and a voluntary-bail tick each leave a verdict artifact carrying the facts above; supervisor exit-code/count behavior is byte-identical to v0.7 §4, sourced from the verdict; a chain reading last-N verdicts sees a reverted tick's violating paths.",
    "notes": "Supersedes three v0.7 partial channels: last-tick.json counts (Dispatcher.ts:80-143), §13 observedFiles footprints, and §15's in-process-only TickOutcome.noCommit (TICKRESULT-NOCOMMIT-CLASSIFICATION). No interpretation fields — facts only, per §5's own directive."
  },
  {
    "tag": "TAG-GRAMMAR-MECHANICAL-SAFETY",
    "summary": "Widen TAG_PATTERN to mechanical safety only (charset, no whitespace, derived length bound); a chain refines further via its §2 extension, per v0.8 §3.",
    "per": {
      "path": "spec/RELEASE-v0.8.md",
      "section": "3. Tag grammar reduces to mechanical safety"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "PENDING-SCHEMA-CORE-EXTENSION-SPLIT"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/PendingSchema.ts",
          "description": "widen TAG_PATTERN (line 71) to conservative charset (letters, digits, ._()-), no whitespace, length bound derived from real consumers (branch/slug/path limits); refinement composes via the §2 extension mechanism"
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "verify tag-derived worktree/branch/filename slugs stay within the derived length bound"
        },
        {
          "path": "tests/PendingSchema.test.ts",
          "description": "DAL-REWIRE(usp_Filter_Get) validates against bare core; a tag with whitespace or a path separator fails"
        },
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "document declaring a tag refinement via the entry extension"
        },
        {
          "path": "CHANGELOG.md",
          "description": "Unreleased/Changed bullet per §3"
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/PendingSchema.test.ts",
        "asserts": "DAL-REWIRE(usp_Filter_Get) validates against the bare core; the rendered tag line states whatever constraint is in force"
      }
    ],
    "acceptance": "DAL-REWIRE(usp_Filter_Get) validates against the bare core; a tag containing whitespace or a path separator fails; the dogfood chain's declared refinement still rejects lowercase top-level tags in dogfood runs; the rendered tag line states the in-force constraint.",
    "notes": "Dogfood chain.ts's ALL-CAPS refinement declaration is operator-applied (§3 dogfood leg, same class as §2/§6) — not part of this entry's files."
  },
  {
    "tag": "PENDING-GATE-BUILTIN",
    "summary": "New pendingGate builtin: composed core+extension validation plus plan-time fence pre-check against the target phase's writablePaths, per v0.8 §6.",
    "per": {
      "path": "spec/RELEASE-v0.8.md",
      "section": "6. `pendingGate`: composed validation and fence pre-check as a builtin"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "PENDING-SCHEMA-CORE-EXTENSION-SPLIT"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/builtinGates.ts",
          "description": "new pendingGate(opts) export validating pending.json against the composed core+extension schema (§2) at commit time; pre-checks each entry's files against the target phase's writablePaths ∪ entryChannelPaths, failing with offending paths named"
        },
        {
          "path": "src/index.ts",
          "description": "export pendingGate alongside the other builtinGates"
        },
        {
          "path": "tests/Gate.test.ts",
          "description": "cover pendingGate: composed core+extension validation catches unknown fields; fence pre-check fails naming offending paths for a declared file outside the target phase's writablePaths; a valid queue passes"
        },
        {
          "path": "CHANGELOG.md",
          "description": "Unreleased/Added bullet per §6"
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/Gate.test.ts",
        "asserts": "a pending entry declaring a file outside the build fence fails the plan-side gate with the path named; a valid queue passes"
      }
    ],
    "acceptance": "a pending entry declaring a file outside the build fence fails the plan-side gate with the path named; a valid queue passes; dogfood runs the builtin with its extension schema enforced.",
    "notes": "Dogfood chain.ts replacing its hand-rolled pending-parse gate with this builtin is an operator leg (§6 dogfood adoption, same class as §2/§3) — not part of this entry's files. Extinguishes the GATECONTEXT-REPOROOT-TESTS/CLI-JUNCTION-SAFE-ENTRY-TESTS re-file class per §6's own text."
  },
  {
    "tag": "SUPERVISOR-POLICY-KNOBS",
    "summary": "Open v0.7 §16's quarantine-scope + abort-threshold constants as an optional chain-declared supervisor-policy block; engine values become defaults, per v0.8 §8.",
    "per": {
      "path": "spec/RELEASE-v0.8.md",
      "section": "8. Supervisor policy knobs ride the boundary"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "quarantine-scope/abort-threshold constants from SUPERVISOR-PROVISION-QUARANTINE (§16) become chain-overridable, read from an optional supervisorPolicy block"
        },
        {
          "path": "src/cli.ts",
          "description": "loop/superviseLoop wiring reads the chain's supervisorPolicy override, falling back to the §16 defaults"
        },
        {
          "path": "src/Phase.ts",
          "description": "Chain interface gains optional supervisorPolicy?: { quarantineScope?, abortThreshold? } (shape mirrors §16's constants)"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "a chain overriding abortThreshold changes supervisor behavior; a chain declaring nothing gets v0.7 §16's defaults byte-identical"
        },
        {
          "path": "CHANGELOG.md",
          "description": "Unreleased/Changed bullet per §8"
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/Dispatcher.test.ts",
        "asserts": "a chain overriding the abort threshold changes supervisor behavior accordingly; a chain declaring nothing gets the defaults, byte-identical to v0.7 §16"
      }
    ],
    "acceptance": "a chain overriding the abort threshold changes supervisor behavior accordingly; a chain declaring nothing gets the defaults, byte-identical to v0.7 §16.",
    "notes": "blockedBy SUPERVISOR-PROVISION-QUARANTINE: §16's mechanism (constants + quarantine/abort logic) must ship first — this entry only opens the values as chain-overridable config, no new behavior."
  },
  {
    "tag": "SECOND-REFERENCE-CHAIN",
    "summary": "Ship a single-phase reference chain (e.g. backlog-groomer) in examples/ with its own extension schema + tag refinement, exercising §§2–4 on the unpatched engine, per v0.8 §7.",
    "per": {
      "path": "spec/RELEASE-v0.8.md",
      "section": "7. Second reference chain — the boundary's proof"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "TAG-GRAMMAR-MECHANICAL-SAFETY"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "examples/backlog-groomer-chain.ts",
          "description": "single-phase reference chain: reads a TODO file, picks an item, ships it — own extension schema + tag refinement, no plan/build split"
        },
        {
          "path": "examples/prompts/backlog-groomer.md",
          "description": "prompt template for the single phase"
        },
        {
          "path": "tests/examples.integration.test.ts",
          "description": "smoke test driving one full tick cycle of the new example against a fixture repo"
        }
      ],
      "edit": [
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "two-chains framing: cascade-chain.ts as the flagship derivation-chain example, backlog-groomer-chain.ts as the peer proving the engine isn't chain-shaped"
        },
        {
          "path": ".github/workflows/ci.yml",
          "description": "add a step driving the new example's tick cycle against a fixture repo (mirrors the existing consumer-install smoke shape)"
        },
        {
          "path": "CHANGELOG.md",
          "description": "Unreleased/Added bullet per §7"
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/examples.integration.test.ts",
        "asserts": "the example chain completes a tick cycle against a fixture repo with zero src/ changes attributable to it"
      }
    ],
    "acceptance": "the example chain completes a tick cycle in CI against a fixture repo with zero src/ changes attributable to it; docs present both chains as peers on one engine.",
    "notes": "blockedBy TAG-GRAMMAR-MECHANICAL-SAFETY (transitively needs §2's extension mechanism too); also wants REQUIRES-CAPABILITY-GENERALIZATION landed for full §§2–4 coverage — single-tag blockedBy can't express both, verify before picking if that lags."
  }
]
