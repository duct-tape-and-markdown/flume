[
  {
    "tag": "INLINE-EXEC-STDIN-TRANSPORT",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Prompt.ts",
          "description": "evaluateInlineExec (187-211) spawns sh with no command argv, writes the command to stdin as UTF-8 and closes it; buffers and enforces the 4 MiB cap itself per §2"
        },
        {
          "path": "src/builtinGates.ts",
          "description": "execGate drops its `export` and the Prompt.ts-consumer clause from its docstring (30-38) - shellGate is its only caller once §4 removes the sharing; behavior unchanged per §4"
        },
        {
          "path": "tests/Prompt.test.ts",
          "description": "retire the execFile-mock argv assertions; assert stdin transport and the U+2014 repro"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "Inline-exec reaches sh through stdin instead of argv - the argv path corrupts any non-ASCII byte on win32, silently blinding a tick's digest spans.",
    "per": {
      "path": "spec/RELEASE-v0.10.md",
      "section": "2. Inline-exec reaches `sh` through stdin, never argv"
    },
    "tests": [
      {
        "path": "tests/Prompt.test.ts",
        "asserts": "sh is spawned with no command argument and the command text arrives on stdin - fails on the pre-fix tree, which passes ['-c', cmd], on every platform. The U+2014 span rendering its real output rides this as the end-to-end case."
      }
    ],
    "acceptance": "A span whose command contains U+2014 renders its real output on win32; no inline-exec path constructs ['-c', cmd] or reaches cmd.exe; execGate's shim behavior is untouched.",
    "notes": "§4 removes the sharing outright (supersedes INLINE-EXEC-NO-WIN32-SHELL-RETRY). §2 wants a pre-fix failure - the transport-shape assert does that on every platform; the U+2014 case only fails pre-fix on win32, so it rides. spawn has no maxBuffer, hence the hand-rolled cap. Verified this tick: execGate is absent from index.ts and Prompt.ts is its only outside caller, so §4 strands the export. §4's still-covered leg is pinned via shellGate at tests/Gate.test.ts:147 (win32-gated) - no test write."
  },
  {
    "tag": "INLINE-EXEC-RENDER-REFUSES",
    "gate": {
      "kind": "blockedBy",
      "tag": "INLINE-EXEC-STDIN-TRANSPORT"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Prompt.ts",
          "description": "delete the exec-failed substitution (203-208); an unresolved span aborts the render, naming every failing span's command text and stderr"
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "widen NoCommitMode for the render abort and classify it at both renderPrompt callsites (1029 singleton, 1583 wave)"
        },
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "rewrite the inline-exec section per §6 - stdin transport, the stdin-consumption consequence, render-fails-loud replacing the 'prompt still sends' paragraph at 837"
        },
        {
          "path": "tests/Prompt.test.ts",
          "description": "replace the three marker pins (311, 338, 357) with refusal assertions"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "cover the render-abort no-commit classification"
        },
        {
          "path": "CHANGELOG.md",
          "description": "Breaking entry for the removal per §6"
        }
      ],
      "retire": []
    },
    "summary": "An unresolvable inline-exec span aborts the render - the agent is never invoked, and the tick classifies as a no-commit outcome distinct from a voluntary bail.",
    "per": {
      "path": "spec/RELEASE-v0.10.md",
      "section": "3. An unresolved inline-exec span fails the tick"
    },
    "tests": [
      {
        "path": "tests/Prompt.test.ts",
        "asserts": "renderPrompt throws on a failing span, naming its command text and stderr; an empty-but-zero-exit command still renders - exit status decides, never output length"
      },
      {
        "path": "tests/Dispatcher.test.ts",
        "asserts": "a render abort produces no agent invocation, a non-zero tick, and a noCommit mode a handoff can tell apart from voluntary-bail"
      }
    ],
    "acceptance": "A chain whose prompt names a failing command produces no agent invocation and a non-zero tick with the failing command in the message; no exec-failed marker string remains in src/, docs/, or tests/.",
    "notes": "§3's 'reuse the existing NoCommitMode union' reads as adding a member to it (Prompt.ts:43): the abort must be distinguishable from voluntary-bail, and no current member means 'could not see'. Retire list is empty on purpose - pendingGate fences every files.retire element as a path (touchedPaths, PendingSchema.ts:480), so a symbol there reverts the plan tick; the marker's deletion rides the Prompt.ts edit description instead."
  },
  {
    "tag": "PENDING-RETIRE-HINT-PATH-ONLY",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/PendingSchema.ts",
          "description": "renderSchemaForPrompt's retire hint (419) names a path only, matching the :23 docstring and the touchedPaths/fence consumer"
        },
        {
          "path": "tests/PendingSchema.test.ts",
          "description": "inline snapshot at 525-545 rides the hint text change; add the fence-compatibility assertion"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "renderSchemaForPrompt advertises files.retire as 'path or symbol', but touchedPaths and pendingGate's fence treat every element as a path - a symbol reverts the tick.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A seam gate reads what the real writer wrote"
    },
    "tests": [
      {
        "path": "tests/PendingSchema.test.ts",
        "asserts": "the rendered retire hint advertises no non-path value - fails on the pre-fix tree, where it reads 'path or symbol'"
      }
    ],
    "acceptance": "The prompt-rendered schema's retire hint names a path only; the inline snapshot carries the new text and the suite is green.",
    "notes": "Cost a reverted plan tick 2026-07-31: an entry declared a retire symbol on the hint's authority and pendingGate refused it as outside the fence. PendingSchema.ts:23 already says 'retire is path-only', so the rendered hint is the outlier - correcting it is the simple fix; widening the fence to skip non-paths is machinery one call site does not justify. parsePending cannot catch this class: any non-empty string passes."
  },
  {
    "tag": "PENDING-PARSE-FAILURE-REFUSES",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "readPending refuses on a parse failure at the reads that decide work (1022 singleton, 1111 fanout) and at the rewrite read (2221); the tick returns failed per the EX_MOUNT_DEAD class. The two pendingAfter report reads (1093, 1536) keep today's tolerance, declared at the site"
        },
        {
          "path": "src/cli.ts",
          "description": "the third reader, same shape: `flume render` (982-997) prints the parse errors then hands ctx.pending an empty array (999) - it refuses instead, so no prompt renders over a queue that failed to resolve"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "no corrupt-pending fixture exists today; add both refusal cases"
        },
        {
          "path": "tests/cli.test.ts",
          "description": "cover the render refusal beside the existing render describe (525)"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "An unparseable pending.json reads as an empty queue - a tick reports nothing pickable and hibernates clean, and a post-wave rewrite commits [] over the whole file.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Loud or nothing"
    },
    "tests": [
      {
        "path": "tests/Dispatcher.test.ts",
        "asserts": "a tick whose pending.json fails to parse invokes no agent and returns failed, instead of nothing-pickable plus a clean hibernation; and a wave whose pending.json is corrupted after tick start leaves the file byte-identical rather than committing []. Both fail on the pre-fix tree"
      },
      {
        "path": "tests/cli.test.ts",
        "asserts": "`flume render <singleton>` over a corrupt pending.json writes no prompt to stdout and exits non-zero - fails on the pre-fix tree, which prints the errors to stderr and renders anyway"
      }
    ],
    "acceptance": "A corrupt pending.json produces a non-zero tick naming the parse errors and no agent invocation; no path derives a rewrite of pending.json, or a rendered prompt, from a failed parse.",
    "notes": "readPending (2183-2194) warns and returns []. Both directions lose data - plan re-derives from an empty <pending-now> and commits it; commitPendingUpdate (2221) derives its rewrite from [] and commits '[]'. pendingParseGate cannot bound it - the empty rewrite parses green. failed/EX_MOUNT_DEAD over a new Axis-C kind: the docstring (731-743) already covers an invalid state root, and fail-fast is wanted - no agent repairs this file without discarding it."
  },
  {
    "tag": "PENDINGGATE-FENCES-DECLARED-FILES",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/PendingSchema.ts",
          "description": "declaredPaths(entry) lands beside touchedPaths (476) as the one answer to 'what does this entry declare'; touchedPaths composes it with observedFiles"
        },
        {
          "path": "src/builtinGates.ts",
          "description": "the fence pre-check (293) reads declaredPaths, matching the docstring it already carries (246-248); the touchedPaths import (24) goes"
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "the two inline declared-file lists (1391-1395, 1738-1742) call declaredPaths instead of re-deriving it; their observedFiles-exclusion comments shrink to the helper"
        },
        {
          "path": "tests/PendingSchema.test.ts",
          "description": "pin the split - neither helper today has a direct test"
        },
        {
          "path": "tests/builtinGates.test.ts",
          "description": "the out-of-fence observedFiles case, beside the lazy-fence describe (42)"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "pendingGate fences touchedPaths, which folds in dispatcher-written observedFiles - its own docstring says declared files, and the sibling write guard excludes them by name.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "The fix lands at the mechanism"
    },
    "tests": [
      {
        "path": "tests/builtinGates.test.ts",
        "asserts": "pendingGate passes an entry whose declared files all sit inside the fence but whose observedFiles names a path outside it - fails on the pre-fix tree, which reports it as a fence violation the authoring phase cannot fix"
      },
      {
        "path": "tests/PendingSchema.test.ts",
        "asserts": "declaredPaths returns files.new+edit+retire only; touchedPaths over the same entry additionally returns observedFiles - a one-sided change to either breaks the pair"
      }
    ],
    "acceptance": "One helper answers what an entry declares; pendingGate's fence pre-check no longer reads observedFiles; no site re-derives the declared-file list inline.",
    "notes": "Verified on disk: builtinGates.ts:246-248 promises declared files, :293 passes touchedPaths (PendingSchema.ts:481 folds in observedFiles); Dispatcher.ts:1730 and :1388 exclude observedFiles by name, re-deriving the list inline at :1391 and :1738. Unreachable under this chain today (buildFence covers every path build's guard admits); reachable when targetFence isn't the phase that recorded the footprint. Plan owns neither field, so it cannot fix such a violation."
  },
  {
    "tag": "LOOP-MAX-NONNUMERIC-ACCEPTED",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/cli.ts",
          "description": "bound the --max value where it is parsed (876-877), per the sibling check `job run --max` already performs (673-682); HELP_SUB.loop gains the exit-2 usage line"
        },
        {
          "path": "tests/cli.test.ts",
          "description": "every --max test supplies a valid number today; add the refusal cases to the §2a loop describe (651)"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "`flume loop --max <garbage>` parses to NaN and runs zero ticks on a 0 exit - a typo'd cap is indistinguishable from a clean hibernation.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Loud or nothing"
    },
    "tests": [
      {
        "path": "tests/cli.test.ts",
        "asserts": "`flume loop --max abc` and `--max` with no value each exit 2 and spawn no tick child; `--max 0` keeps today's zero-tick stop and its 'reached --max 0' line. Fails on the pre-fix tree, which exits 0 after printing 'reached --max NaN'"
      }
    ],
    "acceptance": "A non-numeric, negative, or absent --max value exits 2 naming usage; `--max 0` and `--max 5` behave exactly as today.",
    "notes": "Verified this tick: Dispatcher.ts:2399 `?? 50` catches undefined, never NaN; 2431 `i < NaN` is false so the run does zero ticks; 2580 logs 'reached --max NaN'; loopExitCode then returns 0. `--max 0` is legitimate and load-bearing (tests/cli.test.ts:664, 699), so the check bounds the value rather than requiring truthiness."
  },
  {
    "tag": "STATUS-PENDING-COUNT-UNSHIPPED",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/cli.ts",
          "description": "status (761-809) prints the pending count; HELP_TOP:227 and HELP_SUB.status (268-278) both describe what it actually emits, capability-skip lines included"
        },
        {
          "path": "src/job.ts",
          "description": "the count detection inline at 485-491 becomes the one probe both status surfaces call"
        },
        {
          "path": "docs/CLI.md",
          "description": "the status entry (21-28) gains the count and drops 'no chain is loaded' - false since the friction and capability reads landed"
        },
        {
          "path": "tests/cli.test.ts",
          "description": "extend the status describes (804, 1047) with the count cases; the job status describes pin the shared probe"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "`flume status` never prints the pending entry count §3 requires, and drops an unparsable pending.json silently - `flume job status` reports both.",
    "per": {
      "path": "spec/RELEASE-v0.1.md",
      "section": "3. CLI surface"
    },
    "tests": [
      {
        "path": "tests/cli.test.ts",
        "asserts": "`flume status` names the entry count for a valid pending.json, 'unparsable' for a corrupt one, and 0 when the file is absent - fails on the pre-fix tree, which prints no count line in any of the three states; `flume job status` output is byte-unchanged by the shared probe"
      }
    ],
    "acceptance": "`flume status` prints a pending count in all three states; one probe serves both status surfaces; help text and docs/CLI.md describe the output the code emits.",
    "notes": "§3 names three things status prints - awake phases, pending entry count, last commit. Only the awake set shipped. The 'last commit' leg is parked, not filed: unspecified format, and it restates git. cli.ts:793 drops a parse failure under a catch (805) whose comment claims only chain-load failures, so today a corrupt file also silences the v0.8 §4 capability lines. job.ts:485-491 already holds the detection - share it, never re-derive."
  },
  {
    "tag": "GITIGNORE-RUNTIME-ARTIFACTS",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": ".gitignore",
          "description": "add .flume/last-tick.json, .flume/loop.pid, .flume/tick-verdict.json, .flume/tick-verdicts.jsonl"
        }
      ],
      "retire": []
    },
    "summary": "Root .gitignore misses .flume/last-tick.json (stale), .flume/loop.pid, .flume/tick-verdict.json, .flume/tick-verdicts.jsonl - untracked noise every tick.",
    "per": {
      "path": "spec/RELEASE-v0.1.md",
      "section": "8. Repository hygiene"
    },
    "tests": [],
    "acceptance": "A fresh build tick leaves no untracked .flume/last-tick.json, .flume/loop.pid, .flume/tick-verdict.json, or .flume/tick-verdicts.jsonl in git status.",
    "notes": "last-tick.json is dead - v0.8 §5 (Dispatcher.ts:164) superseded it with tick-verdict.json; the stray file predates that ship, safe to also delete by hand. loop.pid is already in RUNTIME_IGNORES (job.ts:39) for job dirs; this repo's root .flume/ isn't a job dir so it never got the entry. tick-verdicts.jsonl (Dispatcher.ts:234, bounded history log) is the file actually untracked in git status this tick."
  },
  {
    "tag": "CHANGELOG-0.9.0-BACKFILL",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "CHANGELOG.md",
          "description": "backfill 5 omitted post-0.8.0 fixes into the published [0.9.0] section"
        }
      ],
      "retire": []
    },
    "summary": "Published [0.9.0] section omits 5 post-0.8.0 fixes that never got a changelog line: 480b81c, 9f4e62f, 1a67d1c, 58be15d, 3170113.",
    "per": {
      "path": "spec/RELEASE-v0.1.md",
      "section": "8. Repository hygiene"
    },
    "tests": [],
    "acceptance": "[0.9.0] section carries one line per commit below; retroactively satisfies the every-ship-gets-a-line convention for the published section.",
    "notes": "480b81c pendingGate lazy targetFence read; 9f4e62f+1a67d1c renderSchemaForPrompt separator-swallowed-by-comment fix; 58be15d win32 inline-exec ENOENT shell-retry fallback; 3170113 shellGate optional env. Pre-1.0: published npm tarball's copy stays stale, acceptable. changelogGate (chain.ts) prevents recurrence going forward; this is the one-time backfill only."
  },
  {
    "tag": "SCHEMA-PROMPT-AGREEMENT-GATE",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/PendingSchema.test.ts",
          "description": "agreement test: real renderSchemaForPrompt output driven through real parsePending; the 'no drift' test (607-623) gets the title it asserts - today it checks only that each declared name appears in the render, never the converse"
        }
      ],
      "retire": []
    },
    "summary": "renderSchemaForPrompt and parsePending are an ungated agreement seam in PendingSchema.ts - nothing drives the real writer through the real reader.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A seam gate reads what the real writer wrote"
    },
    "tests": [
      {
        "path": "tests/PendingSchema.test.ts",
        "asserts": "entries built from renderSchemaForPrompt's stated hints (tag charset/length, notes/summary caps) parse via parsePending; entries violating a stated hint constraint fail parsePending"
      }
    ],
    "acceptance": "New agreement test passes; it would have caught the tag-charset and notes-cap drift instances this entry supersedes (both already in sync today, now pinned against silent regression).",
    "notes": "Closes the class, not just the two known-fixed instances (v0.8 §3 tag-grammar reduction; notes cap already rendered). Supersedes inbox's TAG-PATTERN-SLICE-CONSTRAINT / PENDING-NOTES-CAP-VISIBILITY concern. Distinct from PENDING-RETIRE-HINT-PATH-ONLY: that seam's consumer is the fence, which parsePending never exercises."
  },
  {
    "tag": "TAG-LENGTH-BOUND-AGREEMENT-PIN",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "extend the §5 revert-note describe (4997) with the agreement case"
        },
        {
          "path": "src/PendingSchema.ts",
          "description": "TAG_MAX_LENGTH doc (56-65) points at the pin instead of restating the writer's arithmetic"
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "slugify doc (368-381) drops 'no runtime check needed' for a pointer to the pin"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "TAG_MAX_LENGTH = 255-39 encodes writeRevertNote's filename overhead; both sides restate it in prose and nothing drives the schema's ceiling through the real writer.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A seam gate reads what the real writer wrote"
    },
    "tests": [
      {
        "path": "tests/Dispatcher.test.ts",
        "asserts": "a gate-revert on an entry whose tag is the longest parsePending accepts writes a revert note that exists on disk with a filename component within 255 chars - the real schema bound through the real writer, so a one-sided change to either breaks it"
      }
    ],
    "acceptance": "The revert-note filename is pinned against the schema's own accepted maximum; neither module's prose restates the other's arithmetic.",
    "notes": "PendingSchema.ts:65 hardcodes 39 = a 24-char stamp + 2 + 2 + 'reverted.md'; Dispatcher.ts:2129 is the writer. Correct today - this pins it, so it does not fail pre-fix (same shape as PARSEPENDINGLOOSE-WRITE-PATH-PIN). A silent break lands as ENAMETOOLONG inside writeRevertNote's best-effort catch (2133): a warn line and a lost operator note."
  },
  {
    "tag": "PARSEPENDINGLOOSE-WRITE-PATH-PIN",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/PendingSchema.test.ts",
          "description": "pin parsePendingLoose's 'never on a write path' contract mechanically"
        }
      ],
      "retire": []
    },
    "summary": "parsePendingLoose's 'never used on a write path' contract (PendingSchema.ts:324-329) is prose-only - no test pins it.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [
      {
        "path": "tests/PendingSchema.test.ts",
        "asserts": "parsePendingLoose has exactly one production call site (job.ts's read-only job-listing) and it never rewrites pending.json"
      }
    ],
    "acceptance": "New test fails if a future call site appears outside the documented read-only listing, forcing deliberate review before it ships.",
    "notes": "Posture-sweep finding, PendingSchema.ts neighborhood (bootstrap rotation, no prior stamp). A pinning test is the proportionate fix - one call site today, correctly used; a type-brand is more machinery than the current risk justifies (collaboration.md complexity signal)."
  },
  {
    "tag": "SESSION-CAPTURE-DEFAULT-FILENAME-COLLIDES",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Agent.ts",
          "description": "defaultCaptureFilename (262-264) discriminates per invocation, not by clock alone; the SessionCaptureOpts docstring (217-222) claims only what the code holds"
        },
        {
          "path": "tests/Agent.test.ts",
          "description": "first coverage of the default-filename path - both capture tests today supply an explicit filename"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "withSessionCapture's default filename is clock-only, so two fanout captures opening in the same millisecond share one path and one transcript is silently lost.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Loud or nothing"
    },
    "tests": [
      {
        "path": "tests/Agent.test.ts",
        "asserts": "two withSessionCapture invocations with distinct cwds under a frozen clock (vi.setSystemTime) land in two files, each holding its own stream - fails on the pre-fix tree, where the second createWriteStream truncates the first"
      }
    ],
    "acceptance": "Concurrent captures under a stopped clock produce one file per invocation; the docstring's no-collision claim is the one the test pins.",
    "notes": "Untested today - both capture tests pass filename, and this repo's chain does too (.flume/chain.ts:316-320 appends the cwd basename), so the collision only reaches a downstream chain taking the default. The chain's own filename is the shape the default should have had. Chain-overridable default, so no boundary question."
  },
  {
    "tag": "RENDER-RELATIVIZE-SEPARATOR-BLIND",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Agent.ts",
          "description": "relativize (459-463) strips the cwd prefix on either separator instead of hardcoding '/'"
        },
        {
          "path": "tests/Agent.test.ts",
          "description": "file_path rendering has no coverage today; add the backslash-cwd case beside the POSIX one"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "withTerminalRenderer's relativize hardcodes '/', so on win32 every Read/Write/Edit line renders an absolute path while the tag beside it shortens correctly.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "The fix lands at the mechanism"
    },
    "tests": [
      {
        "path": "tests/Agent.test.ts",
        "asserts": "a tool_use whose file_path sits under a backslash cwd renders relative, and the POSIX pair keeps today's output - separator-agnostic, so it fails on the pre-fix tree on every platform"
      }
    ],
    "acceptance": "Read(<file>) renders relative under a backslash cwd and a POSIX cwd alike.",
    "notes": "The sibling detection exists: the default tag (307) uses node:path basename and carries a win32-gated regression test (tests/Agent.test.ts:473). path.relative is platform-bound - it would make the case win32-only again; stripping on either separator keeps it runnable everywhere. Display-only: the rendered line, never the captured transcript."
  },
  {
    "tag": "PENDING-GATE-HINT-OPTION",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/builtinGates.ts",
          "description": "add optional hint?: string to PendingGateOptions, appended to both violation messages"
        },
        {
          "path": "tests/builtinGates.test.ts",
          "description": "cover hint present/absent on both violation paths"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "pendingGate has no injection point for chain-authored operator guidance on a violation - a bay's hand-rolled fork carried this, the builtin drops it.",
    "per": {
      "path": ".claude/rules/engine-boundary.md",
      "section": "Capability vs convention"
    },
    "tests": [
      {
        "path": "tests/builtinGates.test.ts",
        "asserts": "hint text appended verbatim to both schema-violation and fence-violation failure output when supplied; output unchanged from today when omitted"
      }
    ],
    "acceptance": "pendingGate({ ..., hint: 'park it, never re-scope' }) echoes the hint in both failure branches; default behavior unchanged.",
    "notes": "Passes the second-implementation test - chain supplies text, engine supplies enforcement. Keep minimal: one string, no per-violation-class customization; richer needs stay a chain-side run() wrap. CHANGELOG.md added this tick: the entry touched src/ without declaring it, which changelogGate would have reverted."
  },
  {
    "tag": "GIT-DEAD-EXPORTS-RETIRE",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/git.ts",
          "description": "delete softReset (45), commitAll (198), isDirty (226) and the commitPaths comment clause naming commitAll (211); the header's 'nine commands' count (4) goes with them - isDirty is the module's only `git status` consumer, so the count is 8 after this"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "git.ts exports softReset, commitAll, and isDirty with zero consumers - LSP resolves one reference each, its own declaration.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "An export earns its consumer"
    },
    "tests": [],
    "acceptance": "src/git.ts holds only functions with a live caller; tsc and vitest green.",
    "notes": "LSP findReferences this tick: git.ts:45, :198, :226 each resolve to their own declaration alone. index.ts does not re-export git.ts, so none is declared public surface. commitPaths superseded commitAll - the :211 comment naming it goes too. A deletion pins nothing decidably; the green suite is the proof (the named exception). files.retire stays empty: the fence reads every element as a path."
  },
  {
    "tag": "CASCADE-ENTRY-PHASE-HUMANONLY",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "examples/cascade-chain.ts",
          "description": "phases (263) leads with plan, so the entry phase is machine-wakeable per v0.5 decision 6; the header's three-phase list (5-11) follows the new order"
        },
        {
          "path": "tests/examples.integration.test.ts",
          "description": "no coverage of any example's phase order today - add the entry-phase pin over every chain in examples/"
        }
      ],
      "retire": []
    },
    "summary": "cascade-chain.ts declares phases [spec, plan, build] with humanOnly ['spec'], so a cold `flume job run` wakes the one phase the chain marks human-woken.",
    "per": {
      "path": "spec/RELEASE-v0.1.md",
      "section": "7. Examples"
    },
    "tests": [
      {
        "path": "tests/examples.integration.test.ts",
        "asserts": "every chain under examples/ names a phases[0] absent from its own humanOnly list - fails on the pre-fix tree, where cascade leads with spec"
      }
    ],
    "acceptance": "cascade's phases[0] is plan; a test refuses a humanOnly entry phase across every example chain; CHAIN-AUTHORING's 'cascade leads with plan' claim is true of the file it quotes.",
    "notes": "Verified this tick: job.ts:297 wakes chain.phases[0] unconditionally (v0.5 decision 6); Phase.ts:224-229 scopes humanOnly to handoff alone, so jobRun is not the defect - the example is. docs/CHAIN-AUTHORING.md:1216-1229 shows [plan, build, spec] and says cascade leads with plan because a fresh job must derive pending first; the file it quotes says the opposite. No doc edit needed. Nothing imports cascade-chain.ts, so tsc is its only check today."
  },
  {
    "tag": "EXAMPLES-INTEGRATION-VACUOUS-GREEN",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/examples.integration.test.ts",
          "description": "populate-then-judge at 122; the nothing-pickable test (158-197) asserts the untouched backlog its name claims"
        }
      ],
      "retire": []
    },
    "summary": "gateResults.every(ok) at :122 passes over zero gates, and the second test's 'leaves the backlog untouched' title is asserted nowhere - both greens prove less than they read.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A green verdict is proven non-vacuous"
    },
    "tests": [
      {
        "path": "tests/examples.integration.test.ts",
        "asserts": "the chain's one afterCommit gate is shown present in gateResults before the every(ok) verdict is read; and the nothing-pickable tick leaves BACKLOG.json byte-identical with no SHIPPED.md written"
      }
    ],
    "acceptance": "No assertion in the file reads a verdict off a set it has not shown to be populated, and every test name's claim has an assertion behind it.",
    "notes": "Verified this tick: the chain declares exactly one gate (backlog-groomer-chain.ts:187), so gateResults holds 1 element today - the pin does not fail pre-fix (the named exception); a gate-scheduling regression is what it catches. Test 2 asserts only committed=false plus noCommit, never re-reading BACKLOG.json. Two sections, one file, one commit: splitting would only serialize on file overlap for a one-line second half."
  },
  {
    "tag": "GATE-TEST-TAUTOLOGICAL-GREENS",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/Gate.test.ts",
          "description": "delete the hand-rolled reset tail (431-437) and the title clause it stood behind (416); spell pendingGate's empty-queue case in the describe at 458"
        }
      ],
      "retire": []
    },
    "summary": "Gate.test.ts's broken-chain tail asserts git reset, not flume; and pendingGate's legitimately-empty queue green is inherited, never spelled in its own test.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A green verdict is proven non-vacuous"
    },
    "tests": [
      {
        "path": "tests/Gate.test.ts",
        "asserts": "every assertion in the chainLoadGate broken-chain test has the gate as its subject; and pendingGate over an empty pending.json is green on its own explicit '0 entries' assertion rather than by inheritance"
      }
    ],
    "acceptance": "No assertion in tests/Gate.test.ts has git as its subject; every test name's claim has an assertion behind it; pendingGate's empty-queue green is asserted in its own test.",
    "notes": "Verified this tick: the real revert+restore property is pinned by the real dispatcher at tests/Dispatcher.test.ts:3565-3567, so 431-437 is a hand-rolled restatement - delete it, and the title's second clause with it. pendingGate returns ok over entries=[] (builtinGates.ts:293-312); [] is a valid drained queue, so the case is legitimate and the rule wants it spelled. Neither half fails pre-fix (the named exception). Overlaps GATECONTEXT-REPOROOT-REQUIRED on this path - independent."
  },
  {
    "tag": "GIT-TEST-REMOVAL-PATH-UNDISCRIMINATED",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/git.test.ts",
          "description": "the two removeWorktree success tests (162, 175) assert which path actually ran, through the rm mock the file already installs (15-18); clear the mock immediately before the act so the outer afterEach's own rm(repo) cannot leak into the count"
        }
      ],
      "retire": []
    },
    "summary": "removeWorktree's 'bare remove clears cleanly' test stays green if the bare path always fails - the fallback clears the directory and no assertion tells the two apart.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A green verdict is proven non-vacuous"
    },
    "tests": [
      {
        "path": "tests/git.test.ts",
        "asserts": "the clean-removal test shows rm was never called - the bare path returned early at git.ts:115; the fallback test shows rm was called on the surviving path with recursive: true. Each test name's path becomes its own subject"
      }
    ],
    "acceptance": "Neither removeWorktree success test would pass over the other's code path; the third (survivor) test is unchanged.",
    "notes": "Sweep finding, tests/git.test.ts neighborhood. Both assertions hold today, so neither fails pre-fix (the named exception) - what they catch is a bare-remove regression silently covered by the fallback, invisible on the non-win32 CI where §7's trigger never fires. No new machinery: rm is already partially mocked at :15-18 and mockClear'd in the describe's afterEach."
  },
  {
    "tag": "TEST-CLI-SUBPROCESS-HARNESS-SHARED",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [
        {
          "path": "tests/helpers/subprocess.ts",
          "description": "one home for the CLI-subprocess harness: the tsx/dist/cli.mjs + src/cli.ts entry paths, hermeticEnv over all three canonical vars, runCli with the optional env param cli.test.ts already takes, plus gitOut (git subprocess + trimEnd) per the job.test.ts/job.integration.test.ts duplicate. Not *.test.ts, so neither vitest lane collects it"
        }
      ],
      "edit": [
        {
          "path": "tests/loop-process-boundary.integration.test.ts",
          "description": "import the helper, dropping the drifted copy (25-33, 134-139); runTick/runLoop keep their local shape over it. The §14 hand-built env (247-251) spreads hermeticEnv() rather than process.env - same leak by a second route. Add the FLUME_JOB repro"
        },
        {
          "path": "tests/cli.test.ts",
          "description": "import the helper; the 414-421 + 473-506 blocks go. Add the hermeticEnv pin"
        },
        {
          "path": "tests/job.test.ts",
          "description": "import the helper; the 46-78 block goes, and the byte-identical gitOut (100-103) drops in favor of the shared one"
        },
        {
          "path": "tests/job.integration.test.ts",
          "description": "import the helper; the 26-58 block goes, and the byte-identical gitOut (60-63) drops in favor of the shared one"
        }
      ],
      "retire": []
    },
    "summary": "loop-process-boundary's hermeticEnv omits FLUME_JOB - one of four hand-rolled copies of the CLI-subprocess harness, drifted; the integration lane goes red under a job resolution.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "The fix lands at the mechanism"
    },
    "tests": [
      {
        "path": "tests/loop-process-boundary.integration.test.ts",
        "asserts": "with FLUME_JOB stubbed on the vitest process, a real `flume tick` still resolves the temp repo's own .flume - fails on the pre-fix tree, where the leaked var retargets the child's state root to .flume/jobs/<outer> and the branch guard exits 1 instead of 78"
      },
      {
        "path": "tests/cli.test.ts",
        "asserts": "hermeticEnv() returns an env carrying none of FLUME_DIR, FLUME_CONFIG_DIR, FLUME_JOB when all three are set - the fast-lane pin, so a future partial copy cannot ship green through build's afterMerge gate"
      }
    ],
    "acceptance": "One module states tsx's dist layout and builds the hermetic env; all four suites import it; the integration lane is green when run under a job resolution.",
    "notes": "Verified this tick: loop-process-boundary.integration.test.ts:134-139 deletes FLUME_DIR+FLUME_CONFIG_DIR only; cli.test.ts:481, job.test.ts:55, job.integration.test.ts:35 also delete FLUME_JOB. cli.ts:136-140 then resolves the child's flumeDir to <tempRepo>/.flume/jobs/<outer>. Integration lane only, so build's gate never sees it. tsx's dist path is a literal in all four. Sweep: gitOut is byte-identical at job.test.ts:100-103/job.integration.test.ts:60-63 - folded here, not a new entry."
  },
  {
    "tag": "RUNTIME-IGNORES-LOOP-VACUITY-PIN",
    "gate": {
      "kind": "deferred",
      "reason": "tests/job.test.ts is v0.11's most-churned file; landing this pin now duplicates work v0.11's test rewrite will redo"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/job.test.ts",
          "description": "the three `for (const entry of RUNTIME_IGNORES)` verdicts (205-207, 287-289, 552-554) show the judged set populated before reading it"
        }
      ],
      "retire": []
    },
    "summary": "Three .gitignore-merge tests read their verdict off a loop over RUNTIME_IGNORES - an emptied constant leaves each one green over zero assertions.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A green verdict is proven non-vacuous"
    },
    "tests": [
      {
        "path": "tests/job.test.ts",
        "asserts": "each RUNTIME_IGNORES loop is preceded by a populated-set assertion, so an emptied constant fails the merge tests themselves instead of passing over nothing"
      }
    ],
    "acceptance": "No assertion in tests/job.test.ts reads a verdict off a set it has not shown to be populated.",
    "notes": "Deferred 2026-07-31 (inbox): the pinned behavior (jobNew ignore-merge) survives v0.11, but tests/job.test.ts is v0.11's most-churned file (RELEASE-v0.11.md §3) - landing this pin now does the work twice. Fold into v0.11's test rewrite once that line derives. job.ts:34/177/219/639 unchanged meanwhile."
  },
  {
    "tag": "PROMPT-TEST-FENCE-SLICE-VACUOUS",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/Prompt.test.ts",
          "description": "the no-entryChannelPaths test (217-241) shows both slice markers present before reading the verdict off the slice - the sibling at 211-214 already does; and asserts the two claims its title makes (fence is exactly entry.files, no stray empty line)"
        }
      ],
      "retire": []
    },
    "summary": "The fence section at 236-240 is sliced between two indexOf results - an absent marker yields '' and the not-toContain verdict stays green over nothing.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A green verdict is proven non-vacuous"
    },
    "tests": [
      {
        "path": "tests/Prompt.test.ts",
        "asserts": "the fence slice is shown non-empty before its not-toContain verdict is read; the fence names exactly src/only.ts and carries no blank bullet line - the title's own two claims"
      }
    ],
    "acceptance": "No assertion in tests/Prompt.test.ts reads a verdict off a slice it has not shown to be populated; every test name's claim has an assertion behind it.",
    "notes": "Sweep finding, tests/Prompt.test.ts neighborhood. Verified on disk: String.indexOf returns -1 for an absent marker, so a rendering that emitted no scoped block at all slices to '' and :240 passes. Does not fail pre-fix (the named exception) - both markers are present today; what it catches is the scoped rendering regressing to the unscoped one. Prompt.ts:275 dedupes the fence, so 'exactly' is assertable."
  },
  {
    "tag": "ERA-SCOPED-NARRATION-PROMPT-NEIGHBORHOOD",
    "gate": {
      "kind": "blockedBy",
      "tag": "INLINE-EXEC-RENDER-REFUSES"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Prompt.ts",
          "description": "lines 47-48, 142, 252 - state the condition or point at the pin"
        },
        {
          "path": "src/builtinGates.ts",
          "description": "lines 77 and 250-251 - same"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "Prompt.ts neighborhood carries era-scoped narration: three 'byte-identical to past release' claims a test already pins, plus a clause citing two shipped-and-gone pending tags.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [],
    "acceptance": "Each cited site states the condition that would change the decision or points at the test holding it; no prose restates a property a test already pins.",
    "notes": "Pinned already, so prose shrinks to a pointer: Prompt.ts:142/:252 by tests/Prompt.test.ts:144; builtinGates.ts:77 by tests/Gate.test.ts:61. builtinGates.ts:250-251 cites GATECONTEXT-REPOROOT-TESTS / CLI-JUNCTION-SAFE-ENTRY-TESTS, both shipped and unresolvable today. Prompt.ts:47-48 is the same shape - and v0.10 §3 rewrites that exact docstring, hence the block; re-read every line number after it lands. Prose-only; no behavior moves."
  },
  {
    "tag": "ERA-SCOPED-NARRATION-DISPATCHER-NEIGHBORHOOD",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "lines 626-632, 2294, 2311, 2392-2393 - state the condition or point at the pin"
        },
        {
          "path": "src/Phase.ts",
          "description": "lines 169-170 and 268-269 - same"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "Dispatcher neighborhood carries era-scoped narration: three 'byte-identical to v0.7 §16' claims tests already pin, plus three sites narrating mechanisms that no longer exist.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [],
    "acceptance": "Each cited site states the condition that would change the decision or points at the test holding it; no prose restates a property a test pins or a mechanism the tree no longer has.",
    "notes": "Pinned already: tests/Dispatcher.test.ts:4202 (undeclared abortThreshold still aborts on the 3rd consecutive tick), :4117 (quarantineScope default). Dispatcher.ts:626-632 narrates a deleted content-hash cache; 2392-2393 compares against the removed in-process loop. Phase.ts:169-170 'existing void-returning implementations are unaffected' is migration-era under the pre-1.0 clean-slate posture. Prose-only; no behavior moves."
  },
  {
    "tag": "LOOP-LOCK-SHARES-LIVELOOPPID",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/cli.ts",
          "description": "the loop lock (885-902) takes liveness from liveLoopPid — already imported at :34 — instead of re-deriving the probe inline"
        },
        {
          "path": "src/job.ts",
          "description": "liveLoopPid docstring (306-311) names the lock among its consumers; its reuse claim only becomes true with this entry"
        },
        {
          "path": "tests/cli.test.ts",
          "description": "add the agreement pin to the §2a loop-lock describe (651); the :800 status-liveness describe already narrates the sharing"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "flume loop's lock re-derives liveLoopPid's pid probe inline — the helper's own docstring says it was exported to prevent exactly this second implementation.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "The fix lands at the mechanism"
    },
    "tests": [
      {
        "path": "tests/cli.test.ts",
        "asserts": "the loop lock and `flume status` return the same liveness verdict over one live and one stale pidfile — one probe, so a future one-sided change breaks it"
      }
    ],
    "acceptance": "Liveness reaches the loop lock through liveLoopPid; no second process.kill pid probe remains in src/cli.ts; the loop-lock and status-liveness suites stay green.",
    "notes": "Verified this tick: cli.ts:886-895 repeats liveLoopPid's read/finite/positive/kill(pid,0) sequence; job.ts:306-311 claims the opposite. Drop-in — the lock wants the pid only for its refusal message, which liveLoopPid returns, and the block is already async. Behavior-identical today, so nothing fails pre-fix (the named exception); the agreement pin is what makes a later divergence fail."
  },
  {
    "tag": "ERA-SCOPED-NARRATION-JOB-NEIGHBORHOOD",
    "gate": {
      "kind": "deferred",
      "reason": ":570 and :793 sit inside extract, which RELEASE-v0.11.md §3 deletes outright; :417 is in code that section rewrites - hold until v0.11 derives"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/job.ts",
          "description": "lines 48, 417, 570, 793 - state the condition or drop the retired framing"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "job.ts carries era-scoped narration: a cite to the shipped-and-gone JOB-RESOLUTION tag, two pre-0.9 migration clauses, and a superseded harvest convention.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [],
    "acceptance": "No site in src/job.ts cites an unresolvable pending tag or scopes itself to a closed migration window; no behavior moves.",
    "notes": "Deferred 2026-07-31 (inbox): :570/:793 sit inside extract, which RELEASE-v0.11.md §3 removes outright; :417's pre-0.9 clause is in code that section rewrites. Only :48 (JOB-RESOLUTION cite) survives v0.11 untouched. Re-scope to :48 alone or drop, once v0.11 derives."
  },
  {
    "tag": "CLI-RESOLVESTATEDIRS-DOCSTRING-ORPHANED",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/cli.ts",
          "description": "move the 77-105 block down onto resolveStateDirs (126); resolveRepoRoot keeps its own 107-114 block. A move, not a rewrite - the text is accurate for its subject"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "resolveStateDirs' 29-line docstring is detached - a second docstring intervenes, so it documents nothing and the v0.6 §3 resolution authority is invisible to hover.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [],
    "acceptance": "Hover on resolveStateDirs shows the job-resolution semantics; hover on resolveRepoRoot is unchanged.",
    "notes": "LSP hover this tick: resolveStateDirs (126) returns the signature alone, no docs; resolveRepoRoot (115) returns only its own block. The 77-105 text sits between JobResolutionConflictError (75) and resolveRepoRoot's docstring, separated by a blank line, so TS attaches it to nothing - the conflict rule, the write-back rationale, and the FLUME_JOB composition case all read as current while reaching no consumer. Prose-only."
  },
  {
    "tag": "GATECONTEXT-REPOROOT-REQUIRED",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Gate.ts",
          "description": "repoRoot (52) becomes required; the 45-51 docstring drops the fixture-compat clause and states the field's meaning alone"
        },
        {
          "path": "tests/Gate.test.ts",
          "description": "the ctx helper (23-31) sets repoRoot; delete the 294-304 describe, which asserts only that object spread works"
        },
        {
          "path": "tests/builtinGates.test.ts",
          "description": "same one-line ctx helper change (21)"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit; Breaking - a hand-built GateContext must now set repoRoot"
        }
      ],
      "retire": []
    },
    "summary": "GateContext.repoRoot is optional only so pre-v0.7 fixtures compile - the engine always sets it, so the type advertises a state no producer creates.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [
      {
        "path": "tests/Gate.test.ts",
        "asserts": "the existing gate suites keep passing over a repoRoot-bearing ctx; no assertion remains whose only subject is the local fixture builder"
      }
    ],
    "acceptance": "GateContext.repoRoot is non-optional, tsc and vitest are green, and no docstring scopes the field to a closed fixture-migration window.",
    "notes": "Verified this tick: every dispatcher-constructed context sets repoRoot (Dispatcher.ts:1219, 1324, 1358, 1752, 1900); the only undefined producers are the two test ctx helpers. v0.7 §6 mandates the field, not its optionality. tsc's required-field check is the pin - no runtime test fails pre-fix (the named exception); the real §6 behavior stays pinned at tests/Dispatcher.test.ts:4459. Chain gates currently branch on a state the engine never emits."
  },
  {
    "tag": "ERA-SCOPED-NARRATION-BARREL-NEIGHBORHOOD",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/index.ts",
          "description": "line 3 - the exports map exists and does enforce; state the condition or drop the clause"
        },
        {
          "path": "src/setupWorktree.ts",
          "description": "lines 4-10 - drop the pre-adoption 'has been hand-rolling' rationale for what the helper does"
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "index.ts and setupWorktree.ts carry expired narration: a 'no exports map yet' claim package.json contradicts, and a hand-rolling rationale the dogfood chain retired.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [],
    "acceptance": "Neither site claims a condition the tree contradicts; no behavior moves.",
    "notes": "Fourth sibling of the ERA-SCOPED-NARRATION-* family. Verified this tick: package.json:26-31 declares an exports map exposing '.' alone, so index.ts:3's 'not enforced - there's no exports map yet' fired its own trigger. setupWorktree.ts:5-7 names flume's dogfood buildSetupWorktree as a hand-rolled hardcode; chain.ts:50 imports the engine helper, :197-201 delegates to it, and :192-193 says so outright. Prose-only."
  },
  {
    "tag": "CLI-TEST-OMISSION-VERDICTS-VACUOUS",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/cli.test.ts",
          "description": "the three not-toContain-only status tests (962, 976, 1064) show `status` produced its output before reading the omission verdict - each positive sibling already does; the walk-up equality test (1165) shows the pair populated before comparing"
        }
      ],
      "retire": []
    },
    "summary": "Four cli.test.ts verdicts read off r.out with nothing showing it populated - a status regressed to empty output passes all three omission tests and the walk-up equality.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A green verdict is proven non-vacuous"
    },
    "tests": [
      {
        "path": "tests/cli.test.ts",
        "asserts": "each omission test shows the status line present before asserting the absent one; the subdirectory walk-up test shows fromRoot.out names the job before asserting fromNested.out equals it"
      }
    ],
    "acceptance": "No assertion in tests/cli.test.ts reads a verdict off output it has not shown to be populated.",
    "notes": "Sweep finding, tests/cli.test.ts neighborhood. Verified on disk: 970, 986, 1072-1073 assert only exit 0 plus not-toContain; 1177 compares two outputs neither shown non-empty. Siblings at 955 and 1147 carry the pin already. Does not fail pre-fix (the named exception) - cli.ts:763 prints 'hibernating' today; what it catches is that line going silent."
  },
  {
    "tag": "CLI-TEST-DOCBLOCKS-ORPHANED",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/cli.test.ts",
          "description": "move the 402-412 block onto the describe it heads (651), and the 585-600 block onto its own describe holding the two v0.8 §8 tests (716-793) that today sit under a title naming the loop lock; the four src/cli.ts cites (425, 512, 937, 1083) name symbols, not lines"
        }
      ],
      "retire": []
    },
    "summary": "Two suite doc blocks sit ~200 lines above their subjects, one claiming what is false of the tests it now heads; all four src/cli.ts line cites in the file point at unrelated lines.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [],
    "acceptance": "Each doc block sits on the describe it describes; no cite in the file names a line number; the suite is green and no test moves between files.",
    "notes": "Verified on disk this tick: isInvokedDirectly is cli.ts:1041 not :854; the render CjsContextLoadError catch is :966 not :872-881; runJobVerb is :449 not :356-387; frictionCountLine is Dispatcher.ts:509, not cli.ts at all. The 402-412 block's 'no chain.ts, no git repo, no child processes' is false of the two supervisorPolicy tests it now heads. Prose + placement only; no assertion changes."
  },
  {
    "tag": "DISPATCHER-TEST-FRICTION-OMISSION-VACUOUS",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "the three friction-omission tests (4268-4292, 4294-4320, 4349-4369) assert a known always-present line first - 'hibernating after' for the two hibernation cases, 'reached --max 2' for the third - before reading the friction-omission verdict, matching the pattern the sibling positive tests (4234-4266, 4322-4347) already use"
        }
      ],
      "retire": []
    },
    "summary": "Three superviseLoop friction-omission tests read infos.some(...).toBe(false) without ever showing infos populated - a silenced logger passes all three.",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A green verdict is proven non-vacuous"
    },
    "tests": [
      {
        "path": "tests/Dispatcher.test.ts",
        "asserts": "each of the three omission tests asserts a known always-emitted line is present in infos before asserting the friction line's absence - fails on the pre-fix tree, which passes even if superviseLoop's logger silently stopped emitting anything"
      }
    ],
    "acceptance": "All three omission tests assert infos non-empty via a known line before the friction-omission check; suite green.",
    "notes": "Sweep finding, tests/Dispatcher.test.ts neighborhood. Verified on disk: lines 4291, 4319, 4368 have no preceding assertion on infos; the --max positive test at 4343 already proves the pattern (asserts 'reached --max 2' before the friction:true check at 4345) - its omission sibling at 4349-4369 lacks the equivalent. Does not fail pre-fix (the named exception) - superviseLoop does log today; what it catches is the logger going silent."
  },
  {
    "tag": "SCHEMADELTA-DEAD-FIXTURE-FIELD",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/partition.test.ts",
          "description": "delete the dead `schemaDelta: \"none\"` line from makeEntry (21)"
        },
        {
          "path": "tests/Prompt.test.ts",
          "description": "same dead field, the entry() helper (86)"
        }
      ],
      "retire": []
    },
    "summary": "Two test fixture builders still set schemaDelta on constructed PendingEntry objects - the field was deleted from the schema and has no consumer.",
    "per": {
      "path": ".claude/rules/posture-sweep.md",
      "section": "A violation counts only when verified on disk this tick"
    },
    "tests": [],
    "acceptance": "No occurrence of schemaDelta remains in tests/partition.test.ts or tests/Prompt.test.ts; vitest green.",
    "notes": "Sweep finding, tests/partition.test.ts neighborhood. 8c52c92 deleted schemaDelta (dead field, no consumer); these two fixture builders still set it (PendingEntry types as Record<string, unknown>, so tsc never caught it). PendingSchema.test.ts:219-226 reuses the same name as its rejected-undeclared-field example, so the leftover is a landmine, not neutral cruft: a real parsePending call would refuse it. A deletion pins nothing decidably; the green suite is the proof (the named exception)."
  }
]
