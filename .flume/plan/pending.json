[
  {
    "tag": "JOB-SEED-RM-COMMIT-MESSAGE-HARDCODED",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/job.ts",
          "description": "JobNewOptions gets an optional commitMessage?: (name: string) => string, called at the seed commit (jobNew, ~209); JobRmOptions gets the same for the rm commit (jobRm, ~341). Default (omitted) reproduces today's exact 'chore(flume): seed job <name>' / 'chore(flume): rm job <name>' text — same pattern as DispatcherOptions.commitMessage (Dispatcher.ts, shipped 4f947fc)."
        },
        {
          "path": "tests/job.test.ts",
          "description": "cover a commitMessage override landing verbatim on jobNew's seed commit and jobRm's rm commit; the omitted-option case stays byte-identical to today (existing assertions at :857-858 anchor the default subjects)."
        },
        {
          "path": "CHANGELOG.md",
          "description": "changelogGate requires a line on any src/-touching commit"
        }
      ],
      "retire": []
    },
    "summary": "jobNew/jobRm hardcode 'chore(flume): seed/rm job <name>' commit text with no override point — same gap as the just-shipped Dispatcher ship-commit fix.",
    "per": {
      "path": ".claude/rules/engine-boundary.md",
      "section": "Capability vs convention"
    },
    "tests": [
      {
        "path": "tests/job.test.ts",
        "asserts": "a JobNewOptions.commitMessage / JobRmOptions.commitMessage override lands verbatim as the seed/rm commit subject; omitting either reproduces today's exact 'chore(flume): seed job <name>' / 'chore(flume): rm job <name>' text"
      }
    ],
    "acceptance": "A caller can override jobNew's seed-commit and jobRm's rm-commit message text via JobNewOptions.commitMessage / JobRmOptions.commitMessage; default output is byte-identical to today; tsc/vitest green.",
    "notes": "Found via posture sweep of src/job.ts and via DISPATCHER-COMMIT-MESSAGE-PREFIX-HARDCODED's own notes, which flagged job.ts:209/341 as 'outside this neighborhood - a later sweep, not this entry's scope.' Verified on disk this tick: both sites still hardcode the literal, no override exists."
  }
]
