[
  {
    "tag": "CLI-FIXTURE-ANCESTOR-PROOF",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        { "path": "tests/helpers/subprocess.ts", "description": "a fixture-rooting idiom beside hermeticEnv()" },
        { "path": "tests/cli.test.ts", "description": "the tmpdir()-rooted fixtures the walk-up reaches past" },
        { "path": "tests/cliJobResolution.test.ts", "description": "same" }
      ],
      "retire": []
    },
    "summary": "CLI fixtures under tmpdir() resolve repoRoot through /tmp's ancestors; root them so a `.flume` above the fixture cannot change a verdict",
    "per": { "path": "spec/cli.md", "section": "Bay discovery walks up to the nearest `.flume`" },
    "tests": [
      "a `.flume` planted above the fixture does not change `flume status`'s verdict",
      "a `.flume` planted above the fixture does not change a job verb's resolved state root"
    ],
    "acceptance": "Both named behaviors pin, and the eight tests a leaked /tmp/.flume red-lined pass with a `.flume` planted above their base. A fixture that must assert the no-ancestor fallback and cannot be rooted is named in the note instead of parking the entry.",
    "notes": "The walk runs to the filesystem root (`resolveRepoRoot`, src/cliJobResolution.ts:48); the cited section ratifies FLUME_DIR/FLUME_CONFIG_DIR as the outright override, so a fixture that sets them never walks. Blast-radius bound, independent of who writes the litter."
  },
  {
    "tag": "TMP-STATE-ROOT-WRITER-NAMED",
    "gate": { "kind": "blockedBy", "tags": ["CLI-FIXTURE-ANCESTOR-PROOF"] },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        { "path": "tests/helpers/subprocess.ts", "description": "the guard, beside the fixture-rooting idiom" },
        { "path": "vitest.config.ts", "description": "wire the guard suite-wide if that is where it lands" }
      ],
      "retire": []
    },
    "summary": "A run intermittently plants /tmp/.flume (awake/ + empty stop) and red-lines 8 CLI tests on later runs; make the suite refuse it and name the writer",
    "per": { "path": ".claude/rules/engineering.md", "section": "Loud or nothing" },
    "tests": [
      "a run that creates a state root above its own fixture fails the suite and names the offender"
    ],
    "acceptance": "`pnpm test --run` fails loudly, naming the test, when a state root appears outside a fixture; the writer it attributes is fixed, or the note says the leak did not reproduce this tick.",
    "notes": "Observed twice 2026-09-06; `rm -rf /tmp/.flume` made all 8 pass unchanged. The Baton constructor mkdirs `awake/` (src/job.ts:494), so any Baton built at a repoRoot that resolved to /tmp is a candidate. No src/ module reads tmpdir()."
  },
  {
    "tag": "HANDOFF-ENTRY-MERGE-OUTCOME",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        { "path": "src/Phase.ts", "description": "FanoutEntryOutcome gains the per-entry merge outcome" },
        { "path": "src/Dispatcher.ts", "description": "the `entries` map reads the wave's own mergeOutcomes" }
      ],
      "retire": []
    },
    "summary": "A park and a cherry-pick conflict read identically on TickResult.entries, so a handoff routing a park wakes plan on a conflict too and pays a declined tick",
    "per": { "path": "spec/chain.md", "section": "What a hook receives" },
    "tests": [
      "TickResult.entries reports the merge outcome of an entry whose cherry-pick conflicted",
      "TickResult.entries reports the merge outcome of an entry the chain declined to ship"
    ],
    "acceptance": "`handoff` separates `cherry-pick-conflict` from `not-shipped` off `result.entries` alone, without reading the verdict log.",
    "notes": "`mergeOutcomes` is already in scope at the `entries` map (src/Dispatcher.ts:2550, :3071); the verdict carrying it is written after `handoff` runs. The cited section enumerates the record as `{tag, committed, shipped, reverted, declined?, noCommit?}` — that list wants the human's amendment once this lands."
  },
  {
    "tag": "CHAIN-AUTHORING-RUNTIME-NAME-AGREEMENT",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        { "path": "docs/CHAIN-AUTHORING.md", "description": "the job-seed `.gitignore` list (:111-:112) and the harness-managed run-in (:69-:70)" },
        { "path": "tests/retired-narration.test.ts", "description": "a `RUNTIME_IGNORES` agreement pin, and the state-list check flipped to equality, in the shipped harness-managed scan" }
      ],
      "retire": []
    },
    "summary": "docs/CHAIN-AUTHORING.md hand-lists RUNTIME_IGNORES and names 5 of the 9 state-root names src/ spells, neither held in the src-to-doc direction",
    "per": { "path": ".claude/rules/engineering.md", "section": "A seam gate reads what the real writer wrote" },
    "tests": [
      "the chain-authoring doc's job-seed gitignore list names every entry RUNTIME_IGNORES carries and no others",
      "the docs' harness-managed state list names every path src/ spells"
    ],
    "acceptance": "Both doc lists equal their writers. The state-list equality fails on the pre-fix tree; the RUNTIME_IGNORES pin is green on arrival and rides a sensitivity control.",
    "notes": "Seed list (:111) equals RUNTIME_IGNORES (src/job.ts:52) today, so its pin needs the sensitivity control the shipped scan rides (tests/retired-narration.test.ts:918). The run-in (:69) omits `stop`, the tick-verdict pair, and `plan/pending.json` of the 9 `namesSrcSpells()` resolves; README already carries all 9. 6abd604's body claims both lists carry them — oversight, not a declared cut."
  },
  {
    "tag": "EXAMPLES-SPEC-PHASE-CUT",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        { "path": "examples/cascade-chain.ts", "description": "drop the `spec` phase, its gate, its writable partition and `humanOnly`" },
        { "path": "docs/CHAIN-AUTHORING.md", "description": "the excerpts quoting the cut phase (:176-:177, :1409)" },
        { "path": "tests/examples.integration.test.ts", "description": "expectations that named the removed phase" }
      ],
      "retire": ["examples/prompts/spec.md"]
    },
    "summary": "Cut examples/prompts/spec.md and cascade's spec phase together, so the shipped example models the plan+build shape flume's own chain runs",
    "per": { "path": ".claude/rules/engineering.md", "section": "An export earns its consumer" },
    "tests": [
      "the cascade example declares plan and build and no spec phase"
    ],
    "acceptance": "The example loads and ticks under tests/examples.integration.test.ts with no `spec` phase, no `specs/active` / `specs/_aligned` / `workshop/_archive` partition, and no prompt file for it.",
    "notes": "Ruling of 2026-09-11: the partition is a shape cascade dropped in June and no current consumer runs. docs/CHAIN-AUTHORING.md quotes the example by agreement pin (tests/retired-narration.test.ts), so its excerpts move in the same commit or the pin reverts the tick."
  },
  {
    "tag": "INTENT-DOC-RECONCILED",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        { "path": "docs/INTENT.md", "description": "the Provenance bullet (:19), the v0 success criterion (:44), the executed corpus-reform item (:54)" }
      ],
      "retire": []
    },
    "summary": "docs/INTENT.md claims the harness verifies typed citations, carries a v0 success criterion whose target no longer exists, and lists an executed decision as pending",
    "per": { "path": ".claude/rules/engineering.md", "section": "Narration is the ladder's bottom rung" },
    "tests": [],
    "acceptance": "The Provenance bullet states the 0.8+ truth — `per` is a chain-declared extension the engine never parses, and citation discipline is the chain's `per cites resolve` gate; the v0 success criterion section is gone; the corpus-reform section is gone.",
    "notes": "Ruling of 2026-09-11: (a) restate, (b) retire outright — the comparison target (`bin/flume-bash`, gen2 specs) exists in no tree and a restated target would be a claim nobody has measured, (c) delete. The quality-lenses section at :72 is untouched: it has not executed."
  },
  {
    "tag": "MIGRATING-012-AFTERMERGE-RESTORATION",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        { "path": "docs/MIGRATING-0.12.md", "description": "§1, one line naming afterMerge placement as a restoration of v0's" }
      ],
      "retire": []
    },
    "summary": "MIGRATING-0.12 §1 reads as a new requirement, so a v0-shaped chain that never moved its correctness gates cannot tell it is already compliant",
    "per": { "path": "spec/chain.md", "section": "Gate placement is the chain's decision" },
    "tests": [],
    "acceptance": "§1 says in one line that afterMerge is where v0 put correctness gates, so a chain that never moved reads itself as compliant rather than as owing a migration step.",
    "notes": "Ruling of 2026-09-11. The cumulative symbol-to-release index from the same backlog is declined until the 1.0 cut and is not in this queue."
  },
  {
    "tag": "CHAIN-AUTHORING-NAMES-LIVING-REFERENCE",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        { "path": "docs/CHAIN-AUTHORING.md", "description": "the opening orientation (:3-:15) names `.flume/chain.ts` as the living reference and the examples as its shipped floor" }
      ],
      "retire": []
    },
    "summary": "The doc calls cascade-chain.ts the reference and never names `.flume/chain.ts`, which is the only chain carrying the current shape",
    "per": { "path": ".claude/rules/engine-boundary.md", "section": "Surface, not prescription" },
    "tests": [],
    "acceptance": "A reader opening the doc learns that `.flume/chain.ts` in this repo is the living reference chain and that `examples/` is the shipped floor — the recommended shape ships by name, not as a default.",
    "notes": "Ruling of 2026-09-11 (option C, first half). The promotions into cascade-chain.ts follow as their own entries, blocked on this one so the doc's claim lands before the shapes it points at."
  },
  {
    "tag": "CASCADE-SHOULDRUN-FROM-DISK",
    "gate": { "kind": "blockedBy", "tags": ["CHAIN-AUTHORING-NAMES-LIVING-REFERENCE"] },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        { "path": "examples/cascade-chain.ts", "description": "a `shouldRun` on one phase computed from TickContext" },
        { "path": "tests/examples.integration.test.ts", "description": "the decline case" }
      ],
      "retire": []
    },
    "summary": "No shipped example declares `shouldRun`, so a new adopter never sees a phase decline a tick from what is on disk",
    "per": { "path": "spec/chain.md", "section": "What a hook receives" },
    "tests": [
      "the cascade example's plan phase declines a tick when nothing on disk gives it work"
    ],
    "acceptance": "cascade-chain.ts declares a `shouldRun` reading `TickContext` — not `process.env`, not a directory scan — and the integration test drives both the decline and the run.",
    "notes": "Ruling of 2026-09-11 (option C): promote the shapes a new adopter most needs, in order. This is the first."
  },
  {
    "tag": "CASCADE-TESTS-JUDGED-BY-GATE",
    "gate": { "kind": "blockedBy", "tags": ["CASCADE-SHOULDRUN-FROM-DISK"] },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        { "path": "examples/cascade-chain.ts", "description": "a gate reading the entry's `tests[]` out of a test reporter's output" },
        { "path": "tests/examples.integration.test.ts", "description": "the refusal case" }
      ],
      "retire": []
    },
    "summary": "cascade declares a `tests[]` extension field nothing judges, so the example teaches the field without the gate that makes it load-bearing",
    "per": { "path": ".claude/rules/engine-boundary.md", "section": "Surface, not prescription" },
    "tests": [
      "the cascade example's test gate refuses an entry whose named behavior has no passing test"
    ],
    "acceptance": "A `tests[]` line on a cascade entry is judged by a gate fed from the reporter's own output, and an entry naming a behavior no test carries fails that gate.",
    "notes": "The shape ships in `.flume/vitestJudge.ts`; the example carries its own, sized to the example. Red-on-base judging is not part of this entry — that fork is still open."
  },
  {
    "tag": "CASCADE-GATE-READS-ENTRY-AND-BASE",
    "gate": { "kind": "blockedBy", "tags": ["CASCADE-TESTS-JUDGED-BY-GATE"] },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        { "path": "examples/cascade-chain.ts", "description": "a gate reading `GateContext.entry` and `baseSha`" },
        { "path": "tests/examples.integration.test.ts", "description": "the gate's context read" }
      ],
      "retire": []
    },
    "summary": "No shipped example reads `GateContext.entry` or `baseSha`, so the two facts a per-entry gate most needs look unavailable to an adopter",
    "per": { "path": "spec/chain.md", "section": "What a gate receives" },
    "tests": [
      "the cascade example's gate reads the entry tag and the span's base sha from its gate context"
    ],
    "acceptance": "A cascade gate scopes its verdict to the entry it is gating and to the span `baseSha..commitSha`, reading both off `GateContext` rather than re-deriving them.",
    "notes": "Ruling of 2026-09-11 (option C), third promotion."
  },
  {
    "tag": "CASCADE-PLAN-SLICES-ORDERED-HANDOFF",
    "gate": { "kind": "blockedBy", "tags": ["CASCADE-GATE-READS-ENTRY-AND-BASE"] },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        { "path": "examples/cascade-chain.ts", "description": "plan split into slices, with a handoff that orders them" },
        { "path": "examples/prompts/plan.md", "description": "per-slice material if the split needs it" },
        { "path": "docs/CHAIN-AUTHORING.md", "description": "the block quoting cascade's `plan` phase, held by an agreement pin" },
        { "path": "tests/examples.integration.test.ts", "description": "the slice order" }
      ],
      "retire": []
    },
    "summary": "cascade's plan is one phase, so the example never shows slices sharing a dispatcher with a dependency-ordered handoff",
    "per": { "path": ".claude/rules/engine-boundary.md", "section": "Surface, not prescription" },
    "tests": [
      "the cascade example's plan slices hand off in dependency order"
    ],
    "acceptance": "cascade declares more than one plan slice, each computing its own liveness from disk, with a handoff that passes the baton down a declared order; the integration test ticks the ladder.",
    "notes": "Ruling of 2026-09-11 (option C), last promotion. docs/CHAIN-AUTHORING.md quotes cascade's `plan` phase verbatim under an agreement pin (tests/retired-narration.test.ts) — the quote moves in the same commit or the pin reverts the tick."
  },
  {
    "tag": "RELEASE-CITES-CUT-DISPATCHER",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        { "path": "src/Dispatcher.ts", "description": "81 `RELEASE-v0.N §M` / `v0.N §M` cites" }
      ],
      "retire": []
    },
    "summary": "src/Dispatcher.ts carries 81 doc cites into spec/RELEASE-v*.md, files the corpus reform deleted — a cite no reader can follow",
    "per": { "path": ".claude/rules/engineering.md", "section": "Narration is the ladder's bottom rung" },
    "tests": [
      "src/Dispatcher.ts carries no release-numbered spec cite"
    ],
    "acceptance": "No `RELEASE-v0.N §M` or `v0.N §M` cite remains in src/Dispatcher.ts. Cites into `spec/*.md` and `.claude/rules/*.md` stand. A sentence that loses its only justification stays if it is still true, and goes if it is not.",
    "notes": "Ruling of 2026-09-11, option B: git carries provenance. Split by module because 183 sites across 19 files is more than one tick can land green; this is the largest."
  },
  {
    "tag": "RELEASE-CITES-CUT-SRC-TAIL",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        { "path": "src/cli.ts", "description": "18 cites" },
        { "path": "src/job.ts", "description": "16" },
        { "path": "src/Prompt.ts", "description": "12" },
        { "path": "src/PendingSchema.ts", "description": "9" },
        { "path": "src/git.ts", "description": "7" },
        { "path": "src/Phase.ts", "description": "7" },
        { "path": "src/cliVerdict.ts", "description": "6" },
        { "path": "src/cliHelp.ts", "description": "6" },
        { "path": "src/cliJobResolution.ts", "description": "4" },
        { "path": "src/builtinGates.ts", "description": "3" },
        { "path": "src/Gate.ts", "description": "2" },
        { "path": "src/cliJobVerbs.ts", "description": "2" },
        { "path": "src/paths.ts", "description": "1" },
        { "path": "src/standardSchema.ts", "description": "1" },
        { "path": "src/flumeApi.ts", "description": "1" }
      ],
      "retire": []
    },
    "summary": "The other 95 dead release cites in src/, across fifteen modules — same grammar, same reason, split off Dispatcher.ts so each lands green in a tick",
    "per": { "path": ".claude/rules/engineering.md", "section": "Narration is the ladder's bottom rung" },
    "tests": [
      "no src/ module outside Dispatcher.ts carries a release-numbered spec cite"
    ],
    "acceptance": "No `RELEASE-v0.N §M` or `v0.N §M` cite remains under src/ outside Dispatcher.ts. Cites into `spec/*.md` and `.claude/rules/*.md` stand.",
    "notes": "Counts verified on disk 2026-09-11. Sibling of RELEASE-CITES-CUT-DISPATCHER; the two collide on nothing and the pin entry waits on both."
  },
  {
    "tag": "RELEASE-CITES-PINNED",
    "gate": { "kind": "blockedBy", "tags": ["RELEASE-CITES-CUT-DISPATCHER", "RELEASE-CITES-CUT-SRC-TAIL"] },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        { "path": "examples/cascade-chain.ts", "description": "2 cites" },
        { "path": "examples/backlog-groomer-chain.ts", "description": "4" },
        { "path": "examples/minimal-chain.ts", "description": "1" },
        { "path": "tests/retired-narration.test.ts", "description": "the refusal, beside the retired-narration pins" }
      ],
      "retire": []
    },
    "summary": "Finish the release-cite cut in examples/ and promote it a rung: a source-shape pin refusing any new cite matching that grammar under src/ or examples/",
    "per": { "path": ".claude/rules/engineering.md", "section": "Narration is the ladder's bottom rung" },
    "tests": [
      "no file under src/ or examples/ carries a release-numbered spec cite outside a declared-historical marker"
    ],
    "acceptance": "The grammar is refused mechanically, so the cut cannot regress, and a site that genuinely wants historical provenance says so in a declared marker the pin recognizes.",
    "notes": "Ruling of 2026-09-11: the promotion rides the same pass. Blocked on both cut entries because the pin is red until they land. docs/ and README are out of scope."
  },
  {
    "tag": "DISPATCHER-EXTRACT-LOOP-SUPERVISOR",
    "gate": { "kind": "blockedBy", "tags": ["RELEASE-CITES-PINNED"] },
    "dependsOnForks": [],
    "files": {
      "new": [
        { "path": "src/loopSupervisor.ts", "description": "superviseLoop and its options/result types, moved whole" }
      ],
      "edit": [
        { "path": "src/Dispatcher.ts", "description": "the loop-supervisor section leaves" },
        { "path": "src/cli.ts", "description": "import path" }
      ],
      "retire": []
    },
    "summary": "The loop supervisor is module-scope code in a 5400-line Dispatcher.ts and shares none of the buildFlumeApi cycle constraint that keeps chain-load colocated",
    "per": { "path": ".claude/rules/posture-sweep.md", "section": "A violation counts only when verified on disk this tick" },
    "tests": [
      "superviseLoop is exported from src/loopSupervisor.ts"
    ],
    "acceptance": "A pure move: no behavior change, `src/index.ts` unchanged, the supervisor's tests move with it and differ only in import path.",
    "notes": "Ruling of 2026-09-11, option C: extract the acyclic concerns; chain-load plus tick execution stay with buildFlumeApi, whose cycle constraint (src/Dispatcher.ts:42-44) is the declared reason. This one is genuinely module-scope already (:4730-:5213) — take it first."
  },
  {
    "tag": "DISPATCHER-EXTRACT-PRIOR-ATTEMPTS",
    "gate": { "kind": "blockedBy", "tags": ["RELEASE-CITES-PINNED"] },
    "dependsOnForks": [],
    "files": {
      "new": [
        { "path": "src/priorAttempts.ts", "description": "the prior-attempt read/write/clear surface and the record builders" }
      ],
      "edit": [
        { "path": "src/Dispatcher.ts", "description": "the prior-attempt section leaves; the class calls the module" },
        { "path": "src/index.ts", "description": "re-export path for slugify / priorAttemptPath / priorAttemptsDir" }
      ],
      "retire": []
    },
    "summary": "Prior-attempt persistence — key, read, write, clear, revert snapshots, record builders — is a separate home inside Dispatcher.ts",
    "per": { "path": ".claude/rules/posture-sweep.md", "section": "A violation counts only when verified on disk this tick" },
    "tests": [
      "the prior-attempt record builders are exported from src/priorAttempts.ts"
    ],
    "acceptance": "A pure move: no behavior change, the package's public surface through `src/index.ts` unchanged, tests move with their subjects and differ only in import path.",
    "notes": "Sections at src/Dispatcher.ts:4106-:4460 plus the builders at :5267-:5359; `slugify`, `priorAttemptPath` and `priorAttemptsDir` are already public and must resolve identically from `flume`."
  },
  {
    "tag": "DISPATCHER-EXTRACT-FRICTION",
    "gate": { "kind": "blockedBy", "tags": ["RELEASE-CITES-PINNED"] },
    "dependsOnForks": [],
    "files": {
      "new": [
        { "path": "src/friction.ts", "description": "harvest, count line, and the friction declaration check" }
      ],
      "edit": [
        { "path": "src/Dispatcher.ts", "description": "the friction helpers leave" },
        { "path": "src/cli.ts", "description": "import path for frictionCountLine" }
      ],
      "retire": []
    },
    "summary": "Friction harvesting, the count line and the friction declaration validator are one job spread through Dispatcher.ts",
    "per": { "path": ".claude/rules/posture-sweep.md", "section": "A violation counts only when verified on disk this tick" },
    "tests": [
      "frictionCountLine is exported from src/friction.ts"
    ],
    "acceptance": "A pure move: no behavior change, `src/index.ts` unchanged, tests move with their subjects and differ only in import path.",
    "notes": "`frictionCountLine` (src/Dispatcher.ts:1032), `validateFrictionDeclaration` (:935), `harvestFriction` (:3738). src/job.ts:444 already exports a helper for `frictionCountLine` to share — the new module keeps that direction."
  },
  {
    "tag": "DISPATCHER-EXTRACT-WORKTREES",
    "gate": { "kind": "blockedBy", "tags": ["RELEASE-CITES-PINNED"] },
    "dependsOnForks": [],
    "files": {
      "new": [
        { "path": "src/worktrees.ts", "description": "create, teardown and the stale sweep" }
      ],
      "edit": [
        { "path": "src/Dispatcher.ts", "description": "the worktree helpers leave; sweepStaleWorktrees stays as the method cli.ts calls" }
      ],
      "retire": []
    },
    "summary": "Worktree provisioning, teardown and the stale sweep are a separate home inside Dispatcher.ts, beside src/setupWorktree.ts which already owns the hook half",
    "per": { "path": ".claude/rules/posture-sweep.md", "section": "A violation counts only when verified on disk this tick" },
    "tests": [
      "worktree provisioning and teardown are exported from src/worktrees.ts"
    ],
    "acceptance": "A pure move: no behavior change, `src/index.ts` unchanged, `dispatcher.sweepStaleWorktrees()` stays callable from cli.ts, tests move with their subjects and differ only in import path.",
    "notes": "`createWorktree` (src/Dispatcher.ts:4050), `teardownWorktreeInstance` (:3847), `sweepStaleWorktrees` (:3931), `worktreeDirName` (:793). Take last of the four: it is the group most entangled with the class's own state."
  }
]
