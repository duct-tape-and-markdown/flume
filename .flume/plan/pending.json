[
  {
    "tag": "RESIDENCY-RESOLUTION",
    "summary": "Job resolution moves only flumeDir; configDir never retargets (stays <repoRoot>/.flume or explicit FLUME_CONFIG_DIR). Conflict narrows to FLUME_DIR; --job + FLUME_CONFIG_DIR composes.",
    "per": {
      "path": "spec/RELEASE-v0.6.md",
      "section": "3. Job resolution — the conflict narrows to `FLUME_DIR`"
    },
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/cli.ts",
          "description": "resolveStateDirs: jobDefault applies to flumeDir only; conflict check drops FLUME_CONFIG_DIR; docstrings + --job help text updated per §2/§3"
        },
        {
          "path": "src/job.ts",
          "description": "jobRun docstring: chain is repo-resident, not 'lives on the job branch' (mechanics unchanged — configDir arrives resolved)"
        },
        {
          "path": "tests/cli.test.ts",
          "description": "resolveStateDirs §3 + real-CLI suites updated in place; new compose/inertness/prompt cases"
        },
        {
          "path": "tests/job.test.ts",
          "description": "suites that assume configDir = job dir updated"
        },
        {
          "path": "tests/job.integration.test.ts",
          "description": "§5b/§5c/§5e fixtures: chain at repo .flume, thin job dirs (shim fixtures retired)"
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/cli.test.ts",
        "asserts": "under --job configDir=<repoRoot>/.flume and repo chain loads even with a job-dir chain.ts (inert); --job + FLUME_CONFIG_DIR composes (state in job dir, chain from env dir); --job + FLUME_DIR exit 2; all three env vars written back"
      },
      {
        "path": "tests/cli.test.ts",
        "asserts": "render/tick under job resolution reads prompts from repo .flume/prompts via unchanged promptPath joins (§6 shared-prompt integration case)"
      }
    ],
    "acceptance": "pnpm test green with the §6 resolution + shared-prompt cases; tsc green",
    "notes": "Invariant per §2: no job-dir probe, no enforcement, no warning — inert by construction. promptPath mechanics untouched; shared-prompts gap dissolves by consequence (§3). Pre-1.0 edit-in-place, no compat lanes (§6)."
  },
  {
    "tag": "SEED-DIR",
    "summary": "Chain.seedDir replaces --template: job new loads the repo chain and seeds the job dir from the configDir-relative seedDir, verbatim skip-existing. --template deleted; bare job, no warning.",
    "per": {
      "path": "spec/RELEASE-v0.6.md",
      "section": "4. Chain-declared seed — `job new` without `--template`"
    },
    "gate": { "kind": "blockedBy", "tag": "RESIDENCY-RESOLUTION" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Phase.ts",
          "description": "Chain.seedDir?: string beside humanOnly (§4)"
        },
        {
          "path": "src/job.ts",
          "description": "jobNew: take configDir, load repo chain (missing chain or declared-but-absent seedDir → JobUsageError/exit 2), cp seedDir force:false; drop template path + empty-dir warning"
        },
        {
          "path": "src/cli.ts",
          "description": "job new verb: drop --template parsing, update usage/help text, pass configDir to jobNew"
        },
        {
          "path": "tests/job.test.ts",
          "description": "job new suites reworked from --template to Chain.seedDir per §6"
        },
        {
          "path": "tests/job.integration.test.ts",
          "description": "fixtures converted from --template to seedDir declarations"
        }
      ],
      "retire": ["JobNewOptions.template", "--template flag (flume job new)"]
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/job.test.ts",
        "asserts": "seedDir seeds + baseline-commits the skeleton; re-run preserves worked file and fills newly added stub (skip-existing); absent seedDir → bare job, no warning; missing chain → exit 2; declared-but-absent seedDir → exit 2; --template rejected as unknown flag"
      }
    ],
    "acceptance": "pnpm test green with the §6 seed cases; tsc green; --template gone from CLI surface",
    "notes": "Runtime ignores, @dtmd/flume link, longpaths pin, baseline commit all unchanged (§4-5). seedDir is configDir-relative — the promptPath idiom."
  },
  {
    "tag": "HARVEST-DECL",
    "summary": "Chain.harvest?: string[] replaces the HARVEST_PATHS constant; extract loads the repo chain for the list. Absent = harvest nothing (no default).",
    "per": {
      "path": "spec/RELEASE-v0.6.md",
      "section": "5. Chain-declared harvest — `job extract`"
    },
    "gate": { "kind": "blockedBy", "tag": "RESIDENCY-RESOLUTION" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Phase.ts",
          "description": "Chain.harvest?: string[] (§5)"
        },
        {
          "path": "src/job.ts",
          "description": "jobExtract: take configDir, load chain for the harvest list; delete HARVEST_PATHS"
        },
        {
          "path": "src/cli.ts",
          "description": "job extract verb: pass configDir into jobExtract"
        },
        {
          "path": "tests/job.test.ts",
          "description": "§5e harvest units updated for declared list + absent case"
        },
        {
          "path": "tests/job.integration.test.ts",
          "description": "§5e integration updated for chain-declared harvest"
        }
      ],
      "retire": ["HARVEST_PATHS (src/job.ts)"]
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/job.test.ts",
        "asserts": "extract harvests exactly the declared list; absent harvest → empty harvest, extract otherwise unchanged; HARVEST_PATHS gone type-level (tsc gate per v0.5 §7)"
      }
    ],
    "acceptance": "pnpm test green with the §6 harvest cases; tsc green; HARVEST_PATHS unreferenced",
    "notes": "No ordering hazard: repo chain survives job consumption (§5). Harvest mechanics (git show off branch tip, stdout routing) unchanged."
  },
  {
    "tag": "DOCS-0-6",
    "summary": "Docs for chain residency, seedDir + harvest declarations, --template removal, migration note (shims delete, prompt gymnastics unnecessary).",
    "per": {
      "path": "spec/RELEASE-v0.6.md",
      "section": "7. Docs"
    },
    "gate": { "kind": "blockedBy", "tag": "HARVEST-DECL" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "README.md",
          "description": "chain-residency contract; static-.flume + thin jobs as native shape; job flow sans --template; endings updated for declared harvest"
        },
        {
          "path": "docs/CLI.md",
          "description": "job new / job extract verb docs updated (no --template; chain-declared seed + harvest)"
        },
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "seedDir + harvest declaration; runtime-owned vs chain-declared; migration note; rework 'Packaging a chain as a job template' framing"
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "no doc references --template, job-local chain.ts, or HARVEST_PATHS; migration note present",
    "notes": "§7 'getting-started' maps to README's quick-start sections — no separate getting-started file exists. Also rests on SEED-DIR (single blockedBy; queue order covers it)."
  },
  {
    "tag": "CUT-0-6-0",
    "summary": "Cut 0.6.0: reconcile CHANGELOG against commits since the 0.5.0 cut with §10 block shape, bump package.json, commit as chore(release): cut 0.6.0.",
    "per": {
      "path": "spec/RELEASE-v0.6.md",
      "section": "10. Versioning — the 0.6.0 cut"
    },
    "gate": { "kind": "blockedBy", "tag": "DOCS-0-6" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "CHANGELOG.md",
          "description": "0.6.0 block: ### Breaking (config never retargets under job resolution, --template removed, HARVEST_PATHS removed) + ### Added per §10"
        },
        {
          "path": "package.json",
          "description": "version → 0.6.0"
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "CHANGELOG 0.6.0 block matches §10 shape; version 0.6.0; commit message exactly 'chore(release): cut 0.6.0'",
    "notes": "Wording derived from the log per §10; spec fixes version, block structure, and Breaking entries' presence. Lockfile expected untouched (version-only bump, as at 0.5.0). npm publish stays a human act."
  }
]
