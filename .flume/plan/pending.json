[
  {
    "tag": "HANDOFF-FACTS-ON-THE-TICK-SURFACE",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "src/Phase.ts",
          "description": "the shipped entry's payload beside its tag on the per-entry outcome"
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "fill it where the wave folds its tag lists"
        },
        {
          "path": "src/flumeApi.ts",
          "description": "the stop-flag path rule on the API, beside priorAttemptPath"
        },
        {
          "path": "src/index.ts",
          "description": "same value on the index list"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "both facts"
        }
      ]
    },
    "summary": "Report the shipped entry's payload and the stop-flag path rule on the surfaces a handoff reads",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A fact the engine holds is reported, never rediscovered"
    },
    "acceptance": "A `handoff` reads a shipped entry's extension field and the stop flag's path off the engine alone — no re-read of `pending.json` at a base sha, no composed `stop` literal.",
    "tests": [
      "a fanout wave reports the shipped entry's payload beside its tag",
      "FlumeApi carries the stop-flag path rule"
    ],
    "notes": "Unblocks HANDOFF-STOPS-ON-A-CONTRACT-TOUCHING-SHIP: the default handoff has shippedTags and flumeDir and can reach neither fact. priorAttemptPath is the standing precedent for a path rule on the API (spec/pending.md, What the package exports)."
  },
  {
    "tag": "HANDOFF-STOPS-ON-A-CONTRACT-TOUCHING-SHIP",
    "gate": { "kind": "blockedBy", "tags": ["HANDOFF-FACTS-ON-THE-TICK-SURFACE"] },
    "files": {
      "edit": [
        {
          "path": "harness/entryExtension.ts",
          "description": "optional contractTouching beside the package fields, with its hint"
        },
        {
          "path": "harness/handoff.ts",
          "description": "write the stop flag after a marked ship"
        },
        {
          "path": "tests/harnessEntryExtension.test.ts",
          "description": "the field"
        },
        {
          "path": "tests/harnessHandoff.test.ts",
          "description": "both legs"
        }
      ]
    },
    "summary": "Package entry extension gains optional `contractTouching`; the default handoff writes the stop flag after shipping one",
    "per": { "path": "spec/loop.md", "section": "One tick is one fresh process" },
    "acceptance": "A shipped entry marked `contractTouching` leaves the stop flag on disk; an unmarked ship leaves none. The ladder's own routing is unchanged in both cases.",
    "tests": [
      "the default handoff writes the stop flag after a contractTouching entry ships",
      "the default handoff writes no stop flag when no shipped entry is contractTouching"
    ],
    "pins": [
      "the package entry extension accepts an entry that omits contractTouching"
    ],
    "notes": "The mechanism the cited section already points at — \"its build handoff writing the stop flag after shipping an entry plan marked contract-touching\". `.flume/PROTOCOL.md` rule 6's single-entry case is the operator's; it is outside every phase lane."
  },
  {
    "tag": "TICK-REPORTS-UNCOMMITTED-TRACKED-EDITS",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "read the worktree's tracked modifications after the agent exits, before teardown; carry them on the verdict"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "both legs"
        }
      ]
    },
    "summary": "Report on the tick verdict the tracked paths a tick modified and did not commit, before teardown removes the worktree",
    "per": {
      "path": "spec/loop.md",
      "section": "Tip verify — one writer per branch, absorption at the merge"
    },
    "acceptance": "Every tick that ran an agent writes its uncommitted tracked paths on the verdict before teardown, so the loss is seen though not preserved.",
    "tests": [
      "a tick that commits nothing reports the tracked paths it modified before teardown",
      "a tick that left no tracked modification reports an empty set"
    ],
    "notes": "Read NUL-separated (.claude/rules/platform-facts.md, git's porcelain quoting). The fact is the engine's; what it means stays the chain's. The package's clean-tree gate re-runs `git status` today and is the fact's intended reader once it rides a gate-reachable surface — a later entry, not this one."
  },
  {
    "tag": "STATE-ROOT-REL-IS-REPORTED-IN-GITS-ALPHABET",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "computeStateRootRel reports through the git-path rule"
        },
        {
          "path": "src/builtinGates.ts",
          "description": "the pending gate composes its pathspec in the same alphabet"
        },
        {
          "path": "tests/builtinGates.test.ts",
          "description": "the gate over a backslash-separated offset"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "the reporter's own spelling"
        }
      ]
    },
    "summary": "Report `stateRootRel` in git's alphabet at the one reporter, and stop composing a gate's pathspec from it through the host separator",
    "per": { "path": "spec/chain.md", "section": "What a gate receives" },
    "acceptance": "A gate reading a tracked state-root file at the gated commit works for a nested state root on win32: the reported offset is forward-slashed and nothing rejoins it through `sep`.",
    "tests": [
      "the pending gate reads the gated commit's queue under a backslash-separated state root"
    ],
    "pins": [
      "computeStateRootRel reports a nested state root in git's alphabet"
    ],
    "notes": "`gitPath` (src/paths.ts) is the rule; `computeStateRootRel` (src/Dispatcher.ts:866) is the one reporter that never took it. The fixture shape is tests/harnessGates.test.ts's `NESTED` — a posix run cannot produce the offset, so it is spelled. src/friction.ts:191,221 compose the same value; check both."
  },
  {
    "tag": "GATE-THROW-CARRIES-ITS-STACK",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "runGate records the throw's stack as that gate's details"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "the recorded details"
        }
      ]
    },
    "summary": "Record a thrown gate's stack as that gate's `details` — the details a returned refusal would have carried",
    "per": { "path": "spec/chain.md", "section": "What a gate returns" },
    "acceptance": "A gate that throws is recorded as `{ ok: false, message, details }`, and the failure the retry reads carries the stack rather than one line of message.",
    "tests": [
      "a gate that throws records its stack as that gate's details"
    ],
    "notes": "`Dispatcher.runGate` (src/Dispatcher.ts:3657) already folds the throw to `{ ok: false, message }`; this is the `details` half of the amended sentence. A non-Error throw has no stack — what that records is build's call, stated at the site."
  },
  {
    "tag": "HOOK-THROWS-ANSWERED-LIKE-THEIR-SEAMS",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "one guard per hook seam, at the site both concurrencies share"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "all four legs"
        }
      ]
    },
    "summary": "Answer a throwing `promptArgs`, `shouldRun`, `handoff` or `shipped` the way its sibling seam already answers, never by losing the tick",
    "per": { "path": "spec/chain.md", "section": "What a hook receives" },
    "acceptance": "Each of the four hooks throwing lands on the outcome its seam already has, with the verdict written and the merge bookkeeping complete.",
    "tests": [
      "a throwing promptArgs is a render-refused no-commit outcome",
      "a throwing shouldRun refuses the tick rather than declining it",
      "a throwing handoff is logged and the tick's facts stand",
      "a throwing shipped leaves the entry pending and names the throw on the verdict"
    ],
    "notes": "Six unguarded call sites, four seams: src/Dispatcher.ts:1724 (handoff), 1849/3284 (shouldRun), 1967/3291 (promptArgs), 2888 (shipped). The singleton and fanout copies share one guard — `runGate` is the precedent (`.claude/rules/engineering.md`, *The fix lands at the mechanism*)."
  },
  {
    "tag": "AGENT-LOADS-ONLY-THE-CHAINS-MCP-CONFIG",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "src/Agent.ts",
          "description": "`inheritUserMcp` on ClaudeCodeOptions, and the flag it suppresses"
        },
        {
          "path": "tests/Agent.test.ts",
          "description": "both argv legs"
        }
      ]
    },
    "summary": "Pass `--strict-mcp-config` on the claude-code argv unless `ClaudeCodeOptions.inheritUserMcp` is declared",
    "per": { "path": "spec/chain.md", "section": "The agent seam" },
    "acceptance": "A tick loads only the MCP configuration the chain hands it: the flag rides the argv by default and is omitted exactly when the option is set.",
    "tests": [
      "the claude-code argv carries --strict-mcp-config by default",
      "inheritUserMcp omits --strict-mcp-config from the argv"
    ],
    "notes": "By-user runtime state is what a stateless tick excludes, and a wedged inherited server has stalled a wave. The typed knob rather than `extraArgs`, on the `model` precedent — the engine types what every consumer varies and declines the rest of the CLI."
  },
  {
    "tag": "API-CHECKOUT-AT-FOR-A-DIFFERENTIAL-GATE",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "src/flumeApi.ts",
          "description": "`checkoutAt` beside the other git verbs"
        },
        {
          "path": "src/worktrees.ts",
          "description": "the detached checkout, placed under the worktree base"
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "reclaim what a gate checked out, at the one gate-run site"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "placement and both reclamation legs"
        }
      ]
    },
    "summary": "Give a differential gate `api.git.checkoutAt` — a detached tree under the state root's worktree base, removed by the engine when the gate returns",
    "per": { "path": "spec/chain.md", "section": "What a gate receives" },
    "acceptance": "A gate needing the tree at `baseSha` asks the API for it and provisions nothing: the checkout lands under the worktree base and is gone when the gate returns, however it returns.",
    "tests": [
      "api.git.checkoutAt plants a detached checkout under the state root's worktree base",
      "the engine removes a gate's checkout when the gate returns",
      "the engine removes a gate's checkout when the gate throws"
    ],
    "notes": "`Dispatcher.runGate` is the one gate-run site and so the one reclamation point. Under the worktree base, not a temp dir, so a run killed mid-flight leaves a directory the startup sweep reclaims. `harness/vitestRunner.ts`'s `runAtBase` is the standing hand-rolled checkout this replaces."
  },
  {
    "tag": "CHAIN-COMPUTES-ITS-WORKTREE-BASE",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "src/Phase.ts",
          "description": "`Chain.worktreesBase?: (paths) => string`"
        },
        {
          "path": "src/paths.ts",
          "description": "the one resolution consults the declared value"
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "evaluate it at load against the resolved roots"
        },
        {
          "path": "tests/worktrees.test.ts",
          "description": "creation and the startup sweep over a declared base"
        },
        {
          "path": "tests/paths.test.ts",
          "description": "the resolution order"
        }
      ]
    },
    "summary": "A chain may declare how to compute its worktree base — a function of the resolved roots, evaluated at load, never a committed path literal",
    "per": {
      "path": "spec/worktrees.md",
      "section": "Placement — the worktree base and the job namespace"
    },
    "acceptance": "A chain declaring `worktreesBase` moves creation, per-wave stale-slug removal and the startup sweep together, without an environment variable set before the engine's own module loads.",
    "tests": [
      "a chain-declared worktreesBase places the tick's worktree",
      "the startup sweep reads the chain-declared base"
    ],
    "notes": "A function, not a path: placement stays machine-local, and a committed literal is the wrong home. The one computation stays `worktreesBase` (src/paths.ts) — a second is the defect field-traced four times. `harness/vitestRunner.ts` reads the same resolution."
  },
  {
    "tag": "RUNNER-FACTORY-TAKES-A-PROVISION",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "harness/runner.ts",
          "description": "the factory's context replaces the bare api"
        },
        {
          "path": "harness/chain.ts",
          "description": "reduce the declared setup to a provisioning function and hand it over"
        },
        {
          "path": "harness/vitestRunner.ts",
          "description": "provision the base checkout through it"
        },
        {
          "path": "harness/declaration.ts",
          "description": "the refusal's wording"
        },
        {
          "path": "tests/harnessChain.test.ts",
          "description": "what the factory is called with, and what the base run provisions"
        }
      ]
    },
    "summary": "Declare the runner as `({ api, provision }) => Runner` — the chain's API beside the declared `setup` reduced to a provisioning function",
    "per": { "path": "spec/harness.md", "section": "The runner interface" },
    "acceptance": "A consumer whose install is not at the repo root judges its base the same way it builds: the base checkout is provisioned by the declared `setup`, not by the installer at the root.",
    "tests": [
      "the chain calls the runner factory with the api and a provision function",
      "the vitest runner provisions a base checkout through the declared setup"
    ],
    "pins": [
      "a consumer declaring no setup provisions a base checkout with the installer at the root"
    ],
    "notes": "`worktreeSetup` (harness/chain.ts:534) already performs that reduction for the Phase hook; the factory receives the same one rather than a second. A consumer passes options to `vitestRunner()` and never the factory, so no declaration module changes."
  },
  {
    "tag": "RUN-AT-BASE-USES-THE-ENGINE-CHECKOUT",
    "gate": { "kind": "blockedBy", "tags": ["API-CHECKOUT-AT-FOR-A-DIFFERENTIAL-GATE"] },
    "files": {
      "edit": [
        {
          "path": "harness/vitestRunner.ts",
          "description": "the base checkout comes from the api; its own worktree plumbing goes"
        },
        {
          "path": "tests/harnessRunner.test.ts",
          "description": "the base run over an engine-supplied checkout"
        }
      ]
    },
    "summary": "The vitest runner's base run takes its detached checkout from the engine instead of provisioning its own",
    "per": { "path": "spec/chain.md", "section": "What a gate receives" },
    "acceptance": "`runAtBase` adds and removes no worktree of its own — the checkout is the API's and the engine reclaims it.",
    "tests": [
      "runAtBase takes its base checkout from the engine's api rather than adding a worktree"
    ],
    "notes": "\"A differential gate never provisions its own\" is the ruling's second half; the named-lines judge is the differential gate and this runner is its only instance here. The `addWorktree`/`removeWorktree`/base-join block at harness/vitestRunner.ts:248-295 is what leaves."
  },
  {
    "tag": "LANE-EXCLUDES-INFORM-THE-NAMED-LINE-HINTS",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "harness/entryExtension.ts",
          "description": "render the running lane's excludes into the tests[]/pins[] hints"
        },
        {
          "path": "harness/chain.ts",
          "description": "build the runner before the extension and hand it the lanes"
        },
        {
          "path": "harness/vitestRunner.ts",
          "description": "retire the site comment claiming a plan-time refusal"
        },
        {
          "path": "tests/harnessEntryExtension.test.ts",
          "description": "all three cases"
        }
      ]
    },
    "summary": "Render the running lane's exclusions into plan's `tests[]` and `pins[]` hints — informed at authorship, never refused on a prediction",
    "per": { "path": "spec/harness.md", "section": "The runner interface" },
    "acceptance": "Plan's rendered schema names the globs no judge will reach; no gate refuses an entry over its declared `files`.",
    "tests": [
      "the tests[] hint names the globs the running lane excludes",
      "the pins[] hint names the globs the running lane excludes",
      "a running lane with no exclusions renders the hint without a lane clause"
    ],
    "notes": "`Runner.lanes` has shipped with no reader; this is the reader the cited section names. The hints stay tool-neutral — the globs come from the consumer's own declaration. `.flume/declaration.ts:61-63` still claims a plan-time refusal; that file is outside every phase lane."
  },
  {
    "tag": "PLAN-DISCIPLINE-NAMES-THE-OVERLAP-RULE",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "harness/prompts/plan-discipline.md",
          "description": "one clause in the named-lines paragraph"
        }
      ]
    },
    "summary": "State in plan's discipline that a `tests[]` line is judgeable only when the entry also changes a file not among the ones holding it",
    "per": { "path": "spec/harness.md", "section": "The judges" },
    "acceptance": "The named-lines paragraph states the overlap condition, so a tests-only entry declares its behavior elsewhere or carries no `tests[]` line.",
    "notes": "Red-on-base lays the merged bytes of the files holding the named tests over the base checkout; for a tests-only diff that is the whole change, so the line passes at the base by construction. Five field instances. The lane half of the same wall is LANE-EXCLUDES-INFORM-THE-NAMED-LINE-HINTS."
  },
  {
    "tag": "README-NAMES-THE-GIT-FLOOR",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "README.md",
          "description": "a prerequisite line, beside the quickstart install"
        },
        {
          "path": "tests/harnessPackaging.test.ts",
          "description": "the line against the manifest's own floor"
        }
      ]
    },
    "summary": "Name the git 2.36 floor beside the node floor in the README's prerequisites, pinned against the manifest",
    "per": { "path": "spec/chain.md", "section": "The package a chain loads through" },
    "acceptance": "A consumer reads both floors before installing, and the node half cannot drift from `package.json`'s `engines`.",
    "tests": [
      "the README prerequisite line names the node floor package.json engines declares",
      "the README prerequisite line names the git floor"
    ],
    "notes": "There is no prerequisite line today — the README names neither floor. `worktree list --porcelain -z` is 2.36+; below it worktree reclamation degrades loudly and nothing else does. The git half has no manifest to agree with, so it is named, not derived."
  },
  {
    "tag": "CLI-DOC-TICK-EXIT-CODES-PINNED",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "docs/CLI.md",
          "description": "§ `flume tick`'s exit-code sentence"
        },
        {
          "path": "tests/cliHelp.test.ts",
          "description": "the page's section against the driven range"
        }
      ]
    },
    "summary": "Pin `docs/CLI.md` § `flume tick`'s exit codes against the real tick range the suite already drives, and correct the section",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A seam gate reads what the real writer wrote"
    },
    "acceptance": "The page's `flume tick` section names exactly the codes the driven `tickExitCode` range plus the named process-level set produce; a one-sided change to either is red.",
    "tests": [
      "docs/CLI.md's flume tick section names every exit code the real tick range produces"
    ],
    "notes": "The section names 0, 69 and 1 and neither 2 nor 78, both of which `flume tick --help` documents. Drive the producer, never compare the page to the help block — both copies drift together in the commit that changes the behavior. The remaining ten verb sections stay unheld."
  },
  {
    "tag": "CHAIN-AUTHORING-IGNORES-POINT-AT-THE-SPEC",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "shrink the runtime-ignore list to a pointer"
        }
      ]
    },
    "summary": "Shrink `docs/CHAIN-AUTHORING.md`'s copy of the runtime ignore list to a pointer at `spec/jobs.md`, *Runtime ignores*",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Derived state is computed, never restated beside its source"
    },
    "acceptance": "The guide names no runtime ignore line; the bullet points at the section that owns the list, and the surrounding seedDir prose still reads standing alone.",
    "notes": "Third of three unpinned copies; pinning is off the table outside the `.d.ts` carve-out, so removing a copy is the whole fix. Leaves `spec/jobs.md` and `RUNTIME_IGNORES` — the pair the human already maintains."
  }
]
