[
  {
    "tag": "GIT-NAME-ONLY-READS-NUL-SEPARATED",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/git.ts",
          "description": "diffNameOnly and showNameOnly read the -z form; drop the per-line trim"
        },
        {
          "path": "tests/git.test.ts",
          "description": "unit pins for both readers over a committed non-ASCII path"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "seam pin: the spelling reaching GateContext.touchedPaths"
        }
      ],
      "retire": []
    },
    "summary": "git.ts's two --name-only readers take git's quoted, newline-separated form, so a non-ASCII committed path reaches the fence misspelled",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Loud or nothing"
    },
    "acceptance": "Both readers return the bytes git committed for a non-ASCII path, and the fence judges that spelling.",
    "tests": [
      "diffNameOnly returns a committed non-ASCII path as git spelled it",
      "showNameOnly returns a committed non-ASCII path as git spelled it",
      "a tick's non-ASCII committed path reaches GateContext.touchedPaths unquoted"
    ],
    "pins": [],
    "notes": "Measured this tick: `git diff --name-only` / `git show --name-only` print `\"src/caf\\303\\251.ts\"` and the same path verbatim under `-z`. Consumers: Dispatcher's commitTouchedPaths (fence + observedFiles) and FlumeApi.git.showNameOnly. Same decode two siblings already learned — harness/gates.ts:cleanTreeGate, src/worktrees.ts:readWorktreeRegistry."
  }
]
