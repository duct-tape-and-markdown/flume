[
  {
    "tag": "namespaced-join-dispatcher-fs-calls",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "namespacedJoin the bare-path fs-call sites per platform-facts.md's idiom: tick-verdict persistence (writeTickVerdict/clearTickVerdict/readTickVerdict/readTickVerdicts, ~:420-475), loadChainModule's existsSync (:805 — single fix point, covers all callers including builtinGates.ts's chainLoadGate and Dispatcher.ts's own default loader), and pendingPath reads/writes in readPending/readPendingTolerant/commitPendingUpdate (~:3666-3801). Leave the already-namespaced PriorAttempt/revert sites (~:2970-3571) untouched."
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "Add win32-gated long-path regression(s) per tests/job.test.ts's existing win32 suite precedent, covering loadChainModule and pendingPath reads not silently misreading an existing chain.ts/pending.json as absent."
        }
      ],
      "retire": []
    },
    "summary": "Dispatcher.ts's tick-verdict, loadChainModule, and pendingPath fs-call sites still use bare paths — same win32 MAX_PATH misread class as job.ts's now-fixed sites",
    "per": {
      "path": ".claude/rules/platform-facts.md",
      "section": "Windows MAX_PATH (~260 chars) breaks fs calls with no long component"
    },
    "tests": [
      "loadChainModule doesn't misread an existing chain.ts as absent when its resolved path exceeds win32's ~260-char total limit",
      "readPending/readPendingTolerant/commitPendingUpdate don't misread an existing or writable pending.json as absent when pendingPath exceeds the limit"
    ],
    "acceptance": "Dispatcher.ts's flagged existsSync/readFile/writeFile/mkdir/rm sites route through namespacedJoin; win32 long-path regression tests pass",
    "notes": "Audit finding (39f429f cross-check): job.ts's chainPath fix is correct, but jobNew/jobRun call loadChainModule(chainPath) right after, unwrapped (:805) — same misread one call downstream. Same-file sweep found tick-verdict and pendingPath sites in the same unfixed state; PriorAttempt/revert sites (~:2970-3571) are already correctly namespaced, so this is the remaining gap in the file."
  }
]
