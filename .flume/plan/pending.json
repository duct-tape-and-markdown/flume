[
  {
    "tag": "CITATION-WRAP-PIN-REFUSES-A-BROKEN-IDENTIFIER-CITE",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/helpers/commentCitations.ts",
          "description": "report a wrapped span by whether its break-closed text is a subject"
        },
        {
          "path": "tests/commentCitations.test.ts",
          "description": "fixture arm for an identifier wrap, then the repo refusal widened off `/`"
        },
        {
          "path": "src/cliVerdict.ts",
          "description": "rewrap so the `TickVerdictMergeOutcome.tag` cite sits on one line"
        }
      ],
      "retire": []
    },
    "summary": "Report a wrapped span that is a subject once its break is closed, not path spans alone; rewrap the identifier cite the wrap broke",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "acceptance": "A wrapped span whose break-closed text passes the subject rule is reported; the repo pin reds on any such wrap rather than on `/` alone, and `src/cliVerdict.ts` cites the type on one line.",
    "tests": [
      "the citation scan reports a wrapped span that names a subject once its break is closed",
      "the repo citation pin refuses any citation broken across a comment line"
    ],
    "pins": [],
    "notes": "Answers the note's fork: no narrower rule and no exclusion. Run over all 31 wrapped spans in `src/`/`harness/`, closing the break and applying the existing subject rule finds exactly `src/cliVerdict.ts:122` (`TickVerdictMergeOutcome.` + `tag`); the `Directory not empty` wraps at `src/git.ts:21`/`:305` keep a prose space and fail `SEGMENT` either way."
  },
  {
    "tag": "CITATION-SCAN-JUDGES-AN-UNBACKTICKED-MD-PAGE-NAME",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/helpers/commentCitations.ts",
          "description": "subject rule admits an unbackticked `*.md` token, same path arm"
        },
        {
          "path": "tests/commentCitations.test.ts",
          "description": "fixture arm first, then the repo pin over the bare names"
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "spell the page cites repo-relative (22 sites)"
        },
        {
          "path": "src/builtinGates.ts",
          "description": "same (4)"
        },
        {
          "path": "src/git.ts",
          "description": "same (3)"
        },
        {
          "path": "src/job.ts",
          "description": "same (2)"
        },
        {
          "path": "src/cliVerdict.ts",
          "description": "same (2)"
        },
        {
          "path": "src/Gate.ts",
          "description": "same (2)"
        },
        {
          "path": "src/worktrees.ts",
          "description": "same (1)"
        },
        {
          "path": "src/paths.ts",
          "description": "same (1)"
        }
      ],
      "retire": []
    },
    "summary": "Judge an unbackticked `*.md` page name as a citation, and spell the 37 src/ sites repo-relative so the path arm answers them",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "acceptance": "An unbackticked `*.md` token in a `src/`/`harness/` comment is judged by the existing path and placeholder rules; every page name those comments carry is spelled repo-relative, sits in the repo pin's judged set, and none dangle.",
    "tests": [
      "the citation scan judges an unbackticked *.md page name in a comment",
      "the citation scan judges no unbackticked *.md name carrying a path placeholder",
      "the repo citation pin judges the unbackticked page names src/ comments carry"
    ],
    "pins": [],
    "notes": "Ruled at a331e06: a page name is a reference backticked or not. 37 sites, all in `src/`, all of the form `(engineering.md \"Section\")` — spelled repo-relative in place and left unbackticked, so the widened arm judges them rather than emptying. Placeholders (`<ISO-timestamp>--<tag>--reverted.md`) fall out on the existing `PATH_SEGMENT` charset; string literals are not comments and stay out."
  }
]
