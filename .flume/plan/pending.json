[
  {
    "tag": "LOOP-BACKSTOP-SIGNATURE-INDEX0-ONLY",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "superviseLoop's consecutive-identical-failure backstop checks every signature in a tick's provisionFailures, not just index 0"
        }
      ],
      "retire": []
    },
    "summary": "The abort backstop compares only provisionFailures[0]?.signature, so a repeated signature shadowed by a varying sibling at index 0 never accumulates a streak and never aborts.",
    "per": {
      "path": "spec/loop.md",
      "section": "Provisioning failures — quarantine, then abort"
    },
    "tests": [
      "a repeated provisioning-failure signature buried behind a varying sibling at provisionFailures[0] on every tick still accumulates the consecutive-failure streak and aborts at the threshold"
    ],
    "acceptance": "The backstop tracks every distinct signature across a tick's provisionFailures, not only the first.",
    "notes": "equilibrium audit, wave durability cluster. runFanout pushes a repo-level prune failure first, then per-entry failures — the untagged repo-level class is exactly what a tagged sibling at index 0 shadows today."
  },
  {
    "tag": "CLI-RENDER-REMOVAL",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/cli.ts",
          "description": "remove the render branch, its arg parsing, HELP_SUB.render, and its HELP_TOP line"
        },
        {
          "path": "docs/CLI.md",
          "description": "remove the render entry"
        },
        {
          "path": "tests/cli.test.ts",
          "description": "remove every assertion on the render subcommand; add the two tests below"
        }
      ],
      "retire": []
    },
    "summary": "Remove flume render per operator ruling 2026-08-03 — it previews with the wrong fence, the wrong prior-attempt state, and its own re-derivation of pickability that disagrees with the dispatcher's.",
    "per": {
      "path": "spec/cli.md",
      "section": "Subcommand surface"
    },
    "tests": [
      "flume render is an unknown subcommand and exits 2",
      "no help text (flume --help, flume -h) names render"
    ],
    "acceptance": "render is gone from src/cli.ts, docs/CLI.md, and tests/cli.test.ts; renderPrompt itself (src/Prompt.ts) is untouched — it is the tick's own render path and a public export.",
    "notes": "Operator ruling 2026-08-03, spec side already landed (76dc4807). Breaking CLI change; the changelog is git-mined at cut per spec/cli.md Versioning policy, so the commit body is the record."
  },
  {
    "tag": "CHAIN-MAXPARALLEL-CHAIN-OVERRIDABLE",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Phase.ts",
          "description": "add maxParallel?: number to Chain.supervisorPolicy, beside quarantineScope/abortThreshold"
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "runFanout/partition resolve maxParallel from chain.supervisorPolicy.maxParallel ?? DispatcherOptions.maxParallel ?? 4"
        },
        {
          "path": "src/cli.ts",
          "description": "forward the resolved value the same way quarantineScope/abortThreshold already are"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "cover the override and the byte-identical default"
        }
      ],
      "retire": []
    },
    "summary": "maxParallel is programmatic-only (DispatcherOptions) with no chain declaration, unlike its supervisorPolicy siblings quarantineScope and abortThreshold.",
    "per": {
      "path": "spec/chain.md",
      "section": "Supervisor policy is a chain-overridable default"
    },
    "tests": [
      "a chain declaring supervisorPolicy.maxParallel overrides the DispatcherOptions default of 4",
      "a chain declaring nothing gets maxParallel: 4 byte-identically to today"
    ],
    "acceptance": "maxParallel is a chain-overridable default alongside quarantineScope/abortThreshold, per engine-boundary.md's policy-constant rule.",
    "notes": "inbox 2026-08-03. No new validation added: spec/pending.md's Fanout partition section already documents a non-positive maxParallel degrading to width-1 batches rather than wedging — only the missing chain-declaration surface is the defect."
  },
  {
    "tag": "WORKTREES-HARVESTFRICTION-COLLISION",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "harvestFriction stamps its destination filename so a retried entry's same-named source file can't silently overwrite an earlier still-unread note; correct the JSDoc's collision-freedom claim to match"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "cover the collision case"
        }
      ],
      "retire": []
    },
    "summary": "harvestFriction composes its destination with no exists-check; rename/copyFile silently replace an existing file, so a retry can destroy an earlier, still-unread friction note.",
    "per": {
      "path": "spec/worktrees.md",
      "section": "Teardown harvest — the delivery guarantee"
    },
    "tests": [
      "two harvests for the same tag with the same agent-chosen source filename land as two distinct files, neither overwriting the other"
    ],
    "acceptance": "harvestFriction's destination naming is collision-free the way writeRevertNote's already is; its JSDoc no longer claims a guarantee the code didn't deliver.",
    "notes": "equilibrium audit, friction harvest + retired vocabulary cluster."
  }
]
