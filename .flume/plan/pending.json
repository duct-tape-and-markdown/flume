[
  {
    "tag": "FORGE-STUB-ANSWERS-BEFORE-THE-HOSTS-OWN-CLI",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/harnessCi.test.ts",
          "description": "`plantForge` puts the stub's dir on a PATH the host's own forge CLI is not on, so the direct spawn reaches the stub or nothing"
        }
      ],
      "retire": []
    },
    "summary": "`plantForge` prepends its dir but leaves the host's PATH behind it, so on a host whose real `gh` the direct spawn can reach, five cases read the forge instead of the stub",
    "per": {
      "path": "spec/cli.md",
      "section": "win32 is a supported host"
    },
    "acceptance": "every lane-reader case is answered by the planted stub on every host, and the vacuity pins on the stub's call count hold for the stub's own invocations rather than for a CLI the fixture never planted.",
    "tests": [],
    "pins": [
      "every lane-reader case reads its planted forge stub, with the host's own forge CLI off PATH"
    ],
    "notes": "Lane-observed twice, now all eight lane-reader cases: the `refuses` case renders the real `gh`'s GH_TOKEN message and the other seven see `calls().length === 0`. On win32 the stub is a `.cmd`, which a direct spawn skips; the search then walks on to the host's `gh.exe` further down PATH. The helper's own doc comment already states the isolation this breaks, and the absent-CLI case already shows the shape — PATH stripped to a single dir, the launcher naming `process.execPath` absolutely."
  },
  {
    "tag": "A-POSIX-ONLY-CASE-DECLARES-ITS-HOST",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "tests/hostDeclarations.test.ts",
          "description": "the declared-host list with a reason each, and the scan holding the suite to it"
        }
      ],
      "edit": [
        {
          "path": "tests/cli.test.ts",
          "description": "the readdir-succeeds/stat-fails friction arms declare their host"
        },
        {
          "path": "tests/cliHelp.test.ts",
          "description": "the same arm under `flume friction --help`"
        },
        {
          "path": "tests/Baton.test.ts",
          "description": "the symlink-loop ELOOP case declares its host"
        },
        {
          "path": "tests/harnessRunner.test.ts",
          "description": "the declared-setup case drops the symlinked `node_modules` and declares its host"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "the EBUSY-survives-removal case declares its host"
        },
        {
          "path": "tests/job.test.ts",
          "description": "the ensureRuntimeIgnores read-denial arm declares its host"
        },
        {
          "path": "tests/bin.test.ts",
          "description": "the two symlink-walk cases declare theirs"
        }
      ],
      "retire": []
    },
    "summary": "the cases with no structural substitute — a stat that fails after a readdir succeeds, a symlink loop, `ln -s`, a sh shim reached through one — red the lane on their fixture, not a defect",
    "per": {
      "path": "spec/cli.md",
      "section": "win32 is a supported host"
    },
    "acceptance": "every case whose subject is POSIX error semantics names its host and the reason it skips elsewhere, so the win32 lane's remaining reds are engine or fixture defects and nothing skips silently.",
    "tests": [],
    "pins": [
      "no case in tests/ denies with a permission bit outside the declared-host list",
      "every declared-host case carries the reason it cannot run on the other host"
    ],
    "notes": "The declared-host list is an allowlist with a reason each, on the `EXTERNAL_VOCABULARY` idiom (`tests/commentCitations.test.ts`): a site added without one is indistinguishable from residue. `harnessRunner`'s `ln -s` is also what `.claude/rules/platform-facts.md`, *pnpm deletes a symlinked `node_modules` on install* already says not to do. `bin/flume` is the sh shim; the published win32 bin is `bin/flume.js`, so the walk's subject is POSIX-only by construction."
  },
  {
    "tag": "LONG-PATH-FIXTURES-COMPOSE-THROUGH-THE-NAMESPACED-JOIN",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/job.test.ts",
          "description": "the two deep-nest fixtures build and run git through the namespaced join the cases themselves pin"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "the long pendingPath fixture, same"
        }
      ],
      "retire": []
    },
    "summary": "the three win32 total-path cases nest their tree with a bare `join` and then run git in it, so the fixture hits the very limit the case exists to prove the engine clears",
    "per": {
      "path": "spec/cli.md",
      "section": "win32 is a supported host"
    },
    "acceptance": "each long-path case builds its fixture through `namespacedJoin` and reaches the engine call it names on win32, so the lane's verdict on these three is the engine's and not the fixture's.",
    "tests": [],
    "pins": [
      "jobRm finds and removes a job whose dir nests past win32's ~260-char limit",
      "jobNew doesn't misread an existing chain.ts or seedDir as absent when configDir/seedDir nests past win32's ~260-char limit",
      "readPending/readPendingTolerant/commitPendingUpdate don't misread an existing or writable pending.json as absent when pendingPath exceeds win32's ~260-char limit"
    ],
    "notes": "Lane-observed: all three failed `spawn git ENOENT`, which is win32 refusing a cwd past the limit rather than a missing git. The idiom is the one the section names and the cases already exercise on the engine side. `job new` pins `core.longpaths` repo-locally on win32; a fixture that inits its own repo wants the same."
  },
  {
    "tag": "MERGING-MARKERS-PROVE-ABSENCE-FROM-THE-PATH",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/fsProbe.ts",
          "description": "the descent `readAll` already runs, in the shared home both readers take it from"
        },
        {
          "path": "src/priorAttempts.ts",
          "description": "drops its private copy onto the shared one"
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "readMergingMarkers proves its dir absent from the path, never from the listing's errno"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "the obstructed-ancestor arm, asserting the reader's own refusal rather than an errno"
        },
        {
          "path": "tests/priorAttempts.test.ts",
          "description": "the standing refusals survive the move"
        }
      ],
      "retire": []
    },
    "summary": "`readMergingMarkers` reads its listing's ENOENT as no interrupted merge, and win32 reports a path through a non-directory as ENOENT — the startup refusal passes over markers it cannot see",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Loud or nothing"
    },
    "acceptance": "the one reader feeding the crash-equals-stop refusal proves its empty answer from the path on both hosts, and the descent has one home rather than two.",
    "tests": [
      "readMergingMarkers refuses an obstructed merging dir with a reading of its own, naming the path that is not a directory"
    ],
    "pins": [
      "readAll refuses when a plain file sits above the prior-attempts root"
    ],
    "notes": "From PRIOR-ATTEMPTS-ABSENCE-IS-PROVEN-NOT-INFERRED's note: the same errno-keyed absent arm stands at every `ENOENT → empty` in src/job.ts and src/friction.ts too. Only this one feeds a refusal — an unseen marker lets a loop start over an interrupted merge — so only this one is filed; the rest are debt until a red or a consumer names them. `Baton.sleep`'s fold is a write no-op and the constructor's mkdir already refuses."
  },
  {
    "tag": "RENDER-NAMES-THE-LANE-THAT-WOKE-THE-TICK",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "harness/windows.ts",
          "description": "one lane read per tick, shared by the liveness leg and the render; each block says whether that lane is the one that made the slice live"
        },
        {
          "path": "harness/ci.ts",
          "description": "an unread degraded from a failing status keeps the run whose log could not be fetched"
        },
        {
          "path": "tests/harnessCi.test.ts",
          "description": "the wake marker, a wake whose log fetch failed, the stamped lane's silence, and the per-lane call count"
        }
      ],
      "retire": []
    },
    "summary": "the liveness verdict is a boolean the render re-derives and the degraded unread drops its run, so a tick woken by a lane it then cannot fetch renders a bare UNREAD",
    "per": {
      "path": "spec/harness.md",
      "section": "CI lanes as a findings source"
    },
    "acceptance": "every rendered lane says whether it is the one that made the slice live, and a wake whose log fetch failed names the run it was woken over — no unread standing over an invisible cause.",
    "tests": [
      "the inbox window names the lane whose undrained failing run made the slice live",
      "a lane woken by a run whose log the forge refuses renders unread over that run, not over nothing",
      "a failing lane already stamped at its latest run renders without the wake marker its unstamped self carries",
      "one tick's liveness leg and render ask the forge once per lane between them"
    ],
    "pins": [],
    "notes": "3b3dca4 accepted the double read as debt riding this entry: `undrainedRedLane` and `renderCiLanes` each walk every lane, and handing the render the liveness verdict is the same edit this sentence forces. `withMaterial`'s unread arm drops the failing status it degraded from, which is why the wake's cause cannot be named today. One window object serves both callers within a tick (`harness/chain.ts` builds it once; each tick is a fresh child)."
  },
  {
    "tag": "TESTS-COMMENTS-ARE-CITATIONS-THE-SCAN-RESOLVES",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/commentCitations.test.ts",
          "description": "the repo scan's tree list gains `tests/`; the standing pins retitle to the three trees the carve-out now names"
        },
        {
          "path": "tests/helpers/commentCitations.ts",
          "description": "the head comment names the carve-out's three trees"
        }
      ],
      "retire": []
    },
    "observedFiles": [
      ".flume/plan/notes/TESTS-COMMENTS-ARE-CITATIONS-THE-SCAN-RESOLVES.md",
      "tests/Dispatcher.test.ts",
      "tests/Gate.test.ts",
      "tests/PendingSchema.test.ts",
      "tests/Prompt.test.ts",
      "tests/builtinGates.test.ts",
      "tests/chain.test.ts",
      "tests/cli.test.ts",
      "tests/cliHelp.test.ts",
      "tests/cliJobResolution.test.ts",
      "tests/commentCitations.test.ts",
      "tests/docComments.test.ts",
      "tests/examples.integration.test.ts",
      "tests/examples.test.ts",
      "tests/exportConsumers.test.ts",
      "tests/git.test.ts",
      "tests/harnessPrompts.test.ts",
      "tests/helpers/chainVocabulary.ts",
      "tests/helpers/commentCitations.ts",
      "tests/helpers/exportGraph.ts",
      "tests/job.test.ts",
      "tests/paths.test.ts",
      "tests/stubRunner.test.ts",
      "tests/subprocessHelper.test.ts",
      "tests/worktrees.test.ts"
    ],
    "summary": "the comment-citation scan judges `src/` and `harness/` only, so the nine `tests/` citations of a rule page — three by section name — stand silently through a rename",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "acceptance": "a citation in a `tests/` comment resolves against the declarations the three trees hold and the working tree, so a deleted symbol or a renamed page reds the suite rather than leaving its citations standing.",
    "tests": [],
    "pins": [
      "every backticked identifier in a src/, harness/ or tests/ comment names a declaration those trees hold",
      "the repo citation pin judges the repo-relative path citations src/, harness/ and tests/ comments carry",
      "the repo citation pin judges the unbackticked page names src/, harness/ and tests/ comments carry"
    ],
    "notes": "Ruled in the inbox; the carve-out already reads `tests/` and `platform-facts.md` already loads there. The widen is `trees: [\"src/\", \"harness/\", \"tests/\"]` in `tests/commentCitations.test.ts`. All three standing repo pins name fewer trees than the widen judges — one names `src/` alone — so all three retitle here. Citations the widen turns up dangling are repaired here too; the nine `platform-facts.md` sites are the first judged set, not the whole one. Vacuity guards want a `tests/` module prefix."
  },
  {
    "tag": "DENIAL-HELPER-CITES-THE-PAGE-THAT-OWNS-ITS-FACTS",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/helpers/denial.ts",
          "description": "the two stat facts shrink to a cite of the page section that now owns them"
        },
        {
          "path": "tests/denial.test.ts",
          "description": "the cover parent-seal comment, same shrink"
        }
      ],
      "retire": []
    },
    "summary": "the denial helper spells out both stat facts `platform-facts.md` now owns, so one measured external fact is held in two prose copies that can drift apart",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "acceptance": "the two measured stat facts have one home, and every site that turns on them points at it rather than restating it.",
    "tests": [],
    "pins": [],
    "notes": "The trigger the closed question named has fired: 4e750c0 gave `.claude/rules/platform-facts.md` *win32 reports a path through a non-directory as not found*, carrying both facts and the deny-the-read-never-the-parent idiom. The helper's second paragraph and the cover's parent-seal comment predate it and restate it whole; the neighbouring *chmod denies nothing on win32* cite in the same comment is the pointer shape. tests/job.test.ts's one-line why stays debt."
  }
]
