[
  {
    "tag": "WRITABLEPATHS-DOC-CLAIMS-MINIMATCH",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Phase.ts",
          "description": "`writablePaths`' hover text — name the dialect the cite ratifies, drop `minimatch-style`, point at `matchesAny`"
        },
        {
          "path": "tests/docComments.test.ts",
          "description": "the scan, in the file's existing `docCommentFor` shape with its vacuity guard"
        }
      ],
      "retire": []
    },
    "summary": "`Phase.writablePaths`' shipped doc comment reads `minimatch-style`; the matcher escapes every special but `*` and `**`",
    "per": {
      "path": "spec/pending.md",
      "section": "The entry-scoped write guard is opt-in, and off by default"
    },
    "tests": [
      "the shipped `writablePaths` doc comment names no glob dialect the engine's matcher does not implement"
    ],
    "pins": [],
    "acceptance": "The scan is red on the pre-fix tree and green after; no `src/` doc comment names a foreign glob dialect.",
    "notes": "Drained from MATCHESANY-QUESTION-MARK-UNESCAPED's note. The cite ratifies the literal reading, so the comment moves and the matcher does not. `?`, `{a,b}` and `[abc]` are literals here and syntax in minimatch, so a chain author trusting the hover text declares a silently narrower fence. `matchesAny`'s own doc states the real dialect — point at it, never a second copy (engineering.md, *Derived state is computed*). Shipped `.d.ts` prose is pinnable per 862e94b."
  }
]
