[
  {
    "tag": "AGENT-VOCAB-AGREEMENT-REAL-READER",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/Agent.test.ts",
          "description": "the two shared-vocabulary agreement tests drive extractFinalMessage, not a stand-in"
        }
      ],
      "retire": []
    },
    "summary": "Drive Agent.test.ts's two shared-vocabulary agreement tests through extractFinalMessage, the real second reader — both assert against hand-rolled stand-ins today",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A seam gate reads what the real writer wrote"
    },
    "tests": [],
    "pins": [
      "withTerminalRenderer's tool_use render and extractFinalMessage's closing prose agree on one assistant transcript",
      "withTerminalRenderer's ERROR head and extractFinalMessage's result text agree on one error result transcript"
    ],
    "acceptance": "Neither agreement test re-implements the classification: each calls extractFinalMessage on the same transcript the renderer consumed, so a one-sided change to either reader's event literals goes red.",
    "notes": "isErrorResult has one production consumer — formatResult's ERROR head (src/Agent.ts). extractFinalMessage never calls it, so the current test's \"Dispatcher-shaped result reader\" names a reader that does not exist: the Dispatcher stopped extracting when finalMessage moved onto AgentResult (6a8dfb1). Drop that leg's fabricated agreement claim rather than inventing a second reader."
  }
]
