[
  {
    "tag": "HARNESS-SUBPATH-EXPORT",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "tests/harnessPackaging.test.ts",
          "description": "the subpath export and every shipped entry path against the emitted layout"
        }
      ],
      "edit": [
        {
          "path": "tsconfig.build.json",
          "description": "include harness/**"
        },
        {
          "path": "package.json",
          "description": "exports ./harness; main, types and exports . follow the layout"
        },
        {
          "path": "bin/flume",
          "description": "cli path follows the layout"
        },
        {
          "path": "bin/flume.js",
          "description": "cli path follows the layout"
        },
        {
          "path": "src/cli.ts",
          "description": "readPackageVersion resolves under both layouts"
        },
        {
          "path": "tests/bin.test.ts",
          "description": "fixture dist tree follows"
        },
        {
          "path": "tests/cliJobVerbs.test.ts",
          "description": "DIST_CLI follows"
        },
        {
          "path": "scripts/smoke-install.mjs",
          "description": "resolve the subpath from the installed tarball"
        }
      ],
      "retire": []
    },
    "summary": "Ship harness/ as the ./harness subpath of @dtmd/flume \u2014 one package, one version, one exports map \u2014 carrying the dist-layout move onto every shipped entry path.",
    "per": {
      "path": "spec/harness.md",
      "section": "Where it lives"
    },
    "tests": [
      "the package exports map resolves ./harness to the built harness entry point",
      "the cli resolves its own package.json under both the checkout and the published layout"
    ],
    "pins": [
      "every shipped entry path resolves inside the layout the build tsconfig emits"
    ],
    "acceptance": "pnpm build emits dist/harness/index.js beside the engine's own entry, @dtmd/flume/harness resolves to it, flume --version still answers, and pnpm run smoke:install resolves the subpath from the installed tarball.",
    "notes": "Measured: tsconfig.build.json declares no rootDir, so adding harness/** moves the inferred root to the repo root and the emit becomes dist/src/ + dist/harness/. main, types, exports '.', both bin shims and src/cli.ts's '../package.json' hop all follow in the same commit \u2014 this is a layout change, not a one-line add. Fence, typecheck include and sweep domain already name harness/ (0c93bd4). smoke-install is outside the fast lane, so it rides acceptance."
  },
  {
    "tag": "HARNESS-DECLARATION-SCHEMA",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "harness/declaration.ts",
          "description": "the strict declaration schema and its refusal"
        },
        {
          "path": "tests/harnessDeclaration.test.ts",
          "description": "one acceptance case, two refusals"
        }
      ],
      "edit": [
        {
          "path": "harness/index.ts",
          "description": "export the schema and its type"
        }
      ],
      "retire": []
    },
    "summary": "Add the package's strict declaration schema \u2014 the eleven fields a consumer declares \u2014 refusing an unknown or a missing required field at load, naming the field and the valid set.",
    "per": {
      "path": "spec/harness.md",
      "section": "What a consumer declares"
    },
    "tests": [
      "the declaration schema parses a declaration naming every field",
      "an unknown declaration field is refused, naming the field and the valid set",
      "a missing required declaration field is refused, naming the field"
    ],
    "pins": [],
    "acceptance": "harness/declaration.ts parses a full declaration and refuses both an unknown field and a missing required one, each refusal naming the field.",
    "notes": "runner types against harness/runner.ts. No field names an engine artifact path, a verdict field, or a prior-attempt mode \u2014 those are the engine's to report. This entry is the shape alone; its consumers are the cite resolver and the chain factory, later entries."
  },
  {
    "tag": "HARNESS-CITE-RESOLVER",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "HARNESS-DECLARATION-SCHEMA"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "harness/citeResolver.ts",
          "description": "per resolution against specLocus, resolver declarable"
        },
        {
          "path": "tests/harnessCiteResolver.test.ts",
          "description": "the two refusals and the declared resolver"
        }
      ],
      "edit": [
        {
          "path": "harness/index.ts",
          "description": "export the resolver and its verdict type"
        }
      ],
      "retire": []
    },
    "summary": "Add the package's per resolver: the path matches a specLocus glob and is in the gated commit, the section is a heading in it, and a consumer may declare a resolver that keys sections instead.",
    "per": {
      "path": "spec/harness.md",
      "section": "The cite resolver"
    },
    "tests": [
      "a per cite whose path matches no specLocus glob is refused, naming the path",
      "a per cite whose section is no heading at the gated commit is refused, naming the section",
      "a declared resolver keys a section without changing the verdict shape"
    ],
    "pins": [],
    "acceptance": "harness/citeResolver.ts resolves a per against a declared specLocus at a commit and yields one verdict shape whether the section is found by heading or by a declared resolver.",
    "notes": "The at-ref reader is injected, not imported, so every case stays in the fast lane. .flume/chain.ts's perResolvesGate and sectionOf are the chain-side copy this retires at the cutover."
  }
]
