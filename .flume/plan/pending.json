[
  {
    "tag": "DANGLING-CITATION-PIN",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [
        {
          "path": "tests/helpers/commentCitations.ts",
          "description": "resolve backticked identifiers in src/ and harness/ comments against the declarations those trees hold"
        },
        {
          "path": "tests/commentCitations.test.ts",
          "description": "the pin over src/ and harness/, plus the scanner's own flagging case"
        }
      ],
      "edit": [],
      "retire": []
    },
    "summary": "Promote the deleted-symbol verdict: a pin that reds on a backticked identifier in a src/ or harness/ comment naming no declaration those trees hold",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "acceptance": "the suite carries a pin that reds on a comment citing a name src/ and harness/ no longer declare, and is green on the tip",
    "tests": [
      "the citation scan flags a backticked identifier no src/ or harness/ declaration holds"
    ],
    "pins": [
      "every backticked identifier in a src/ or harness/ comment names a declaration those trees hold"
    ],
    "notes": "The token, never its meaning — a name either appears as a declaration in those two trees or it does not. Subjects are camelCase/PascalCase only, so backticked prose words are not judged. Vacuity guard: assert the subject set is non-empty before the emptiness assertion; crude probe on this tip reports 1010 subjects, 0 misses. No historical tree reds on the property (the deletion and its hand-repair shipped in one commit), so the tests[] line is the detector's own flagging case over a fixture."
  }
]
