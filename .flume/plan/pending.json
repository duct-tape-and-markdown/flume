[
  {
    "tag": "THE-QUEUES-DECLARED-WRITER-RUNS-OVER-AN-UNPARSEABLE-QUEUE",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Phase.ts",
          "description": "the queue's parse failure as a tick fact on TickContext"
        },
        {
          "path": "src/singletonTick.ts",
          "description": "the decide-read's carve-out, keyed on the phase's declared writable paths"
        },
        {
          "path": "src/waveTick.ts",
          "description": "the same carve-out at the wave's decide-read"
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "the catch stops stating a hand edit as the remedy"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "both fates over an unparseable queue, and the reported fact"
        }
      ],
      "retire": []
    },
    "summary": "the strict read refuses before any phase runs, so an unparseable queue stops the plan tick whose rewrite is the repair and only a hand edit clears it",
    "per": { "path": "spec/pending.md", "section": "Queue reads are strict" },
    "acceptance": "a phase whose declared writable paths include the queue path is invoked over an unparseable queue with the parse failure handed to it as a tick fact; a phase that cannot write the queue is refused as now.",
    "tests": [
      "a phase whose writable paths include the queue is invoked over an unparseable queue",
      "a phase that cannot write the queue is refused over an unparseable one",
      "the tick context carries the queue's parse failure as a fact"
    ],
    "pins": [],
    "notes": "Keyed on the declared fence, never an inferred intent (`engine-boundary.md`, *Told, not inferred*). The fact is reported, not a verdict: what to do with it is the chain's. `src/Dispatcher.ts`'s catch states the old remedy in its own words — \"until a human fixes it\" — and `spec/loop.md` now points at the cite above instead."
  },
  {
    "tag": "THE-PLAN-SLICES-TAKE-A-QUEUE-PARSE-FAILURE-AS-INPUT",
    "gate": {
      "kind": "blockedBy",
      "tags": ["THE-QUEUES-DECLARED-WRITER-RUNS-OVER-AN-UNPARSEABLE-QUEUE"]
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "harness/sliceWindow.ts",
          "description": "the parse failure reaches the liveness legs as a tick fact"
        },
        {
          "path": "harness/inboxWindow.ts",
          "description": "a queue that failed to parse makes the slice live"
        },
        {
          "path": "harness/prompts/plan-inbox.md",
          "description": "the failure as the drain's input, the rewrite as the repair"
        },
        {
          "path": "tests/harnessWindows.test.ts",
          "description": "liveness and render over a parse failure"
        }
      ],
      "retire": []
    },
    "summary": "the package's slices read a parsed queue only, so once the engine admits them over an unparseable one nothing makes a slice live for it or tells the agent to repair it",
    "per": { "path": "spec/harness.md", "section": "The gates the discipline needs" },
    "acceptance": "a queue that fails to parse makes the plan slice live and its render carries the failure, so the next plan tick's rewrite is the repair and no queue state needs a hand edit.",
    "tests": [
      "a queue that fails to parse makes the inbox slice live",
      "the inbox render carries the queue's parse failure as the drain's input"
    ],
    "pins": [],
    "notes": "The prompt already cats the raw queue bytes (`PENDING_PATH`), so the agent sees the file; what is missing is the failure stated as a fact and a liveness leg that fires on it. Blocked on the engine leg that reports the fact."
  },
  {
    "tag": "THE-LOOP-LOCK-STATES-THE-INSTANT-IT-WAS-TAKEN",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "docs/MIGRATING-0.17.md",
          "description": "the mixed-version reclaim hazard, named"
        }
      ],
      "edit": [
        {
          "path": "src/cli.ts",
          "description": "the lock writes pid then instant; status reads the run's start from that statement"
        },
        {
          "path": "src/job.ts",
          "description": "the liveness read takes the first line"
        },
        {
          "path": "src/git.ts",
          "description": "the tip claim takes the same shape, read the same way"
        },
        {
          "path": "src/tickVerdict.ts",
          "description": "the window reader's doc names the stated instant, not an mtime"
        },
        {
          "path": "tests/cli.test.ts",
          "description": "status over a two-line lock, and the spend window it bounds"
        },
        { "path": "tests/job.test.ts", "description": "the first-line pid" },
        { "path": "tests/git.test.ts", "description": "the claim's shape" },
        { "path": "docs/CLI.md", "description": "the lock's contents" },
        { "path": "CHANGELOG.md", "description": "under 0.17's `### Breaking`" }
      ],
      "retire": []
    },
    "summary": "nothing states when a run started, so status measures it off loop.pid's mtime — a filesystem property no writer contracts and any archive restore or touch moves",
    "per": { "path": "spec/loop.md", "section": "The loop lock and the tip claim" },
    "acceptance": "the loop lock and the tip claim each carry the holder's pid on the first line and the claim instant on the second, every liveness reader takes the first line, and the live run's spend is bounded by the stated instant.",
    "tests": [
      "the loop lock records the pid on the first line and the claim instant on the second",
      "the tip claim records the pid on the first line and the claim instant on the second",
      "a two-line loop lock still reports its holder live",
      "flume status bounds the live run's spend to the instant the lock states"
    ],
    "pins": [],
    "notes": "Pid stays the first line so every same-version reader is unaffected. Breaking across versions: a 0.16 reader parses the whole file to `NaN`, reads stale, and reclaims a live lock — mixed versions on one state root. That is what the migration note names.",
    "contractTouching": true
  },
  {
    "tag": "FLUME-HELP-FLAG-ANSWERS-FOR-A-SUBCOMMAND",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/cli.ts",
          "description": "the trailing-name arm covers the flag spelling too, through the same decider"
        },
        {
          "path": "tests/cliHelp.test.ts",
          "description": "the answer and the refusal on the flag spelling"
        },
        {
          "path": "docs/CLI.md",
          "description": "the flag form beside the verb form"
        }
      ],
      "retire": []
    },
    "summary": "`flume --help <name>` prints the top-level page and discards the name — the one place left in the CLI where argv is dropped rather than honored or refused",
    "per": { "path": "spec/cli.md", "section": "Subcommand surface" },
    "acceptance": "`flume --help <subcommand>` answers that subcommand's page and an unknown name there is usage-shaped (exit 2), through the same decider the verb spelling uses.",
    "tests": [
      "flume --help status prints the status subcommand's usage",
      "flume --help with an unknown name exits 2 with usage"
    ],
    "pins": [],
    "notes": "The trailing-name lookup is gated on the bare verb today, so the flag arm falls through to the top-level page. One decider serves every spelling — the site's own comment already condemns the drop for the verb arm."
  },
  {
    "tag": "A-DECLARED-COMMAND-GATE-NAMES-ITS-SHELL",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "harness/declaration.ts",
          "description": "`shell` on the declaration, default `sh`"
        },
        {
          "path": "harness/declaredGates.ts",
          "description": "a shell or script gate runs under the declared shell, and the load-time probe"
        },
        {
          "path": "tests/harnessDeclaration.test.ts",
          "description": "the field and its default"
        },
        {
          "path": "tests/harnessGates.test.ts",
          "description": "the declared shell reaches the child, and the refusal"
        },
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "the command-gate section stops spelling `sh -c` as the package's choice"
        }
      ],
      "retire": []
    },
    "summary": "every declared shell and script gate spawns `sh -c`, a shell win32 resolves from one launch shell and not another, chosen by the package and named by no declaration",
    "per": { "path": "spec/harness.md", "section": "What a consumer declares" },
    "acceptance": "a declared `shell` runs every shell and script gate, absent means `sh`, and chain load refuses a shell the host does not resolve, naming the gate.",
    "tests": [
      "a declared shell runs a command gate's command",
      "chain load refuses a shell the host does not resolve, naming the gate"
    ],
    "pins": ["an undeclared shell runs a command gate under sh"],
    "notes": "Both arms of the reported fork, since a default that silently fails on a supported host is the shape *Loud or nothing* refuses. `spec/cli.md` rules win32 a supported host; the probe is at load, never at the tick that needed the gate."
  },
  {
    "tag": "THE-SCRIPT-RUNNER-READS-THROUGH-A-DECLARED-READER",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "harness/scriptRunner.ts",
          "description": "`read` on the options — the declared reader, the line reader as default"
        },
        {
          "path": "tests/harnessRunner.test.ts",
          "description": "a real document-emitting script driven through a declared reader, beside the line one"
        },
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "the adoption price names the declared reader, not the line shape alone"
        }
      ],
      "retire": []
    },
    "summary": "the script runner reads verdict lines and nothing else, so a validator emitting one JSON document needs a wrapper script — the wrapper the runner row exists to remove",
    "per": { "path": "spec/harness.md", "section": "The runner interface" },
    "acceptance": "`scriptRunner` takes a reader the declaration names over the command's whole stdout; absent, the line reader; a validator emitting one document declares a function over that document and needs no wrapper script.",
    "tests": [
      "a declared reader turns a validator's one JSON document into a verdict per name"
    ],
    "pins": ["an undeclared reader takes one verdict line per name from stdout"],
    "notes": "The fixture is the agreement gate's (`engineering.md`, *A seam gate reads what the real writer wrote*): a real script emitting a real document through the real reader, beside the line fixture already there. A document whose clean run omits its drift key is the shape priced."
  },
  {
    "tag": "A-LANE-STAMP-CARRIES-THE-FAILING-TITLES-IT-WOKE-ON",
    "gate": {
      "kind": "parked",
      "reason": "decision on where a lane's current failing-title set comes from — open-questions, 'Where does a lane's failing-title set come from?'"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "harness/planState.ts",
          "description": "a drained-run stamp is the run and the failing titles it reported"
        },
        {
          "path": "harness/ciLane.ts",
          "description": "the wake compares title sets, not run identity alone"
        },
        {
          "path": "harness/ci.ts",
          "description": "the failing titles reach the stamp the slice writes"
        },
        {
          "path": "tests/harnessPlanState.test.ts",
          "description": "the stamp's shape, and a bare-string stamp already on disk"
        },
        {
          "path": "tests/harnessCi.test.ts",
          "description": "wake and no-wake across matching and differing title sets"
        }
      ],
      "retire": []
    },
    "summary": "a red lane wakes the inbox slice once per run, so a red that persists across runs wakes it again on the same titles — the stamp is the run alone",
    "per": {
      "path": "spec/harness.md",
      "section": "CI lanes as a findings source"
    },
    "acceptance": "the drained-run stamp is the run and the failing titles it reported, and a later failing run whose title set matches the stamp advances the stamp at the next tick that runs anyway without waking the slice.",
    "tests": [
      "a drained-run stamp carries the failing titles the run reported",
      "a failing run whose title set matches the stamp does not make the inbox slice live",
      "a failing run whose title set differs from the stamp makes the inbox slice live"
    ],
    "pins": [],
    "notes": "Parked on build's report: the entry's premise was wrong. Nothing under `harness/` or `src/` harvests a test title, and the lane reader refuses the harvest by name — a finding out of material the package did not author is the agent's. The stamp shape and render are mechanical; the liveness leg needs the current run's title set, which no surface produces."
  },
  {
    "tag": "THE-INBOX-SLICE-READS-A-DECLARED-FRICTION-DIRECTORY",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "harness/declaration.ts",
          "description": "the friction channel as a declared findings source"
        },
        {
          "path": "harness/chain.ts",
          "description": "pass-through to Chain.friction"
        },
        {
          "path": "harness/inboxWindow.ts",
          "description": "a fourth-leg read of the declared friction dir, as it reads the inbox"
        },
        { "path": "tests/harnessDeclaration.test.ts", "description": "the field" },
        { "path": "tests/harnessChain.test.ts", "description": "the pass-through" },
        {
          "path": "tests/harnessWindows.test.ts",
          "description": "the render and the liveness leg"
        }
      ],
      "retire": []
    },
    "summary": "the harness declaration mirrors no part of Chain.friction and the inbox slice reads none, so a consumer routing its own friction notes writes the prompt paragraph the package owes",
    "per": {
      "path": "spec/harness.md",
      "section": "Declared findings sources"
    },
    "acceptance": "a declared friction directory reaches Chain.friction and the inbox slice reads, renders and drains its files as it does the inbox's — one record per file, removed the way a record is drained.",
    "tests": [
      "a declared friction directory reaches Chain.friction",
      "the inbox slice renders a declared friction directory's files as records",
      "the inbox slice is live for a waiting friction file"
    ],
    "pins": [],
    "notes": "`Chain.friction` exists on the engine and the declaration mirrors none of it — no schema field, no pass-through. The table row landed with the ruling (*What a consumer declares*, `friction`), so the field mirrors the engine's value as `supervisor` does. Blocked on the liveness rule so the fourth leg lands under it."
  },
  {
    "tag": "A-DECLARED-FINDINGS-SCRIPT-WRITES-RECORDS",
    "gate": {
      "kind": "blockedBy",
      "tags": ["THE-INBOX-SLICE-READS-A-DECLARED-FRICTION-DIRECTORY"]
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "harness/declaration.ts",
          "description": "`findings`: declared scripts, each a name and a command"
        },
        {
          "path": "harness/inboxWindow.ts",
          "description": "the scripts run at the tip before the render; each unseen stdout line becomes a record file"
        },
        {
          "path": "harness/planState.ts",
          "description": "the drained record-name set, stamped as a lane's run is"
        },
        {
          "path": "harness/prompts/plan-discipline.md",
          "description": "the plan state's field list takes the stamp"
        },
        {
          "path": "tests/harnessDeclaration.test.ts",
          "description": "the field and its shape"
        },
        {
          "path": "tests/harnessWindows.test.ts",
          "description": "the run, the materialization, the delta, and the empty case"
        }
      ],
      "retire": []
    },
    "summary": "the declaration names no findings scripts and the inbox slice runs none, so a consumer's per-tick measurement has no route into the drain the package already runs",
    "per": {
      "path": "spec/harness.md",
      "section": "Declared findings sources"
    },
    "acceptance": "declared findings scripts run at the tip before the slice renders, the slice stamps the record names it drained and materializes only names it has not seen since the stamp, and those files render and drain exactly as the inbox's do.",
    "tests": [
      "a declared findings script's unseen stdout lines are materialized as record files",
      "a findings script re-emitting a stamped record name materializes nothing",
      "the inbox slice renders a materialized findings record beside the inbox's",
      "a findings script yielding no lines leaves the record queue untouched"
    ],
    "pins": [],
    "notes": "Wire shape ratified: one `{ name, body }` object per line. The delta is the package's, never the script's — a consumer measuring a standing set re-emits it every tick and pays only its first drain. Watch the records gate: the package writes the materialized file and the slice drains it the same tick. Blocked on the friction leg, which rewrites the same window."
  },
  {
    "tag": "THE-DECLARATION-ASSERTS-ITS-CAPABILITIES",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "harness/declaration.ts",
          "description": "`capabilities` on the declaration"
        },
        {
          "path": "harness/chain.ts",
          "description": "pass-through to Chain.capabilities, whole"
        },
        { "path": "tests/harnessDeclaration.test.ts", "description": "the field" },
        {
          "path": "tests/harnessChain.test.ts",
          "description": "the pass-through, declared and absent"
        }
      ],
      "retire": []
    },
    "summary": "the harness declaration asserts no capabilities, so under the package a requiresCapability entry is unpickable forever and no declaration can change it",
    "per": { "path": "spec/harness.md", "section": "What a consumer declares" },
    "acceptance": "a declared `capabilities` list reaches Chain.capabilities whole, and a declaration that names none asserts none.",
    "tests": ["a declared capabilities list reaches Chain.capabilities whole"],
    "pins": [
      "a declaration naming no capabilities leaves Chain.capabilities unasserted"
    ],
    "notes": "Engine side is complete: `Chain.capabilities` (`src/Phase.ts`) decides a `requiresCapability` entry's pickability and `flume status` already names one held back — nothing in `src/` moves. The pass-through mirrors the `supervisor` row's (`supervisorPolicy`, `harness/chain.ts`). Per-job capabilities add to this set and ride THE-DECLARATION-NAMES-ITS-JOBS."
  },
  {
    "tag": "THE-DECLARATION-CARRIES-SETUP-SERIALIZE",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "harness/declaration.ts",
          "description": "`serialize` on the setup object"
        },
        {
          "path": "harness/chain.ts",
          "description": "the provisioning reduction serializes the restore across a wave"
        },
        { "path": "tests/harnessDeclaration.test.ts", "description": "the field" },
        {
          "path": "tests/harnessChain.test.ts",
          "description": "serialized and concurrent restores across a wave"
        }
      ],
      "retire": []
    },
    "summary": "a restore whose shared cache is not safe to warm concurrently has no way to say so; every fanout wave warms every worktree at once",
    "per": { "path": "spec/harness.md", "section": "What a consumer declares" },
    "acceptance": "`setup.serialize: true` runs the declared restore one worktree at a time across a fanout wave while the wave's other provisioning stays parallel; absent, restores run as they do now.",
    "tests": [
      "setup.serialize runs a fanout wave's restores one worktree at a time",
      "setup.serialize leaves the wave's other provisioning parallel",
      "an undeclared serialize runs restores concurrently"
    ],
    "pins": [],
    "notes": "The reduction is `provisioning(api, declaration)` in harness/chain.ts, built once per chain load — so the serialization is that function's own queue, not a second mechanism beside it. The `setup` row of the declaration table already states the behavior."
  },
  {
    "tag": "THE-DECLARATION-NAMES-ITS-JOBS",
    "gate": {
      "kind": "blockedBy",
      "tags": ["THE-DECLARATION-ASSERTS-ITS-CAPABILITIES"]
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "harness/declaration.ts",
          "description": "`jobs`: a map from job name to specLocus, fence and optional slots"
        },
        {
          "path": "harness/chain.ts",
          "description": "the named job's values applied over the shared declaration, and the package's own seedDir"
        },
        {
          "path": "tests/harnessDeclaration.test.ts",
          "description": "the field and its shape"
        },
        {
          "path": "tests/harnessChain.test.ts",
          "description": "resolution under a job's state root, and the one-job case"
        },
        {
          "path": "tests/harnessInit.test.ts",
          "description": "the skeleton a job root seeds from"
        }
      ],
      "retire": []
    },
    "summary": "a bay has no way to say a job is a named fence over shared values, so each job carries a second declaration and a loader the package should own",
    "per": { "path": "spec/harness.md", "section": "What a consumer declares" },
    "acceptance": "`jobs` maps a job name to a specLocus, a fence and optional slots and capabilities, a chain loaded under a job's state root applies that job's values over the shared declaration — capabilities added to the shared set rather than replacing it — and an absent `jobs` resolves the repo-level one-job case.",
    "tests": [
      "a chain loaded under a named job's state root takes that job's specLocus and fence",
      "a named job's slots override the declaration's shared slots",
      "a named job's capabilities add to the declaration's shared set",
      "a declaration naming no jobs resolves the repo-level values",
      "the chain the factory builds declares a seedDir at the package's skeleton"
    ],
    "pins": [],
    "notes": "The engine's job partition already gives each job its state root (spec/jobs.md); which job the chain is under is read from api.paths.flumeDir. `flume job new` seeds from the package skeleton through the factory's declared seedDir — harness/ declares none today, so that is part of this entry. Blocked on the shared capabilities field this one adds to."
  },
  {
    "tag": "A-JOB-FENCE-STOPS-AT-A-SIBLINGS-ARTIFACTS",
    "gate": { "kind": "blockedBy", "tags": ["THE-DECLARATION-NAMES-ITS-JOBS"] },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "harness/declaration.ts",
          "description": "the job-fence refusal at chain load, naming the glob and the root"
        },
        {
          "path": "tests/harnessDeclaration.test.ts",
          "description": "the refusal, its message, and the admitted consumer file"
        },
        {
          "path": "tests/harnessChain.test.ts",
          "description": "the refusal arrives at load, not at a tick"
        }
      ],
      "retire": []
    },
    "summary": "nothing refuses a job fence whose globs reach the package's own artifacts under a sibling job's state root, so one job's build writes another job's queue",
    "per": { "path": "spec/harness.md", "section": "What a consumer declares" },
    "acceptance": "chain load refuses a job fence glob that can match the package's queue, plan state, a record or a note under any state root but the job's own, naming the glob and the root; a consumer's own file under a sibling root still loads.",
    "tests": [
      "a job fence reaching the package's queue under a sibling job's root refuses the load",
      "the refusal names the offending glob and the root it reaches",
      "a job fence reaching a consumer's own file under a sibling root loads"
    ],
    "pins": [],
    "notes": "The engine declines cross-job fence policing by design (spec/jobs.md) and every package gate keys on the tick's own root, so the refusal is the package's alone. The artifact set is the package's own layout — queue, plan state, records, notes — never a consumer's product; where a bay keeps its product is the bay's."
  },
  {
    "tag": "THE-PICKABLE-SET-CARRIES-A-CHAIN-DECLARED-REFUSAL",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Phase.ts",
          "description": "the per-entry refusal a chain declares, and what the engine reports about it"
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "enforcement on the pickable set, and the fact reported for each entry held back"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "the set with and without a declared refusal, and the reported facts"
        },
        { "path": "tests/chain.test.ts", "description": "the declaration surface" }
      ],
      "retire": []
    },
    "summary": "the engine's pickable set carries no per-entry refusal a chain can declare, so a chain that must decline one entry can only decline the whole phase",
    "per": { "path": "spec/harness.md", "section": "The default `handoff`" },
    "acceptance": "a chain-declared per-entry refusal removes an entry from the reported pickable set, and the engine reports which entries it held back as a fact rather than a verdict.",
    "tests": [
      "a chain-declared per-entry refusal removes an entry from the reported pickable set",
      "the engine reports each entry a chain-declared refusal held back",
      "a chain declaring no per-entry refusal leaves the pickable set unchanged"
    ],
    "pins": [],
    "notes": "The capability, not the policy: the chain supplies the predicate, the engine enforces and reports (`engine-boundary.md`, *Capability vs convention*). Every input a predicate needs is already on the tick — the entry and its prior-attempt record. Contract-touching: a resident supervisor and a fresh tick child must agree on the Chain shape.",
    "contractTouching": true
  },
  {
    "tag": "THE-HANDOFF-DECLINES-A-CLEAN-EXIT-AT-THIS-HEAD",
    "gate": {
      "kind": "blockedBy",
      "tags": ["THE-PICKABLE-SET-CARRIES-A-CHAIN-DECLARED-REFUSAL"]
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "harness/handoff.ts",
          "description": "the package's declaration of the engine's per-entry refusal"
        },
        {
          "path": "harness/chain.ts",
          "description": "the refusal reaches the chain the factory builds"
        },
        {
          "path": "tests/harnessHandoff.test.ts",
          "description": "the three fates by mode and anchor"
        },
        {
          "path": "tests/harnessChain.test.ts",
          "description": "the declaration arrives on the chain"
        }
      ],
      "retire": []
    },
    "summary": "the default handoff re-dispatches an entry whose latest attempt was a clean exit at the current HEAD — the same dispatch against an unchanged world, for the same outcome",
    "per": { "path": "spec/harness.md", "section": "The default `handoff`" },
    "acceptance": "the package declares the engine's per-entry refusal, and an entry whose latest prior attempt is a clean exit at the tick's own HEAD is not handed to build while one at an older HEAD still is.",
    "tests": [
      "an entry whose latest clean exit is at the current HEAD is not handed to build",
      "an entry whose latest clean exit is at an older HEAD is handed to build",
      "an entry whose latest attempt is a gate revert at the current HEAD is handed to build"
    ],
    "pins": [],
    "notes": "Read from the mode and the anchor the engine reports on the record — PriorAttempt's `mode` and `headSha` (src/Prompt.ts) — never a heuristic of the package's own. Only clean-exit qualifies; every other mode describes a world the next tick can change."
  },
  {
    "tag": "A-DOC-SECTION-CUTTER-HAS-ONE-HOME",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "tests/helpers/docSections.ts",
          "description": "the markdown section cutter, one home"
        }
      ],
      "edit": [
        {
          "path": "tests/examples.test.ts",
          "description": "three local cutters give way to the import"
        },
        {
          "path": "tests/harnessChain.test.ts",
          "description": "the inline copy gives way to the import"
        },
        {
          "path": "tests/harnessPackaging.test.ts",
          "description": "the module-level copy gives way to the import"
        }
      ],
      "retire": []
    },
    "summary": "the \"heading line through the line before the next heading\" cutter is spelled five times across three test files, and every new doc pin copies it again",
    "per": { "path": ".claude/rules/engineering.md", "section": "A module is one job" },
    "acceptance": "one markdown section cutter lives under tests/helpers/ and every doc-section pin in the suite imports it; no test file spells its own.",
    "tests": [],
    "pins": [],
    "notes": "Behavior-free: the typecheck and the suite hold it, and each call site keeps its own anchor assertion. Three copies in tests/examples.test.ts, one in tests/harnessPackaging.test.ts, one inline in tests/harnessChain.test.ts — the last added because it could not import one from a sibling test file."
  },
  {
    "tag": "THE-ADOPTION-PAGE-NAMES-THE-OPERATIONS-ONCE",
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "the lead-in points at the bullets instead of re-listing the set"
        },
        {
          "path": "tests/examples.test.ts",
          "description": "the standing pin reads every place the section names the set"
        }
      ],
      "retire": []
    },
    "summary": "the adoption section spells `run`, `runAtBase` and `lanes` three lines above the bullets the standing pin walks, so a fourth operation strands the copy no pin reads",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Derived state is computed, never restated beside its source"
    },
    "acceptance": "the adoption section names Runner's operation set in one place, and the standing pin's reader covers every place in that section that names it.",
    "tests": [],
    "pins": [
      "docs/CHAIN-AUTHORING.md's adoption section names Runner's operation set in one place"
    ],
    "notes": "Same defect the three counts had, one rung down: a list rather than a count, so the count deletion left it. The bullet regex the pin walks does not reach the lead-in sentence. Either the inline list goes or the reader widens — build's call, but one copy is the target shape."
  }
]
