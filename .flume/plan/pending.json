[
  {
    "tag": "SPAN-RENDERING-CASES-DECLARE-THE-LANE-BUDGET",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/helpers/spawnBudget.ts",
          "description": "the seed gains the engine entry a case's spans start a shell through"
        },
        {
          "path": "tests/subprocessHelper.test.ts",
          "description": "the fixture proving the widened seed fires, beside the lane's own assertion"
        },
        {
          "path": "tests/examples.test.ts",
          "description": "the span-rendering cases the widened scan reports declare the shared budget"
        }
      ],
      "retire": []
    },
    "summary": "a default-lane case whose subject is inline-exec spans starts a shell per span, inherits vitest's 5s default, and the budget scan is blind to it",
    "per": {
      "path": "spec/worktrees.md",
      "section": "The default test lane must stay fast"
    },
    "acceptance": "the spawn scan reports a default-lane case that renders inline-exec spans, and every site it reports declares the shared budget.",
    "tests": [
      "the spawn scan reports a case that renders inline-exec spans without a declared budget"
    ],
    "pins": [],
    "notes": "Observed: tests/examples.test.ts's unreadable-artifact case timed out at vitest's 5s under full-suite contention, ~650ms alone. `sh` is no node launcher and the scan never follows an import, so NODE_COMMANDS cannot reach it — seed the render entry the spans go through, declared where NODE_COMMANDS is. The afterMerge gate runs this lane once per cherry-picked entry, so the flake reverts an innocent entry.",
    "observedFiles": [
      ".flume/plan/notes/SPAN-RENDERING-CASES-DECLARE-THE-LANE-BUDGET.md",
      "tests/Prompt.test.ts",
      "tests/examples.test.ts",
      "tests/harnessBuildArgs.test.ts",
      "tests/harnessChain.test.ts",
      "tests/harnessPrompts.test.ts",
      "tests/helpers/spawnBudget.ts",
      "tests/priorAttempts.test.ts",
      "tests/subprocessHelper.test.ts"
    ]
  },
  {
    "tag": "INBOX-SLICE-RENDERS-THE-DECLARED-CI-LANE",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "harness/ci.ts",
          "description": "the lane reader: latest completed run of the declared workflow job for the tip's branch, through the forge CLI; its failing material, or the reason it is unread"
        },
        {
          "path": "tests/harnessCi.test.ts",
          "description": "the reader over a stub forge CLI on PATH — failing, green, absent CLI, no completed run"
        }
      ],
      "edit": [
        {
          "path": "harness/windows.ts",
          "description": "the inbox window's third arg and data key, rendered beside the records and bounded as the other windows are"
        },
        {
          "path": "harness/prompts/plan-inbox.md",
          "description": "the lane block, and the paragraph routing each failing title the way a record drains"
        },
        {
          "path": "tests/harnessWindows.test.ts",
          "description": "the inbox window's arg map over a declared lane"
        },
        {
          "path": "tests/harnessPrompts.test.ts",
          "description": "the new key is declared data, not authored text"
        }
      ],
      "retire": []
    },
    "summary": "a declared CI lane is read by nothing: the inbox slice renders records and build refusals only, so a red lane reaches no tick",
    "per": {
      "path": "spec/harness.md",
      "section": "CI lanes as a findings source"
    },
    "acceptance": "with a lane declared, the inbox slice's prompt carries that lane's latest completed run under its lane name — the failing job's log, or an explicit unread reason when the forge CLI is absent or no run has completed — and says which when the run is green.",
    "tests": [
      "the inbox window renders the declared lane's failing run under its lane name",
      "the inbox window renders a lane as unread when the forge CLI is absent",
      "the inbox window renders a lane as unread when no completed run for the tip's branch exists",
      "the inbox window renders a lane whose latest completed run passed as green"
    ],
    "pins": [],
    "notes": "No title parser: the run is rendered and the slice's agent takes the titles, as it takes a record's content — the generic parse the boundary rule fences. The liveness fork is ruled and the spec holds it; derive files the leg and this entry ships under it unchanged. Name the reader's lane type distinctly: `lane` already means a Runner test lane in harness/. This repo's declaration now names the lane (`ci:`, 8d5dc56), so the first drain of the real run is the agreement case."
  },
  {
    "tag": "INBOX-SLICE-IS-LIVE-ON-A-RED-UNDRAINED-LANE",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "INBOX-SLICE-RENDERS-THE-DECLARED-CI-LANE"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "harness/windows.ts",
          "description": "the inbox window's lane leg, beside the record and refusal legs"
        },
        {
          "path": "harness/ci.ts",
          "description": "the reader reports the run's identity and whether it failed, for the leg to compare against the stamp"
        },
        {
          "path": "harness/prompts/plan-inbox.md",
          "description": "the slice stamps the run it drained, as it stamps a cursor"
        },
        {
          "path": "tests/harnessWindows.test.ts",
          "description": "the lane leg over the stub forge CLI: red past the stamp, red already stamped, green, unreadable"
        }
      ],
      "retire": []
    },
    "summary": "a declared lane never wakes the inbox slice, so a quiet tree hibernates over a red lane — and with no closing condition a woken lane would re-drain the same run every tick",
    "per": {
      "path": "spec/harness.md",
      "section": "CI lanes as a findings source"
    },
    "acceptance": "the inbox slice is live for a declared lane exactly when that lane's latest completed run for the tip's branch failed and is past the lane's stamp; a green run, an already-stamped run, and a lane it cannot read each make it live for nothing.",
    "tests": [
      "the inbox slice is live when a declared lane's latest completed run failed past the lane's stamp",
      "the inbox slice is not live when the lane's failing run is the one already stamped",
      "the inbox slice is not live when the lane's latest completed run passed",
      "the inbox slice is not live when the forge CLI cannot read the lane"
    ],
    "pins": [],
    "notes": "`PlanSliceWindow.live` is sync and runs on the selection path (planState.ts), so the lane read has a sync half — once per tick per lane, beside the render's. The agent writes the stamp as it writes a cursor, so the rendered lane block must name the run it would stamp. `harness/ci.ts` is INBOX-SLICE-RENDERS-THE-DECLARED-CI-LANE's new file; this widens its report with the run's identity and verdict."
  },
  {
    "tag": "EXPORT-GRAPH-COMMENT-POINTS-AT-THE-RESOLUTION-FACT",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/helpers/exportGraph.ts",
          "description": "the directory-resolution paragraph on declarationProgram shrinks to a pointer at the page that now owns the fact"
        }
      ],
      "retire": []
    },
    "summary": "the TypeScript directory-resolution fact now lives in platform-facts.md, and the site comment that hand-held it still states it in full",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "acceptance": "the fact is stated once, on the rule page; the helper's comment names the page and stops restating it.",
    "tests": [],
    "pins": [],
    "notes": "The promoting commit (8d5dc56) landed the page and could not reach tests/ — rule pages are outside every phase fence, so the shrink is the follow-on half. Claims no property: the comment-citation scan judges src/ and harness/ only, so nothing pins the pointer. Keep the first paragraph (what the program is); only the resolution paragraph goes."
  }
]
