[
  {
    "tag": "ABORT-SIGNATURE-NAMES-ITS-STAGE",
    "gate": { "kind": "open" },
    "files": {
      "edit": [
        {
          "path": "src/loopSupervisor.ts",
          "description": "report the aborting streak's stage on SuperviseResult.repeatedFailure"
        },
        {
          "path": "src/cliVerdict.ts",
          "description": "loopCompletionSummary renders the reported stage"
        },
        {
          "path": "src/cliHelp.ts",
          "description": "exit-1 prose and the exit-code table generalize past provisioning"
        },
        {
          "path": "src/Phase.ts",
          "description": "Chain.supervisorPolicy knob docs generalize past provisioning"
        },
        {
          "path": "tests/loopSupervisor.test.ts",
          "description": "stage on the abort shape"
        },
        {
          "path": "tests/cliVerdict.test.ts",
          "description": "summary names the stage"
        },
        {
          "path": "tests/cliHelp.test.ts",
          "description": "help text agreement"
        }
      ]
    },
    "summary": "Report the aborting streak's stage on repeatedFailure; the summary and help text call every backstop abort a worktree-provisioning failure",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A fact the engine holds is reported, never rediscovered"
    },
    "tests": [
      "superviseLoop reports the aborting streak's stage on repeatedFailure",
      "loopCompletionSummary names the aborting stage rather than worktree provisioning",
      "flume loop --help names all three abort stages, not provisioning alone"
    ],
    "pins": [
      "repeatedFailure.signature carries the raw comparison key with no stage prefix"
    ],
    "acceptance": "A merge- or gate-stage abort names its own stage on repeatedFailure and in the completion summary; no reporting surface states the backstop as provisioning-only.",
    "notes": "Stage rides a sibling field: repeatedFailure.signature stays the raw key as its doc declares — never prefix it. superviseLoop holds f.stage at the abort site (failureStreaks is keyed `${stage}:${signature}`). The toEqual(repeatedFailure) assertions in tests/loopSupervisor.test.ts need the new field. Phase.ts:552/563/572 and cliHelp.ts:139-143,325 narrate the backstop as provisioning-only — same generalization, one commit."
  }
]
