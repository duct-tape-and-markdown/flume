[
  {
    "tag": "THE-SHARED-BASE-REFUSAL-NAMES-THE-STAMP",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "docs/MIGRATING-0.17.md",
          "description": "shared-base paragraph: attribute the refusal to the stamp check, not the registry judgment alone"
        }
      ],
      "retire": []
    },
    "summary": "MIGRATING-0.17's shared-base paragraph credits the registry judgment for a refusal only the stamp check makes",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "acceptance": "The page's shared-base guidance names the mechanism that actually refuses a colliding sibling worktree, and the hand-clearance the stamp arm costs.",
    "tests": [],
    "pins": [],
    "notes": "createWorktree now judges an occupied path on registry then this root's stamp (4ea5039d); the registry alone never refused a registered sibling's tree, which is the exact case this paragraph advises on. Docs-carve-out cite: a page stating what a shipped interface does is read against that interface. Spec twin is a human question (same wording, spec/worktrees.md, Placement)."
  },
  {
    "tag": "THE-BUILT-INS-INSTANCES-TABLE-CITES-ITS-EXCEPTION",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/examples.test.ts",
          "description": "instances record in the shellGate case: cite the section its hand-spelling diverges from"
        }
      ],
      "retire": []
    },
    "summary": "The shellGate case's hand-spelled instances table declares its reason but cites no section, so a sweep can still file against it",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Derived state is computed, never restated beside its source"
    },
    "acceptance": "The table's comment names the section it diverges from and why the divergence is the right depth, so the divergence is declared and cited at the site.",
    "tests": [],
    "pins": [],
    "notes": "Not derivable: each value is a constructed instance with per-gate args, and the classification is that case's subject. The toEqual against the walked set already reds on an added gate. Neighbour risk: docWalk's SuppliedSet doc says a caller passing a literal list has written the copy it exists to avoid — a reader lands on this table next."
  }
]
