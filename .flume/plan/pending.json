[
  {
    "tag": "gate-chainload-configdir",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Gate.ts",
          "description": "add configDir to GateContext, mirroring flumeDir, per §Told, not inferred"
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "thread opts.configDir into the three gate.run() context literals (~L1668, L2151, L2860)"
        },
        {
          "path": "src/builtinGates.ts",
          "description": "chainLoadGate: join(ctx.configDir, \"chain.ts\") instead of ctx.cwd + hardcoded CHAIN_REL_PATH; drop the stale 'universal convention' rationale"
        },
        {
          "path": "tests/Gate.test.ts",
          "description": "pin chainLoadGate firing when the touched chain.ts lives under a relocated configDir"
        }
      ],
      "retire": []
    },
    "summary": "chainLoadGate hardcodes .flume/chain.ts under ctx.cwd instead of reading configDir, so it silently no-ops when FLUME_CONFIG_DIR relocates chain.ts",
    "per": {
      "path": ".claude/rules/engine-boundary.md",
      "section": "Told, not inferred"
    },
    "tests": [
      "chainLoadGate loads and validates the post-commit chain when the touched chain.ts lives under a non-default configDir, instead of skipping on the cwd-relative path mismatch"
    ],
    "acceptance": "chainLoadGate detects and validates a touched chain.ts under a relocated configDir (FLUME_CONFIG_DIR != <flumeDir>/.flume default); existing default-configDir behavior unchanged",
    "notes": "builtinGates.ts:206 hardcodes '.flume/chain.ts' under ctx.cwd; the 'universal convention' comment is stale -- configDir is independently relocatable via FLUME_CONFIG_DIR (spec/cli.md), and Dispatcher.ts:1560,2539 already thread opts.configDir for promptPath. GateContext (Gate.ts:35-68) has flumeDir but no configDir. Mirror flumeDir's pattern: add configDir, thread it at Dispatcher.ts:1668,2151,2860's gate.run() calls. Sibling pendingGate already does this right via its overridable pendingPath."
  }
]
