[
  {
    "tag": "PHASE-AGENT",
    "summary": "Phase.agent?: Agent — per-phase delegation seam; per-tick resolution phase.agent ?? chainModule.agent ?? opts.agent. Per §4.",
    "per": {
      "path": "spec/RELEASE-v0.4.md",
      "section": "4. Per-phase agent assignment"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Phase.ts",
          "description": "Optional agent field."
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "Resolution (:394) gains the inner phase scope."
        },
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "Per-phase model pattern via claudeCode({extraArgs:[\"--model\",…]}) + chain-local helper amortizing the decorator stack."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/Dispatcher.test.ts",
        "asserts": "phase.agent used for that phase and default elsewhere; chainModule.agent still overrides opts.agent when the phase is silent"
      }
    ],
    "acceptance": "§7 §4 cases green; CHAIN-AUTHORING documents the per-phase model pattern",
    "notes": "Mechanism only — no Phase.model string sugar (§8, §9.3)."
  },
  {
    "tag": "FIX-EXTRAENV-JSDOC-SCOPE",
    "summary": "Correct WorktreeSetupResult.extraEnv JSDoc in src/Phase.ts: agent-invocation-only — drop the \"(and gates run inside the worktree)\" claim contradicting §2b's recorded semantics. Comment-only.",
    "per": {
      "path": "spec/RELEASE-v0.4.md",
      "section": "2b. v0.1.2 worktree hooks (OQ backfill)"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Phase.ts",
          "description": "WorktreeSetupResult.extraEnv JSDoc (:164-174) — scope claim aligned with dispatcher reality (extraEnv reaches agent.invoke only, src/Dispatcher.ts:995; gates spawn from the dispatcher env)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "Phase.ts JSDoc states agent-only scope, no gate mention remains; tsc green",
    "notes": "Audit finding from 9ac9d61: the docs commit verified agent-only scope against invokeAgent and documented it in CHAIN-AUTHORING, but the stale source JSDoc it flagged was outside its declared files. §2c \"no shape changes\" respected — comment-only."
  }
]
