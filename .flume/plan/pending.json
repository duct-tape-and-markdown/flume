[
  {
    "tag": "GLOBDIALECT-SCAN-EVERY-CHAIN-GLOB-OPTION",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/docComments.test.ts",
          "description": "widen the FOREIGN_GLOB_DIALECTS judge from `writablePaths` alone to every chain-facing glob option"
        }
      ],
      "retire": []
    },
    "summary": "Widen the foreign-glob-dialect scan from `writablePaths` alone to all four chain-facing glob options' shipped doc comments",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "The fix lands at the mechanism"
    },
    "tests": [],
    "pins": [
      "the shipped chain-facing glob options' doc comments name no glob dialect the engine's matcher does not implement"
    ],
    "acceptance": "The dialect scan judges `writablePaths`, `entryChannelPaths`, `supervisorPolicy.partitionIgnore` (src/Phase.ts) and `PartitionOptions.ignore` (src/partition.ts), each behind its own vacuity guard.",
    "notes": "`expectNoChainVocabulary` (tests/helpers/chainVocabulary.ts) already has the many-subjects shape; rename the `writablePaths`-only judge, never duplicate it. All four blocks are clean today — verified this tick, hence `pins`. `PartitionOptions` reaches the shipped .d.ts via `partitionByFileOverlap` (src/index.ts), so its hover text is engine surface."
  }
]
