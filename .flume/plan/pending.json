[
  {
    "tag": "DANGLING-CITATION-SCAN",
    "gate": {
      "kind": "open"
    },
    "files": {
      "new": [
        {
          "path": "tests/helpers/commentCitations.ts",
          "description": "resolve backticked identifiers in src/ and harness/ comments against the declarations those trees hold"
        },
        {
          "path": "tests/commentCitations.test.ts",
          "description": "the scan over src/ and harness/, plus the scanner's own flagging case"
        }
      ],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "repair the two citations naming no declaration"
        }
      ],
      "retire": []
    },
    "summary": "Promote the deleted-symbol verdict: a scan that reds on a backticked identifier in a src/ or harness/ comment naming no declaration those trees hold",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "acceptance": "the suite reds on a comment citing a name src/ and harness/ no longer declare, and is green on the tip with Dispatcher's two stale citations repaired",
    "tests": [
      "the citation scan flags a backticked identifier no src/ or harness/ declaration holds",
      "every backticked identifier in a src/ or harness/ comment names a declaration those trees hold"
    ],
    "pins": [],
    "notes": "Prior note's `0 misses` was a tokenizer artifact. This tip: 951 subjects (backticked, internal capital, no `_`), 15 misses. Two real, both src/Dispatcher.ts — `hasPickable` (names nothing repo-wide; the real surface is `TickResult.pickableAfter`) and `cherryPick` (`cherryPickRange`/`cherryPickAbort`). Rest is external vocabulary: resolve via the TS checker (precedent tests/helpers/exportGraph.ts) over lib globals, imported bindings, module basenames; the residue is a cited exclusion."
  }
]
