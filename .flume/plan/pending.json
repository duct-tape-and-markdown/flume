[
  {
    "tag": "CHAIN-VOCABULARY-DETECTED-TWICE",
    "gate": { "kind": "open" },
    "files": {
      "new": [
        {
          "path": "tests/helpers/chainVocabulary.ts",
          "description": "the one exported list of this-repo vocabulary both shipped-surface judges assert absence of"
        }
      ],
      "edit": [
        {
          "path": "tests/docComments.test.ts",
          "description": "drop the local CHAIN_VOCABULARY literal; read the shared list"
        },
        {
          "path": "tests/PendingSchema.test.ts",
          "description": "drop the inline chain-vocabulary regex in the rendered-schema pin; read the shared list"
        }
      ],
      "retire": []
    },
    "summary": "One home for the chain-vocabulary detection the doc-comment judge and the rendered-schema judge each re-derive",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "The fix lands at the mechanism"
    },
    "tests": [],
    "pins": [
      "the shipped `forkResolver` and `entryChannelPaths` doc comments name no term in the shared chain-vocabulary list",
      "the rendered pending schema names no term in the shared chain-vocabulary list"
    ],
    "acceptance": "Both judges read one exported list; a term added there is asserted against the rendered schema and the two shipped doc comments alike.",
    "notes": "Lists already diverged: `workshop` rides PendingSchema.test.ts:962 only; open-questions is spelled two ways. Share the class patterns only — the per-subject ones (files' language incidentals, dependsOnForks' RESOLVED, retire's symbol) stay local. Measured: the union is green on both subjects today, so this is consolidation, not a fix. Not liftable to every doc comment — `.flume/` is engine-default prose in Gate.ts/Phase.ts."
  }
]
