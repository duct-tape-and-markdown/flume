[
  {
    "tag": "MERGE-INTERRUPTED-MARKER",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/paths.ts",
          "description": "the merging-marker dir and path"
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "written before the pick, removed after the ship bookkeeping"
        },
        {
          "path": "src/cli.ts",
          "description": "the startup refusal, under the tip claim and ahead of the sweep"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "the marker's lifecycle"
        },
        {
          "path": "tests/cli.test.ts",
          "description": "the refusal"
        }
      ],
      "retire": []
    },
    "summary": "A crash between the cherry-pick and the ship bookkeeping leaves an ungated commit on trunk with its entry still open; mark the merge and refuse the next start",
    "per": {
      "path": "spec/loop.md",
      "section": "Crash equals stop"
    },
    "tests": [
      "the merge stage writes a merging marker naming the branch, the base sha and the entry before the pick",
      "the merging marker is gone once the ship bookkeeping has landed",
      "a surviving merging marker refuses the next loop start with EX_CONFIG"
    ],
    "acceptance": "`<flumeDir>/merging/<slug>.json` exists only between the pick and the bookkeeping; a survivor makes `loop` / `job run` exit 78 naming the file, the branch and the entry, touch nothing, and never reach the startup sweep.",
    "notes": "Ruling of 2026-09-11 (gh#19, field-traced 0.12.0). The fact is the marker the engine wrote, never commit shape or authorship. Removal is the operator's acknowledgement — no engine verb performs it, as with the stop flag. The check precedes `sweepStaleWorktrees` (src/cli.ts:857) so the branch survives for the operator."
  },
  {
    "tag": "RUNTIME-IGNORES-FRICTION-AT-DEFAULT-ROOT",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/job.ts",
          "description": "`frictionIgnoreEntry` stops being module-private"
        },
        {
          "path": "src/cli.ts",
          "description": "the best-effort chain resolve hoists above the merge and feeds it the friction entry"
        },
        {
          "path": "tests/cli.test.ts",
          "description": "the friction entry at the default root"
        }
      ],
      "retire": []
    },
    "summary": "The loop / job run ignore merge passes no `extra`, so a chain declaring `Chain.friction` still leaves its channel dir trackable at the default state root",
    "per": {
      "path": "spec/jobs.md",
      "section": "Runtime ignores"
    },
    "tests": [
      "a loop start merges a declared Chain.friction dir into the default state root's .gitignore"
    ],
    "acceptance": "`loop` / `job run` merge `RUNTIME_IGNORES` plus the declared friction entry through the same `frictionIgnoreEntry` spelling `job new` uses; a chain that fails to resolve still merges the base set.",
    "notes": "Drained from RUNTIME-IGNORES-AT-DEFAULT-STATE-ROOT's note. src/cli.ts:879 passes no `extra`; `job new` (src/job.ts:238) passes `frictionIgnoreEntry(chain.friction)`, module-private. Hoist the existing best-effort `resolveChain()` (src/cli.ts:902) above the merge, catch intact. Re-spelling the normalization in cli.ts would be re-derived detection (`engineering.md`, *The fix lands at the mechanism*)."
  },
  {
    "tag": "PRIOR-ATTEMPT-STALE-RECORDS-CLEARED",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Prompt.ts",
          "description": "every PriorAttempt variant carries its keyspace"
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "write and validate the keyspace; clear stale entry-keyed records at the wave's queue read; report them on the verdict"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "the clear, the phase-record exemption, the verdict field"
        },
        {
          "path": "tests/Prompt.test.ts",
          "description": "the keyspace on the record"
        }
      ],
      "retire": []
    },
    "summary": "A prior-attempt record whose entry left the queue unshipped stands forever; clear it at the wave's queue read and report the keys cleared",
    "per": {
      "path": "spec/loop.md",
      "section": "Prior-outcome feedback to the retrying tick"
    },
    "tests": [
      "a prior-attempt record names the keyspace its key belongs to",
      "a wave clears a prior-attempt record whose entry tag the queue no longer carries",
      "a wave leaves a phase-keyed prior-attempt record standing",
      "the tick verdict reports the prior-attempt keys the wave cleared"
    ],
    "acceptance": "Every record carries `key: \"entry\" | \"phase\"`; the wave that reads the queue clears each entry-keyed record whose tag is absent and reports the keys as `clearedPriorAttempts`. A record without the discriminator reads as absent, like an unanchored one.",
    "notes": "Ruling of 2026-09-11 against the recommended option C. Four such records stood at a83836f. `priorAttemptKey` (src/Dispatcher.ts:4114) has no on-disk discriminator, so 'key not in the queue' cannot tell a retired tag from a live phase record — the keyspace field is what makes the clear decidable. Closes the parked question."
  },
  {
    "tag": "QUARANTINE-KEYS-THE-ENTRY-AS-READ",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "the key, the per-entry failure records that carry it, the drop filter, the supervisor's set"
        },
        {
          "path": "src/Phase.ts",
          "description": "quarantinedTags reports the key beside the tag"
        },
        {
          "path": "src/cli.ts",
          "description": "the FLUME_QUARANTINED_SLUGS parse"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "the re-scope lift and the reported key"
        }
      ],
      "retire": []
    },
    "summary": "A run-scoped quarantine keyed on the slug alone survives a re-scope and forces stop-and-relaunch; key on the entry as read and report the key",
    "per": {
      "path": "spec/loop.md",
      "section": "Repeated identical failures — quarantine, then abort"
    },
    "tests": [
      "a quarantine key is the entry's slug and a hash of its bytes in the queue",
      "an entry re-scoped on trunk is pickable again without a relaunch",
      "a quarantined tag is reported with the key its hold stands under"
    ],
    "acceptance": "`FLUME_QUARANTINED_SLUGS` carries `slug@hash` values (the name stands), the drop filter matches the entry as read, and `TickResult.quarantinedTags` names the key beside each tag.",
    "notes": "Ruling of 2026-09-11 (field report, 0.12.0). The supervisor holds only the verdict, so the key rides the per-entry failure records it already tags (ProvisionFailure / MergeFailure / GateFailure) rather than a second read of pending.json (`engineering.md`, *A fact the engine holds is reported*). Closes the parked question."
  },
  {
    "tag": "CLEAN-EXIT-TAXONOMY",
    "gate": {
      "kind": "parked",
      "reason": ".flume/chain.ts:852 compares noCommit === \"voluntary-bail\"; off every phase lane, so the paired chore(flume): lands first or tsc reverts the tick"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Prompt.ts",
          "description": "the NoCommitMode member, the record variant, the rendered block"
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "classification, wave precedence, the record builder, the verdict's errored formula, the quarantine exclusion"
        },
        {
          "path": "src/Agent.ts",
          "description": "the two narration sites"
        },
        {
          "path": "src/Phase.ts",
          "description": "the narration site"
        },
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "the taxonomy prose"
        },
        {
          "path": "tests/Prompt.test.ts",
          "description": "the record and its rendering"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "classification and precedence"
        },
        {
          "path": "tests/cliVerdict.test.ts",
          "description": "the summary lines"
        },
        {
          "path": "tests/chain.test.ts",
          "description": "the mode literals"
        },
        {
          "path": "tests/examples.integration.test.ts",
          "description": "the expected mode"
        }
      ],
      "retire": []
    },
    "summary": "`voluntary-bail` names an intent the engine inferred; rename it `clean-exit` and record the final message's tail without saying what the exit meant",
    "per": {
      "path": "spec/loop.md",
      "section": "The no-commit taxonomy"
    },
    "tests": [
      "a clean exit with no commit is classified clean-exit",
      "the prior-attempt block quotes the prior attempt's final message without naming a refused constraint"
    ],
    "acceptance": "`NoCommitMode` carries `clean-exit` in place of `voluntary-bail` across src/, the record carries the bounded final-message tail under a neutral field, and the rendered block says what happened without saying why.",
    "notes": "Ruling of 2026-09-11. Pre-1.0 clean slate: rename in place, no alias. chain.ts already reads both modes through a ReadonlySet<string> (:573); the one `===` at :852-854 is what parks this. Precedence (:2312), the verdict's errored formula (:4942) and the builder (:5272) carry the old name. The decline bullets it edits carry a pin: exactly one names both singleton and fanout — do not reintroduce the pair."
  },
  {
    "tag": "HANDOFF-ENTRY-MERGE-OUTCOME",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Phase.ts",
          "description": "FanoutEntryOutcome gains the per-entry merge outcome"
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "the `entries` map reads the wave's own mergeOutcomes"
        }
      ],
      "retire": []
    },
    "summary": "A park and a cherry-pick conflict read identically on TickResult.entries, so a handoff routing a park wakes plan on a conflict too and pays a declined tick",
    "per": {
      "path": "spec/chain.md",
      "section": "What a hook receives"
    },
    "tests": [
      "TickResult.entries reports the merge outcome of an entry whose cherry-pick conflicted",
      "TickResult.entries reports the merge outcome of an entry the chain declined to ship"
    ],
    "acceptance": "`handoff` separates `cherry-pick-conflict` from `not-shipped` off `result.entries` alone, without reading the verdict log.",
    "notes": "`mergeOutcomes` is already in scope at the `entries` map (src/Dispatcher.ts:2550, :3071); the verdict carrying it is written after `handoff` runs. The cited section enumerates the record as `{tag, committed, shipped, reverted, declined?, noCommit?}` — that list wants the human's amendment once this lands."
  },
  {
    "tag": "CASCADE-VITEST-AFTERMERGE",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "examples/cascade-chain.ts",
          "description": "vitest moves to afterMerge; the gate-lifecycle comment at :152-:160"
        },
        {
          "path": "tests/examples.integration.test.ts",
          "description": "the placement"
        }
      ],
      "retire": []
    },
    "summary": "cascade places every gate at afterCommit, so the shipped example teaches the staleness window spec/chain.md's own placement guidance argues against",
    "per": {
      "path": "spec/chain.md",
      "section": "Gate placement is the chain's decision"
    },
    "tests": [
      "the cascade example places its vitest gate at afterMerge and its type gate at afterCommit"
    ],
    "acceptance": "cascade's build phase runs its expensive correctness gate at `afterMerge` and its cheap structural gates at `afterCommit`, and the lifecycle comment stops reading \"an afterMerge failure (none here)\" and stops calling the revert wave-wide.",
    "notes": "Drained from MIGRATING-012-AFTERMERGE-RESTORATION's note; verified on disk. `vitestGate` is afterCommit-fixed and `PkgManagerOverride` carries no `when` (src/builtinGates.ts:128) — compose `shellGate` as `.flume/chain.ts:701` does, never spread the builtin (its `name` is non-enumerable). Collides on file with EXAMPLES-SPEC-PHASE-CUT; the partitioner serializes them."
  },
  {
    "tag": "EXAMPLES-SPEC-PHASE-CUT",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "examples/cascade-chain.ts",
          "description": "drop the `spec` phase, its gate, its writable partition and `humanOnly`"
        },
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "the excerpts quoting the cut phase (:176-:177, :1409)"
        },
        {
          "path": "tests/examples.integration.test.ts",
          "description": "expectations that named the removed phase"
        }
      ],
      "retire": [
        "examples/prompts/spec.md"
      ]
    },
    "summary": "Cut examples/prompts/spec.md and cascade's spec phase together, so the shipped example models the plan+build shape flume's own chain runs",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "An export earns its consumer"
    },
    "tests": [
      "the cascade example declares plan and build and no spec phase"
    ],
    "acceptance": "The example loads and ticks under tests/examples.integration.test.ts with no `spec` phase, no `specs/active` / `specs/_aligned` / `workshop/_archive` partition, and no prompt file for it.",
    "notes": "Ruling of 2026-09-11: the partition is a shape cascade dropped in June and no current consumer runs. docs/CHAIN-AUTHORING.md quotes the example by agreement pin (tests/retired-narration.test.ts), so its excerpts move in the same commit or the pin reverts the tick."
  },
  {
    "tag": "CHECK-NO-FANOUT-SKIP-IN-PROSE",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/cliHelp.ts",
          "description": "`check`'s exit-code block, the 0 paragraph (:209)"
        },
        {
          "path": "docs/CLI.md",
          "description": "the `flume check` entry and its sample output (:176-:194)"
        },
        {
          "path": "tests/cliHelp.test.ts",
          "description": "both prose surfaces against the message cli.ts prints"
        }
      ],
      "retire": []
    },
    "summary": "`check`'s help text and docs/CLI.md describe the fence as unconditional, so the no-fanout skip the spec ratifies is a third way to 0 nobody documents",
    "per": {
      "path": "spec/cli.md",
      "section": "Subcommand surface"
    },
    "tests": [
      "flume check --help names the no-fanout skip among the ways check exits 0",
      "docs/CLI.md's flume check section names the no-fanout skip"
    ],
    "acceptance": "Both prose surfaces name the no-consumer route to 0, and each pin reads the skip line `src/cli.ts:549` actually prints rather than a hand-copied phrase.",
    "notes": "Drained from CHECK-NO-FANOUT-VACUOUS-PASS's note. The skip shipped at src/cli.ts:549; spec/cli.md already states it (`vacuous by design and spelled`). tests/cliHelp.test.ts:21 is the standing precedent for holding a help block against the code's real range."
  },
  {
    "tag": "WORKTREE-BASE-DOCS-PINNED",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "README.md",
          "description": "the base formula at :225-:227"
        },
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "the base formula at :770-:772"
        },
        {
          "path": "tests/retired-narration.test.ts",
          "description": "the agreement pin, beside the harness-state and job-seed pins"
        }
      ],
      "retire": []
    },
    "summary": "README and CHAIN-AUTHORING each spell the worktree-base formula by hand, a third and fourth copy of what worktreesBase now single-sources, held by nothing",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A seam gate reads what the real writer wrote"
    },
    "tests": [
      "the docs' worktree-base formula agrees with the base worktreesBase resolves",
      "the worktree-base scan flags a doc formula that drifts from worktreesBase"
    ],
    "acceptance": "Both doc claims are driven against `worktreesBase` (src/paths.ts) itself — default and override — and a doc edited away from the resolver turns the pin red.",
    "notes": "Drained from WORKTREE-BASE-RESOLVED-ONCE's note; both copies are correct today. Same seam as DOC-HARNESS-STATE-OWNERSHIP and CHAIN-AUTHORING-RUNTIME-NAME-AGREEMENT, whose pins in tests/retired-narration.test.ts already read src/ for their claims."
  },
  {
    "tag": "INTENT-WORKSHOP-LAYER-RETIRED",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "docs/INTENT.md",
          "description": "the prose-surfaces list (:31) and the Hardcoded-chain non-goal (:41)"
        }
      ],
      "retire": []
    },
    "summary": "docs/INTENT.md names a retired `workshop` layer twice, and the worse of the two states a default chain neither the rules nor .flume/chain.ts builds",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [],
    "acceptance": "Neither site names a `workshop` layer; the non-goal bullet names the living layers as one default while its claim — the chain is not hardcoded — survives intact.",
    "notes": "Drained from INTENT-DOC-RECONCILED's note; both sites verified on disk. `spec-plan-build.md` names spec / plan / inbox / notes / code, and `.flume/chain.ts` builds three plan slices plus build. No pin: a word-ban on \"workshop\" across a design-intent doc is tighter than the finding."
  },
  {
    "tag": "CHAIN-AUTHORING-NAMES-LIVING-REFERENCE",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "the opening orientation (:3-:15) names `.flume/chain.ts` as the living reference and the examples as its shipped floor"
        }
      ],
      "retire": []
    },
    "summary": "The doc calls cascade-chain.ts the reference and never names `.flume/chain.ts`, which is the only chain carrying the current shape",
    "per": {
      "path": ".claude/rules/engine-boundary.md",
      "section": "Surface, not prescription"
    },
    "tests": [],
    "acceptance": "A reader opening the doc learns that `.flume/chain.ts` in this repo is the living reference chain and that `examples/` is the shipped floor — the recommended shape ships by name, not as a default.",
    "notes": "Ruling of 2026-09-11 (option C, first half). The promotions into cascade-chain.ts follow as their own entries, blocked on this one so the doc's claim lands before the shapes it points at."
  },
  {
    "tag": "CASCADE-SHOULDRUN-FROM-DISK",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "CHAIN-AUTHORING-NAMES-LIVING-REFERENCE"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "examples/cascade-chain.ts",
          "description": "a `shouldRun` on one phase computed from TickContext"
        },
        {
          "path": "tests/examples.integration.test.ts",
          "description": "the decline case"
        }
      ],
      "retire": []
    },
    "summary": "No shipped example declares `shouldRun`, so a new adopter never sees a phase decline a tick from what is on disk",
    "per": {
      "path": "spec/chain.md",
      "section": "What a hook receives"
    },
    "tests": [
      "the cascade example's plan phase declines a tick when nothing on disk gives it work"
    ],
    "acceptance": "cascade-chain.ts declares a `shouldRun` reading `TickContext` — not `process.env`, not a directory scan — and the integration test drives both the decline and the run.",
    "notes": "Ruling of 2026-09-11 (option C): promote the shapes a new adopter most needs, in order. This is the first."
  },
  {
    "tag": "CASCADE-TESTS-JUDGED-BY-GATE",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "CASCADE-SHOULDRUN-FROM-DISK"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "examples/cascade-chain.ts",
          "description": "a gate reading the entry's `tests[]` out of a test reporter's output"
        },
        {
          "path": "tests/examples.integration.test.ts",
          "description": "the refusal case"
        }
      ],
      "retire": []
    },
    "summary": "cascade declares a `tests[]` extension field nothing judges, so the example teaches the field without the gate that makes it load-bearing",
    "per": {
      "path": ".claude/rules/engine-boundary.md",
      "section": "Surface, not prescription"
    },
    "tests": [
      "the cascade example's test gate refuses an entry whose named behavior has no passing test"
    ],
    "acceptance": "A `tests[]` line on a cascade entry is judged by a gate fed from the reporter's own output, and an entry naming a behavior no test carries fails that gate.",
    "notes": "The shape ships in `.flume/vitestJudge.ts`; the example carries its own, sized to the example. Red-on-base judging is not part of this entry — that fork is still open."
  },
  {
    "tag": "CASCADE-GATE-READS-ENTRY-AND-BASE",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "CASCADE-TESTS-JUDGED-BY-GATE"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "examples/cascade-chain.ts",
          "description": "a gate reading `GateContext.entry` and `baseSha`"
        },
        {
          "path": "tests/examples.integration.test.ts",
          "description": "the gate's context read"
        }
      ],
      "retire": []
    },
    "summary": "No shipped example reads `GateContext.entry` or `baseSha`, so the two facts a per-entry gate most needs look unavailable to an adopter",
    "per": {
      "path": "spec/chain.md",
      "section": "What a gate receives"
    },
    "tests": [
      "the cascade example's gate reads the entry tag and the span's base sha from its gate context"
    ],
    "acceptance": "A cascade gate scopes its verdict to the entry it is gating and to the span `baseSha..commitSha`, reading both off `GateContext` rather than re-deriving them.",
    "notes": "Ruling of 2026-09-11 (option C), third promotion."
  },
  {
    "tag": "CASCADE-PLAN-SLICES-ORDERED-HANDOFF",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "CASCADE-GATE-READS-ENTRY-AND-BASE"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "examples/cascade-chain.ts",
          "description": "plan split into slices, with a handoff that orders them"
        },
        {
          "path": "examples/prompts/plan.md",
          "description": "per-slice material if the split needs it"
        },
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "the block quoting cascade's `plan` phase, held by an agreement pin"
        },
        {
          "path": "tests/examples.integration.test.ts",
          "description": "the slice order"
        }
      ],
      "retire": []
    },
    "summary": "cascade's plan is one phase, so the example never shows slices sharing a dispatcher with a dependency-ordered handoff",
    "per": {
      "path": ".claude/rules/engine-boundary.md",
      "section": "Surface, not prescription"
    },
    "tests": [
      "the cascade example's plan slices hand off in dependency order"
    ],
    "acceptance": "cascade declares more than one plan slice, each computing its own liveness from disk, with a handoff that passes the baton down a declared order; the integration test ticks the ladder.",
    "notes": "Ruling of 2026-09-11 (option C), last promotion. docs/CHAIN-AUTHORING.md quotes cascade's `plan` phase verbatim under an agreement pin (tests/retired-narration.test.ts) — the quote moves in the same commit or the pin reverts the tick."
  },
  {
    "tag": "RELEASE-CITES-CUT-DISPATCHER",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "81 `RELEASE-v0.N §M` / `v0.N §M` cites"
        }
      ],
      "retire": []
    },
    "summary": "src/Dispatcher.ts carries 81 doc cites into spec/RELEASE-v*.md, files the corpus reform deleted — a cite no reader can follow",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [
      "src/Dispatcher.ts carries no release-numbered spec cite"
    ],
    "acceptance": "No `RELEASE-v0.N §M` or `v0.N §M` cite remains in src/Dispatcher.ts. Cites into `spec/*.md` and `.claude/rules/*.md` stand. A sentence that loses its only justification stays if it is still true, and goes if it is not.",
    "notes": "Ruling of 2026-09-11, option B: git carries provenance. Split by module because 183 sites across 19 files is more than one tick can land green; this is the largest."
  },
  {
    "tag": "RELEASE-CITES-CUT-SRC-TAIL",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/cli.ts",
          "description": "18 cites"
        },
        {
          "path": "src/job.ts",
          "description": "16"
        },
        {
          "path": "src/Prompt.ts",
          "description": "12"
        },
        {
          "path": "src/PendingSchema.ts",
          "description": "9"
        },
        {
          "path": "src/git.ts",
          "description": "7"
        },
        {
          "path": "src/Phase.ts",
          "description": "7"
        },
        {
          "path": "src/cliVerdict.ts",
          "description": "6"
        },
        {
          "path": "src/cliHelp.ts",
          "description": "6"
        },
        {
          "path": "src/cliJobResolution.ts",
          "description": "4"
        },
        {
          "path": "src/builtinGates.ts",
          "description": "3"
        },
        {
          "path": "src/Gate.ts",
          "description": "2"
        },
        {
          "path": "src/cliJobVerbs.ts",
          "description": "2"
        },
        {
          "path": "src/paths.ts",
          "description": "1"
        },
        {
          "path": "src/standardSchema.ts",
          "description": "1"
        },
        {
          "path": "src/flumeApi.ts",
          "description": "1"
        }
      ],
      "retire": []
    },
    "summary": "The other 95 dead release cites in src/, across fifteen modules — same grammar, same reason, split off Dispatcher.ts so each lands green in a tick",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [
      "no src/ module outside Dispatcher.ts carries a release-numbered spec cite"
    ],
    "acceptance": "No `RELEASE-v0.N §M` or `v0.N §M` cite remains under src/ outside Dispatcher.ts. Cites into `spec/*.md` and `.claude/rules/*.md` stand.",
    "notes": "Counts verified on disk 2026-09-11. Sibling of RELEASE-CITES-CUT-DISPATCHER; the two collide on nothing and the pin entry waits on both."
  },
  {
    "tag": "RELEASE-CITES-PINNED",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "RELEASE-CITES-CUT-DISPATCHER",
        "RELEASE-CITES-CUT-SRC-TAIL"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "examples/cascade-chain.ts",
          "description": "2 cites"
        },
        {
          "path": "examples/backlog-groomer-chain.ts",
          "description": "4"
        },
        {
          "path": "examples/minimal-chain.ts",
          "description": "1"
        },
        {
          "path": "tests/retired-narration.test.ts",
          "description": "the refusal, beside the retired-narration pins"
        }
      ],
      "retire": []
    },
    "summary": "Finish the release-cite cut in examples/ and promote it a rung: a source-shape pin refusing any new cite matching that grammar under src/ or examples/",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Narration is the ladder's bottom rung"
    },
    "tests": [
      "no file under src/ or examples/ carries a release-numbered spec cite outside a declared-historical marker"
    ],
    "acceptance": "The grammar is refused mechanically, so the cut cannot regress, and a site that genuinely wants historical provenance says so in a declared marker the pin recognizes.",
    "notes": "Ruling of 2026-09-11: the promotion rides the same pass. Blocked on both cut entries because the pin is red until they land. docs/ and README are out of scope."
  },
  {
    "tag": "DISPATCHER-EXTRACT-LOOP-SUPERVISOR",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "RELEASE-CITES-PINNED"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "src/loopSupervisor.ts",
          "description": "superviseLoop and its options/result types, moved whole"
        }
      ],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "the loop-supervisor section leaves"
        },
        {
          "path": "src/cli.ts",
          "description": "import path"
        }
      ],
      "retire": []
    },
    "summary": "The loop supervisor is module-scope code in a 5400-line Dispatcher.ts and shares none of the buildFlumeApi cycle constraint that keeps chain-load colocated",
    "per": {
      "path": ".claude/rules/posture-sweep.md",
      "section": "A violation counts only when verified on disk this tick"
    },
    "tests": [
      "superviseLoop is exported from src/loopSupervisor.ts"
    ],
    "acceptance": "A pure move: no behavior change, `src/index.ts` unchanged, the supervisor's tests move with it and differ only in import path.",
    "notes": "Ruling of 2026-09-11, option C: extract the acyclic concerns; chain-load plus tick execution stay with buildFlumeApi, whose cycle constraint (src/Dispatcher.ts:42-44) is the declared reason. This one is genuinely module-scope already (:4730-:5213) — take it first."
  },
  {
    "tag": "DISPATCHER-EXTRACT-PRIOR-ATTEMPTS",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "RELEASE-CITES-PINNED"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "src/priorAttempts.ts",
          "description": "the prior-attempt read/write/clear surface and the record builders"
        }
      ],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "the prior-attempt section leaves; the class calls the module"
        },
        {
          "path": "src/index.ts",
          "description": "re-export path for slugify / priorAttemptPath / priorAttemptsDir"
        }
      ],
      "retire": []
    },
    "summary": "Prior-attempt persistence — key, read, write, clear, revert snapshots, record builders — is a separate home inside Dispatcher.ts",
    "per": {
      "path": ".claude/rules/posture-sweep.md",
      "section": "A violation counts only when verified on disk this tick"
    },
    "tests": [
      "the prior-attempt record builders are exported from src/priorAttempts.ts"
    ],
    "acceptance": "A pure move: no behavior change, the package's public surface through `src/index.ts` unchanged, tests move with their subjects and differ only in import path.",
    "notes": "Sections at src/Dispatcher.ts:4106-:4460 plus the builders at :5267-:5359; `slugify`, `priorAttemptPath` and `priorAttemptsDir` are already public and must resolve identically from `flume`."
  },
  {
    "tag": "DISPATCHER-EXTRACT-FRICTION",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "RELEASE-CITES-PINNED"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "src/friction.ts",
          "description": "harvest, count line, and the friction declaration check"
        }
      ],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "the friction helpers leave"
        },
        {
          "path": "src/cli.ts",
          "description": "import path for frictionCountLine"
        }
      ],
      "retire": []
    },
    "summary": "Friction harvesting, the count line and the friction declaration validator are one job spread through Dispatcher.ts",
    "per": {
      "path": ".claude/rules/posture-sweep.md",
      "section": "A violation counts only when verified on disk this tick"
    },
    "tests": [
      "frictionCountLine is exported from src/friction.ts"
    ],
    "acceptance": "A pure move: no behavior change, `src/index.ts` unchanged, tests move with their subjects and differ only in import path.",
    "notes": "`frictionCountLine` (src/Dispatcher.ts:1032), `validateFrictionDeclaration` (:935), `harvestFriction` (:3738). src/job.ts:444 already exports a helper for `frictionCountLine` to share — the new module keeps that direction."
  },
  {
    "tag": "DISPATCHER-EXTRACT-WORKTREES",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "RELEASE-CITES-PINNED"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "src/worktrees.ts",
          "description": "create, teardown and the stale sweep"
        }
      ],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "the worktree helpers leave; sweepStaleWorktrees stays as the method cli.ts calls"
        }
      ],
      "retire": []
    },
    "summary": "Worktree provisioning, teardown and the stale sweep are a separate home inside Dispatcher.ts, beside src/setupWorktree.ts which already owns the hook half",
    "per": {
      "path": ".claude/rules/posture-sweep.md",
      "section": "A violation counts only when verified on disk this tick"
    },
    "tests": [
      "worktree provisioning and teardown are exported from src/worktrees.ts"
    ],
    "acceptance": "A pure move: no behavior change, `src/index.ts` unchanged, `dispatcher.sweepStaleWorktrees()` stays callable from cli.ts, tests move with their subjects and differ only in import path.",
    "notes": "`createWorktree` (src/Dispatcher.ts:4050), `teardownWorktreeInstance` (:3847), `sweepStaleWorktrees` (:3931), `worktreeDirName` (:793). Take last of the four: it is the group most entangled with the class's own state."
  }
]
