[
  {
    "tag": "AGENT-STREAMJSON-VOCAB-DEDUP",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Agent.ts",
          "description": "Export the assistant/result/is_error/subtype event-type checks renderStreamJsonLine already computes, as shared helpers."
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "finalAgentMessage consumes the shared helpers from Agent.ts instead of re-testing the same literals independently."
        },
        {
          "path": "tests/Agent.test.ts",
          "description": "Pin the shared helper's classification for representative assistant/result/error NDJSON lines."
        }
      ],
      "retire": []
    },
    "summary": "Hoist the duplicated stream-json event-type vocabulary out of Agent.ts and Dispatcher.ts into one shared, exported check.",
    "per": {
      "path": "spec/chain.md",
      "section": "The agent seam"
    },
    "tests": [
      "Agent.ts's renderer and Dispatcher's finalAgentMessage classify a given NDJSON line's event type through the same shared function, not two independent literal comparisons"
    ],
    "acceptance": "the 'assistant'/'result'/is_error/subtype literals appear once, in one exported location both readers import",
    "notes": "Weakest/lowest-priority of the three breaches 82c0a12 filed — mechanical dedup only, per The fix lands at the mechanism (engineering.md). Whether a provider should supply its own transcript extractor is a separate design fork, parked as an open question rather than decided here."
  }
]
