[
  {
    "tag": "gate-failing-files-suspect-flake",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Gate.ts",
          "description": "GateResult: add failingFiles?: string[]."
        },
        {
          "path": "src/Prompt.ts",
          "description": "GateRevertAttempt: add suspectFlake?: boolean."
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "at the gate-revert prior-attempt write site, derive suspectFlake when the gate result's failingFiles is present and every entry is disjoint from the reverted span's footprint."
        },
        {
          "path": "tests/Gate.test.ts",
          "description": "type coverage."
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "disjoint vs overlapping failingFiles, and the absent-field case."
        }
      ],
      "retire": []
    },
    "summary": "GateResult.failingFiles lets Dispatcher derive suspectFlake on a gate-revert record from list disjointness, never from gate prose.",
    "per": {
      "path": "spec/chain.md",
      "section": "What a gate returns"
    },
    "tests": [
      "a gate-revert record whose failingFiles is disjoint from the reverted span's footprint is marked suspectFlake:true",
      "a gate-revert record whose failingFiles overlaps the footprint earns no marker",
      "a gate result with no failingFiles earns no marker (today's behavior)"
    ],
    "acceptance": "suspectFlake is present only when both lists are present and provably disjoint; an absent field is never inferred as flaky.",
    "notes": "Also spec'd at spec/loop.md Prior-outcome feedback to the retrying tick. No builtin gate is required to populate failingFiles by this entry — none currently can report it."
  },
  {
    "tag": "git-cherry-pick-abort-bystander-checkpoint",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/git.ts",
          "description": "cherryPickAbort: guard the --abort call on sequencer state (CHERRY_PICK_HEAD or sequencer/ present) — never blind."
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "before a primary-checkout pick range begins, checkpoint any staged bystander state as a dangling commit (git stash create shape) and record its sha on the tick verdict."
        },
        {
          "path": "tests/git.test.ts",
          "description": "no-op abort when no sequence started; --abort issued when sequencer state is present."
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "checkpoint sha recorded and staged content recoverable from it after an abort/reset."
        }
      ],
      "retire": []
    },
    "summary": "Primary-checkout cherry-pick --abort only fires against a sequence that started; staged bystander state is checkpointed to a recoverable sha first.",
    "per": {
      "path": "spec/loop.md",
      "section": "Crash equals stop"
    },
    "tests": [
      "cherryPickAbort issues no git command when no sequencer state is present",
      "cherryPickAbort issues --abort when CHERRY_PICK_HEAD/sequencer state is present",
      "staged bystander content present when a pick range begins is recoverable via the sha recorded on the tick verdict"
    ],
    "acceptance": "A pick failing before a sequence starts never touches bystander index/working-tree state; staged bystander content always has a recorded recovery sha.",
    "notes": "Ratified 2026-08-18 (271fb13), sequenced to the 0.13 line now that 0.12.0 shipped. The checkpoint-sha field name on TickVerdict is build's call."
  },
  {
    "tag": "worktree-harvest-tracked-at-head",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "harvestFriction: bound moves from base-commit-relative (readFileAtRef against baseRef) to untracked-at-worktree-HEAD, so a note this tick's own agent committed is delivered once, not duplicated under a stamped name."
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "a committed friction note is harvested zero times; the existing new-since-base case stays covered."
        }
      ],
      "retire": []
    },
    "summary": "Teardown harvest moves files untracked at the worktree's HEAD, not merely new since base — a committed note is delivered once, via its own commit.",
    "per": {
      "path": "spec/worktrees.md",
      "section": "Teardown harvest — the delivery guarantee"
    },
    "tests": [
      "a friction-channel file the tick's agent committed is not harvested a second time under a stamped name",
      "a file untracked at worktree HEAD but absent from the base tree is still harvested",
      "a file the checkout itself brought in (tracked at both base and HEAD) is never harvested"
    ],
    "acceptance": "Harvest count for a wave where the agent committed its own friction note matches one delivery, not two.",
    "notes": "Fixes the field-reported duplication (one committed note became eight stamped copies across two waves)."
  },
  {
    "tag": "verdict-anchor-head-sha-at",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "TickVerdict: add headSha: string, at: string (ISO timestamp), populated by writeTickVerdict. Add readLatestVerdictsSync(flumeDir): most-recent verdict per phaseName, synchronous, reading the same verdict log readTickVerdicts does."
        },
        {
          "path": "src/index.ts",
          "description": "export readLatestVerdictsSync."
        },
        {
          "path": "src/flumeApi.ts",
          "description": "export readLatestVerdictsSync."
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "headSha/at correctness; readLatestVerdictsSync per-phase latest reduction."
        }
      ],
      "retire": []
    },
    "summary": "Every TickVerdict carries headSha/at; readLatestVerdictsSync gives shouldRun/handoff a synchronous per-phase anchor read.",
    "per": {
      "path": "spec/loop.md",
      "section": "The tick verdict — one facts artifact"
    },
    "tests": [
      "a written verdict's headSha matches the trunk tip and at is a valid ISO timestamp",
      "readLatestVerdictsSync returns the most recent verdict per phase name from the verdict log",
      "headSha/at are written on a quiet (no-commit) tick too, not only a committed one"
    ],
    "acceptance": "A chain compares a verdict's headSha to the live tip without inferring it from touched paths, and reads it from shouldRun/handoff without await.",
    "notes": "readTickVerdicts stays async; readLatestVerdictsSync is the new sync sibling for the two synchronous chain hooks."
  },
  {
    "tag": "verdict-invocations-usage",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Agent.ts",
          "description": "AgentResult gains per-invocation usage: model id, turn count, duration, and token counts (input/output/cacheCreation/cacheRead), parsed from the same stream-json result event withTerminalRenderer's formatResult already reads — one source, not a second parser."
        },
        {
          "path": "src/Dispatcher.ts",
          "description": "TickVerdict: add invocations[] — one row per agent run this tick (tag present for a fanout entry, absent for a singleton), populated from each AgentResult."
        },
        {
          "path": "tests/Agent.test.ts",
          "description": "usage extraction from a result event, including a field the agent didn't report."
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "singleton vs fanout invocations[] shape."
        }
      ],
      "retire": []
    },
    "summary": "TickVerdict.invocations[] carries one usage row per agent run (model, turns, duration, token counts incl. cache split); absent fields the agent didn't report.",
    "per": {
      "path": "spec/loop.md",
      "section": "The tick verdict — one facts artifact"
    },
    "tests": [
      "a singleton tick's verdict carries one invocations[] row with no tag",
      "a fanout tick's verdict carries one invocations[] row per provisioned entry, each tagged",
      "a field the agent's result didn't report is absent on the row, not zero"
    ],
    "acceptance": "Cost/telemetry for a tick is readable from the verdict alone, without re-parsing the agent's stdout stream in a decorator.",
    "notes": "Collides with verdict-anchor-head-sha-at on TickVerdict/Dispatcher.ts — expected, both edit the same type."
  },
  {
    "tag": "prior-attempt-anchor-export",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "slugify and priorAttemptPath become exported free functions: priorAttemptPath(flumeDir, tag) (internally slugifies). Update priorAttemptKey/readPriorAttempt/writePriorAttempt call sites to the new signature."
        },
        {
          "path": "src/Prompt.ts",
          "description": "all 5 PriorAttempt variants gain headSha: string and at: string."
        },
        {
          "path": "src/index.ts",
          "description": "export slugify, priorAttemptPath."
        },
        {
          "path": "src/flumeApi.ts",
          "description": "export slugify, priorAttemptPath."
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "priorAttemptPath(flumeDir, tag) matches the dispatcher's own read/write path; headSha/at on each variant."
        },
        {
          "path": "tests/Prompt.test.ts",
          "description": "render coverage carrying the two new fields through the five variants."
        }
      ],
      "retire": []
    },
    "summary": "priorAttemptPath/slugify become exported so a chain reads the engine's own rule; every prior-attempt record anchors on headSha/at.",
    "per": {
      "path": "spec/loop.md",
      "section": "Prior-outcome feedback to the retrying tick"
    },
    "tests": [
      "priorAttemptPath(flumeDir, tag) matches the path the dispatcher itself reads/writes for that tag",
      "every one of the 5 prior-attempt record variants carries headSha and at when written",
      "a chain's shouldRun can compare a stale record's headSha to the live tip instead of the file's mtime"
    ],
    "acceptance": "A chain-authored shouldRun locates and interprets a prior-attempt record using only exported engine rules.",
    "notes": "Export is also spec'd at spec/pending.md What the package exports. Collides with gate-failing-files-suspect-flake on Prompt.ts's GateRevertAttempt — expected."
  }
]
