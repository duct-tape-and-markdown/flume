[
  {
    "tag": "ODD-ROOT-ARTIFACT-TABLES-DROP-A-MISSED-DETECTOR-GREEN",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/harnessPrompts.test.ts",
          "description": "per-artifact coverage pin on the odd-root loop"
        },
        {
          "path": "tests/examples.test.ts",
          "description": "same on the odd-root loop at :453"
        }
      ],
      "retire": []
    },
    "summary": "the odd-root loops skip an artifact whose detector matches no span and pin only asserted>0, so a table entry can stop being covered green",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "A green verdict is proven non-vacuous"
    },
    "acceptance": "Breaking one artifact's detector in either table reds the default lane instead of silently narrowing the judged set",
    "tests": [],
    "pins": [
      "every artifact in the harness prompt odd-root table is read by at least one shipped prompt",
      "every artifact in the example prompt odd-root table is read by at least one shipped template"
    ],
    "notes": "Sites: tests/harnessPrompts.test.ts:294-309 (`if (!spanSubstitutes(...)) continue`, then `asserted > 0`); tests/examples.test.ts:452-469 (`if (reads.length === 0) continue`). The stronger pin already sits in the same file at examples.test.ts:656,691 and was not shared with its siblings (`engineering.md`, *The fix lands at the mechanism*). Equality with table length is wrong here — an artifact several prompts read counts several times; the property is per-artifact coverage."
  }
]
