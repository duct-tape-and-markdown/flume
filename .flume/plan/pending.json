[
  {
    "tag": "cli-hermeticenv-export-strip-keys",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/helpers/subprocess.ts",
          "description": "export the literal FLUME_* key list hermeticEnv() deletes from (L42-46) so it becomes the one source of truth, instead of 5 unexported individual delete statements."
        },
        {
          "path": "tests/cli.test.ts",
          "description": "replace the local STRIPPED_VARS literal (L668-673) with an import of subprocess.ts's exported list, closing the seed-side restatement CLI-HERMETICENV-PIN-GENERICIZE-PREFIX-CHECK left open."
        }
      ],
      "retire": []
    },
    "summary": "cli.test.ts's STRIPPED_VARS still hand-restates hermeticEnv()'s strip set to seed test input, even though a03f0c9 genericized the assertion side",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "Derived state is computed, never restated beside its source"
    },
    "tests": [
      "a var added to hermeticEnv()'s exported strip-key list is automatically seeded and checked by the hermeticEnv test, with zero test-file edit"
    ],
    "acceptance": "subprocess.ts exports its strip-key list; cli.test.ts's STRIPPED_VARS is replaced by an import of it",
    "notes": "Audited this tick: a03f0c9 closed the assertion-side restatement (CLI-HERMETICENV-PIN-GENERICIZE-PREFIX-CHECK) but left the seed-side list hand-authored — same class, one layer over. hermeticEnv() has no exported list today (5 individual `delete env.X` statements); convert to a loop over an exported const array."
  }
]
