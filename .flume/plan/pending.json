[
  {
    "tag": "CLI-STATUS-LOOPPID-EXISTS-LOUD",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        { "path": "src/cli.ts", "description": "the status supervisor gate's existence probe (`:318`)" },
        { "path": "src/job.ts", "description": "`existsLoud` (`:54`) is module-private; share it" },
        { "path": "tests/cli.test.ts", "description": "the behavior test; the loop-lock source pin (`:2448`) spells `existsSync`" }
      ],
      "retire": []
    },
    "summary": "`flume status` reads an unreadable `loop.pid` as absent and says nothing over a live loop — narrow the gate to ENOENT",
    "per": { "path": ".claude/rules/engineering.md", "section": "Loud or nothing" },
    "tests": ["flume status exits non-zero when loop.pid exists but cannot be stat'd"],
    "pins": [],
    "acceptance": "`flume status` errors naming `loop.pid` when it exists but cannot be stat'd, instead of printing no supervisor line; every other status line unchanged.",
    "notes": "ELOOP on a self-referential `loop.pid` is the reachable non-ENOENT case here; EACCES needs the parent dir, which status reads through anyway. `existsLoud` (src/job.ts:54) is the shipped probe, module-private today — shared home is build's call. No new printed reading: spec/cli.md's two supervisor readings stand, this is the unspecified third case."
  }
]
