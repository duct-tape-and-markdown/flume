[
  {
    "tag": "WORKTREE-PRIORATTEMPT-EXISTS-LOUD",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        { "path": "src/worktrees.ts", "description": "createWorktree's occupancy probe (:178) → existsLoud" },
        { "path": "src/priorAttempts.ts", "description": "PriorAttempts.read's record probe (:208) → existsLoud" },
        { "path": "tests/worktrees.test.ts", "description": "occupied-but-unstattable refusal" },
        { "path": "tests/priorAttempts.test.ts", "description": "unstattable record refusal" }
      ],
      "retire": []
    },
    "summary": "createWorktree's occupancy probe and PriorAttempts.read swap existsSync for existsLoud — false-absent skips a clobber refusal / hides an attempt",
    "per": { "path": ".claude/rules/engineering.md", "section": "Loud or nothing" },
    "tests": [
      "createWorktree refuses when the worktree path is present but unstattable",
      "PriorAttempts.read throws on a record present but unstattable, never reporting no prior attempt"
    ],
    "pins": [],
    "acceptance": "an unstattable worktree path reaches the registry refusal instead of a blind add; an unstattable prior-attempt record refuses instead of clearing the quarantine count",
    "notes": "Stakes: false-absent at worktrees.ts:178 bypasses the git-registry clobber check the block below it exists for; at priorAttempts.ts:208 it is the false signal spec/loop.md 'Repeated identical failures' counts on. ELOOP self-symlink is the root-safe fixture (tests/git.test.ts, cherryPickAbort)."
  },
  {
    "tag": "CHAINLOAD-PROBE-EXISTS-LOUD",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        { "path": "src/Dispatcher.ts", "description": "loadChainModule's chain.ts probe (:1022) → existsLoud" },
        { "path": "tests/Dispatcher.test.ts", "description": "unstattable chain.ts names the stat failure" }
      ],
      "retire": []
    },
    "summary": "loadChainModule probes chain.ts with existsLoud, the check its own caller jobNew (src/job.ts:214) already runs one line earlier",
    "per": { "path": ".claude/rules/engineering.md", "section": "The fix lands at the mechanism" },
    "tests": [
      "loadChainModule surfaces the stat failure when chain.ts is present but unstattable, not 'chain config not found'"
    ],
    "pins": [],
    "acceptance": "the two chain.ts existence checks agree on an unstattable file, and loadChainModule's 'single fix point for this check' docstring holds",
    "notes": "Refuses either way, so no silent proceed — the defect is the misreport (absence named for a file that is there) and the sibling divergence the docstring already claims not to have."
  }
]
