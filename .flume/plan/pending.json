[
  {
    "tag": "EXTRACT-WORKTREE-GUARD",
    "summary": "job extract: refuse up front when job/<name> is checked out in another worktree — today step 5's branch -D fails post-pick, losing the harvest and stranding the clean branch.",
    "per": {
      "path": "spec/RELEASE-v0.5.md",
      "section": "5e. `flume job extract <name> --onto <base> [--intake <path>]...`"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/job.ts",
          "description": "worktree-occupancy guard beside the dirty-tree/live-loop refusals; exempt the current worktree"
        },
        {
          "path": "src/cli.ts",
          "description": "extract exit-1 refusal list in help"
        },
        {
          "path": "tests/job.test.ts",
          "description": "guard units"
        },
        {
          "path": "tests/job.integration.test.ts",
          "description": "recipe-worktree e2e per §7"
        },
        {
          "path": "docs/CLI.md",
          "description": "add refusal to extract step 1"
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/job.test.ts",
        "asserts": "refuses (exit 1, job + tree untouched) when job/<name> is checked out in a different worktree; extract run from inside that worktree still succeeds"
      }
    ],
    "acceptance": "§5e suite green incl. worktree-guard cases",
    "notes": "§6-recipe jobs always hit this: the recipe worktree keeps job/<name> checked out after the loop stops, and git branch -D refuses branches checked out anywhere. Same machinery-safety family as the dirty-tree/live-loop guards. Alternative (auto-remove the operator worktree) rejected — clobbers operator work. Probe via git worktree list --porcelain."
  },
  {
    "tag": "JOB-DOCS",
    "summary": "README: job flow (new→tune→run→rm/extract), two endings, §6 concurrency recipe, HEAD-is-truth contract. CHAIN-AUTHORING: template expectations under job new, phases[0] entry convention.",
    "per": {
      "path": "spec/RELEASE-v0.5.md",
      "section": "8. Docs"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "README.md",
          "description": "job flow, endings, concurrency recipe, trunk contract"
        },
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "template expectations, phases[0] convention"
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "docs describe the shipped verb surface; tsc + vitest unaffected",
    "notes": "Also closes §2's 'document HEAD-is-truth' bullet and §6's documentation obligation (spec statement, no machinery). Per-command CLI.md sections already ride the verb entries."
  }
]
