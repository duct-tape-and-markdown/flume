[
  {
    "tag": "friction-harvest-error-isolation",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "harvestFriction's base-delta probe (git.readFileAtRef, ~2923-2932) gets the same try/catch-and-log-per-item handling as its sibling readdir/mkdir/rename failure modes, instead of propagating uncaught."
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "A git.readFileAtRef failure while probing one candidate friction file's base-delta status is logged and does not abort teardown of the wave's remaining worktrees/branches."
        }
      ],
      "retire": []
    },
    "summary": "harvestFriction's new base-delta probe has no error handling, so one probe failure aborts the whole wave's teardown instead of isolating to that file.",
    "per": {
      "path": "spec/worktrees.md",
      "section": "Teardown harvest — the delivery guarantee"
    },
    "tests": [
      "a readFileAtRef failure during the base-delta probe is caught, logged per item, and does not abort the wave's teardown of remaining worktrees"
    ],
    "acceptance": "harvestFriction's base-delta probe failure is isolated per file, matching every other failure mode in the function; teardown of remaining worktrees/branches in the wave proceeds regardless.",
    "notes": "Audit finding against friction-harvest-base-delta (418234d): the new probe is the only failure mode in harvestFriction without a try/catch, contradicting 'harvest failure never aborts the wave.' Accepted separately as debt: a file modified-but-present at base is silently excluded — matches spec's literal 'untracked, or absent from the base tree' wording, not a divergence."
  },
  {
    "tag": "startup-sweep-namespace-scope",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "The startup sweep's worktree-base enumeration (~3061-3070), when this.opts.namespace is unset, must not treat every top-level entry under a shared FLUME_WORKTREES_DIR as this instance's own residue — scope removal to entries this job's own naming actually owns."
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "An unnamespaced instance's startup sweep, run against a shared FLUME_WORKTREES_DIR holding a live namespaced sibling job's worktree directory, leaves that sibling's directory and branches untouched."
        }
      ],
      "retire": []
    },
    "summary": "An unnamespaced instance's startup sweep can delete a sibling namespaced job's live worktree directory under a shared FLUME_WORKTREES_DIR.",
    "per": {
      "path": "spec/worktrees.md",
      "section": "Startup sweep — a dead wave's residue is removed at the next start"
    },
    "tests": [
      "an unnamespaced instance's startup sweep does not remove a sibling namespaced job's worktree directory or branches under a shared worktree base"
    ],
    "acceptance": "The startup sweep removes only directories/branches this job's own naming owns; a sibling namespaced job's live worktree tree survives an unnamespaced sweep of a shared base.",
    "notes": "Audit finding against startup-sweep-residue (7036f2f): when namespace is unset, sweepBase's readdir enumerates every top-level entry including a sibling namespace's subdirectory, and removeWorktree's win32 rm -rf fallback deletes it outright — contradicts 'removes only its own job's directories... by the same namespace.'"
  }
]
