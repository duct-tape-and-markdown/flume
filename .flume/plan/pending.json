[
  {
    "tag": "HANDOFF-MERGE-FATE-ONE-CLASSIFICATION",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        { "path": "harness/handoff.ts", "description": "export the merge-fate classification beside PLAN_RESOLVES_NO_COMMIT" },
        { "path": "harness/windows.ts", "description": "compose PLAN_RESOLVES_STANDING from both engine tables; prose shrinks to a pointer" },
        { "path": "tests/harnessWindows.test.ts", "description": "agreement pin over the two real readers; the tip-moved standing arm" }
      ],
      "retire": []
    },
    "summary": "Give the not-shipped/tip-moved 'is this plan's to resolve' verdict one home instead of two hand-kept tables",
    "per": {
      "path": ".claude/rules/engineering.md",
      "section": "The fix lands at the mechanism"
    },
    "acceptance": "PLAN_RESOLVES_STANDING states no merge fate of its own; both fates resolve through the handoff's table, and the two readers cannot disagree.",
    "pins": [
      "the inbox window and the build handoff agree on every prior-attempt mode",
      "a standing tip-moved prior-attempt record leaves the inbox window shut"
    ],
    "notes": "harness/handoff.ts:133 PLAN_RESOLVES_MERGE and harness/windows.ts:249 PLAN_RESOLVES_STANDING each classify not-shipped (true) and tip-moved (false) by hand. windows.ts:234 justifies the copy with 'no TickResult field carries them as a mode at all' — entries[].mergeOutcome carries both, and handoff.ts:160 reads them there. A divergence splits routing between the two evidences: the failure handoff.ts:107-113 exports its other table to prevent."
  }
]
