[
  {
    "tag": "DOCS-SETUPWORKTREE-PNPM",
    "summary": "Rewrite CHAIN-AUTHORING.md setupWorktree guidance: pnpm-install default, no node_modules symlink, enableGlobalVirtualStore as flagged-experimental opt-in",
    "per": { "path": "spec/RELEASE-v0.1.md", "section": "6. Documentation" },
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "Replace `### setupWorktree for fanout` symlink sample (L219-234) + correct the hook-table row (L49) per §6 acceptance. Keep ≤400 lines (file is currently 401)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "setupWorktree section teaches `pnpm install --frozen-lockfile` default + flags enableGlobalVirtualStore experimental opt-in; zero node_modules-symlink recommendation; file ≤400 lines",
    "notes": "Resolves runner-review inbox finding (pnpm/pnpm#9973). `.flume/chain.ts buildSetupWorktree` switch + afterCommit sanity gate = harness/chore lane (off build writablePaths), tracked out-of-band — not this entry. examples/cascade-chain.ts already symlink-free."
  },
  {
    "tag": "CI-PACK-FILESET-GUARD",
    "summary": "Add CI step asserting `npm pack` packed file set equals package.json \"files\" allowlist (catches over- and under-inclusion)",
    "per": { "path": "spec/RELEASE-v0.1.md", "section": "8. Repository hygiene" },
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": ".github/workflows/ci.yml",
          "description": "New publish-acceptance step: assert `npm pack --json` packed set == `\"files\"` allowlist, fail on drift either direction. Per §8 L155; mechanic in §4 L107."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "ci.yml fails when packed tarball file set ≠ package.json `\"files\"` allowlist (over- or under-inclusion); green when equal",
    "notes": "Closes §4 L107 parked open-question (human chose option A: CI guard). Distinct from existing consumer-smoke (negative-direction only — missing file). attw esm-only already shipped via chore 285c8b7."
  }
]
