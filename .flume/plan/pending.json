[
  {
    "tag": "EXIT-CODE-CONTRACT-COUNTS",
    "summary": "loop/job run exits 0 with errors named when ≥1 tick errored but ≥1 entry shipped, per spec/RELEASE-v0.7.md §4",
    "per": {
      "path": "spec/RELEASE-v0.7.md",
      "section": "4. Exit-code contract — the run never lies to CI"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "superviseLoop's only per-iteration signal is the child's exit code (stdio: inherit, ~L1795-1813) — add a small per-tick disk artifact (disk-is-truth) the supervisor reads between iterations to recover shipped/errored counts; exit non-zero iff ≥1 tick errored AND 0 entries shipped."
        },
        {
          "path": "src/cli.ts",
          "description": "loop/job run completion summary names surfaced errors even on a zero exit (partial success)."
        },
        {
          "path": "CHANGELOG.md",
          "description": "Add bullet under Added per §13."
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "a run with one errored tick and one shipped entry reports shipped>0 and errored>0, exits 0, error named in summary."
        },
        {
          "path": "tests/cli.test.ts",
          "description": "same scenario surfaces correctly at the CLI/loop boundary."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/Dispatcher.test.ts",
        "asserts": "run with one errored tick + one shipped entry: exit 0, shipped>0, errored>0, error named."
      },
      {
        "path": "tests/cli.test.ts",
        "asserts": "same scenario surfaces correctly at the CLI/loop boundary."
      }
    ],
    "acceptance": "A run with one errored tick and one shipped entry exits 0 and its summary names the error.",
    "notes": "Design ratified by the operator's 2026-07-29 §4 amendment: exit codes carry class only (settled/errored/mount-dead); a per-tick disk artifact carries counts. Child stdio stays inherit — piped-and-parsed stdout is explicitly declined (live-streamed agent output is load-bearing)."
  },
  {
    "tag": "CJS-CONTEXT-REFUSAL",
    "summary": "Detect CJS-context chain-load failure and refuse with usage-shaped message per spec/RELEASE-v0.7.md §5",
    "per": {
      "path": "spec/RELEASE-v0.7.md",
      "section": "5. CJS-context host: detect and refuse"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "loadChainModule (src/Dispatcher.ts:234-274): catch tsx module-context signature family (tsx 4.21 'Cannot use import statement outside a module'; tsx 4.23 ERR_MODULE_NOT_FOUND with tsImport '?namespace' percent-encoded) and rethrow a distinguishable usage-shaped error naming \"type\": \"module\" as the fix, per §5. Non-matching errors pass through unchanged (no false positives)."
        },
        {
          "path": "src/cli.ts",
          "description": "Surface the usage-shaped CJS-context refusal as exit 2, consistent with other usage errors and with the EXIT-CODE-CONTRACT taxonomy this entry rides on top of. Underlying loader error stays available as debug/verbose detail, not the headline."
        },
        {
          "path": "CHANGELOG.md",
          "description": "Add bullet under [Unreleased] Fixed."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/Dispatcher.test.ts",
        "asserts": "loadChainModule against the two known tsx CJS-context signatures (4.21 and 4.23 shape) throws the usage-shaped refusal naming \"type\": \"module\"; a genuinely missing dependency's error passes through unchanged."
      },
      {
        "path": "tests/cli.test.ts",
        "asserts": "chain load in a CJS-context fixture prints the usage-shaped refusal and exits 2."
      }
    ],
    "acceptance": "Chain load in a CJS-context fixture (the npm init -y smoke shape, pre-fix) prints the usage-shaped refusal naming \"type\": \"module\" and exits 2; a genuinely missing dependency still surfaces as itself.",
    "notes": "Blocked on EXIT-CODE-CONTRACT: exit 2 must sit consistently alongside that entry's mount-dead/errored-vs-shipped taxonomy rather than being designed twice. Detection signature is the empirical tsx 4.21/4.23 family from the inbox finding (c11d96b fixed the two known fixture instances by adding type:module; this entry makes the failure legible generically, not just those two fixtures)."
  },
  {
    "tag": "EXIT-CODE-CONTRACT-DOC-DRIFT",
    "summary": "Fix stale exit-code prose left behind by EXIT-CODE-CONTRACT: a Dispatcher.ts comment and docs/CLI.md still describe the pre-§4 contract",
    "per": {
      "path": "spec/RELEASE-v0.7.md",
      "section": "4. Exit-code contract — the run never lies to CI"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "Inline comment ~L492-499 (above the chainLoader try/catch) still says the supervisor 'logs and proceeds' on chain-resolution failure and exits non-zero — stale since 3a8d75e made this an EX_MOUNT_DEAD abort. Reword to match."
        },
        {
          "path": "docs/CLI.md",
          "description": "tick (~L30), loop (~L38), and job run (~L96) sections still document only exit 1 for chain/harness failure — add EX_MOUNT_DEAD (69) alongside, per §4."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "Neither the Dispatcher.ts comment nor docs/CLI.md contradicts the shipped EX_MOUNT_DEAD (69) abort-on-first-occurrence contract.",
    "notes": "Audit finding from this tick's cross-check of 3a8d75e (EXIT-CODE-CONTRACT build) against §4: the entry updated the exported JSDoc and the two --help strings in src/cli.ts but missed this comment and docs/CLI.md, neither of which was in its declared files. Doc/comment-only, no behavior change."
  },
  {
    "tag": "BAY-DISCOVERY-WALKUP",
    "summary": "repoRoot walks up from cwd to the nearest .flume, mirroring git's .git resolution, per spec/RELEASE-v0.7.md §9",
    "per": {
      "path": "spec/RELEASE-v0.7.md",
      "section": "9. Bay discovery walk-up"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/cli.ts",
          "description": "Replace the literal repoRoot = process.cwd() (~L515) with a walk-up resolver per §9: cwd-is-.flume special case, no-.flume-found fallback to today's behavior, FLUME_DIR/FLUME_CONFIG_DIR overrides unchanged."
        },
        {
          "path": "CHANGELOG.md",
          "description": "Add bullet under Added per §13."
        },
        {
          "path": "tests/cli.test.ts",
          "description": "Cover: subdirectory-below-bay invocation resolves the same repoRoot as bay root; invocation from inside .flume; no .flume anywhere above cwd keeps cwd-as-root."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/cli.test.ts",
        "asserts": "cd .flume && flume job status resolves the same bay as running from repo root (no false 'no jobs'); a tree with no .flume anywhere above keeps today's cwd-as-root default."
      }
    ],
    "acceptance": "cd .flume && pnpm flume job status (or any subcommand) resolves the same bay as running from the repo root; a tree with no .flume anywhere above it keeps today's cwd-as-root default.",
    "notes": ""
  },
  {
    "tag": "ENGINE-PIN-HANDSHAKE",
    "summary": "Global bin re-execs a bay's local install when present, refuses loudly when pinned-but-absent, per spec/RELEASE-v0.7.md §10",
    "per": {
      "path": "spec/RELEASE-v0.7.md",
      "section": "10. Engine↔pin handshake via launcher-defers-to-pin (the gradlew pattern)"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/cli.ts",
          "description": "Before subcommand dispatch: three arms per §10 — local install at <bay>/.flume/node_modules/@dtmd/flume resolves → re-exec its CLI with the same argv/stdio; pinned in package.json but install absent → refuse exit 2 naming pin + readPackageVersion() (~L53-60); unpinned → unchanged."
        },
        {
          "path": "bin/flume.js",
          "description": "Adjust only if the arms can't live entirely inside src/cli.ts's entry — build's call which site hosts the re-exec."
        },
        {
          "path": "bin/flume",
          "description": "POSIX sibling; same caveat as bin/flume.js."
        },
        {
          "path": "CHANGELOG.md",
          "description": "Add bullet under Added per §13."
        },
        {
          "path": "tests/cli.test.ts",
          "description": "Version-stamped smoke fixtures distinguishing local vs global install per §10 acceptance."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/cli.test.ts",
        "asserts": "bay with local install present → any invocation re-execs it regardless of global version; local install removed on a pinned bay → refuses exit 2 naming pin + both versions; unpinned bay → byte-identical to today."
      }
    ],
    "acceptance": "A bay with the local install present re-execs it regardless of the global engine's version; the same bay with the local install removed refuses (exit 2) naming the pin and both versions; an unpinned bay behaves byte-identical to today.",
    "notes": "Reuses the existing win32-junction link machinery job new already provisions as the resolution signal — not redesigned. One hop only. Hosting site (cli.ts main() vs bin/ shims) is build's call; all candidate files declared so either choice stays in-fence."
  },
  {
    "tag": "SETUP-WORKTREE-HELPER",
    "summary": "Ship a pnpm/npm lockfile-aware setupWorktree helper; migrate the dogfood chain onto it, per spec/RELEASE-v0.7.md §11",
    "per": {
      "path": "spec/RELEASE-v0.7.md",
      "section": "11. `setupWorktree` package-manager-aware helper"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "src/setupWorktree.ts",
          "description": "New exported helper, sibling to builtinGates.ts's shellGate/tscGate precedent: pnpm-lock.yaml → pnpm install --frozen-lockfile; package-lock.json → npm ci; neither → refuse cleanly, per §11."
        },
        {
          "path": "tests/setupWorktree.test.ts",
          "description": "New test file asserting lockfile-detection behavior per §11."
        }
      ],
      "edit": [
        {
          "path": "src/index.ts",
          "description": "Re-export the new helper alongside the existing builtinGates re-exports (~L30-37)."
        },
        {
          "path": ".flume/chain.ts",
          "description": "Replace buildSetupWorktree (~L95-103) with the new helper — dogfood discipline."
        },
        {
          "path": "docs/CHAIN-AUTHORING.md",
          "description": "Rewrite the setupWorktree worked example (~L418-458), currently teaching hardcoded pnpm install, to show the new helper as the recommended default."
        },
        {
          "path": "CHANGELOG.md",
          "description": "Add bullet under Added per §13."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/setupWorktree.test.ts",
        "asserts": "pointed at a pnpm-lock.yaml fixture dir runs pnpm install --frozen-lockfile; pointed at a package-lock.json fixture dir runs npm ci; pointed at neither refuses cleanly."
      }
    ],
    "acceptance": "The helper runs pnpm install --frozen-lockfile against a pnpm-lock.yaml fixture, npm ci against a package-lock.json fixture, and refuses cleanly against neither.",
    "notes": "Non-goals per spec: yarn/bun lockfile support; migrating connect's own chain.ts (that repo's own follow-up, outside this entry's blast radius)."
  }
]
