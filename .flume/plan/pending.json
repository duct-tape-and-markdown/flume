[
  {
    "tag": "TESTS-TSC-GATE",
    "summary": "Gate-enforce type-level tests per §7: tsconfig include gains tests/**/*; fix the one exactOptionalPropertyTypes error in tests/Gate.test.ts in the same commit.",
    "per": {
      "path": "spec/RELEASE-v0.5.md",
      "section": "7. Tests"
    },
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        { "path": "tsconfig.json", "description": "include += tests/**/*" },
        { "path": "tests/Gate.test.ts", "description": "fix TS2375 at :22 — ctx object's flumeDir is optional but GateContext requires it" }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      { "path": "tests/Dispatcher.test.ts", "asserts": "no new file — the deliverable is that existing type-level assertions (e.g. TrunkBranchPurged) now bind under the tscGate" }
    ],
    "acceptance": "pnpm tsc --noEmit green with tests/** in the program; a type-level test regression now fails the tscGate",
    "notes": "Probe re-run this tick (temp extending tsconfig): exactly one error, tests/Gate.test.ts(22) TS2375 — spec's single-error claim holds. No other files enter the program (vitest.config.ts stays outside include)."
  },
  {
    "tag": "CUT-0-5-0",
    "summary": "Cut 0.5.0 per §11: reconcile CHANGELOG against v0.3.1..HEAD (### Breaking + ### Added block shape spec-fixed), bump package.json to 0.5.0.",
    "per": {
      "path": "spec/RELEASE-v0.5.md",
      "section": "11. Versioning — the 0.5.0 cut"
    },
    "gate": { "kind": "blockedBy", "tag": "TESTS-TSC-GATE" },
    "files": {
      "new": [],
      "edit": [
        { "path": "CHANGELOG.md", "description": "[Unreleased] → [0.5.0] block; structure fixed by §11, wording derived from the build: commits since the 0.3.1 cut (9be3c0f)" },
        { "path": "package.json", "description": "version 0.3.1 → 0.5.0" }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "CHANGELOG carries [0.5.0] with ### Breaking (trunkBranch removed, §2) + ### Added per §11's enumeration; package.json at 0.5.0; gates green",
    "notes": "Commit message is chore(release): cut 0.5.0 — spec-fixed, not build: (precedent 9be3c0f). Blocked on TESTS-TSC-GATE so the cut lands last as the line's completion marker (§7 amendment is v0.5 surface). npm publish stays human. Version bump does not touch pnpm-lock.yaml."
  }
]
