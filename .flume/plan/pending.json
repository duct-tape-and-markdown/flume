[
  {
    "tag": "job-namespaced-join-jobnew-checks",
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/job.ts",
          "description": "Use namespacedJoin for jobNew's remaining existsSync checks per platform-facts.md, at chainPath (:168) and seedPath (:180) — same fix already applied to this file's other fs-call sites."
        },
        {
          "path": "tests/job.test.ts",
          "description": "Add a win32 regression per the existing JOB-EXISTSSYNC-WIN32-PATH-TOTAL-LIMIT suite, covering jobNew's chainPath/seedPath checks."
        }
      ],
      "retire": []
    },
    "summary": "jobNew's chain.ts and seedDir existsSync checks still use bare paths, missed by the prior sweep's namespacedJoin fix in this same file",
    "per": {
      "path": ".claude/rules/platform-facts.md",
      "section": "Windows MAX_PATH (~260 chars) breaks fs calls with no long component"
    },
    "tests": [
      "jobNew doesn't silently misread an existing chain.ts or seedDir as absent when configDir/seedDir path exceeds MAX_PATH on win32"
    ],
    "acceptance": "job.ts's jobNew existsSync checks at chainPath/seedPath use namespacedJoin; win32 long-path regression test passes",
    "notes": "Audit finding: this tick's cross-check of f041392/de7b9fb found two existsSync sites in jobNew (chainPath :168, seedPath :180) the prior sweep's site list omitted. Same silent-misread shape: existsSync reads a too-long path as absent, so jobNew would wrongly throw JobUsageError('no chain'/'seedDir does not exist') for a chain/seedDir that genuinely exists."
  }
]
