[
  {
    "tag": "PROMPT-PATH-RESOLVE",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Dispatcher.ts",
          "description": "both render sites resolve promptPath instead of joining it"
        },
        {
          "path": "src/Phase.ts",
          "description": "the promptPath doc: relative beneath configDir, absolute as given"
        },
        {
          "path": "src/Prompt.ts",
          "description": "RenderOptions.promptFile doc — resolved, not joined"
        },
        {
          "path": "tests/Dispatcher.test.ts",
          "description": "the absolute address at both render sites"
        }
      ],
      "retire": []
    },
    "summary": "Resolve phase.promptPath against configDir at both dispatcher render sites, so an absolute path from a package is an address rather than a path beneath the chain.",
    "per": {
      "path": "spec/prompt.md",
      "section": "The render pipeline"
    },
    "tests": [
      "a singleton tick reads an absolute promptPath as the prompt file's address",
      "a fanout tick reads an absolute promptPath as the prompt file's address"
    ],
    "pins": [
      "a relative promptPath resolves beneath the chain's config directory"
    ],
    "acceptance": "A phase whose promptPath is absolute renders from that file at both the singleton and the fanout render site.",
    "notes": "One call each at src/Dispatcher.ts:1934 and :3259; the two doc comments are engine surface reachable from the .d.ts and say join today. This is what gives a package-shipped prompt an address — HARNESS-PROMPTS and the factory both wait on it."
  },
  {
    "tag": "NO-COMMIT-MODES-VALUE",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/Prompt.ts",
          "description": "NO_COMMIT_MODES as the declared value; the type derives from it"
        },
        {
          "path": "src/index.ts",
          "description": "export the value beside the type"
        },
        {
          "path": "tests/Prompt.test.ts",
          "description": "the exported value"
        }
      ],
      "retire": []
    },
    "summary": "Declare the no-commit taxonomy once as an exported runtime value, NO_COMMIT_MODES, and derive the NoCommitMode type from it.",
    "per": {
      "path": "spec/loop.md",
      "section": "The no-commit taxonomy"
    },
    "tests": [
      "the engine exports NO_COMMIT_MODES carrying the four no-commit modes"
    ],
    "pins": [],
    "acceptance": "NO_COMMIT_MODES is exported from the package root, NoCommitMode is derived from it, and src/ holds no second list.",
    "notes": "src/Prompt.ts:57 is the union today, with no runtime value a prompt or a chain can name. What the package's prompts name the modes from, so an engine rename cannot strand one; .flume/prompts/plan-inbox.md:33 spells them in prose and is the chain-side copy the cutover deletes."
  },
  {
    "tag": "HARNESS-PLAN-STATE",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "harness/planState.ts",
          "description": "the declared state fields, their schema, and the read/write accessor"
        },
        {
          "path": "tests/harnessPlanState.test.ts",
          "description": "the two cursors, the continuation signal, the refusal, the absent case"
        }
      ],
      "edit": [],
      "retire": []
    },
    "observedFiles": [
      ".flume/plan/notes/HARNESS-PLAN-STATE.md",
      "harness/index.ts",
      "harness/planState.ts",
      "tests/harnessPackaging.test.ts",
      "tests/harnessPlanState.test.ts"
    ],
    "summary": "Add the package's plan-state accessor: the derive and sweep cursors and the continuation signal as declared fields of a typed artifact, never a line regexed out of prose.",
    "per": {
      "path": "spec/harness.md",
      "section": "Plan state as declared state"
    },
    "tests": [
      "the plan state accessor reads the derive and sweep cursors as fields",
      "the plan state accessor reads the continuation signal and its covered set",
      "a plan state artifact missing a required field is refused, naming the field",
      "an absent plan state artifact reads as no cursor rather than throwing"
    ],
    "pins": [],
    "acceptance": "harness/planState.ts reads and writes both cursors and the continuation signal as typed fields and refuses a malformed artifact by name.",
    "notes": "Typed artifact, not a parser over prose — the section forecloses the regex, and pending.json is the precedent for agent-written, schema-gated plan output. The narrative state.md carries today moves to the plan commit body (collaboration.md, *Artifacts are the present; git is the log*). Migrating the live .flume/plan/state.md is chain-side and rides the cutover. Twin at .flume/chain.ts:563."
  },
  {
    "tag": "HARNESS-RUNTIME-IGNORES",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "harness/ignores.ts",
          "description": "the consumer state-root ignore lines, derived from the engine's path record"
        },
        {
          "path": "tests/harnessIgnores.test.ts",
          "description": "the derivation, and the derived set driven against this repo's .gitignore"
        }
      ],
      "edit": [
        {
          "path": ".gitignore",
          "description": "drop the retired .flume/last-tick.json line the derived set does not name"
        }
      ],
      "retire": []
    },
    "observedFiles": [
      ".flume/plan/notes/HARNESS-RUNTIME-IGNORES.md",
      ".gitignore",
      "harness/ignores.ts",
      "harness/index.ts",
      "tests/harnessIgnores.test.ts"
    ],
    "summary": "Derive the consumer's state-root ignore lines from the engine's path record, and drive that output against this repo's .gitignore so a retired runtime name cannot linger.",
    "per": {
      "path": "spec/harness.md",
      "section": "The runtime ignore set"
    },
    "tests": [
      "the consumer ignore set prefixes every engine runtime path with the declared state root",
      "the consumer ignore set names no path outside the engine's path record",
      "this repository's .gitignore carries no state-root line the derived set does not name"
    ],
    "pins": [],
    "acceptance": "harness/ignores.ts derives .flume's ignore lines from the engine's path record and a test drives that output against this repo's .gitignore.",
    "notes": "Imports src/paths.ts directly, as harness/vitestRunner.ts does, so src/index.ts widens for no one. Retires .gitignore's .flume/last-tick.json. Not RUNTIME_IGNORES (src/job.ts) — that is the job dir's set and stays; this is the consumer root's, derived rather than copied, so it is not a fourth copy of the list parked in open-questions.md."
  },
  {
    "tag": "HARNESS-CITE-RESOLVER",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "harness/citeResolver.ts",
          "description": "per resolution against specLocus, resolver declarable"
        },
        {
          "path": "tests/harnessCiteResolver.test.ts",
          "description": "the two refusals and the declared resolver"
        }
      ],
      "edit": [
        {
          "path": "harness/index.ts",
          "description": "export the resolver and its verdict type"
        }
      ],
      "retire": []
    },
    "summary": "Add the package's per resolver: the path matches a specLocus glob and is in the gated commit, the section is a heading in it, and a consumer may declare a resolver that keys sections instead.",
    "per": {
      "path": "spec/harness.md",
      "section": "The cite resolver"
    },
    "tests": [
      "a per cite whose path matches no specLocus glob is refused, naming the path",
      "a per cite whose section is no heading at the gated commit is refused, naming the section",
      "a declared resolver keys a section without changing the verdict shape"
    ],
    "pins": [],
    "acceptance": "harness/citeResolver.ts resolves a per against a declared specLocus at a commit and yields one verdict shape whether the section is found by heading or by a declared resolver.",
    "notes": "The at-ref reader is injected, not imported, so every case stays in the fast lane. .flume/chain.ts's perResolvesGate and sectionOf are the chain-side copy this retires at the cutover."
  },
  {
    "tag": "HARNESS-JUDGES",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "harness/judge.ts",
          "description": "the tests[]/pins[] judge over the runner interface"
        },
        {
          "path": "tests/harnessJudge.test.ts",
          "description": "the refusal, the green-on-base fact, the pins lane, the empty case"
        }
      ],
      "edit": [],
      "retire": []
    },
    "summary": "Extract the tests[]/pins[] judge onto the runner interface — named lines green on the merged tree and red at the base, pins green only — reporting green-on-base as a fact.",
    "per": {
      "path": "spec/harness.md",
      "section": "The judges"
    },
    "tests": [
      "a tests[] line no passing test carries is refused, naming the line",
      "a tests[] line already green at the base is reported as a named fact",
      "a pins[] line is judged on the merged tree and never run at the base",
      "a judge over no named lines reports the empty case rather than a green verdict"
    ],
    "pins": [],
    "acceptance": "harness/judge.ts judges an entry's tests[] and pins[] through a declared runner and hands back the per-line verdict, green-on-base included.",
    "notes": "Drives harness/runner.ts and never names vitest; the runner cuts over with the judge that drives it (the cutover ruling). Twins: .flume/vitestJudge.ts and .flume/chain.ts:812. The reported fact is what retires .flume/chain.ts:632's regex over the gate's own message (engineering.md, *A fact the engine holds is reported, never rediscovered*).",
    "observedFiles": [
      ".flume/plan/notes/HARNESS-JUDGES.md",
      "harness/entryExtension.ts",
      "harness/index.ts",
      "harness/judge.ts",
      "tests/harnessJudge.test.ts"
    ]
  },
  {
    "tag": "HARNESS-PROMPTS",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "NO-COMMIT-MODES-VALUE"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "harness/prompts.ts",
          "description": "each phase's prompt address, resolved from the package's own location, and the shared args"
        },
        {
          "path": "harness/prompts/build.md",
          "description": "the build prompt, package-owned"
        },
        {
          "path": "harness/prompts/plan-inbox.md",
          "description": "the inbox slice's prompt"
        },
        {
          "path": "harness/prompts/plan-derive.md",
          "description": "the derive slice's prompt"
        },
        {
          "path": "harness/prompts/plan-sweep.md",
          "description": "the sweep slice's prompt"
        },
        {
          "path": "harness/prompts/plan-discipline.md",
          "description": "the discipline the slices point at"
        },
        {
          "path": "tests/harnessPrompts.test.ts",
          "description": "the addresses, the modes arg, the orphan check"
        }
      ],
      "edit": [],
      "retire": []
    },
    "summary": "Ship the package's five prompts under harness/prompts/, addressed absolutely from the package's own location and naming the no-commit modes from the engine's exported value.",
    "per": {
      "path": "spec/harness.md",
      "section": "The prompts and their discipline"
    },
    "tests": [
      "the package addresses each phase's prompt absolutely from its own location",
      "the package's prompt args name the no-commit modes from the engine's exported value",
      "every prompt address the package names is a file it ships"
    ],
    "pins": [],
    "acceptance": "harness/prompts.ts addresses the five prompts the package ships, and no prompt restates the engine's no-commit vocabulary.",
    "notes": "Content starts from .flume/prompts/ minus what only this repo can say — the per hint's spec filenames, and vitest (spec/harness.md, *The judges*). The .flume/ copies die chain-side at the cutover. Absolute addressing is *Where it lives*; the packing route is HARNESS-PROMPT-PACKING. Not harness/index.ts: the factory exposes it."
  },
  {
    "tag": "HARNESS-GATES",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "HARNESS-CITE-RESOLVER"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "harness/gates.ts",
          "description": "the per, records, clean-tree and pending gates as one set per phase"
        },
        {
          "path": "tests/harnessGates.test.ts",
          "description": "one refusal per gate, plus the ordering"
        }
      ],
      "edit": [],
      "retire": []
    },
    "summary": "Extract the four gates the discipline needs — per, records, clean-tree, and the pending gate wired to the consumer's fence — as the package's always-first gate set per phase.",
    "per": {
      "path": "spec/harness.md",
      "section": "The gates the discipline needs"
    },
    "tests": [
      "the per gate refuses a commit whose cited file is not in it, naming the tag",
      "the records gate refuses a record written outside the tick's own tag",
      "the records gate refuses a record past the byte cap",
      "the clean-tree gate refuses a leftover path the phase may not write",
      "the package's gates precede a consumer's declared gates for the same phase"
    ],
    "pins": [],
    "acceptance": "harness/gates.ts returns the four gates for a phase ahead of a consumer's declared ones, each refusing on its own case.",
    "notes": "The per gate wraps HARNESS-CITE-RESOLVER's verdict rather than re-resolving it; the pending gate takes the declaration's fence as targetFence and the package's entry extension. Twins at .flume/chain.ts:264, :340, :386."
  },
  {
    "tag": "HARNESS-HANDOFF",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "HARNESS-PLAN-STATE"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "harness/handoff.ts",
          "description": "the default next-phase ladder and the refusal leg"
        },
        {
          "path": "tests/harnessHandoff.test.ts",
          "description": "each rung, the refusal route, the no-self-rewake case, the override"
        }
      ],
      "edit": [],
      "retire": []
    },
    "summary": "Extract the package's default handoff — first live slice, else build while the engine reports anything pickable, else hibernate — with a build refusal routing to the inbox slice.",
    "per": {
      "path": "spec/harness.md",
      "section": "The default `handoff`"
    },
    "tests": [
      "the default handoff names the first live slice ahead of build",
      "the default handoff names build when no slice is live and the engine reports a pickable entry",
      "the default handoff hibernates when no slice is live and nothing is pickable",
      "a build tick the chain declined routes to the inbox slice whatever is pickable",
      "a slice that committed nothing is not named by its own handoff",
      "a declared handoff replaces the package's default"
    ],
    "pins": [],
    "acceptance": "harness/handoff.ts maps the engine's reported pickable set and no-commit facts onto the next phase, and a consumer's declaration replaces it.",
    "notes": "Liveness comes from harness/records.ts and harness/planState.ts; the slice set is a parameter until the factory exists. Reads the engine's reported pickableAfter and no-commit facts only — never a re-parse of an agent stream. Twins at .flume/chain.ts:686 and :920."
  },
  {
    "tag": "HARNESS-PROMPT-PACKING",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "HARNESS-PROMPTS"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "scripts/pack-harness-prompts.mjs",
          "description": "the copy step, callable by the build and by the packaging test"
        }
      ],
      "edit": [
        {
          "path": "package.json",
          "description": "the build script runs the copy step"
        },
        {
          "path": "tests/harnessPackaging.test.ts",
          "description": "the emitted prompts, off the real build"
        }
      ],
      "retire": []
    },
    "summary": "Copy the package's prompts beside the emitted dist/harness/ at build, and assert the emitted set from the real build output.",
    "per": {
      "path": "spec/harness.md",
      "section": "Where it lives"
    },
    "tests": [
      "the build emits every prompt the package addresses beside dist/harness"
    ],
    "pins": [
      "the package's files allowlist covers the emitted harness prompts"
    ],
    "acceptance": "The packaging suite drives the real build and finds every addressed prompt under the emitted dist/harness.",
    "notes": "tsc emits no .md, and tests/harnessPackaging.test.ts drives tsc directly rather than `pnpm build`, so the copy step needs one home both can call — that file is the agreement gate's real writer. files: [\"dist\"] already covers the destination, which is why the allowlist line is a pin."
  },
  {
    "tag": "HARNESS-INIT-BIN",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "HARNESS-RUNTIME-IGNORES"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "bin/flume-harness.js",
          "description": "the package's own bin shim, argv through and exit code out"
        },
        {
          "path": "harness/init.ts",
          "description": "the init verb and what it writes"
        },
        {
          "path": "harness/templates/PROTOCOL.md",
          "description": "the PROTOCOL skeleton init writes"
        },
        {
          "path": "tests/harnessInit.test.ts",
          "description": "the four written artifacts and the no-overwrite refusal"
        }
      ],
      "edit": [
        {
          "path": "package.json",
          "description": "the second bin entry, and the files allowlist covering the template"
        }
      ],
      "retire": []
    },
    "summary": "Ship flume-harness, the bin the package carries beside the engine's flume, and its init verb: the declaration skeleton, the state root, the ignore set and PROTOCOL.md.",
    "per": {
      "path": "spec/harness.md",
      "section": "Adoption and upgrade"
    },
    "tests": [
      "flume-harness init writes a declaration.ts skeleton the package's schema parses",
      "flume-harness init writes the state root and its derived ignore lines",
      "flume-harness init writes PROTOCOL.md from the package's own template",
      "flume-harness init refuses a state root that already exists rather than overwriting it"
    ],
    "pins": [
      "the engine's flume bin exposes no harness verb"
    ],
    "acceptance": "node bin/flume-harness.js init in an empty repo writes a declaration the package's schema parses, the state root, the ignore set and PROTOCOL.md.",
    "notes": "The shim is bin/flume.js's shape — argv through, stdio inherited, exit code out, no options of its own (spec/cli.md, *Distribution*). The files allowlist must cover the template: tsc emits no .md into dist/, and CI asserts the packed set both ways. The skeleton is a TypeScript module (*What a consumer declares*), so it cannot sit under harness/ as a .ts file — tsc would compile it. The engine's verb set stays closed; that is why the verb lives here."
  },
  {
    "tag": "HARNESS-PHASES",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "PROMPT-PATH-RESOLVE",
        "HARNESS-PROMPTS",
        "HARNESS-JUDGES",
        "HARNESS-GATES",
        "HARNESS-HANDOFF"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "harness/chain.ts",
          "description": "the factory: a complete Chain from one declaration"
        },
        {
          "path": "tests/harnessChain.test.ts",
          "description": "the phase set, the disabled slice, build's shape, the prompts, the policy"
        }
      ],
      "edit": [
        {
          "path": "harness/index.ts",
          "description": "export the factory and its options type"
        }
      ],
      "retire": []
    },
    "summary": "Add the package's chain factory: a complete Chain from one declaration — three plan slices and the fanout build phase, each with its prompt, gates and handoff.",
    "per": {
      "path": "spec/harness.md",
      "section": "The phases"
    },
    "tests": [
      "the factory returns the three plan slices and the build phase from a declaration",
      "a plan slice the declaration does not enable is absent from the returned chain",
      "the returned build phase is fanout and carries the declaration's fence",
      "each returned phase names its prompt by an absolute path the package ships",
      "the factory passes the declared supervisor policy to the chain whole"
    ],
    "pins": [],
    "acceptance": "harness/chain.ts turns a parsed declaration into a Chain whose phases, prompts, gates and handoff are the package's.",
    "notes": "The assembly point: it wires the gates, the judge, the handoff, the entry extension and the prompt addresses, and carries setup, agents and slots through. .flume/chain.ts becomes this factory over .flume/declaration.ts at the cutover — an interactive chore(flume):, never build's (the cutover ruling, 6286ba8)."
  }
]
