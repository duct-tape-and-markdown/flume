{
  "tag": "THE-LANE-BLOCK-STATES-THE-FORGE-CALL-IT-MADE",
  "gate": { "kind": "open" },
  "files": {
    "edit": [
      {
        "path": "harness/ci.ts",
        "description": "the reader reports the invocation it made and the raw conclusion the forge gave the declared job, beside the run identity it already reports"
      },
      {
        "path": "harness/ciLane.ts",
        "description": "`renderRun` states both beside each verdict, with the run id and created instant it already names"
      },
      {
        "path": "tests/harnessCi.test.ts",
        "description": "the invocation and conclusion arms, beside the green and failing render cases"
      }
    ]
  },
  "summary": "A lane block names a verdict with no evidence of the read behind it, so a divergence between the forge's answer and the lane's verdict is reconstructed a day later",
  "per": { "path": "spec/harness.md", "section": "CI lanes as a findings source" },
  "acceptance": "every lane verdict the reader reached a run for names the forge invocation that chose it and the raw conclusion the forge gave the declared job, beside the run id and created instant the block already states, so a divergence evidences itself in the block that stated it",
  "tests": [
    "a lane's block names the forge invocation the reader made for it",
    "a green lane's block names the raw conclusion the forge gave its declared job",
    "a failing lane's block names the raw conclusion the forge gave its declared job"
  ],
  "notes": "Fork (1) of the stale-lane record, ruled 1b150761. Verified this tick: `renderRun` (`harness/ciLane.ts:206`) states id, branch, title, instant and url — never the argv; `FORGE_CLI` reaches prose only in `forgeJson`'s throws (`harness/ci.ts:590`), and no arm keeps `job.conclusion` past its `PASSED`/`FAILED` test (`:557`). Scoped to the run-bearing arms: an unread that never reached a job has no conclusion, and its reason already quotes the call where one was made. Seams `harness/ci.ts`."
}
