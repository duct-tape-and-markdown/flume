{
  "tag": "THE-CONTRACT-TOUCHING-HINT-COUNTS-CHILD-TO-CHILD",
  "gate": {
    "kind": "open"
  },
  "priority": 30,
  "files": {
    "edit": [
      {
        "path": "harness/entryExtension.ts",
        "description": "the `contractTouching` hint and the doc above it count the contracts two tick children share beside the supervisor-to-child ones"
      },
      {
        "path": "tests/harnessEntryExtension.test.ts",
        "description": "the rendered hint's own case, beside the omitted-field one"
      }
    ]
  },
  "summary": "The contract-touching hint names the supervisor-to-child contract alone, so an entry moving the entry-claim path ships unmarked and a live wave keeps staking a spelling no drain reads",
  "per": {
    "path": "spec/loop.md",
    "section": "One tick is one fresh process"
  },
  "acceptance": "the rendered `contractTouching` hint and the field's own doc count the contracts two tick children share — the entry claims, the locks, the branch grammar — beside the supervisor-to-child ones, so the plan tick that must set the field reads the whole rule at the field, and the default handoff's stop-after-ship reaches a child-to-child move",
  "tests": [
    "the rendered contract-touching hint names the contracts two tick children share"
  ],
  "notes": "Ruled f3276d72. The hint (`harness/entryExtension.ts:178`) and its doc (`:75`) name a resident supervisor and a fresh child only. Measured: THE-TICK-BRANCH-AND-ENTRY-CLAIM-CARRY-THE-CHECKOUT moved `claims/<slug>` to `claims/<checkout>/<slug>` unmarked, and the 13:23 drain read no claims off a wave still staking the old path. No gate: the spec keeps the marking plan's judgment, un-engineered until a second livelock, and the handoff half already ships (`tests/harnessHandoff.test.ts:617`)."
}
