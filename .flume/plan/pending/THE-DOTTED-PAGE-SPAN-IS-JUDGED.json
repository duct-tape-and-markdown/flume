{
  "tag": "THE-DOTTED-PAGE-SPAN-IS-JUDGED",
  "gate": {
    "kind": "open"
  },
  "files": {
    "edit": [
      {
        "path": "tests/helpers/commentCitations.ts",
        "description": "the declaration-subject rule admits a dotted span instead of refusing every dot"
      },
      {
        "path": "tests/helpers/pageAnchors.ts",
        "description": "the page scan resolves a dotted span's two readings — a member of the surface, a file of the working tree"
      },
      {
        "path": "tests/pageAnchors.test.ts",
        "description": "the arm's cases, and the interface-page judgement's non-vacuity over the dotted set"
      },
      {
        "path": "tests/helpers/externalVocabulary.ts",
        "description": "the provider and platform names a dotted span reaches"
      },
      {
        "path": "docs/CHAIN-AUTHORING.md",
        "description": "a dotted span the arm reds"
      },
      {
        "path": "docs/CLI.md",
        "description": "a dotted span the arm reds"
      },
      {
        "path": "README.md",
        "description": "a dotted span the arm reds"
      }
    ]
  },
  "summary": "A dotted backticked span on the interface pages is judged by nothing, so ~138 spans across three pages resolve against neither the package's surface nor the tree",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Narration is the ladder's bottom rung"
  },
  "acceptance": "a dotted span on an interface page is resolved — the member reading against the package's surface, the filename reading against the working tree — and one that answers neither is reported; no filename reds on its extension",
  "tests": [
    "a dotted span naming a member the package's surface does not hold is a page finding"
  ],
  "pins": [
    "a dotted span naming a file the working tree holds is not a page finding"
  ],
  "notes": "Per note THE-INTERFACE-PAGES-NAME-SURFACE-THE-PACKAGE-HOLDS, verified this tick: `isDeclarationSubject` (`tests/helpers/commentCitations.ts`) refuses any dot and its own doc names the class unresolved — which the citation paragraph files as an entry, not a question. Measured: 142 dotted spans, 63 distinct, over the three pages. The note's discriminator: head against the surface, whole span against the tree, judged where exactly one answers; `ELIDED_RECEIVER` already reads the `ctx.` shape."
}
