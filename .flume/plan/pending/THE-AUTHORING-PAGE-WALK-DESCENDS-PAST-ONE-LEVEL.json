{
  "tag": "THE-AUTHORING-PAGE-WALK-DESCENDS-PAST-ONE-LEVEL",
  "gate": {
    "kind": "open"
  },
  "files": {
    "edit": [
      {
        "path": "tests/harnessDeclaration.test.ts",
        "description": "the subfield walk recurses rather than returning the keys at one level, and its vacuity guard names a key two levels down"
      },
      {
        "path": "docs/CHAIN-AUTHORING.md",
        "description": "the `slices` bullet names the two keys the sweep slice reads"
      }
    ]
  },
  "summary": "The authoring page's subfield walk stops one level down, so `slices.sweep.posturePages` — typed by hand by every adopter enabling plan-sweep — is named nowhere the suite reads",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Narration is the ladder's bottom rung"
  },
  "acceptance": "the authoring page is held to every key an author types by hand under a declared field however deep the schema nests it, the walk's vacuity guard naming one two levels down; the `slices` bullet names both keys the sweep slice reads",
  "tests": [
    "docs/CHAIN-AUTHORING.md names every key an author types by hand under a declared field"
  ],
  "notes": "Off note THE-AUTHORING-PAGE-WALK-REACHES-THE-SHIPPED-SUBFIELDS, re-measured this drain: the schema's only third-level keys are `slices.sweep.domain` and `slices.sweep.posturePages`, nothing deeper — the descent is a recursion, not a special case. `posturePages` appears nowhere on the page; the backticked `domain` at :159 is `slots.domain`, so that half is green by collision. Measured: the added span leaves the interface-page identifier scan green, resolving against `packageSurface`."
}
