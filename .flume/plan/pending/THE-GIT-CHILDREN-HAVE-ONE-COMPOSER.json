{
  "tag": "THE-GIT-CHILDREN-HAVE-ONE-COMPOSER",
  "gate": { "kind": "open" },
  "files": {
    "edit": [
      {
        "path": "src/git.ts",
        "description": "a leg per operation the three modules spawn by hand — the admin dir, the worktree listing, the commit subject/body, the reverted diffstat — each composed through `spawnGit` like every other leg here"
      },
      {
        "path": "src/worktrees.ts",
        "description": "`worktreeAdminDir` and `readWorktreeRegistry` call those legs; the module-local `execFileP` goes with them"
      },
      {
        "path": "src/tickAttempt.ts",
        "description": "`capturedCommitMessage` calls the leg; the module-local `execFileP` goes"
      },
      {
        "path": "src/priorAttempts.ts",
        "description": "`capturedDiffStat` calls the leg; the module-local `execFileP` goes"
      }
    ]
  },
  "summary": "Five git children are composed outside `src/git.ts`'s one composer, each re-spelling cwd and a cap and taking none of the dialect `spawnGit` sets",
  "per": {
    "path": ".claude/rules/posture-sweep.md",
    "section": "Routing"
  },
  "acceptance": "every git child the engine spawns is composed by `spawnGit` (`src/git.ts`), so the three modules hold no `promisify(execFile)` of their own and a dialect, cap or flag the composer gains reaches every leg; each site's declared cap rides its own leg rather than the call",
  "notes": "Third body of this family: 6a9e103c, a00124e7, this one. Verified on disk this tick: `src/worktrees.ts:159` (`rev-parse --absolute-git-dir`, 64 KiB) and `:293` (`worktree list --porcelain -z`, 16 MiB), `src/tickAttempt.ts:703`/`:708` (`show -s --format`, 4 MiB), `src/priorAttempts.ts:643` (`show --stat`, 4 MiB) — against `spawnGit` (`src/git.ts:81`), 16 MiB under `literalPathspecEnv`. No pathspec at any of the five, so no verdict is wrong today. Claims no property."
}
