{
  "tag": "THE-INTERFACE-PAGES-NAME-SURFACE-THE-PACKAGE-HOLDS",
  "gate": { "kind": "open" },
  "files": {
    "edit": [
      {
        "path": "tests/helpers/pageAnchors.ts",
        "description": "the page-prose reader gains the identifier arm over the three named pages"
      },
      {
        "path": "tests/helpers/commentCitations.ts",
        "description": "the backticked-span subject rule is shared, never re-spelled for pages"
      },
      {
        "path": "tests/helpers/exportGraph.ts",
        "description": "hands out the public surface the arm resolves against"
      },
      {
        "path": "tests/pageAnchors.test.ts",
        "description": "the fixture arm and the repo verdict over the three pages"
      }
    ]
  },
  "summary": "An interface page's backticked identifiers are resolved by nothing; a retired export name rots there until CI finds it",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Narration is the ladder's bottom rung"
  },
  "acceptance": "docs/CHAIN-AUTHORING.md, docs/CLI.md and README.md are judged for the identifiers they backtick, on the subject rule the comment scan already spells; a name the package's surface does not hold reds the default lane",
  "tests": [
    "an interface page naming a backticked identifier the package's surface does not hold reds the page scan"
  ],
  "pins": [
    "every backticked identifier docs/CHAIN-AUTHORING.md, docs/CLI.md and README.md carry names a symbol the package's surface holds"
  ],
  "notes": "Ruled a613be82; per note THE-AUTHORING-PAGE-STATES-RETIRED-CLAIMS. Measured this tick: an exports-map-only resolution reds ~91 correct spans on the authoring page (writablePaths, afterCommit, flumeDir — members and literal arms of exported types), so the surface is what the exports reach, as tests/helpers/exportGraph.ts already walks it. The retired names red either way. MIGRATING-*/surveys are out by construction."
}
