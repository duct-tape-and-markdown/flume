{
  "tag": "THE-SLICE-STATE-GATE-DOC-NAMES-ONLY-ITS-OWN-CHECK",
  "gate": {
    "kind": "open"
  },
  "files": {
    "edit": [
      {
        "path": "harness/gates.ts",
        "description": "the slice-state gate's doc states the cursor check it makes itself and points at the table for the rest, listing no slice's rule"
      }
    ]
  },
  "summary": "The slice-state gate's doc rosters the invariants the table holds, and already omits the inbox lane rule — the second copy the spec just deleted for that reason",
  "per": {
    "path": "spec/harness.md",
    "section": "The gates the discipline needs"
  },
  "acceptance": "the gate's doc names the ancestry check it makes over every declared cursor and points at `SLICE_STATE_RULES` for the rest, naming no slice's own rule, so a fourth rule joins the table with no roster to rejoin",
  "notes": "Verified this tick: `harness/gates.ts:477`-`:480` cites the section for a list of three, where `SLICE_STATE_RULES` (`harness/planState.ts:461`) holds the sweep's two and the inbox's `lanesKeepTheStampsTheyHave` — already forgotten by the copy. The paragraph at `:482` states the pointer, so the fix is the roster's deletion. No property: an internal doc sits on no exported `.d.ts`. THE-SWEEP-CARRIES-A-RETIRED-CLAIM-CURSOR adds the next rule this roster would miss."
}
