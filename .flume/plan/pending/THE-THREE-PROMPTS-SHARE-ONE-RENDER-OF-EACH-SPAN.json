{
  "tag": "THE-THREE-PROMPTS-SHARE-ONE-RENDER-OF-EACH-SPAN",
  "gate": {
    "kind": "open"
  },
  "priority": 0,
  "files": {
    "edit": [
      {
        "path": "harness/prompts.ts",
        "description": "a placeholder and its render for each shared block, beside `QUESTIONS_INDEX` in the key roster and the arg map"
      },
      {
        "path": "harness/prompts/plan-inbox.md",
        "description": "each shared block becomes its placeholder"
      },
      {
        "path": "harness/prompts/plan-derive.md",
        "description": "each shared block becomes its placeholder"
      },
      {
        "path": "harness/prompts/plan-sweep.md",
        "description": "each shared block becomes its placeholder"
      },
      {
        "path": "harness/prompts/build.md",
        "description": "the one span this prompt shares with the three slices — the `project conventions` line — becomes its placeholder"
      },
      {
        "path": "tests/harnessPrompts.test.ts",
        "description": "the seam: the `PLAN_SLICES` pins reading a block that becomes a placeholder, shared with THE-PROTOCOL-SEAM-IS-PINNED-AT-EVERY-PHASE-PROMPT"
      }
    ]
  },
  "summary": "Six blocks are spelled byte-identically in all three slice prompts — the family three plan commit bodies have now accepted as debt, and the queue named two of them",
  "per": {
    "path": ".claude/rules/posture-sweep.md",
    "section": "Routing"
  },
  "acceptance": "every block the three slice prompts spell byte-identically is one placeholder rendered from one `promptArgs` value, the shape `QUESTIONS_INDEX` already takes — the `project conventions` line all four phase prompts spell alike included — and every rendered prompt is unchanged",
  "notes": "Third body of this family: 0fe5ae63, 7f0bdf4e, a7c044e8. Target shape is the tree's own: `QUESTIONS_INDEX` (`harness/prompts.ts:200`, `:270`) is one render three prompts share. Measured byte-identical across the three slice prompts: `<pending-now>`, `<plan-state>`, `<plan-state-shape>`, `<artifacts>`, the `<open-questions-index>` and `<schema>` wrappers; and, off note THE-PROTOCOL-SEAM-IS-PINNED-AT-EVERY-PHASE-PROMPT, the `project conventions` line in all four `<artifacts>` blocks. No property."
}
