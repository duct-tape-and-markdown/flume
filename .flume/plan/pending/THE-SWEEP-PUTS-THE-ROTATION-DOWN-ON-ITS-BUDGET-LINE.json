{
  "tag": "THE-SWEEP-PUTS-THE-ROTATION-DOWN-ON-ITS-BUDGET-LINE",
  "gate": {
    "kind": "blockedBy",
    "tags": [
      "THE-PUT-DOWN-PROMPT-NAMES-THE-LINE-THE-ENGINE-SENDS"
    ]
  },
  "files": {
    "edit": [
      {
        "path": "harness/prompts/plan-sweep.md",
        "description": "the per-tick bound is the put-down statement the blocker homes, and every frontier module the tick read is recorded covered before it puts the rotation down"
      },
      {
        "path": "harness/chain.ts",
        "description": "the sweep slice's description drops the one-neighborhood bound"
      }
    ]
  },
  "summary": "The sweep prompt and the slice description still bound a tick at one neighborhood, which prices a phrase delta's whole-domain rotation at roughly a hundred ticks",
  "per": {
    "path": ".claude/rules/posture-sweep.md",
    "section": "The frontier is decidable; the neighborhood is judged"
  },
  "acceptance": "the sweep tick's stated bound is its budget line's put-down rather than a count, and what it records covered is every frontier module it read",
  "notes": "Ruled off the phrase-delta record: none of the three forks — the frontier stays whole and the tick's bound moves. Sites: `harness/prompts/plan-sweep.md:38` (\"One neighborhood per tick\") and `:54`, `harness/chain.ts:91`; nothing pins either string. Blocked on the put-down statement's one home — the sweep agent needs the thresholds, and a second copy spelled here is the drift *Narration is the ladder's bottom rung* names. Prompt prose, so no property."
}
