{
  "tag": "THE-PROTOCOL-SEAM-IS-PINNED-AT-EVERY-PHASE-PROMPT",
  "gate": { "kind": "open" },
  "priority": 20,
  "files": {
    "edit": [
      {
        "path": "tests/harnessPrompts.test.ts",
        "description": "one case over `PHASES` at both ends of the `PROTOCOL` seam, replacing the build-only case it subsumes"
      }
    ]
  },
  "summary": "The `PROTOCOL` seam is pinned at build alone, so any of the three plan prompts could drop `{{PROTOCOL}}` and stay green — the defect just shipped, wearing another phase",
  "per": {
    "path": "spec/harness.md",
    "section": "The prompts and their discipline"
  },
  "acceptance": "every phase prompt the package renders is judged at both ends of the `PROTOCOL` seam — the placeholder where the package holds it, the producer's own state-root-relative path in the render — through one case over `PHASES`, and the build-only case it subsumes is gone",
  "pins": [
    "every phase prompt the package renders substitutes the consumer's PROTOCOL path"
  ],
  "notes": "Off note THE-BUILD-PROMPT-NAMES-THE-CONSUMERS-CONVENTIONS, item 1. Verified: `tests/harnessPrompts.test.ts:1168` judges `BUILD_PHASE` alone while all four prompts name `{{PROTOCOL}}`; `TURN_BOUNDARY`'s case (`:1095`) is the shape — `PHASES` loop, non-vacuity pin, both ends. Replaces that case, never joins it. Seam: THE-THREE-PROMPTS-SHARE-ONE-RENDER-OF-EACH-SPAN moves `<artifacts>`'s markdown end into a shared render, so the naming half follows it; declared on that entry."
}
