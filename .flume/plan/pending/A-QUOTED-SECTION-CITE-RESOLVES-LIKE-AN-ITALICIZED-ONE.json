{
  "tag": "A-QUOTED-SECTION-CITE-RESOLVES-LIKE-AN-ITALICIZED-ONE",
  "gate": { "kind": "open" },
  "files": {
    "edit": [
      {
        "path": "tests/helpers/commentCitations.ts",
        "description": "`SECTION_CITE` admits the quoted section half beside the italicized one; the doc above it names both spellings"
      },
      {
        "path": "tests/commentCitations.test.ts",
        "description": "the fixture's quoted pairs and the arm's cases, beside the italicized ones"
      },
      {
        "path": "src/cli.ts",
        "description": "the quoted `spec/chain.md` cite the widened arm reds, repointed to the heading whole"
      },
      {
        "path": "tests/Dispatcher.test.ts",
        "description": "the quoted `engineering.md` cite the widened arm reds, repointed to the section whole"
      },
      {
        "path": "src/cliHelp.ts",
        "description": "the same abbreviated `Chain.friction` cite in help text, which sits in a literal no arm reads"
      }
    ]
  },
  "summary": "`SECTION_CITE` matches the italicized half alone, so the 269 quoted `(page.md, \"Section\")` cites the rule now admits resolve against nothing",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Narration is the ladder's bottom rung"
  },
  "acceptance": "the quoted and the italicized section half resolve identically in the comment scan, and every cite the widened arm reds is repointed to the section its page states",
  "tests": [
    "a comment's (`<page>.md`, \"Section\") pair resolves its section half against the page's headings, bolded bullet leads and a bolded lead opening a blockquote line"
  ],
  "notes": "Ruled b91f906b. Measured this drain over `src/`, `harness/`, `tests/` comments: 269 quoted pairs against 1,017 italicized, and exactly two red once the arm lands — `src/cli.ts:774` and `tests/Dispatcher.test.ts:1977`. The third the question named, `src/cliHelp.ts:416`, is the same drift inside a help literal no arm reads, so it rides along by hand. Seam with THE-DOTTED-PAGE-SPAN-IS-JUDGED: both edit `tests/helpers/commentCitations.ts`."
}
