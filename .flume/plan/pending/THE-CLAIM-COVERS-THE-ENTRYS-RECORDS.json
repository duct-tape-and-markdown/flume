{
  "tag": "THE-CLAIM-COVERS-THE-ENTRYS-RECORDS",
  "gate": { "kind": "open" },
  "files": {
    "edit": [
      {
        "path": "src/builtinGates.ts",
        "description": "the claim check's subject is the entry's ledger file plus whatever paths the chain declares as that entry's records — a declared resolver, never a note layout the engine spells"
      },
      {
        "path": "harness/gates.ts",
        "description": "the package declares its own note paths as that resolver, off `notePaths`"
      },
      {
        "path": "harness/inboxWindow.ts",
        "description": "no note, park, or record whose entry is claimed renders; it stays on disk for a later drain"
      },
      {
        "path": "tests/builtinGates.test.ts",
        "description": "the refusal over a claimed entry's note path, beside the ledger-file claim arms"
      },
      {
        "path": "tests/harnessWindows.test.ts",
        "description": "the withheld record, and the unclaimed sibling that still renders"
      }
    ]
  },
  "summary": "A drain deletes and folds the records of an entry a build tick is carrying: the window renders them and the claim check reads only the ledger file",
  "per": {
    "path": "spec/pending.md",
    "section": "Claims — an entry in flight is left alone"
  },
  "acceptance": "a claimed entry's note, park and record are withheld from the producer's window and left on disk, and a commit that changes one of them is refused naming the entry and its holder, exactly as one changing its ledger file already is",
  "tests": [
    "the inbox window withholds a record whose entry another tick holds a claim on",
    "pendingGate refuses a commit that changes a claimed entry's note file"
  ],
  "pins": [
    "the inbox window renders a record whose entry no tick has claimed"
  ],
  "notes": "Field report, 0.19: a modify/delete conflict quarantined an entry, and the claim check refused four whole plan commits in three runs. Verified this tick: `touchedEntryFiles` (`src/builtinGates.ts:451`) is scoped to `queueDirRel`, and `renderRecords` (`harness/inboxWindow.ts:111`) takes no claimed set though `ctx.claimed` reaches the slice args (`harness/chain.ts:278`). The engine holds no note layout, so the record set arrives declared."
}
