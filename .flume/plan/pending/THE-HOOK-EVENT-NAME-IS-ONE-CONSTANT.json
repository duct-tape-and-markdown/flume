{
  "tag": "THE-HOOK-EVENT-NAME-IS-ONE-CONSTANT",
  "gate": {
    "kind": "open"
  },
  "files": {
    "edit": [
      {
        "path": "src/budgetHook.ts",
        "description": "the provider's hook event name as a named constant, beside the flag constants this same seam already shares"
      },
      {
        "path": "src/Agent.ts",
        "description": "the settings' event key names that constant instead of a second literal"
      }
    ]
  },
  "summary": "The provider's `PostToolUse` event name is spelled twice — the settings key registering the hook and the output the hook echoes back — kept in agreement by discipline alone",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Derived state is computed, never restated beside its source"
  },
  "acceptance": "the hook's event name is declared once in `src/budgetHook.ts`, and both the registration that arms the hook and the output the hook writes read that one declaration",
  "notes": "Verified this tick: `src/Agent.ts:403` keys the inline settings on `PostToolUse`; `src/budgetHook.ts:293` writes `hookEventName: \"PostToolUse\"`. The two must agree for the line to reach the agent’s next turn, and nothing checks. `src/budgetHook.ts:63`-`:67` already makes that argument for this seam’s argv flags. `Agent.ts` imports `budgetHook.ts`, not the reverse, so the constant lands there with no cycle. Seam: THE-PROVIDER-USAGE-TOKENS-HAVE-ONE-DECODE edits `src/Agent.ts` too."
}
