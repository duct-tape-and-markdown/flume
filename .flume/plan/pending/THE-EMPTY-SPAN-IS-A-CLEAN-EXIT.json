{
  "tag": "THE-EMPTY-SPAN-IS-A-CLEAN-EXIT",
  "gate": { "kind": "open" },
  "files": {
    "edit": [
      {
        "path": "src/tickAttempt.ts",
        "description": "a span whose diff against its base is empty classifies as `clean-exit` and returns before the gates and the merge stage, beside the `headSha === spanBase` arm"
      },
      {
        "path": "src/priorAttempts.ts",
        "description": "the clean-exit record's no-message fallback stops asserting the agent did not commit"
      },
      {
        "path": "src/Prompt.ts",
        "description": "the record carries the span's shas, so a declined tick and one that committed nothing are tellable apart"
      },
      {
        "path": "tests/Dispatcher.test.ts",
        "description": "the empty-span arm, beside the clean-exit and merge-failure cases"
      }
    ]
  },
  "summary": "An empty span reaches the merge stage, where `git cherry-pick` exits 1 and the failure quarantines the entry for the rest of the run as a merge failure",
  "per": { "path": "spec/loop.md", "section": "The no-commit taxonomy" },
  "acceptance": "a span whose diff against its base is empty is classified `clean-exit`, dies with the worktree, and reaches neither the gate loop nor the merge stage or its quarantine; the union stays closed at four and the record's shas tell a declined tick from one that committed nothing",
  "tests": [
    "an empty span is classified as a clean exit rather than reaching the merge stage",
    "an empty span leaves no merge-stage quarantine against its entry"
  ],
  "notes": "Ruled off the empty-span record, option 2, and `spec/loop.md`'s own `clean-exit` row now reads it. Verified this tick: `headSha === spanBase` (`src/tickAttempt.ts:283`) is the only no-commit arm and an empty commit moves past it; no empty-diff check exists in `src/`; both merge sites (`src/waveMerge.ts:451`, `src/singletonTick.ts:356`) then run `cherryPickRange`. `buildCleanExit` (`src/priorAttempts.ts:701`) spells \"without committing\" in its fallback."
}
