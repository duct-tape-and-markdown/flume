{
  "tag": "THE-MEMBER-CITE-RESOLVES-AGAINST-ITS-OWN-DECLARATION",
  "gate": {
    "kind": "open"
  },
  "files": {
    "edit": [
      {
        "path": "tests/helpers/commentCitations.ts",
        "description": "the member half of a dotted citation resolves against its head's own declaration, beside the name/path and page/section pairs, and the header paragraph stating the segment rule"
      },
      {
        "path": "tests/commentCitations.test.ts",
        "description": "the arm over a member its declaration lacks, and over one it holds"
      },
      {
        "path": "src/cliVerdict.ts",
        "description": "one red \u2014 the merge-outcome field named at a spelling the type does not carry"
      },
      {
        "path": "src/worktrees.ts",
        "description": "one red \u2014 the gate-run site named on the class the split moved it off"
      },
      {
        "path": "tests/cliVerdict.test.ts",
        "description": "the same red in the case's own comment"
      },
      {
        "path": "tests/Dispatcher.test.ts",
        "description": "one red \u2014 a module-local function named as a dispatcher member"
      },
      {
        "path": "tests/helpers/exportGraph.ts",
        "description": "one red \u2014 a phase field named on the chain"
      }
    ]
  },
  "summary": "A backticked `Type.member` cite resolves segment by segment repo-wide, so five comments naming a member their own declaration does not have read green",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Narration is the ladder's bottom rung"
  },
  "acceptance": "the tail of a backticked dotted citation whose head names a type, interface or class the judged trees declare resolves against that declaration's own members rather than repo-wide, leaving the module-basename and path arms to answer the spans they already answer, and each measured red names the member its declaration holds",
  "tests": [
    "a backticked Type.member citation naming no member of that declaration is reported"
  ],
  "pins": [
    "a backticked Type.member citation naming a member that declaration holds is not reported"
  ],
  "notes": "Five sites measured this tick, green because each segment resolves repo-wide: `TickVerdictMergeOutcome.tag` (`src/cliVerdict.ts:186`, `tests/cliVerdict.test.ts:608`) \u2014 the field is `entryTag` (`src/tickVerdict.ts:348`); `Dispatcher.runGate` (`src/worktrees.ts:457`) \u2014 `src/gateRun.ts:56`; `Dispatcher.waveNoCommitCause` (`tests/Dispatcher.test.ts:10376`) \u2014 `src/waveMerge.ts:179`; `Chain.writablePaths` (`tests/helpers/exportGraph.ts:461`) is `Phase`'s. The name/path pair is the precedent."
}
