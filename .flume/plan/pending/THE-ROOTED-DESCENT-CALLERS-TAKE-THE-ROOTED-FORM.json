{
  "tag": "THE-ROOTED-DESCENT-CALLERS-TAKE-THE-ROOTED-FORM",
  "gate": {
    "kind": "open"
  },
  "files": {
    "edit": [
      {
        "path": "src/mergingMarkers.ts",
        "description": "the state-root-to-`merging/` descent goes through the rooted form, and the doc names it"
      },
      {
        "path": "src/priorAttempts.ts",
        "description": "the state-root-to-`prior-attempts/` descent goes through the rooted form; the keyspace fan beneath it keeps the list form, and the doc names both"
      },
      {
        "path": "tests/examples.test.ts",
        "description": "the cascade inbox block's doc names the probe the example now reads"
      }
    ]
  },
  "summary": "Two engine callers hand-spell the two-rung descent `isDirectoryOrAbsentUnder` composes, and a sibling's doc already states that both run it",
  "per": {
    "path": ".claude/rules/posture-sweep.md",
    "section": "Routing"
  },
  "acceptance": "the two callers holding a root and one directory beneath it take `isDirectoryOrAbsentUnder`, the shape `src/fsProbe.ts`'s own doc routes them to, so `harness/dirListing.ts`'s claim that both run that descent reads true on disk; the keyspace fan under an already-proven root keeps `isDirectoryOrAbsent`",
  "notes": "Third plan body: e00eaedd item 3, db75a751. Verified on disk \u2014 `src/mergingMarkers.ts:84` and `src/priorAttempts.ts:445` each pass a root and one segment to `isDirectoryOrAbsent` as a list, the shape `src/fsProbe.ts:101` routes to the rooted form, and `harness/dirListing.ts:60` already names both as running it. Behavior-identical today (`src/paths.ts:429`, `:432` are one segment). `src/priorAttempts.ts:448` stays. `tests/examples.test.ts:1059` still names `api.isDirectoryOrAbsent`."
}
