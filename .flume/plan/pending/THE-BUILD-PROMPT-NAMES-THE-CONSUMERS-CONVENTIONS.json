{
  "tag": "THE-BUILD-PROMPT-NAMES-THE-CONSUMERS-CONVENTIONS",
  "gate": { "kind": "open" },
  "priority": 30,
  "files": {
    "edit": [
      {
        "path": "harness/prompts/build.md",
        "description": "the conventions span its three sibling prompts carry, off the shared arg this render is already handed"
      },
      {
        "path": "tests/harnessPrompts.test.ts",
        "description": "the rendered build prompt's own case, beside the slice prompts' spans"
      }
    ]
  },
  "summary": "The three plan prompts render `project conventions: {{PROTOCOL}}`; the build prompt names none, so a build tick never reads the page init wrote for it",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "A fact the engine holds is reported, never rediscovered"
  },
  "acceptance": "a rendered build prompt names the consumer's `PROTOCOL.md` under this tick's state root, the span its three siblings already carry, so a consumer's conventions reach build without a hand-built `extraArgs` copy",
  "tests": [
    "a rendered build prompt names the consumer's PROTOCOL path"
  ],
  "notes": "Downstream report, 0.19 item 14; the consumer rebuilt it with `--append-system-prompt` in `extraArgs` — a chain restating a value the package already holds. Verified: `PROTOCOL` is a shared arg (`harness/prompts.ts:195`, `:266`) and build spreads `shared(BUILD_PHASE, ctx)` (`harness/chain.ts:361`), so the value reaches this render and the template drops it; no `PROTOCOL` in `harness/prompts/build.md` or any test. Seams `tests/harnessPrompts.test.ts` with two standing entries."
}
