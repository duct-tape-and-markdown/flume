{
  "tag": "THE-SHIPPED-PAGE-CITE-RESOLVES-AGAINST-THE-PACKED-SET",
  "gate": {
    "kind": "blockedBy",
    "tags": [
      "THE-RENDERED-PAGE-SCAN-LEAVES-A-CONSUMER-TREE-PATH"
    ]
  },
  "files": {
    "edit": [
      {
        "path": "tests/helpers/commentCitations.ts",
        "description": "a shipped surface's page names resolve against the file set the manifest packs, not the working tree"
      },
      {
        "path": "tests/commentCitations.test.ts",
        "description": "the arm's own case, and the vacuity anchors of both shipped scans, which name spec pages today"
      },
      {
        "path": "harness/templates/PROTOCOL.md",
        "description": "the two `spec/harness.md` cites name a shipped page"
      },
      {
        "path": "src/cliHelp.ts",
        "description": "the three rendered `spec/` cites in the loop and friction pages name a shipped page"
      },
      {
        "path": "docs/CHAIN-AUTHORING.md",
        "description": "the records passage the template's cite needs a shipped home for \u2014 the paths, the title line, the byte cap"
      }
    ]
  },
  "summary": "Five cites in pages the package ships point into the spec corpus, which ships nowhere, and the pin over them reads the author's tree rather than the reader's",
  "per": {
    "path": "spec/harness.md",
    "section": "Adoption and upgrade"
  },
  "acceptance": "a shipped page's `*.md` names are resolved against the file set `package.json` packs rather than the tree they were written in, and no page the package ships names the spec corpus \u2014 each of the five sites cites a `docs/` page instead, with `docs/CHAIN-AUTHORING.md` carrying the records passage no shipped page states today",
  "tests": [
    "the rendered page scan reports a page name a shipped page states that the package does not pack",
    "no page the package ships names a page of the spec corpus"
  ],
  "notes": "Ruled by the inbox record answering the shipped-cite question, fork (b), no phrase edit. Measured this tick \u2014 the whole set `shippedPages()` states: `harness/templates/PROTOCOL.md:11`,`:58` (`spec/harness.md`), `src/cliHelp.ts:225`,`:266` (`spec/loop.md`), `:420` (`spec/chain.md`). `docs/CLI.md` already states the loop and friction facts, so only the records passage is new. Packed set is the manifest's `files`, negation included; the template's `spec/*.md` and `{{...}}` fail `PATH_SEGMENT`."
}
