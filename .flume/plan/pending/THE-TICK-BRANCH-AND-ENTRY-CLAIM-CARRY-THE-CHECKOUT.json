{
  "tag": "THE-TICK-BRANCH-AND-ENTRY-CLAIM-CARRY-THE-CHECKOUT",
  "gate": {
    "kind": "open"
  },
  "priority": 30,
  "files": {
    "edit": [
      {
        "path": "src/git.ts",
        "description": "the checkout's own segment, off the git-dir/common-dir pair this module already resolves"
      },
      {
        "path": "src/worktrees.ts",
        "description": "`createWorktree`'s branch carries that segment, and the doc that spells the short form moves with it"
      },
      {
        "path": "src/entryClaims.ts",
        "description": "the claim address and the store's walk carry the same segment, so a sibling checkout's claim is neither read nor hidden behind"
      },
      {
        "path": "src/pidClaim.ts",
        "description": "the doc spelling the entry-claim address beside the tip claim's"
      },
      {
        "path": "docs/CHAIN-AUTHORING.md",
        "description": "the two passages stating the branch a tick's worktree is minted on"
      },
      {
        "path": "README.md",
        "description": "the branch-naming passage, and the two-efforts rationale it rests the isolation on"
      },
      {
        "path": "tests/git.test.ts",
        "description": "the segment's own case — primary checkout against a linked one"
      },
      {
        "path": "tests/worktrees.test.ts",
        "description": "two linked checkouts at one singleton phase, beside the round-trip agreement gate"
      },
      {
        "path": "tests/entryClaims.test.ts",
        "description": "a sibling checkout's same-spelled claim, beside the slug cases"
      },
      {
        "path": "tests/Dispatcher.test.ts",
        "description": "the two observed-branch assertions that spell the old form"
      },
      {
        "path": "tests/builtinGates.test.ts",
        "description": "the claim fixtures the pending gate's claim check reads, addressed through `entryClaimPath`"
      },
      {
        "path": "tests/harnessGates.test.ts",
        "description": "the held-claim fixture addressed through `entryClaimPath`"
      }
    ]
  },
  "summary": "Linked checkouts share one ref namespace, so the spec's own two-effort recipe collides on `flume/<phase>` and a sibling's claim on a same-spelled tag hides an entry",
  "per": {
    "path": "spec/worktrees.md",
    "section": "Fanout is the engine's declared navigation carve-out"
  },
  "acceptance": "the branch a tick's worktree is minted on and the claim a build tick stakes both carry a segment the checkout owns — stable for its life, distinct between any two checkouts of one repository — so two linked checkouts at one singleton phase each mint their own branch and neither's claim hides the other's entry; the ship and worktree locks stay shared, and the startup sweep still reaps residue under the old names off the registry",
  "tests": [
    "two linked checkouts of one repository each provision the same singleton phase at once and both succeed",
    "a sibling checkout's entry claim on a tag of the same spelling does not hide this checkout's entry"
  ],
  "notes": "Ruling record beside field report item 4; spec 6e2c4033. Measured: `git rev-parse --git-dir` equals `--git-common-dir` in the primary checkout and is `<common>/worktrees/<name>` in a linked one — git's own fact; the fold keeping the primary distinct from a linked tree of the same name is build's. `createWorktree` (`src/worktrees.ts:471`) and `EntryClaimStore` both hold `repoRoot`. Claims half: `spec/pending.md`, *Claims*. `tests/Dispatcher.test.ts:562`, `:5215` spell the old form."
}
