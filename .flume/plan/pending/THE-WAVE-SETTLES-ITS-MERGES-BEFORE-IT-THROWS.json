{
  "tag": "THE-WAVE-SETTLES-ITS-MERGES-BEFORE-IT-THROWS",
  "gate": {
    "kind": "open"
  },
  "files": {
    "edit": [
      {
        "path": "src/waveTick.ts",
        "description": "an entry leg's own throw is held in the holder the merge throw already takes, so one settle covers both and the leg leaves once"
      },
      {
        "path": "tests/Dispatcher.test.ts",
        "description": "the entry-leg throw over a wave with a sibling's span still unmerged, beside the merge-throw cases"
      }
    ]
  },
  "summary": "A throw out of an entry's own leg rejects the wave's `Promise.all` on the spot, leaving a sibling's cherry-pick unqueued or in flight — the settle the merge holder beside it exists for",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Loud or nothing"
  },
  "acceptance": "a throw out of an entry's leg leaves the wave the way a throw out of a merge does — held, every finished sibling's span carried and its queued merges settled, then propagated — so no leg leaves with git work it started still running",
  "tests": [
    "a wave whose entry leg throws carries every finished sibling's span onto trunk before the throw leaves it"
  ],
  "notes": "Verified this tick: `mergeError` (`src/waveTick.ts:296`) holds a merge throw precisely so \"the siblings still running have to settle before this leg can leave\", but the `Promise.all` at `:305` is over `runFanoutEntry`, which rejects unguarded — `ctx.attempts.read` (`src/tickAttempt.ts:235`), `git.revParse` (`:267`, `:278`), a non-`InlineExecRenderError` render throw (`:257`). Same holder shape is the fix. Seams both files with the in-flight THE-FREED-SLOT-PULLS-THE-NEXT-DISJOINT-ENTRY."
}
