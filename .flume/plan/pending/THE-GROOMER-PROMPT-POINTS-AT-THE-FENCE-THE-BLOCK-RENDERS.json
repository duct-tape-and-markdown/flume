{
  "tag": "THE-GROOMER-PROMPT-POINTS-AT-THE-FENCE-THE-BLOCK-RENDERS",
  "gate": {
    "kind": "open"
  },
  "files": {
    "edit": [
      {
        "path": "examples/prompts/backlog-groomer.md",
        "description": ":29's fence restatement points at the `<harness>` block the engine prepends, as the sibling example prompt's write rule now does"
      }
    ]
  },
  "summary": "The groomer example's OUTPUT section spells its phase fence in prose beside a harness block that renders the chain's real writable paths \u2014 a second stored copy, and the one an agent reads first",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Derived state is computed, never restated beside its source"
  },
  "acceptance": "the groomer example's write rule names the fence the `<harness>` block states rather than spelling its paths, so the chain's declaration is the only place those two filenames are stated",
  "notes": "Off note THE-EXAMPLE-BUILD-PROMPT-NAMES-THE-FENCE-THAT-BINDS, its second finding, which the shipping tick declined to widen into. Verified this drain: `:29` reads \"Touch only `BACKLOG.json` and `SHIPPED.md`\" against `writablePaths: [BACKLOG_PATH, SHIPPED_PATH]` (`examples/backlog-groomer-chain.ts:312`), and `src/Prompt.ts:12` prepends the block to every rendered prompt, so the copy sits beside its source. No property: the ladder's prompt-prose carve-out leaves `examples/prompts/` unjudged."
}
