{
  "tag": "THE-RETIRED-MEMBER-CITE-IS-EXCUSED-BY-NAME",
  "gate": {
    "kind": "open"
  },
  "files": {
    "edit": [
      {
        "path": "tests/chain.test.ts",
        "description": "the retired cite is spelled as the member it names, and the paragraph states the exemption carrying it rather than the spelling that avoided the arm"
      },
      {
        "path": "tests/helpers/external-vocabulary.json",
        "description": "`Chain.seedDir` against its own reason"
      },
      {
        "path": "tests/helpers/externalVocabulary.ts",
        "description": "the header's reason class reaches surface this repo retired, past \"something outside owns it\""
      },
      {
        "path": "tests/commentCitations.test.ts",
        "description": "the exclusion block's owner roster names the retired class beside the two it already names"
      }
    ]
  },
  "summary": "A retired member is cited by a spelling the resolver declines rather than by name in the exclusion list, so the one exemption channel that carries a reason never sees it",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Narration is the ladder's bottom rung"
  },
  "acceptance": "the retired `Chain.seedDir` cite is spelled as the member it names and excused by name and reason in the exclusion list, so the exemption is the one that list's own non-vacuity arm counts rather than a spelling a paragraph explains; both scopes stating what the list excuses name the retired class beside the two owners they hold",
  "notes": "Off note THE-MEMBER-CITE-RESOLVES-AGAINST-ITS-OWN-DECLARATION. Verified: `Chain` declares no `seedDir` in `src/` or `harness/`; the comment (`tests/chain.test.ts:386`) spells `\"seedDir\"` and `:390` states why, so the member arm never reads it. The list is the mechanism — `Box.lid` and two siblings are member cites it excuses, filtered at `tests/commentCitations.test.ts:1823`, counted at `:1807`. Claims no property: the check exists, the site moves under it."
}
