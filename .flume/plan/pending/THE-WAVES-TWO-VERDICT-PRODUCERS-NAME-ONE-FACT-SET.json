{
  "tag": "THE-WAVES-TWO-VERDICT-PRODUCERS-NAME-ONE-FACT-SET",
  "gate": {
    "kind": "open"
  },
  "files": {
    "edit": [
      {
        "path": "tests/Dispatcher.test.ts",
        "description": "one wave's facts driven through both real verdict producers, the refused verdict read against the completing one's field set, over a wave populating the omitted-when-empty facts"
      }
    ]
  },
  "summary": "The wave's completing and ledger-refusal verdicts are two hand-assembled argument lists over one optional-everywhere shape, so an omitted fact compiles and ships green",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "A seam gate reads what the real writer wrote"
  },
  "acceptance": "one wave runs twice — once completing, once with its ledger rewrite refused — and the refused verdict names every fact field the completing verdict names, over a wave asserted to populate facts `buildTickVerdict` omits when empty rather than its unconditional floor, both verdicts taken from the real producers rather than hand-authored",
  "pins": [
    "a wave's ledger-refusal verdict names every fact field that wave's completing verdict names",
    "the wave both verdict comparisons read populates fact fields buildTickVerdict omits when empty"
  ],
  "notes": "Off note THE-LEDGER-REFUSAL-VERDICT-NAMES-THE-STAKE-LOSSES, fork (2); fork (1) — one shared facts value — is unavailable: `src/Dispatcher.ts:1056` serves singletons too. Sharpened off 43ea774f: `buildTickVerdict` (`src/tickVerdict.ts:770`) omits 8 of the 18 fact fields when empty, so a clean wave's verdict names 10 keys and \"every field\" reads as complete over half the shape. `tests/Dispatcher.test.ts:8302` is the refusal leg."
}
