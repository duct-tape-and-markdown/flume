{
  "tag": "THE-CITE-SCAN-COVERAGE-READS-THE-DECLARED-SWEEP-DOMAIN",
  "gate": { "kind": "open" },
  "files": {
    "edit": [
      {
        "path": "tests/commentCitations.test.ts",
        "description": "`SWEEP_DOMAIN` is read off the declaration's `slices.sweep.domain` rather than spelled a second time"
      }
    ]
  },
  "summary": "The citation scans' coverage pin spells the sweep domain by hand, so the declaration dropping `tests/` left the copy naming a tree the page no longer does",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Derived state is computed, never restated beside its source"
  },
  "acceptance": "the coverage pin reads the sweep domain off this repository's declaration, so a tree the declared domain gains or loses moves the assertion with it",
  "pins": [
    "the two citation scans cover every tree the repository's declared sweep domain names"
  ],
  "notes": "Verified this tick: `tests/commentCitations.test.ts:505` still lists `tests/`, which `.flume/declaration.ts:133` dropped in the commit that changed the page; the doc at `:501` claims to read the page as it reads this tick. Green either way today — `repoScan` covers `tests/` and `:1704` pins that on its own — so the defect is the copy, not the verdict. `tests/chain.test.ts:31` already imports the declaration. `slices.sweep` is optional in the type; narrow, never assert."
}
