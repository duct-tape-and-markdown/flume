{
  "tag": "THE-SMOKE-SHELL-REFUSES-AN-ARGV-IT-WOULD-REWRITE",
  "gate": {
    "kind": "open"
  },
  "files": {
    "edit": [
      {
        "path": "scripts/smoke-install.mjs",
        "description": "a step whose argv reaches the shell refuses a value that shell would re-parse, naming it, through the engine's own predicate rather than a second spelling; the helper's doc states what Node does with an argv array under `shell: true` rather than claiming it quotes one"
      },
      {
        "path": "package.json",
        "description": "`smoke:install` loads the script under tsx, so the import of the engine's predicate resolves"
      },
      {
        "path": ".github/workflows/ci.yml",
        "description": "the posix lane's `--scratch` invocation, which calls the script past the package script"
      },
      {
        "path": ".github/workflows/release.yml",
        "description": "the registry-install invocation, the third site that calls the script past the package script"
      }
    ]
  },
  "summary": "The smoke takes a shell for every win32 step under a doc claiming Node quotes the argv array — measured false — and the tarball path it composes rides that argv",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Loud or nothing"
  },
  "acceptance": "a step whose argv reaches a shell refuses a value that shell would re-parse, naming the value and the step, through the engine's own rewrite predicate rather than a second spelling of it — so every site that invokes the script loads it under tsx; the run either installs the tarball it packed or says why it cannot, and the site's own account of the platform matches what Node does rather than denying the re-parse",
  "notes": "Fork settled off note THE-SHIM-RETRY-REFUSES-AN-ARGV-IT-WOULD-REWRITE: share, never duplicate (`engineering.md`, *The fix lands at the mechanism*). `wordShimRetryWouldRewrite`/`shimRetryRefusal` (`src/spawnShim.ts`) shipped; bare `node` runs the script, so its three sites take `--import tsx`, and `pnpm install` precedes each. Measured: node quotes nothing under `shell: true`; `:83` claims it does, `installTarget` (`:217`) rides that argv. No `tests[]`: `IS_WIN` only, the named exception."
}
