{
  "tag": "THE-SWEEP-PROMPT-POINTS-AT-THE-PAGES-IT-RESTATES",
  "gate": {
    "kind": "open"
  },
  "files": {
    "edit": [
      {
        "path": "harness/prompts/plan-sweep.md",
        "description": "the routing-bar and rotation/stamp paragraphs shrink to pointers at the pages that own them, keeping the plan-state field names the slice writes"
      }
    ]
  },
  "summary": "The sweep prompt restates its posture page's routing bar and cursor rulings two lines after ordering the pages read in full, and the routing copy drops the page's cohesion route",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Narration is the ladder's bottom rung"
  },
  "acceptance": "the sweep prompt states no ruling its posture pages own \u2014 the routing bar, the rotation's close and the stamp's sha are pointers \u2014 and carries only what the prompt itself must: the plan-state field names this slice writes",
  "notes": "Verified this tick: `harness/prompts/plan-sweep.md:42` restates *Routing* (`.claude/rules/posture-sweep.md`), dropping its cohesion route and three-notes rule, so the lossy half can change a finding's route; `:44` restates *The rotation closes when the frontier empties* and *The stamp*. Both sit two lines under `:38`, which orders the pages read in full. No property: this section's carve-out keeps harness prose out of the suite. Seam: the file is also THE-SWEEP-CARRIES-A-RETIRED-CLAIM-CURSOR's."
}
