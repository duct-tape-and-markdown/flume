{
  "tag": "THE-AUTHORING-PAGE-PRICES-A-WAVE-IN-MEMORY",
  "gate": { "kind": "open" },
  "priority": 30,
  "files": {
    "edit": [
      {
        "path": "docs/CHAIN-AUTHORING.md",
        "description": "the supervisor-policy section prices a wave in memory beside the knobs that size one"
      }
    ]
  },
  "summary": "The authoring page states what `maxTicks` schedules and never what it costs, so an operator sized an eight-wide wave at 3 and the kernel killed the supervisor",
  "per": {
    "path": "spec/chain.md",
    "section": "Supervisor policy is a chain-overridable default"
  },
  "acceptance": "the authoring page's supervisor-policy section states what one tick holds — an agent, plus a judge suite under the ship lock — and how `maxTicks` and `maxParallel` multiply into a host's memory, with a measured host as the worked example",
  "notes": "Downstream report, 0.19 item 12. Verified: the `maxTicks` bullet (`docs/CHAIN-AUTHORING.md:2336`) states scheduling and declaration order alone, and the corpus's one memory sentence is `docs/MIGRATING-0.19.md:383`. The worked example the reporter asked for is this repo's own edge, two four-wide waves on an 11 GB host. No property: the figures are measurements, not a pinnable claim. Seams the page with THE-VERDICT-CARRIES-A-TIMING-PER-GATE-RUN-AND-MERGE."
}
