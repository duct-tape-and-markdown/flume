{
  "tag": "AN-UNKNOWN-PHASE-EXITS-2-AT-EVERY-VERB",
  "gate": { "kind": "open" },
  "priority": 1,
  "files": {
    "edit": [
      {
        "path": "src/cliVerdict.ts",
        "description": "`tickExitCode` returns 1 on `undeclaredPhase`; the usage class is 2, as `wake`/`sleep`/`render` already answer"
      },
      {
        "path": "src/cliHelp.ts",
        "description": "the `--phase <name>` bullet moves from the exit-1 block to the exit-2 block"
      },
      {
        "path": "docs/CLI.md",
        "description": "the `tick` exit-code paragraph states 1 for an undeclared `--phase`"
      },
      {
        "path": "tests/cliHelp.test.ts",
        "description": "the outcome/exit-code agreement matrix"
      }
    ]
  },
  "summary": "An undeclared `--phase` name exits 1 at `tick` and 2 at every other verb; one refusal, one code",
  "per": { "path": "spec/cli.md", "section": "Subcommand surface" },
  "acceptance": "`flume tick --phase <undeclared>` exits 2, and the help text and CLI page say so",
  "tests": [
    "flume tick --phase naming a phase the chain does not declare exits 2"
  ],
  "notes": "Ruled 2026-09-24 (inbox): the supervisor never spawns a child for an undeclared phase, so the code is argv's alone. `spec/loop.md`, *Baton* already says exit 2."
}
