{
  "tag": "THE-LEDGER-REFUSAL-VERDICT-NAMES-THE-STAKE-LOSSES",
  "gate": { "kind": "open" },
  "files": {
    "edit": [
      {
        "path": "src/waveMerge.ts",
        "description": "`WaveMergeRequest` carries the wave's stake losses; the refusal's verdict names them"
      },
      {
        "path": "src/waveTick.ts",
        "description": "the merge request is handed the losses the leg already computed"
      },
      {
        "path": "tests/Dispatcher.test.ts",
        "description": "a wave that loses a stake race and then refuses the ledger rewrite"
      }
    ]
  },
  "summary": "The ledger-refusal verdict names no stake losses, so a wave that lost an entry and then refused reports it simply absent",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "A fact the engine holds is reported, never rediscovered"
  },
  "acceptance": "the ledger-refusal verdict names the wave's stake losses exactly as the completing path's verdict already does",
  "tests": [
    "a wave that lost a stake race and then refused the ledger rewrite reports the losses on the thrown verdict"
  ],
  "pins": [
    "a wave that completes after losing a stake race reports the losses on its verdict"
  ],
  "notes": "Verified this tick. `WaveMergeRequest` (`src/waveMerge.ts:206`) carries provisioned, provisionFailures and clearedPriorAttempts and no losses, so the refusal's builder call (`:776`) omits them; `Dispatcher.tick()` (`src/Dispatcher.ts:962`) takes `err.verdict` verbatim. `src/waveTick.ts:189` computes them and puts them on the leg's return alone (`:445`, `:457`). Per note ONE-BUILDER-SHAPES-EVERY-TICK-VERDICT."
}
