{
  "tag": "THE-TIP-CLAIM-NAMES-THE-STATE-ROOT-IT-HOLDS-FOR",
  "gate": {
    "kind": "open"
  },
  "priority": 30,
  "files": {
    "edit": [
      {
        "path": "src/pidClaim.ts",
        "description": "the guard statement's third line, the tip claim's alone, beside the pid and the instant"
      },
      {
        "path": "src/git.ts",
        "description": "the tip claim writes the root it was handed, and `TipClaimHeldError` names it beside the ref and the path"
      },
      {
        "path": "src/cli.ts",
        "description": "both `acquireTipClaim` sites hand the state root they resolved"
      },
      {
        "path": "tests/pidClaim.test.ts",
        "description": "the third line's render and decode, beside the instant's cases"
      },
      {
        "path": "tests/git.test.ts",
        "description": "the refusal naming both roots, beside the tip-claim stake cases"
      },
      {
        "path": "tests/cli.test.ts",
        "description": "two relocated roots in one checkout, the second tick's refusal naming the first's root"
      }
    ]
  },
  "summary": "The tip claim states pid and instant only, so a second effort in one checkout under its own FLUME_DIR refuses as a busy tip, naming neither state root",
  "per": {
    "path": "spec/loop.md",
    "section": "The loop lock and the tip claim"
  },
  "acceptance": "the tip claim's statement carries the state root its holder resolved on a third line, and a live-holder refusal names that root beside the ref and the claim path, so two efforts in one checkout read as two state roots on one tip rather than as a busy tip; a reader that needs only liveness reads the first line unchanged",
  "tests": [
    "the tip claim states the state root its holder resolved on its third line",
    "a second tick in one checkout under its own state root refuses naming the root that holds the tip"
  ],
  "notes": "Ruled 76262932, closing `questions/does-the-second-root-refusal-reach-two-relocated-roots.md` on none of its three forks — a checkout has one tip. Verified: `renderPidClaim` (`src/pidClaim.ts`) writes two lines; `TipClaimHeldError` (`src/git.ts:1123`) names ref, pid and path only; `flumeDir` is in scope at both calls (`src/cli.ts:1239`, `:1474`). An absent third line follows `atMs`'s precedent at the same site. Seams `src/cli.ts` with THE-SUPERVISOR-CLAIM-HANDOFF-IS-DECODED-ONCE."
}
