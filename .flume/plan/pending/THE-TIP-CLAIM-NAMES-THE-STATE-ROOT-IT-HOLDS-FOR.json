{
  "tag": "THE-TIP-CLAIM-NAMES-THE-STATE-ROOT-IT-HOLDS-FOR",
  "gate": {
    "kind": "open"
  },
  "priority": 30,
  "files": {
    "edit": [
      {
        "path": "src/pidClaim.ts",
        "description": "the guard statement's third line, the tip claim's alone, beside the pid and the instant"
      },
      {
        "path": "src/git.ts",
        "description": "the tip claim writes the root it was handed, and `TipClaimHeldError` names it beside the ref and the path"
      },
      {
        "path": "src/cli.ts",
        "description": "both `acquireTipClaim` sites hand the state root they resolved"
      },
      {
        "path": "tests/pidClaim.test.ts",
        "description": "the third line's render and decode, beside the instant's cases"
      },
      {
        "path": "tests/git.test.ts",
        "description": "the refusal naming both roots, beside the tip-claim stake cases"
      },
      {
        "path": "tests/cli.test.ts",
        "description": "two relocated roots in one checkout, the second tick's refusal naming the first's root"
      }
    ]
  },
  "summary": "The tip claim states pid and instant only, so a second effort in one checkout under its own FLUME_DIR refuses as a busy tip, naming neither state root",
  "per": {
    "path": "spec/loop.md",
    "section": "The loop lock and the tip claim"
  },
  "acceptance": "the tip claim's statement carries the state root its holder resolved on a third line, and a live-holder refusal names that root beside the root the refused effort resolved, the ref and the claim path, so two efforts in one checkout read as two state roots on one tip rather than as a busy tip; a reader that needs only liveness reads the first line unchanged",
  "tests": [
    "the tip claim states the state root its holder resolved on its third line",
    "a second tick in one checkout under its own state root refuses naming both the root that holds the tip and the root it resolved"
  ],
  "notes": "Ruled 76262932, closing the two-relocated-roots question on none of its forks \u2014 a checkout has one tip. Verified: `renderPidClaim` (`src/pidClaim.ts`) writes two lines; `TipClaimHeldError` (`src/git.ts:1117`) names ref, pid and path only, and both CLI sites print `err.message` alone (`src/cli.ts:1242`, `:1478`), so the refused root is unnamed too. `flumeDir` is in scope at both calls. Seams `src/cli.ts` with THE-SUPERVISOR-CLAIM-HANDOFF-IS-DECODED-ONCE."
}
