{
  "tag": "THE-TICK-BUDGET-DEFAULT-HAS-ONE-HOME",
  "gate": {
    "kind": "open"
  },
  "priority": 0,
  "files": {
    "edit": [
      {
        "path": "src/cli.ts",
        "description": "`loop`'s `--max` default reads `DEFAULT_TICK_BUDGET` rather than its own literal"
      },
      {
        "path": "src/cliHelp.ts",
        "description": "the top-level page's loop cap is interpolated from that constant, as the loop page's own line already is"
      },
      {
        "path": "tests/docComments.test.ts",
        "description": "the literal scan over both sites, beside the `abortThreshold` one it copies"
      }
    ]
  },
  "summary": "`flume loop`'s default cap and the top-level help page each spell 50 beside `DEFAULT_TICK_BUDGET`, whose own doc claims one home \u2014 the constant decides nothing",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Derived state is computed, never restated beside its source"
  },
  "acceptance": "the value `flume loop` caps a run at and every help page stating that cap both read `DEFAULT_TICK_BUDGET`, so moving the constant moves the behavior and both pages together",
  "tests": [
    "the loop verb and the top-level help page restate no DEFAULT_TICK_BUDGET literal"
  ],
  "notes": "Verified this tick: `src/cli.ts:1160` is `let max = 50` and always passes `tickBudget: max`, so `DEFAULT_TICK_BUDGET` (`src/loopSupervisor.ts:64`) is unreachable from the CLI; `src/cliHelp.ts:81` spells 50 while `:211` interpolates it. Move the constant and the loop page moves alone, against a cap and a top-level page that did not. `tests/docComments.test.ts:106` is the existing arm for the sibling default. Seams `src/cliHelp.ts` with THE-TICK-EXIT-CODE-CAUSE-IS-LABELLED-AT-ITS-ARM."
}
