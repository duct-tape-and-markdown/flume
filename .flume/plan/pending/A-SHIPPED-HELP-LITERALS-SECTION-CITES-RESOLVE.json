{
  "tag": "A-SHIPPED-HELP-LITERALS-SECTION-CITES-RESOLVE",
  "gate": {
    "kind": "open"
  },
  "files": {
    "edit": [
      {
        "path": "tests/helpers/commentCitations.ts",
        "description": "the section-cite reader runs over the shipped help literals beside the comment runs it already reads"
      },
      {
        "path": "tests/commentCitations.test.ts",
        "description": "the arm's cases, and the repo claim over the literal set with its own vacuity guard"
      },
      {
        "path": "src/cliHelp.ts",
        "description": "the three cites spelled in the one grammar; the abbreviated one repointed at its page's whole heading"
      }
    ]
  },
  "summary": "The cites a shipped help literal states are read by no arm, and one already names a section its page does not carry",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Narration is the ladder's bottom rung"
  },
  "acceptance": "every section cite a shipped help literal states resolves against the page it names, through the reader that already resolves a comment's; an abbreviated half reds there as it does in a comment",
  "tests": [
    "a section cite in a shipped help literal naming a section its page does not carry is a finding",
    "every section a shipped help literal cites is a section its page still carries"
  ],
  "notes": "Per note A-QUOTED-SECTION-CITE-RESOLVES-LIKE-AN-ITALICIZED-ONE. Verified: the scan reads `commentLines(sf)` alone (`tests/helpers/commentCitations.ts:957`), so `src/cliHelp.ts:221`, `:262`, `:416` are unread. `:221` cites \"Graceful stop\"; the heading is `Graceful stop — the stop flag` and no lead spells the short form — the abbreviation arm (`tests/commentCitations.test.ts:1229`) would red it. `:221`/`:262` also want the pair's comma and parens."
}
