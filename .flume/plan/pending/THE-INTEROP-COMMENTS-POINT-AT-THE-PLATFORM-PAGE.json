{
  "tag": "THE-INTEROP-COMMENTS-POINT-AT-THE-PLATFORM-PAGE",
  "gate": { "kind": "open" },
  "files": {
    "edit": [
      {
        "path": "src/chainLoad.ts",
        "description": ":291's unconditional export-shape cause becomes a pointer at the page that now owns the fact"
      },
      {
        "path": "harness/init.ts",
        "description": ":247's copy of the same cause becomes the same pointer"
      }
    ]
  },
  "summary": "Two code comments state tsx's interop shape as a property of a module's own exports; the ruling homed that fact on a page and the measurement makes the comments false",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Derived state is computed, never restated beside its source"
  },
  "acceptance": "neither site states the interop cause itself — each points at the page section that owns it, in the backticked page-and-section form the citation pin resolves — and the normalization's own reason at the loader survives the shrink",
  "notes": "Ruled 91a361eb, option 1: `.claude/rules/platform-facts.md` § *tsx decides a module's interop shape from its whole import graph* now owns it. Verified this drain: both comments stand and both assert the export shape as the cause. No property claimed — the citation pin holds each pointer once the span is backticked. Seams `src/chainLoad.ts` with THE-SECTION-CITE-READER-TAKES-THE-PAREN-LESS-SPELLING, serializing the two."
}
