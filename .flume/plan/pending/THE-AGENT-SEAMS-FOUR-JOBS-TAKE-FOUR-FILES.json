{
  "tag": "THE-AGENT-SEAMS-FOUR-JOBS-TAKE-FOUR-FILES",
  "gate": { "kind": "open" },
  "files": {
    "new": [
      {
        "path": "src/claudeCode.ts",
        "description": "the provider: `ClaudeCodeOptions`, `claudeCode`, `budgetSettings`, `abortError`, `combineSignals`, `extractFinalMessage`"
      },
      {
        "path": "src/sessionCapture.ts",
        "description": "the capture decorator: `SessionCaptureOpts`, `withSessionCapture`, `attachCause`, `defaultCaptureFilename`"
      },
      {
        "path": "src/terminalRender.ts",
        "description": "the stream-json renderer: `TerminalRendererOpts`, `withTerminalRenderer`, `readStreamJsonLine` through `truncate`, and the provider tool-name table `summarizeToolArg` holds"
      }
    ],
    "edit": [
      {
        "path": "src/Agent.ts",
        "description": "the provider-agnostic seam alone — `Agent`, `AgentInvocation`, `AgentResult`, `AgentUsage` — and a header that states it"
      },
      { "path": "src/index.ts", "description": "the moved exports come from their new homes" },
      { "path": "src/flumeApi.ts", "description": "the three value imports repoint" },
      { "path": "src/cli.ts", "description": "the `claudeCode` import repoints" },
      { "path": "src/processTree.ts", "description": "three cites naming the agent's starter" },
      { "path": "src/spawnShim.ts", "description": "the cite naming the shim's caller" },
      { "path": "src/budgetHook.ts", "description": "the cite naming the settings composer" },
      { "path": "src/Prompt.ts", "description": "the cite naming `extractFinalMessage`" },
      { "path": "src/priorAttempts.ts", "description": "the cite naming `extractFinalMessage`" },
      { "path": "src/tickAttempt.ts", "description": "the cite naming the abort's own shape" },
      { "path": "tests/Agent.test.ts", "description": "the cases follow the jobs they exercise" },
      { "path": "tests/agentBudget.test.ts", "description": "the provider import and its cites repoint" },
      { "path": "tests/harnessChain.test.ts", "description": "the `ClaudeCodeOptions` import repoints" },
      { "path": "tests/Dispatcher.test.ts", "description": "the renderer/provider imports and the source-path constant repoint" },
      { "path": "tests/cli.test.ts", "description": "the source-path constant repoints" },
      { "path": "tests/spawnShim.test.ts", "description": "the cite naming the provider" },
      { "path": "tests/budgetHook.test.ts", "description": "the cite naming the settings composer" },
      { "path": "docs/CHAIN-AUTHORING.md", "description": "the engine-file listing names the new homes" }
    ]
  },
  "summary": "`src/Agent.ts` carries four jobs under three dividers — the seam, the `claudeCode` provider, the capture decorator, and a stream-json renderer holding a provider tool-name table",
  "per": {
    "path": ".claude/rules/posture-sweep.md",
    "section": "Routing"
  },
  "acceptance": "each of the four jobs the dividers already spell lives in the file its name is, `src/Agent.ts`'s header claims exactly the seam it then holds, every cite naming a fact that moved names its new home, and the package's exported surface is unchanged in what it offers and from where `src/index.ts` offers it",
  "notes": "Fourth body of this family: eeeb7dcc, 4854eca1, 73a9228f, this one — past the three the routing bar files at. Verified on disk this tick: dividers at `src/Agent.ts:179`, `:444`, `:537`; `claudeCode` `:251`, `withSessionCapture` `:481`, `withTerminalRenderer` `:573`, `summarizeToolArg` `:734`. Behavior-free: held by the typecheck, the export pin and the citation pin. Claims no property; wants a wave of its own."
}
