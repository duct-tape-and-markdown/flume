{
  "tag": "THE-AUTHORING-PAGE-PAIRS-WRITE-SCOPING-WITH-ITS-CHANNEL",
  "gate": { "kind": "open" },
  "priority": 30,
  "files": {
    "edit": [
      {
        "path": "docs/CHAIN-AUTHORING.md",
        "description": "the pairing and its two measured sides, beside the phase table's two rows for the fields"
      }
    ]
  },
  "summary": "The authoring page describes both write-scoping fields and never that a phase writing shared files pairs them, so a consumer meets the fence as parks instead",
  "per": {
    "path": "spec/pending.md",
    "section": "The entry-scoped write guard is opt-in, and off by default"
  },
  "acceptance": "the authoring page states beside those two fields that a phase which legitimately writes shared files pairs `scopeWritesToEntry` with `entryChannelPaths`, and carries both measured sides of the trade — the reporter's three of ten conflicts on paths no entry declared, and this repo's own first-batch width under the flag — so a consumer weighs it with both rather than one",
  "notes": "Ruled (b) on the build-phase write-scoping question, field report item 15. Verified: `docs/CHAIN-AUTHORING.md:524`-`:525` describe each field alone and `:305` reaches for it only against a sidecar fence; this repo declares `false` (`.flume/declaration.ts:61`). No property: the figures are measurements, not a pinnable claim. Seams the page with THE-AUTHORING-PAGE-PRICES-A-WAVE-IN-MEMORY (in flight) and THE-VERDICT-CARRIES-A-TIMING-PER-GATE-RUN-AND-MERGE."
}
