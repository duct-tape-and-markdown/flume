{
  "tag": "THE-BATON-CONSTRUCTION-REFUSES-AN-OBSTRUCTED-ROOT",
  "gate": { "kind": "open" },
  "priority": 20,
  "files": {
    "edit": [
      {
        "path": "src/cli.ts",
        "description": "the obstructed-root refusal is taken once at the baton construction seam, and `status`'s own copy folds into it"
      },
      {
        "path": "src/cliHelp.ts",
        "description": "the `wake` and `sleep` pages stop calling bay discovery their only file read"
      },
      {
        "path": "docs/CLI.md",
        "description": "the same two sections' 74 clause, which the doc pin reads against the pages"
      },
      {
        "path": "tests/cli.test.ts",
        "description": "both verbs' refusals, beside the status one"
      },
      {
        "path": "tests/cliHelp.test.ts",
        "description": "the two widened rows, through the readers already scoped to one code"
      }
    ]
  },
  "summary": "`wake` and `sleep` mkdir `<flumeDir>/awake` unguarded, so an obstructed state root exits 1 on a raw stack under pages whose 74 row calls discovery their only file read",
  "per": {
    "path": "spec/loop.md",
    "section": "Exit codes — the run never lies to CI"
  },
  "acceptance": "the obstructed-state-root refusal is taken once at the baton construction seam rather than copied per verb, so `wake` and `sleep` answer `EX_IOERR` naming the root they resolved, and every page stating their exit range names that cause",
  "tests": [
    "flume wake refuses an obstructed state root with EX_IOERR naming the resolved root",
    "flume sleep refuses an obstructed state root with EX_IOERR naming the resolved root"
  ],
  "notes": "Off note THE-STATUS-VERB-REFUSES-AN-OBSTRUCTED-STATE-ROOT, item 1. Verified: `new Baton(flumeDir)` is unguarded at `src/cli.ts:657` and `:674`; `bayDiscoveryRow` (`src/cliHelp.ts:62`) states 74 is \"this verb's only ones\" for both, and `docs/CLI.md:69`, `:77` say the same. An obstructed root stats clean at discovery and fails the mkdir after it. One guard at the seam, not five at the callers (`engineering.md`, *The fix lands at the mechanism*)."
}
