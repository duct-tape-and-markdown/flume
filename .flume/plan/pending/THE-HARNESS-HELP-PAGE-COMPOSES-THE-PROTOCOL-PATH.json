{
  "tag": "THE-HARNESS-HELP-PAGE-COMPOSES-THE-PROTOCOL-PATH",
  "gate": {
    "kind": "open"
  },
  "files": {
    "edit": [
      {
        "path": "harness/cliHelp.ts",
        "description": "the protocol page the `init` entry names is composed from the layout, as the state root beside it already is"
      },
      {
        "path": "tests/commentCitations.test.ts",
        "description": "the shipped page's protocol path read against the layout's own composer, beside the rendered-surface arms"
      }
    ]
  },
  "summary": "The harness help page hand-spells `PROTOCOL.md` beside the constant it interpolates, and the `*.md` scan over it resolves against this repo's own page rather than the verb's",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Derived state is computed, never restated beside its source"
  },
  "acceptance": "the protocol path `flume-harness --help` states is the one `protocolPath` composes over the default state root, read off the layout rather than spelled, and a rename of that page reds the shipped page with the verb that writes it",
  "pins": [
    "the flume-harness help page states the protocol path the layout composes"
  ],
  "notes": "`harness/cliHelp.ts:28` is `${DEFAULT_STATE_ROOT}/PROTOCOL.md` \u2014 a hand join beside `PROTOCOL_REL` (`harness/layout.ts:114`) and `protocolPath` (`:298`), the one home 8b40e4e6 created for this drift, whose doc says a page named at two paths resolves nowhere. The rendered `*.md` scan resolves `.flume/PROTOCOL.md` (`tests/helpers/commentCitations.ts:1118`) against this repo's own page, which no `PROTOCOL_REL` rename touches \u2014 green over a page advertising a path `init` stopped writing."
}
