{
  "tag": "THE-HARNESS-HELP-PAGE-IS-READ-BY-THE-SHIPPED-SCAN",
  "gate": { "kind": "open" },
  "files": {
    "new": [
      {
        "path": "harness/cliHelp.ts",
        "description": "the `flume-harness` help page — the job `src/cliHelp.ts` holds for the engine's verbs"
      }
    ],
    "edit": [
      {
        "path": "harness/cli.ts",
        "description": "the argv half reads its page from that module and keeps the entrypoint's job alone"
      },
      {
        "path": "tests/helpers/shippedHelp.ts",
        "description": "the harness page joins the shipped surfaces; the doc's standing exclusion of it goes"
      },
      {
        "path": "tests/commentCitations.test.ts",
        "description": "the repo help scan over the widened surface set"
      }
    ]
  },
  "summary": "`harness/cli.ts` spells its help page beside a `main` it runs at import, so the shipped page an adopter reads first is the one surface the cite scan cannot import to read",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "A module is one job"
  },
  "acceptance": "the page `flume-harness --help` prints is read off the module that holds it, by the same scan that reads the engine's pages, with the entrypoint's import side effect out of the way",
  "tests": [
    "the shipped help surfaces include the page flume-harness --help prints"
  ],
  "notes": "Per note A-SHIPPED-HELP-LITERALS-SECTION-CITES-RESOLVE, class 3, verified this drain: `HELP` (`harness/cli.ts:23`) is module-local and the file closes on a top-level `process.exitCode = await main(...)`, so nothing may import it — `tests/helpers/shippedHelp.ts` says exactly that in its own doc. `src/cliHelp.ts` beside `src/cli.ts` is the precedent split. The page states no cite today, so the arm reds nothing on landing; what it buys is the next one."
}
