{
  "tag": "THE-SECOND-STATE-ROOT-IN-A-CHECKOUT-REFUSES-EARLY",
  "gate": {
    "kind": "open"
  },
  "priority": 30,
  "files": {
    "edit": [
      {
        "path": "src/cliStateDirs.ts",
        "description": "the second-root refusal, beside the cross-repo one this function already carries, off the bay probe it already makes"
      },
      {
        "path": "src/cli.ts",
        "description": "the refusal mapped at the resolution seam, with its sibling's"
      },
      {
        "path": "tests/cliStateDirs.test.ts",
        "description": "the refusal and the agreeing root that resolves unchanged"
      },
      {
        "path": "tests/cli.test.ts",
        "description": "the repro: a second root's singleton tick, refused before provisioning"
      }
    ]
  },
  "summary": "A second state root in one checkout is permitted at resolution and collides several steps on as git's error over a branch the first root's tick holds",
  "per": {
    "path": "spec/jobs.md",
    "section": "The checkout is the unit of isolation"
  },
  "acceptance": "a state root resolved in a checkout whose own flume state names a different one is refused where the roots resolve — naming both roots and the checkout, in the usage-shaped family that function's cross-repo refusal already takes — before anything is provisioned, and a root agreeing with the checkout's resolves unchanged",
  "tests": [
    "a state root resolved in a checkout holding a different flume state root is refused naming both roots",
    "a second state root's tick refuses at resolution rather than at worktree provisioning"
  ],
  "notes": "Ruled (a) on the two-roots-in-one-clone question, field report item 4. Verified: `resolveStateDirs` (`src/cliStateDirs.ts:101`) carries one refusal today — cross-repo, mapped at `src/cli.ts:389` — and reads no disk; the decidable evidence is the probe bay discovery already makes (`resolveRepoRoot`, `:61`). The ref-namespace half of the same field report item is THE-TICK-BRANCH-AND-ENTRY-CLAIM-CARRY-THE-CHECKOUT; disjoint files, no seam."
}
