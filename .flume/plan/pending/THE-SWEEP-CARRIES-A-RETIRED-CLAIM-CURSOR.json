{
  "tag": "THE-SWEEP-CARRIES-A-RETIRED-CLAIM-CURSOR",
  "gate": {
    "kind": "open"
  },
  "dependsOnForks": [],
  "priority": 0,
  "files": {
    "new": [],
    "edit": [
      {
        "path": "harness/planState.ts",
        "description": "`retiredThrough` on the sweep state, with its may-move rule in the slice-state table"
      },
      {
        "path": "harness/sweepWindow.ts",
        "description": "the retired-claim delta is drawn from that cursor, and the window names the sha a searching tick may advance it to"
      },
      {
        "path": "harness/prompts/plan-sweep.md",
        "description": "advancing the cursor as the tick searches, beside the stamp's own rule"
      },
      {
        "path": "tests/harnessPlanState.test.ts",
        "description": "the field and its rule"
      },
      {
        "path": "tests/harnessWindows.test.ts",
        "description": "the delta drawn from the cursor, and from the stamp where none is written"
      }
    ],
    "retire": []
  },
  "summary": "The retired-claim delta re-renders every line the locus deleted since the stamp on every tick of a rotation, because nothing records that a tick searched them",
  "per": {
    "path": "spec/harness.md",
    "section": "Plan state as declared state"
  },
  "acceptance": "the sweep window draws the retired-claim delta from the retired-claim cursor, a tick that searched advances it while the stamp stays put, and a claim is searched once per rotation rather than once per tick",
  "tests": [
    "the retired-claim delta renders the lines deleted since the retired-claim cursor",
    "a sweep state carrying no retired-claim cursor draws the delta from the stamp"
  ],
  "pins": [],
  "notes": "Ruled off the retired-claim-cursor record, fork (1). Absent reads as the stamp: only the sweep slice rewrites its state, so a required field refuses its own window. Measured this drain: `AnyCursorField` (`harness/planState.ts`) admits string-valued fields alone and refuses an optional one, so joining `CURSORS` means widening it — probed to admit exactly this field. The rule joins `SLICE_STATE_RULES`; the gate and its tests need no edit. 852 deleted lines against a 1200-line budget."
}
