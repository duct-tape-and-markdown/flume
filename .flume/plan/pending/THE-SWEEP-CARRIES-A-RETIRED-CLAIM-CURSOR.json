{
  "tag": "THE-SWEEP-CARRIES-A-RETIRED-CLAIM-CURSOR",
  "gate": {
    "kind": "open"
  },
  "dependsOnForks": [],
  "priority": 30,
  "files": {
    "new": [],
    "edit": [
      {
        "path": "harness/planState.ts",
        "description": "`retiredThrough` on the sweep state, with its may-move rule in the slice-state table"
      },
      {
        "path": "harness/sweepWindow.ts",
        "description": "the retired-claim delta is drawn from that cursor, and the window names an advance sha only where the delta rendered whole"
      },
      {
        "path": "harness/prompts/plan-sweep.md",
        "description": "advancing the cursor to the sha the window named as it searches, beside the stamp's own rule"
      },
      {
        "path": "tests/harnessPlanState.test.ts",
        "description": "the field and its rule"
      },
      {
        "path": "tests/harnessWindows.test.ts",
        "description": "the delta drawn from the cursor, from the stamp where none is written, and the truncated render naming no advance sha"
      }
    ],
    "retire": []
  },
  "summary": "The retired-claim delta re-renders every line the locus deleted since the stamp on every tick of a rotation, because nothing records that a tick searched them",
  "per": {
    "path": "spec/harness.md",
    "section": "Plan state as declared state"
  },
  "acceptance": "the sweep window draws the retired-claim delta from the retired-claim cursor and names an advance sha only where the whole delta rendered, so a tick that searched advances it while the stamp stays put and a claim is searched once per rotation rather than once per tick",
  "tests": [
    "the retired-claim delta renders the lines deleted since the retired-claim cursor",
    "a sweep state carrying no retired-claim cursor draws the delta from the stamp",
    "a retired-claim delta cut by the tick's budget names no cursor advance sha"
  ],
  "pins": [],
  "notes": "Fork (1) of the retired-claim-cursor record; the all-or-nothing advance off THE-RETIRED-CLAIM-DELTA-NAMES-THE-PAGE-IT-LEFT — `retiredBlock` (`harness/sweepWindow.ts:250`) cuts mid-page, so no sha follows a prefix. Absent reads as the stamp: only the sweep slice rewrites its state, so a required field refuses its own window. `AnyCursorField` (`harness/planState.ts`) takes string fields alone, so joining `CURSORS` widens it — probed. The rule joins `SLICE_STATE_RULES`; the gate needs no edit."
}
