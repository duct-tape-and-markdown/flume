{
  "tag": "THE-HARNESS-READERS-PROVE-THEIR-OWN-ABSENCE",
  "gate": { "kind": "open" },
  "files": {
    "edit": [
      {
        "path": "harness/planState.ts",
        "description": "`readPlanState` proves the descent from the state root it holds before reading an absent state file as no state yet"
      },
      {
        "path": "harness/prompts.ts",
        "description": "`inTree`'s reader proves the descent from the cwd it closed over before reading an absent artifact as none"
      },
      {
        "path": "tests/harnessPlanState.test.ts",
        "description": "the state read's absence arm over an obstructed root"
      },
      {
        "path": "tests/harnessPrompts.test.ts",
        "description": "a span's absence arm over an obstructed ancestor"
      }
    ]
  },
  "summary": "`readPlanState` and the prompt's tree reader silence ENOENT off one stat, so an unreachable state file re-derives a whole corpus and an unreachable artifact renders as an empty block",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Loud or nothing"
  },
  "acceptance": "both harness readers prove the descent from the root they already hold — one call to the shipped composer each — so an unreachable plan state refuses instead of reading as unwritten, and an unreachable artifact refuses instead of rendering its empty placeholder",
  "pins": [
    "readPlanState refuses a state root that is present and is not a directory",
    "a prompt span refuses an artifact whose ancestor is present and is not a directory"
  ],
  "notes": "Verified this tick: `readPlanState` (`harness/planState.ts:310`) silences ENOENT and `onDisk` composes the path from the `stateRoot` it was handed; `inTree` (`harness/prompts.ts:589`) does the same under its `cwd`. `isDirectoryOrAbsentUnder` (`src/fsProbe.ts:135`) is the composer `listUnderStateRoot` already adopted. No `tests[]`: posix ENOTDIR already throws through both, so the `windows` lane is what the pins protect."
}
