{
  "tag": "THE-CLI-PAGE-IS-PINNED-PER-EXIT-CODE-ARM",
  "gate": {
    "kind": "open"
  },
  "priority": 30,
  "files": {
    "edit": [
      {
        "path": "tests/cliHelp.test.ts",
        "description": "`sentencesNamingExitCode`'s one-code window read against the arm's own label, per code"
      },
      {
        "path": "docs/CLI.md",
        "description": "an arm whose sentence the pin finds does not carry its phrase"
      }
    ]
  },
  "summary": "`docs/CLI.md` § `flume tick` states a cause per code that nothing reads back — the standing pin compares the set of codes and nothing else",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Derived state is computed, never restated beside its source"
  },
  "acceptance": "each `flume tick` exit code's sentences on the page are read against that arm's labelled cause, and each verb section's copy of the shared state-root cause is read against the row the help block renders for it, through the reader already scoped to one code, with the page keeping its own one-flowing-sentence-per-verb register rather than the help block's clauses spliced in",
  "pins": [
    "docs/CLI.md's flume tick section states each exit code's cause under the phrase its arm is labelled with",
    "each docs/CLI.md verb section states the shared state-root exit-code cause the help block renders for that verb"
  ],
  "notes": "Second half of 46446dc9: pinned against the producer's map, never the help text — two prose copies agree while both are wrong. `tickExitCauses` (`src/cliVerdict.ts:139`) is the source each sentence carries; CLI-DOC-TICK-EXIT-CODES-PINNED (`tests/cliHelp.test.ts:556`) pins the code set alone. Widened off note THE-OBSTRUCTED-STATE-ROOT-REFUSES-AT-ONE-SEAM: `SHARED_ROOT_LINES` renders the shared `74` cause once, and the page hand-copies it in wake, sleep, stop, render and a status variant."
}
