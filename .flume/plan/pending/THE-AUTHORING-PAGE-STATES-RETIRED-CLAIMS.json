{
  "tag": "THE-AUTHORING-PAGE-STATES-RETIRED-CLAIMS",
  "gate": { "kind": "open" },
  "files": {
    "edit": [
      {
        "path": "docs/CHAIN-AUTHORING.md",
        "description": "§10 names `parsePending` (3 sites) and `composePendingList`, neither exported; the `humanOnly` paragraph says cascade has two phases"
      },
      {
        "path": "examples/cascade-chain.ts",
        "description": "the `humanOnly: []` comment says \"both phases\"; the chain declares four"
      },
      {
        "path": "tests/examples.test.ts",
        "description": "the \"entry phase is machine-wakeable\" doc comment rests on an entry-point-by-position claim the cited section no longer makes; re-site it onto what the pin buys"
      }
    ]
  },
  "summary": "The authoring page and two of its citers assert exports and phase counts the tree no longer has",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Narration is the ladder's bottom rung"
  },
  "acceptance": "no in-repo prose names `parsePending` or `composePendingList`; the cascade phase count reads as declared; the examples-scan doc comment states the property its pin actually holds",
  "notes": "Regression cannot be pinned decidably without widening the citation resolver to identifiers on docs/ pages — the named exception, and the open question `the-citation-pin-does-not-reach-docs-identifiers.md` carries the fork. The retired claims come from THE-LEDGER-IS-A-DIRECTORY (the export rename) and THE-CASCADE-EXAMPLE-DECLARES-BUILD-FIRST (the paragraph rewrite that stranded its citers)."
}
