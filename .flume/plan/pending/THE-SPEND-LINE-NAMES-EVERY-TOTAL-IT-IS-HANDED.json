{
  "tag": "THE-SPEND-LINE-NAMES-EVERY-TOTAL-IT-IS-HANDED",
  "gate": {
    "kind": "open"
  },
  "files": {
    "edit": [
      {
        "path": "src/cliVerdict.ts",
        "description": "the paragraph claiming every total is named shrinks to a pointer at the pin that holds it"
      },
      {
        "path": "tests/cliVerdict.test.ts",
        "description": "the pin: a real `totalAgentUsageByPhase` fold drives the real spend line, and every total key that fold produced is named in what it rendered"
      }
    ]
  },
  "summary": "`phaseUsageSegment` hand-spells the eight totals it prints while the fold producing them is compile-exhaustive, so a ninth is summed and silently dropped",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Narration is the ladder's bottom rung"
  },
  "acceptance": "every key a real `totalAgentUsageByPhase` fold puts on a `PhaseAgentUsage` is named in the line `agentUsageLine` renders from it, read off the producer's own output rather than a hand-written roster, and the prose that carried the property points at the pin instead",
  "pins": [
    "the agent-usage spend line names every total totalAgentUsageByPhase puts on a phase's row"
  ],
  "notes": "Probed this tick: a `number` field on `AgentUsage` (`src/Agent.ts:94`) reds `src/tickVerdict.ts:470` and `:485` — `SummableUsageKey` (`:433`) makes the fold exhaustive — and leaves `phaseUsageSegment` (`src/cliVerdict.ts:161`) green, so the total is summed onto `PhaseAgentUsage` and never printed. `:155` claims the opposite. The standing case (`tests/loopSupervisor.test.ts:2282`) is a hand-written string over a hand-written fixture. `agentUsageLine` serves both surfaces (`src/cli.ts:624`)."
}
