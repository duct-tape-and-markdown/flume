{
  "tag": "THE-RETIRED-CLAIM-DELTA-NAMES-THE-PAGE-IT-LEFT",
  "gate": { "kind": "open" },
  "files": {
    "edit": [
      {
        "path": "harness/gitRange.ts",
        "description": "`deletedLines` keeps the path git's own file header named, beside the line it deleted"
      },
      {
        "path": "harness/sweepWindow.ts",
        "description": "the retired-claim delta renders its lines under the page each left"
      },
      {
        "path": "tests/harnessWindows.test.ts",
        "description": "the delta over two locus pages, each line under its own"
      }
    ]
  },
  "summary": "The retired-claim delta renders a flat list of deleted lines, so a tick cannot tell which locus page each left — or a reworded claim from a retired one",
  "per": {
    "path": ".claude/rules/posture-sweep.md",
    "section": "The frontier is decidable; the neighborhood is judged"
  },
  "acceptance": "every line the retired-claim delta renders names the locus page it was deleted from, so the frontier a tick searches is decidable from the window rather than from a second diff the tick runs itself",
  "tests": [
    "the retired-claim delta names the locus page each deleted line left"
  ],
  "notes": "Verified this tick: `deletedLines` (`harness/gitRange.ts:277`) filters the range diff to lines starting with `DELETED_MARK` (`:291`), dropping every `diff --git` and `+++` header the same diff carried. This tick's own window shows it — a flat block, no page named — and this tick spent a `git diff` over the locus to learn that one deletion was a heading rename, not a retired claim. Seams both files with THE-SWEEP-CARRIES-A-RETIRED-CLAIM-CURSOR."
}
