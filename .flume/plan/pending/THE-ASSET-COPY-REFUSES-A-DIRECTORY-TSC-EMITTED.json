{
  "tag": "THE-ASSET-COPY-REFUSES-A-DIRECTORY-TSC-EMITTED",
  "gate": { "kind": "open" },
  "files": {
    "edit": [
      {
        "path": "scripts/pack-harness-assets.mjs",
        "description": "a candidate directory carrying a TypeScript module refuses the copy naming it, instead of standing in for package content"
      },
      {
        "path": "tests/harnessPackaging.test.ts",
        "description": "the refusal, driven over the same emit the prompt and template cases already build"
      }
    ]
  },
  "summary": "The asset copy reads every directory under `harness/` as package content and rm -rf's the emit beneath it, so a subdirectory of modules would publish sources over the .js tsc wrote",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Loud or nothing"
  },
  "acceptance": "a `harness/` subdirectory carrying a TypeScript module refuses the asset copy naming that directory, rather than removing the emit tsc wrote at the same path and copying sources over it",
  "tests": [
    "the asset copy refuses a harness subdirectory carrying a TypeScript module"
  ],
  "notes": "Verified this tick: `assetDirs` (`scripts/pack-harness-assets.mjs:100`) is every directory under `harness/`, and `:118` removes `dist/harness/<name>` recursively before copying sources in. The doc at `:13`-`:19` states the precondition — every directory there is content tsc does not emit — as prose alone. `harness/` holds prompts/ and templates/ today, no `.ts` under either; the packaging suite pins the prompts and the PROTOCOL template, never the classification."
}
