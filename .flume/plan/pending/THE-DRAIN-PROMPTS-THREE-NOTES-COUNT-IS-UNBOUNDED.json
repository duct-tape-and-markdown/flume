{
  "tag": "THE-DRAIN-PROMPTS-THREE-NOTES-COUNT-IS-UNBOUNDED",
  "gate": { "kind": "open" },
  "files": {
    "edit": [
      {
        "path": "harness/prompts/plan-inbox.md",
        "description": "the three-notes bar carries the count the spec rules, authoring no window of its own"
      }
    ]
  },
  "summary": "The drain prompt counts the three-notes bar over \"this slice's own\" commit bodies — a window it authored, narrower than the unbounded count the spec now rules",
  "per": { "path": "spec/harness.md", "section": "The phases" },
  "acceptance": "the drain prompt's three-notes bar states the count the spec rules — every plan commit body, unbounded, and why it self-terminates — and authors no window of its own",
  "notes": "Ruled c684223f, the question's fork (a). The prompt cannot point at that sentence by path: `spec/` is off the package's `files` allowlist, so a consumer's prompt would name a page that does not resolve — the bar stays spelled in the prompt and only the window changes. The line is `harness/prompts/plan-inbox.md:62`. No property: this prose ships to consumers, and this section's carve-out keeps harness prose out of the suite. Seams the file with THE-QUEUE-LISTING-MARKS-THE-ENTRIES-IN-FLIGHT."
}
