{
  "tag": "THE-SHIPPED-HELP-PAGE-NAMES-A-PAGE-THAT-RESOLVES",
  "gate": {
    "kind": "open"
  },
  "files": {
    "edit": [
      {
        "path": "tests/helpers/commentCitations.ts",
        "description": "the rendered-surface scan reads the page-name arm beside the section arm, and the paragraph declaring the section arm as all that reaches a literal"
      },
      {
        "path": "tests/helpers/shippedHelp.ts",
        "description": "the shipped-surface reader answers the `*.md` assets packed beside the emit too, derived the way the packer derives them rather than listed"
      },
      {
        "path": "tests/commentCitations.test.ts",
        "description": "the arm over every shipped page, non-vacuous in the names it scanned"
      },
      {
        "path": "harness/cliHelp.ts",
        "description": "one red — the protocol file named at no path the tree holds"
      },
      {
        "path": "harness/prompts/plan-derive.md",
        "description": "the same red in a packed asset, in this page's entry-shape cite"
      },
      {
        "path": "harness/prompts/plan-discipline.md",
        "description": "the same red in a packed asset, in this page's opening pointer"
      }
    ]
  },
  "summary": "A `*.md` name inside a page the package ships is resolved by nothing, and three of them name a bare `PROTOCOL.md` at no path the tree holds",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Narration is the ladder's bottom rung"
  },
  "acceptance": "every `*.md` page name a page the package ships states — the pages the bins print and the assets packed beside the emit — is resolved on disk through the reader the comment scan already uses, and each red names the protocol file at the path init writes it to",
  "tests": [
    "every *.md page name a page the package ships states resolves on disk"
  ],
  "notes": "Off note THE-HARNESS-HELP-PAGE-IS-READ-BY-THE-SHIPPED-SCAN item 2: the carve-out files an unresolved class once a drift in it is measured. Widened off the 95ed6b52 revert. Measured: `shippedHelpPages()` names four, three resolve, `PROTOCOL.md` reds (`harness/cliHelp.ts:28`); the packed assets add `plan-derive.md:40` and `plan-discipline.md:3`, both bare. Init writes it under the state root (`PROTOCOL_REL`). Asset set derived, not listed (`scripts/pack-harness-assets.mjs`)."
}
