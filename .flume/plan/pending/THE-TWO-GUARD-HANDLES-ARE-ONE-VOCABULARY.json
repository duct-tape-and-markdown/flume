{
  "tag": "THE-TWO-GUARD-HANDLES-ARE-ONE-VOCABULARY",
  "gate": { "kind": "open" },
  "files": {
    "edit": [
      {
        "path": "src/pidClaim.ts",
        "description": "the one handle type every guard returns, beside the drop that makes it true"
      },
      {
        "path": "src/waitLock.ts",
        "description": "keeps only the wait-and-reclaim loop; its acquire returns the shared handle"
      },
      {
        "path": "src/git.ts",
        "description": "the two composed wait locks and the tip claim name the one type"
      },
      {
        "path": "src/waveTick.ts",
        "description": "the staked list's element type"
      },
      {
        "path": "src/cli.ts",
        "description": "the two paired citations the rename strands"
      }
    ]
  },
  "summary": "`WaitLock` (`src/waitLock.ts`) and `StakedPidClaim` (`src/pidClaim.ts`) are one vocabulary spelled twice — a readonly path and a release, both the guard file this process created",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "A module is one job"
  },
  "acceptance": "one handle type in `src/pidClaim.ts` beside the drop that makes it true; `src/waitLock.ts` holds only the wait-and-reclaim loop, and every site that named either spelling names the one",
  "notes": "Per note A-WAIT-LOCK-RELEASE-DROPS-AT-MOST-ONCE. Both already return `atMostOnceDrop` as their release, so the shapes agree in meaning, not just in fields. The name must not read as a stake — the wait locks do not build on `stakePidClaim`, and `src/pidClaim.ts`'s module doc says so. `src/cli.ts:1048` and `:1247` pair `StakedPidClaim` with its file, so a rename strands two citations the pin resolves; they move in the same commit. Verified: `src/entryClaims.ts` never names the type."
}
