{
  "tag": "AN-ELIDED-FOOTPRINT-STAYS-PLANS-TO-RESOLVE",
  "gate": {
    "kind": "blockedBy",
    "tags": ["A-CONTINUATION-ROUTES-BACK-TO-BUILD-NOT-THE-DRAIN"]
  },
  "files": {
    "edit": [
      {
        "path": "tests/harnessHandoff.test.ts",
        "description": "the classification over a not-shipped record whose path list was elided"
      }
    ]
  },
  "summary": "A not-shipped record's `touchedPaths` is bounded, so the put-down classifier reads a wide commit's footprint with the deciding path possibly missing",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Loud or nothing"
  },
  "acceptance": "the arm the blocker adds is pinned on a record carrying `omittedPaths`: an elided footprint decides no continuation and the record stays plan's to resolve",
  "pins": [
    "a not-shipped record whose touchedPaths was elided stays a standing refusal"
  ],
  "notes": "Per note A-CONTINUATION-KEEPS-ITS-ENTRY-IN-THE-QUEUE. `buildNotShipped` (`src/priorAttempts.ts`) slices to 200 paths and states `omittedPaths`, so the list the classifier reads may not carry the continuing note. The safe reading already falls out of the blocker's shape; nothing pins it. Claims no behavior — drop this if the blocker's tick pinned it already."
}
