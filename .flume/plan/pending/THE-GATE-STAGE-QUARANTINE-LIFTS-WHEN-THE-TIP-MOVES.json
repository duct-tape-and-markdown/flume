{
  "tag": "THE-GATE-STAGE-QUARANTINE-LIFTS-WHEN-THE-TIP-MOVES",
  "gate": { "kind": "open" },
  "files": {
    "edit": [
      {
        "path": "src/loopSupervisor.ts",
        "description": "the run-scoped hold records the stage that placed it and the tip it was placed at, and a gate-stage one is dropped before the next child's slug set is composed"
      },
      {
        "path": "tests/loopSupervisor.test.ts",
        "description": "the lift, and the provision-stage hold that does not lift"
      },
      {
        "path": "docs/CHAIN-AUTHORING.md",
        "description": "the per-tick chain-resolution passage names that a gate is read from each worktree at its base, so a gate fixed on trunk mid-run does not reach a tick already provisioned"
      }
    ]
  },
  "summary": "A quarantine placed by a gate outlives the tree that gate judged: a gate fixed on trunk mid-run still held four entries until an operator restarted the loop",
  "per": {
    "path": "spec/loop.md",
    "section": "Repeated identical failures — quarantine, then abort"
  },
  "acceptance": "a gate-stage hold is dropped once the tip has moved past the tick that placed it, while a provision-stage hold stays for the run; the consecutive-identical-failure abort is unchanged and remains the backstop either way",
  "tests": [
    "a gate-stage quarantine is lifted once the tip moves past the tick that placed it",
    "a provision-stage quarantine survives a tip that moved"
  ],
  "notes": "Field report, 0.19: four reverts from a stale gate, then holds that took a restart to lift. Verified this tick: `quarantinedSlugs` (`src/loopSupervisor.ts:434`) is a bare key set, and the stage the failure came from is on the fold at `:669` and dropped at `:684`. No lift verb — the ruling names the quarantine the run's memory and a stop-and-relaunch its operator door."
}
