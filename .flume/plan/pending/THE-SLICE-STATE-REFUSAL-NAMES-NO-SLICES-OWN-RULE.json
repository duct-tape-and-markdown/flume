{
  "tag": "THE-SLICE-STATE-REFUSAL-NAMES-NO-SLICES-OWN-RULE",
  "gate": {
    "kind": "open"
  },
  "files": {
    "edit": [
      {
        "path": "harness/gates.ts",
        "description": "the slice-state refusal's headline states the check this gate makes itself and points at the clauses the table already produced, naming no slice's rule"
      },
      {
        "path": "tests/harnessGates.test.ts",
        "description": "the headline read over a lane-stamp drop, beside that case's existing detail assertions"
      }
    ]
  },
  "summary": "The slice-state gate's refusal headline rosters two slices' rules, so an inbox lane-stamp drop is reported as a cursor or a covered set — the roster a959c048 shrank out of the doc beside it",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "Derived state is computed, never restated beside its source"
  },
  "acceptance": "the slice-state gate's refusal headline names only what this gate judges itself and how many clauses it is handing back, with every rule's own wording coming off `SLICE_STATE_RULES` through the detail lines, so a slice's rule joining the table changes no prose in this gate",
  "tests": [
    "the slice-state gate's refusal over a dropped lane stamp names no slice's own rule in its headline"
  ],
  "notes": "Verified this tick: `harness/gates.ts:633` is the one refusal, reading \"a cursor stepped past commits nobody derived or swept, or coverage dropped from a rotation still open\" — derive's and the sweep's rules, over a case the suite drives as the inbox's (`tests/harnessGates.test.ts:1118`, which asserts `details` and never `message`). Every clause is already the table's own (`SLICE_STATE_RULES`, `harness/planState.ts`). Seams the test file with THE-WHOLE-ARTIFACT-NEGATIVES-READ-THEIR-OWN-BLOCKS."
}
