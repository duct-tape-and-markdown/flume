{
  "tag": "THE-PROVIDER-USAGE-TOKENS-HAVE-ONE-DECODE",
  "gate": { "kind": "open" },
  "files": {
    "edit": [
      {
        "path": "src/Agent.ts",
        "description": "the usage object's token decode is one reader beside `AgentUsage`; `extractResultUsage` and `formatResult` apply their own absent/zero policy over it"
      },
      {
        "path": "src/budgetLine.ts",
        "description": "the context sum reads that decode's typed fields instead of respelling the provider's names"
      }
    ]
  },
  "summary": "The provider's usage-token field names are spelled three ways in two modules, outside the `AgentUsage` vocabulary that owns them",
  "per": {
    "path": ".claude/rules/engineering.md",
    "section": "A module is one job"
  },
  "acceptance": "the provider's usage-token field names are spelled once, in the module declaring `AgentUsage`, and all three readers take their numbers from that one decode",
  "notes": "Verified this tick. `src/Agent.ts:768`-`:774` decodes the four names into `AgentUsage`; `:783`-`:784` re-reads two raw through `num`; `src/budgetLine.ts:102`-`:106` spells three as a literal tuple and `:208` sums them. `AgentUsage` is the declared home — exported (`src/index.ts:7`) and mechanically walked for its summable keys (`src/tickVerdict.ts:447`). Behavior-free: each reader keeps its own absent-vs-zero policy. Claims no property; tsc and the two suites hold it."
}
