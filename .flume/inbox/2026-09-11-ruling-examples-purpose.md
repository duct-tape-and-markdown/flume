# Ruling: examples/ carries the recommended shape, by promotion into cascade (human)

Closes *The shipped examples/ trail the reference chain's shape*. Option C,
in full over time: `docs/CHAIN-AUTHORING.md` names `.flume/chain.ts` as the
living reference and says the examples are its shipped floor; and
`examples/cascade-chain.ts` gains, one entry each in this order, the shapes
a new adopter most needs — a `shouldRun` computed from disk, a `tests[]`
line judged by a reporter-fed gate, `GateContext.entry` / `baseSha` in a
gate, then plan split into slices with a dependency-ordered handoff. Each
held green by `tests/examples.integration.test.ts`. No fourth example file.
