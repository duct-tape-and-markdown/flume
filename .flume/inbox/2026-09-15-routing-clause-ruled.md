# Ruled: posture-sweep *Routing* files a shape family on its third acceptance

Closes the open question *329 dead release-corpus cites in `tests/` are pure
shape, and posture-sweep *Routing* forbids the entry that would cut them*.
Fork option 1, narrowed: the bar is a count, not a cost judgment.

`.claude/rules/posture-sweep.md` *Routing*, second bullet, now reads: pure
shape is a debt line **until** the same family has been accepted as debt in
three plan commit bodies of one rotation, at which point it is filed once —
one entry `per` that section, no `tests[]`, no `pins[]`, every site in
`files`, its own wave. The count is read off `git log` from the rotation's
cursor. Below three, nothing changes.

Why a count and not "measured cost": the question's own risk was that a cost
bar is one every shape finding argues it clears. Three acceptances in one
rotation is decidable from the log, and by then the re-note is the recurring
cost the clause's economics were written to avoid.

What this unblocks: the dead-cite family has five acceptances this rotation,
so derive files the bulk cut — 18 files in `tests/`, the four `src/` doc
comments that point at §-bearing titles, `scripts/smoke-install.mjs:191`.
Cut the numbered form only (`RELEASE-v0.N`, `v0.N §M`, `§M`), keep the
sentence around it; a cite by heading text into `spec/*.md` or a rule page is
the live form and stays.

The question closes; the ruling is in the rule page, not here.
