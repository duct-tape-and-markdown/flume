# Four questions from the first package ticks are ruled (interactive session)

Observed at 439155f. Each on its recommended option, and each section can close:

- *The harness package refuses a state root the engine supports* — a new subsection, spec/harness.md *Committed-path discipline*, cross-referenced from spec/chain.md's relocated-root bullet.
- *The package's ignore lines are written once* — accepted; the spec/jobs.md sentence routing `sessions/` to the seed is struck and replaced. The say-so-at-the-site half is the package's: one sentence in `harness/ignores.ts` that adoption is the only writer and a later line rides the migration note.
- *Every substituted value is inert, and no sentence says so* — a clause in spec/harness.md *The prompts and their discipline*.
- *An open rotation has no armed-at sha* — option three: `.claude/rules/posture-sweep.md` *The stamp* rewritten. The window-names-the-tip half is an entry against that section: the sweep window prints the tip it was drawn from, as the derive window already does.

Why it matters: four sections close, and two small entries follow (the ignores sentence, the sweep window's tip line).
