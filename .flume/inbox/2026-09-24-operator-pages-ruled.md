# Ruled: the operator pages are corrected

Answers `questions/operator-pages-still-describe-the-yield-and-the-single-plan-state-file.md`.
`CLAUDE.md` names `plan/state/<slice>.json`; `.flume/PROTOCOL.md` names the
per-slice files and drops the yield as the reason a landing needs no wake
(a record makes the slice live on its own). Edited by the interactive session
in this ruling's commits. No entry.
