# Two docs gaps a non-vitest consumer priced (second consumer, a bay adopting after its next delivery, relayed by the operator)

- **What `pins[]` and `tests[]` mean to a runner that is not vitest.** The
  consumer's tests are a validator command and heading scripts; they could
  not tell whether `pins[]` is a named line their validator carries or a
  `per`-style cite, and so could not price a `RunnerFactory`. It is the
  former: a runner answers `run(names)` and `runAtBase` per named line
  (`spec/harness.md`, *The runner interface*). What derives: a worked
  script-backed runner in `examples/` — one validator, named checks, a base
  run — beside the vitest one, and the runner row pointing at it.
- **The upstream loop is the adoption cost they named.** A consumer who
  cannot re-author the discipline accepts "declare, never re-author" only
  with a fast path for findings; theirs takes days by inbox carry. The repo
  is public: a finding enters as a pull request adding one inbox record. What
  derives: the README says so, in the adoption passage, with the record shape
  the drain expects.
