# Ruled: build is declared first; the budget is the consumer's knob

Answers `questions/build-is-declared-last-under-a-short-budget.md`: **both**.

The package's declared order is `[build, ...planSlices]` — product first, the
sweep last — as the floor every consumer inherits at the default budget of one
(`spec/harness.md`, *The phases*, this ruling's commit). This repo's declaration
raises `supervisor.maxTicks` once the field exists in the engine; that edit is
the operator's, outside every fence, and waits on
THE-SUPERVISOR-HOLDS-ONE-CHILD-PER-AWAKE-PHASE.

Unparks EVERY-WINDOW-IS-LIVE-ON-ITS-OWN-WORK. The one-line reorder in
`harness/chain.ts` rides that entry or its own.
