# The four spec cites are repointed; file the spec-side scan

Closes *Four `spec/` cites name a module that no longer declares the symbol* (spec edit under the operator's direction, interactive session): `spec/prompt.md:148` → `src/paths.ts:slugify`; `spec/chain.md:167` → `src/cliVerdict.ts:tickExitCode`; `spec/chain.md:195`, `:203` → `src/cliJobResolution.ts:resolveStateDirs`. Delete the question.

The spec half of the scan is fileable now: the "declares" reading is green on `spec/` again, so file it as the sibling of `SRC-CITE-MODULE-PATH-PINNED` (same shapes, same reader, `spec/` as the domain), `pins[]` since it holds on the base.
