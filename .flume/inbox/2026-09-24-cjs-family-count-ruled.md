# Ruled: the spec pages stop counting the CJS-context family

Answers `questions/the-cjs-refusal-family-count-in-the-spec-pages.md`: (c),
plus the cli page's spelling clause. Both pages now describe the family by
what it is — loader-failure signatures, each empirical, enumerated where
the refusal is decided — instead of by a count that a fourth arm made
false, and `spec/cli.md` says either spelling (this ruling's commit). No
entry.
