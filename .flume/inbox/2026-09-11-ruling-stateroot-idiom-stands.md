# Ruling: the stateRootRel idiom stands; no queue field on GateContext (human)

Closes *stateRootRel is spec'd as the queue-read idiom*. The spec's
generic idiom is the answer and the second read is the price of genericity;
a queue-specific field would be one artifact's convention on the engine's
surface. No spec change, no entry. The chain's read stays as it is, cited at
the site as the sanctioned idiom rather than restatement.
