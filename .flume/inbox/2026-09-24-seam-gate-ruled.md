# Ruled: the sibling-seam directive stays prose

Answers `questions/the-seam-directive-as-a-gate-over-sibling-entries.md`:
option 1. The false positive is standing in the queue today, the repair a
gate would force degrades the prediction the partition consumes, and option
4 needs a fact the queue does not carry to key on a proxy the question
itself doubts. A gate that needs a new field and an arity rule to avoid
refusing correct queues is the complicated solution. The directive now sits
where derive reads it; a sibling collision recurring after it is the
evidence that reopens this. No entry.
