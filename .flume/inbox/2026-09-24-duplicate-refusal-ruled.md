# Ruled: the duplicate refusal is dropped; the drain folds a duplicate

Answers `questions/duplicate-refusal-cannot-see-a-decomposition.md`: the
gate goes. An ancestry predicate inside an `afterMerge` gate is the
complicated solution, and the question's own reading shows why — every
decomposition matches the shape. The cheap path already exists: the inbox
drain amends an entry that covers a finding rather than filing a sibling, and
a duplicate that reaches build costs one clean-exit record the next drain
drops. `spec/harness.md`, *The gates the discipline needs* and *The phases*
now say so (this ruling's commit). No entry to file.
