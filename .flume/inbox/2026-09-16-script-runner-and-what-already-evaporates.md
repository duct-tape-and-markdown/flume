# A script runner beside vitest's, and four surfaces the package already owns (a consumer bay's full chain audit, 947 lines measured, relayed by the operator)

Ruled at `spec/harness.md` *The runner interface*: the package ships a
script runner factory — a declared command run once per operation with the
named lines as arguments, one verdict line per name on stdout. The consumer's
53-line `scriptGate` is what every non-vitest consumer writes; the adoption
table's runner row was pricing a factory nobody should author.

Already the package's, verified on this tree, so the audit's dispositions
are docs: `perPathsExist` (56 lines) is the package's `per` gate at a ref;
the `withTerminalRenderer(withSessionCapture(claudeCode()))` stack is what
the factory builds from `agents`; the schema-plus-hint entry contract is
the entry extension the package renders consumer fields through; the
serialized restore is `setup.serialize`; telemetry, structural liveness and
the zombie brake are today's rulings. What derives: the migration note's § 6
mapping names each of these by the consumer surface it retires, so the next
audit does not have to measure them.
