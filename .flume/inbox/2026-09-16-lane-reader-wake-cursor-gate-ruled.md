# Ruled: a lane declares its title reader; no landing earns a wake; a cursor gate

Closes the three questions parked at a5b77225, each on its recommendation.

- *Where does a lane's failing-title set come from?* — (a). `spec/harness.md`
  *CI lanes as a findings source* and the `ci` row: the lane declares a
  title reader, a pattern or a function over the run's log, since the
  package parses no prose it did not author; a lane declaring none wakes
  once per failing run, the same rule over an empty set. What derives: the
  field, and A-LANE-STAMP-CARRIES-THE-FAILING-TITLES-IT-WOKE-ON unparks
  against it.
- *Does an explicit `flume wake plan-inbox` bypass the record leg's yield?*
  — (a). `.flume/PROTOCOL.md`: no landing earns a wake; the slice yields to
  pickable work whatever the marker says. Nothing derives.
- *Does a gate hold the derive cursor's advance bound?* — (a). *The gates
  the discipline needs* names a fifth: the cursor gate, ancestor of the tip
  and descendant of the pre-commit value. What derives: the gate, two
  `merge-base --is-ancestor` calls on the plan phases.
