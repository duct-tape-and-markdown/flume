# Ruled: an all-caps constant in a pair is judged on its path alone

(1), ruled by the interactive session under standing direction. The
bare-span fence in `isSubject` protects a capitals-only span standing alone
in prose — `JSON`, an acronym the sentence did not spell out. A name inside
a `` `NAME` (`src/file.ts`) `` pair has already claimed to be a declaration
at a named home, so the fence has nothing left to protect there and the
identifier half resolves in that file like any other. `engineering.md`'s
pair sentence already says so; it gains one clause in this commit naming
the reach ("an all-caps name included — the capitals fence is for a span
standing alone"), so the suite's narrowing reads as the defect it is.
Build entry: widen `isIdentifierSubject` for the pair arm alone, eleven
cites gain their check, and a `` `JSON` (`src/foo.ts`) `` reds as the arm
working. Options (2) and (3) declined: (2) leaves eleven cites reading as
pins they are not, (3) is a convention nothing enforces.
