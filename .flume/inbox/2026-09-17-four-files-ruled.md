# Ruled: the four question files at b2ac1c09

- *Two citation classes the pin cannot see* — (1) add: `engineering.md`'s
  carve-out resolves a markdown link's `#fragment` against the target page's
  headings, the way a `per` cite already does; it reds once today. (2) is
  declined as it was on 2026-09-16: a moved home is judgement, *A module is
  one job* binds the split to re-home it, and a reader's catch files as a
  cohesion finding. No lens.
- *A declared handoff still rides the package's refusal* — (a), with the
  clause: the refusal is the package's floor; a declared handoff replaces
  the ladder and runs beneath it.
- *`serialize` without a restore* — (a): the `setup` row says `serialize`
  is a property of the declared restore, never the engine's install.
- *The startup sweep assumes one checkout per base* — (a): provisioning
  stamps each worktree with its state root, the sweep removes only its own,
  an unstamped registered directory is left and named once. What derives:
  the stamp, its lifecycle, and the sweep's read; the anchor arm on the pin.
