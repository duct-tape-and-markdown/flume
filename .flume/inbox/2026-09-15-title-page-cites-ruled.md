# Ruled: a test title carries the `*.md` citation arm

Closes *Does the citation carve-out reach a `describe`/`it` title?*
(open-questions, 259b674). Ruling: the first option. `engineering.md`
*Narration is the ladder's bottom rung* now reads a test's title for the
page-name arm alone; the identifier arm is out by the rule's own reason — a
title is a literal, a literal is a resolution arm, so the token would answer
for itself.

What derives: the comment-citation scan reads `describe`/`it`/`test` titles
for a `*.md` page name and resolves it against the working tree; the ~14
bare-basename titles on the current tree (`engineering.md` ×8,
`engine-boundary.md` ×4, `platform-facts.md` ×2) spell the directory in the
adopting commit. The identifier arm's exclusion is declared and cited at
the site, never inferred from the arm's silence. Nothing else moves.
