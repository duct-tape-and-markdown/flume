# Seven package-era questions ruled (interactive session)

Observed at 9b2ee11. Each on its recommended option; the sections can close:

- *Rosters that read exhaustive* — the seven spec sites state the property now; site 5 (the chain guide's harness-managed-state list) has its spec home in spec/jobs.md *Runtime ignores* and files as a pointer entry.
- *Expired narration in three files* — all three edited.
- *Does the declaration expose inheritUserMcp* — yes, on the `agents` row; the schema half derives.
- *A shipped predicate that throws* — the errored allowlist narrows to a `not-shipped` the chain returned; the loop supervisor's clause derives.
- *FLUME_WORKTREES_DIR outranks a declared base* — ratified as shipped.
- *runAtBase is gate-time* — stated; the checkoutAt doc comment's sentence files inside build's fence.
- *afterMerge gate order* — discipline first, judge after the consumer's declared gates at the same `when`; the factory's ordering derives.
