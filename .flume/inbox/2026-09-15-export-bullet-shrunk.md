# Amended: the export bullet shrank to a pointer at its pin

Closes the open question *`An export earns its consumer` still hand-holds the
property `tests/exportConsumers.test.ts` now pins*.

`.claude/rules/engineering.md` *An export earns its consumer* now has, in
place of its second and third bullets: one bullet stating the mechanical
verdict over the package's exports — every `src/` and `harness/` export
resolved against the `exports` map and cross-module references through the
TypeScript program, reachability by type positions only — and one bullet
keeping the wider dead-symbol case exactly as it was: a symbol-resolving
search, LSP as the instrument, the host prerequisite named, a host without
it leaving the verdict unmade. The queued-promotion bullet is gone; the
pin it queued shipped at 243f394.

Both limits the question set are honored: the pointer claims the export
verdict only, and it names the type-positions bound.

The question closes; the rule page holds the amendment.
