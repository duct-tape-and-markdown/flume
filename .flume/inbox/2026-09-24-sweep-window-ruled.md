# Ruled: the sweep's three-notes count drops its rotation window

Answers `questions/does-the-sweeps-three-notes-count-keep-its-rotation-window.md`:
(a). `.claude/rules/posture-sweep.md`, *Routing* now counts over every plan
commit body, unbounded, for the reason `spec/harness.md`, *The phases*
gives — the count self-terminates, and a window is how a family re-noted
once per rotation never files (this ruling's commit). The two bars agree
again; the sweep prompt's pointer is already queued. No entry.
