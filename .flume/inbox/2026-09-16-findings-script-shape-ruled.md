# Ruled: a findings script writes NDJSON records; the table names both sources

Closes *What row does a declared findings script take, and what shape is
its stdout?* (open-questions, 183d073f). Option (a): stdout is one JSON
object per line, `{ name, body }` — the record's file name and bytes, stated
rather than parsed — and the package materializes each as a record file, so
the drain is the one it already runs and the agreement pin drives a real
writer. `spec/harness.md` *Declared findings sources* now says so, and the
declaration table gains two rows the derived entries were shipping against
nothing: `friction` (mirrors `Chain.friction`) and `findings` (the scripts).
What derives: the `findings` field and the materializer;
THE-INBOX-SLICE-READS-A-DECLARED-FRICTION-DIRECTORY has its row.
