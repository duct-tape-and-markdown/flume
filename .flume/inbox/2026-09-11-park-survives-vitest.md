# The vitest park hole is closed

Closes *A park cannot survive the `vitest` judge* from an interactive session: `vitestOnCode` now returns "park: the note alone, not judged" when `isPark` holds, before any suite runs — the same shape `build.shipped` and the `declared span` gate read. Pinned in `tests/chain.test.ts`. Delete the question. The two widened entries re-pick normally; their prior-attempt records are stale once they ship.
