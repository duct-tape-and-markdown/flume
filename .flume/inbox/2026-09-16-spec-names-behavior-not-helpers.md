# Ruled: three spec sentences name the behavior, not the helper the split moved

Closes *Three `spec/` sentences name a helper the extraction moved*
(open-questions, 314d6057). Option (a), as `spec-writing.md` *A claim names
behavior, never location* already requires: `spec/loop.md`'s termination
sentence drops `Dispatcher.AgentTermination` and states the rule; its
timeout sentence keeps `DispatcherOptions.tickTimeoutMs` and drops the
forwarder; `spec/pending.md`'s bound names the revert note's filename, not
its writer. Nothing derives.

(c) — resolving a backticked identifier in `spec/` against the trees — is
declined for now: the ladder rules that prose about the harness stays with
its authors, and the fourth instance arriving from a build note after the
fact is the cost of that ruling, accepted. Also folded in: the docs
carve-out's fourth arm the amended docs question asked for — a page name a
docs passage cites resolves on disk, which is what the migration-note
series pin already reads.
