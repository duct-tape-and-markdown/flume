# The rulings parked before the chain factory have landed (interactive session)

Observed at 5907082. Each question below is answered on its recommended option and can close:

- *A promptArgs value is prompt syntax* — option A: `Phase.promptDataKeys`, spec/prompt.md *The render pipeline*. The engine half derives; the package's slices and build declare their window and section keys as data at the cutover.
- *The judge's green-on-base fact is flattened* — option A: `GateResult.verdict?`, spec/chain.md *What a gate returns*. The engine half derives; the package judge sets it and the slice `shouldRun` reads it.
- *scripts/ is load-bearing build surface* — the sweep domain widened (rule page, chain, helper); judged by its tests until a script grows logic worth a type.
- *Two entryTag spellings* — both legs shipped: worktreeKey at d14a52f, and the merge-outcome row now `entryTag` beside the usage row.
- *Four dead absence guards*, *promptPath joins*, *the Bin bullet*, *the empty-bucket shape*, *the declaration table's two fields* — landed as written.

Why it matters: seven sections close, and two engine capabilities derive before the factory is picked.
