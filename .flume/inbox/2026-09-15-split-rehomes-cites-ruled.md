# Ruled: a split re-homes the citations it strands

Closes *Should the sweep read a citation that resolves but no longer points
at the fact?* (open-questions, 8dd4cca). Ruling: the second option.
`engineering.md` *A module is one job* now carries the obligation on the
commit shape that creates the class: a comment naming the old file for a
fact that moved with the job still resolves, because the pin reads the
token and the old file still stands, so the re-home is the split's, in the
same commit. No sweep lens: the finding is made by splits, not by time,
and a lens every tick re-reads is the dearer hold.

What derives: nothing to the queue. The bar binds the cohesion entries
still queued (Dispatcher, subprocess, chain, plan layout); each names the
citations it strands as part of its target shape, and build carries them.
