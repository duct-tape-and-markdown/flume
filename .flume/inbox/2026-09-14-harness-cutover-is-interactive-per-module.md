# The chain cutover is performed interactively, one module at a time (interactive session)

Observed at 5983a49. Rules the question derive parked on 767e298 about *What this repo is*: `.flume/chain.ts` and `.flume/prompts/**` are outside every phase lane, so no entry can replace a chain-side block with its harness twin.

Ruling: the interactive session performs each cutover at a loop stop, as soon as the harness module that replaces the block has a consumer at the same level — the runner cuts over together with the judge that drives it (*The judges*), not before, so the chain is rewritten once per seam rather than twice. Build's fence does not widen to the chain: a loop rewriting its own chain mid-run is a failure class this repo does not take on. Until a cutover lands, the twin stands as declared debt in the entry notes, as derive already recorded.

Why it matters: the question can close, and each extraction entry's note should name the chain-side twin it leaves so the cutover is a mechanical read at the stop. Also closed by 5983a49: the nine-subsection promotion and `flume-harness init` as the package's own bin.
