# Ruled: the stop write is a floor beside the per-entry refusal

Answers `questions/the-stop-write-is-a-floor-or-the-default-handoffs-alone.md`
as recommended: `beneathTheFloor` stays, and `spec/harness.md`, *The default
`handoff`* names both floors a declared handoff runs beneath (this ruling's
commit). A resident supervisor absorbing a contract its children no longer
share is the same livelock whoever names the next phase. No entry.
