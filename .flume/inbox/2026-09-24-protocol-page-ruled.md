# Ruled: this repo's PROTOCOL points at the harness's note homes, never restates them

Answers `questions/no-phase-can-write-this-repos-own-protocol-page.md`.
Neither fork: the template's own header says what does not belong on this
page — anything the harness package already owns — and the note homes are
the package's. The stale line is deleted rather than corrected, and the page
points at `spec/harness.md`, *Records as one file each* for the homes (this
ruling's commit). A fact the page does not carry cannot drift; the page
stays the human's, in no fence. No entry.
