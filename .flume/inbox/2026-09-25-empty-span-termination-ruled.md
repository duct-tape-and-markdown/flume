# Ruled: termination is consulted whenever the tick produced no usable commit

Answers `questions/does-an-empty-span-consult-the-agents-termination.md`:
(1). The bullet under the taxonomy now keys on a usable commit, not the
ref, so an agent the platform killed over an empty span stays a preempt
(this ruling's commit). The shipped classifier is right. No entry.
