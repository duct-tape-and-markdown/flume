# Ruled: the three question files open at e2450574

- *The chai truncation fact lives in a doc comment, no page* — (1): the
  fact lands on `.claude/rules/platform-facts.md` as its own section, in
  this commit. The doc comment on `renderFindings` shrinks to a cite of it
  in the adopting build commit (`engineering.md`, *Narration is the
  ladder's bottom rung*: a shrink re-homes what it covered for).
- *`spec/chain.md` names one spelling of the CJS namespace query* — the
  sentence now says "in either spelling, literal or percent-encoded", in
  this commit. Nothing to queue.
- *A gate-revert blames the entry for a suite that was already red*, the
  open half — spelling **1**: `GateResult` gains `blamesSpan?: false`, a
  declared fact the engine reads to withhold the entry-scoped half of a
  stage failure (no quarantine key, no blame on the record) while the
  revert and the consecutive-failure count stand. And `suspectFlake`
  **retires**: its disjointness premise is false in general (a span's edit
  can red a pin in a test module it never touched — today's `§ N` revert
  was exactly that and would have read as a flake), and it is the inferred
  read of the fact the field declares. `spec/chain.md`, *What a gate
  returns* and `spec/loop.md`, *Prior-outcome feedback* say so in this
  commit; the engine change — the field's reader, the marker's removal,
  the harness judge declaring `blamesSpan: false` on base-red — derives
  from them.
