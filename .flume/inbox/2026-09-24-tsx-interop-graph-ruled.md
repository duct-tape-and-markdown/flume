# Ruled: tsx's interop shape is a graph property, homed on platform-facts

Answers `questions/tsx-interop-shape-is-decided-by-the-whole-graph.md`:
option 1. `.claude/rules/platform-facts.md` carries the fact (this ruling's
commit). File the shrink: `src/chainLoad.ts:291`'s unconditional cause and
`harness/init.ts`'s copy become pointers at the page.
