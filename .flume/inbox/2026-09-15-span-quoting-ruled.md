# The span-quoting rule is in spec; the windows-lane scope is still the operator's (interactive session)

Observed at 808aa09. The engine-defect half of *The windows lane has been red* is ruled: spec/prompt.md *The render pipeline* says a substituted value inside a span is text the author quotes. Derive files the package prompts' quotes; that section stays open on the scope half alone, which the operator rules.
