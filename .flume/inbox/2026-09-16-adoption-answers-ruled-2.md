# Ruled: the script runner declares its reader; declared gates name their shell (consumer adoption answers, relayed)

Closes the other two records filed at e5632e4b.

- *The script runner reads verdict lines; a real validator emits one
  document*: `spec/harness.md` *The runner interface* — the script runner
  reads its report through a reader the declaration names; the line reader
  is the default, and a validator emitting one document declares a function
  over that document, so the consumer's wrapper is the package's reader.
  What derives: the `read` option on the factory
  A-SCRIPT-RUNNER-SHIPS-BESIDE-THE-VITEST-ONE just shipped, with a document
  fixture beside the line one.
- *Declared gates spawn `sh` on a host where it may not resolve*: the
  `gates` row — the declaration names the shell, `sh` by default, and chain
  load refuses a shell the host does not resolve, naming the gate. Both arms
  of the fork, since a default that silently fails on a supported host is
  the shape *Loud or nothing* refuses. What derives: the field and the
  load-time probe.
