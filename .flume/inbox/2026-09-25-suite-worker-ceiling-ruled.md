# Ruled: the suite keeps four workers; the warrant states its condition

Answers `questions/is-four-still-the-suites-worker-ceiling-under-the-ship-lock.md`.
(a). The host is shared with another project's loop, so the headroom the
question counts is not free, and nothing shows the suite's three minutes per
merge hurting. Priority 20.

One entry: `vitest.config.ts`'s warrant drops the retired "one suite per
entry in parallel" clause and states the live figures (one suite at a time
under the ship lock, about 3.2 GB) and the condition that would change the
number: the host's other load gone and suite wall clock the bottleneck. The
declaration's half is done in this ruling's commit, since no phase may write
that file.
