# Ruled: one verdict file per phase, `tick-verdict/<phase>.json`

Answers `questions/the-tick-verdict-is-one-path-for-every-supervisor-child.md`:
the first fork. A phase never runs twice at once and the supervisor names each
child's phase, so the reader opens the file it named the child by — no new
contract, no inference. `spec/loop.md`, *The tick verdict — one facts
artifact* now states it (this ruling's commit). Contract-touching: the entry
carries the mark so the run restarts on it. This is the gate on raising this
repo's `maxTicks`; the operator raises it once the entry ships.
