# Ruled: the sweep's own page stays off the posture list; config stays with the inbox

Answers `questions/what-the-sweeps-declaration-must-name.md`.
Fork 1, (b): `.claude/rules/posture-sweep.md` now says it is procedure and
deliberately off the declared list, so a lens edit reaches every later
neighborhood and reopens none (this ruling's commit). A full rotation per
wording change is the cost the operator asked to keep off the loop.
Fork 2, (c): root config and the declaration stay outside the domain and
arrive through the inbox; the same paragraph says so. The declaration's
restated memory figure is now a pointer to the docs table (this commit).
The declaration's narration is the operator's alone, by design: no phase
writes it, and a finding there is a record, never an entry.
