# Ruled: the slice-state sentence names the kind and points at the table

Answers `questions/does-the-slice-state-sentence-name-the-rules-or-point-at-them.md`:
fork 2. `spec/harness.md`, *The gates the discipline needs* keeps one
clause for what the gate refuses — a slice's file moving as its own
invariants forbid — names the cursor check every declared cursor takes,
and points at the table for the set; the roster is gone (this ruling's
commit). No entry.
