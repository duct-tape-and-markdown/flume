# Docs and doc comments still describe inbox.md and the open-questions park (human)

Records became one file each (`.flume/PROTOCOL.md`, *Records: one file
each*, 2026-09-11): `.flume/inbox/<date>-<slug>.md` and
`.flume/plan/notes/<TAG>.md`; `inbox.md` is gone and build no longer writes
`open-questions.md`. Still asserting the old shape: `README.md:150-152`,
`docs/CHAIN-AUTHORING.md:66-69`, `src/Phase.ts:298` (doc comment on
`entryChannelPaths`), `src/Dispatcher.ts:4234`. `examples/cascade-chain.ts`
keeps its own park file — that is its convention, not a defect. Retired-
claim delta; the protocol is the source.
