# Ruled: render joins the quarantine accounting as a fourth stage

Answers `questions/what-bounds-an-entry-whose-render-refuses-identically-every-wave.md`.
(c). The premise sentence of `spec/loop.md`, *Repeated identical failures*
stands and the enumeration lifts to meet it: a **render** bullet now sits
beside provision, merge, and gate. A render hold lifts on a changed tip like
gate and merge, since a chain's hook fixed on trunk is a new render; a host
missing a binary retries early and the backstop bounds it. The premise's
"full agent price" now says what a render lap costs instead.

`spec/chain.md` names the verdict's fourth class, `renderFailures`, beside
the other three, so the supervisor folds a fact the verdict records rather
than one it infers. Contract-touching as the question says: the verdict
field, then the stage roster the supervisor and a child share.
