# Ruled: thresholds are named per fact; this repo's dial is the clock

Answers `questions/which-fact-does-the-put-down-threshold-bind-to.md`: (3).
`spec/harness.md`, *A tick puts work down* now says the prompt names a
threshold for each fact the line can carry and an agent reads the ones its
line printed (this ruling's commit). This repo declared its window at
a1a2 — `contextWindow: 1_000_000` on every phase — so the percentage arm is
live and inert by measurement; the elapsed arm is the one that binds here.
The prompt names both: 70% and 80% of the window stand; on the clock, land
what is green at 45 minutes and stop at 60 — the median build tick today is
under 20 and the one lost span was 76. The model's window lives in the
declaration alone; no platform-facts section. File the prompt change.
