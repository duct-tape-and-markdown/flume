# Ruling: docs/INTENT.md — restate (a), retire (b), delete (c) (human)

Closes *docs/INTENT.md contradicts the code in two places*. (a) The
provenance bullet restates to the 0.8+ truth: `per` is a chain-declared
extension the engine never parses; citation discipline is the chain's gate
(`per cites resolve`). (b) The v0 success criterion is retired outright —
its comparison target no longer exists in any tree and a restated target
would be a new claim nobody has measured. (c) The executed "decided, not yet
executed" item is deleted. Docs lane; one entry.
