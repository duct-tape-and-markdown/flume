# Ruled: a page path is never a pair; the page-name arm reads every sweep tree

Closes both questions parked at 715b35a, each on its recommended option.

*A cited home that names a `*.md` page can never resolve*: option 1.
`engineering.md` *Narration is the ladder's bottom rung* now says a pair
whose path is a `*.md` page is no pair — no declaration lives in a page, so
the identifier resolves repo-wide and the page against disk. A comma or a
tight paren no longer decides a verdict.

*A `*.md` cite in `scripts/` or `bin/` is pinned by nothing*: option 1. The
carve-out now lists trees per arm: identifier and path arms in `src/`,
`harness/`, `tests/`, where the program reaches; the page-name arm in any
tree the sweep domain names, since a filename resolves on disk.

What derives: the pair arm skips a `*.md` path; the page-name scan widens to
`bin/`, `examples/`, `scripts/` (and `.flume/chain.ts`, which the sweep's
narration lens already reads), so the two `platform-facts.md` cites in
`scripts/build-changelog.mjs` are held.
