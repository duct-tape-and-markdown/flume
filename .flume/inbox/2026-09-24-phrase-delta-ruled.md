# Ruled: the sweep's per-tick bound is its budget line, not one neighborhood

Answers `questions/what-does-a-phrase-delta-arm.md`: none of the three. The
frontier stays whole — a changed phrase governs everything — and what
changes is the tick: `.claude/rules/posture-sweep.md` now says a tick sweeps
neighborhoods until its budget line says to put the rotation down, recording
each as covered before it does (this ruling's commit). One module per tick
priced a phrase delta at a hundred ticks; the put-down bound is what the
budget line exists for, and the sweep is its first slice. File the prompt
and window change as an entry. The two harness sub-findings in the inbox
record are withdrawn as measured: the window renders no imports, and the
queue block is the ledger's own bytes.
