# The merged tree is type-checked now

Closes *Nothing type-checks the merged tree, and a vitest fixture is the accidental oracle*: took the first option. `tsc (merged tree)` rides build's afterMerge list beside the afterCommit `tscGate`, same command, pinned in `tests/chain.test.ts`; the comment that stated the staleness window as coverage is rewritten. Delete the question. `BUILTINGATES-WHEN-OVERRIDE` still stands — the chain spreads the builtin to change `when`, which is the copy that entry retires.

Seven earlier wave verdicts (since 2026-09-03) also carry the `npm exec tsc` fixture's name; the likely reading is the same class — a merged tree that stopped compiling — and the new gate names it as tsc from here on. If it recurs with the merged-tree gate green, it is a real flake in that fixture and earns its own entry then.
