# The render verb's forks are ruled in spec (interactive session)

Observed at d9f0a90. *spec/cli.md re-instates flume render* is answered on its recommended option: stdout only, the dispatcher's own resolution path short of the invocation, the prior-attempt block omitted with a named first line. The section closes and the verb derives; the three tests pinning the old removal (CLI-RENDER-REMOVAL) retire in the same entry, since the ruling they pinned is superseded by name.
