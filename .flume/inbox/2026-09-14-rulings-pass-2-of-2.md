# Rulings pass, two of two: docs copies and docs drift, plus what stays with the operator (interactive session)

Observed at c96e071. Two close by ruling and file as entries:

- *The runtime ignore list has three unpinned copies* — shrink `docs/CHAIN-AUTHORING.md`'s copy to a pointer at spec/jobs.md *Runtime ignores*.
- *Nothing arms on a docs claim that drifted out of src* — pin the exit codes `docs/CLI.md` states against a real run, the shape `tests/cliHelp.test.ts` already holds; observed exit codes only, never prose.

Ruled in spec, rule pages and the intent doc this same pass, each on its recommended option: throwing hooks, the fast-lane carve-out, the path alphabet, the git floor and its two measured facts, the install fixtures, the runner factory's provisioning, `status` refusing on an unreadable file, lanes informing not refusing, a checkout-at-sha on the API, a chain-computed worktree base, strict MCP by default, a `render` verb, a title asserted by its body, the lenses' arming condition.

Left for the operator, deliberately: the tagged CI publish.
