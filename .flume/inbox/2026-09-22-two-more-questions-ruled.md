# Ruled: the two question files open at 3511b28b

Ruled by the interactive session under standing direction; both are
rules-page edits, landed in this commit.

- *A vitest report claiming success over a non-zero exit* — (1): the fact
  is a section on `.claude/rules/platform-facts.md`, stating the measured
  behavior and the condition that retires it (a vitest that folds unhandled
  errors into `success`). The fixture comment on `UNHANDLED_TEST` keeps its
  own vacuity rationale and cites the page for the vitest behavior — that
  pointer is a build entry (`engineering.md`, *Narration is the ladder's
  bottom rung*).
- *The blockquote title arm outruns the rule page's enumeration* — (1): the
  section arm in `engineering.md` now reads "headings, bolded bullet leads,
  and a bolded lead opening a blockquote line". One-word leads are admitted
  as they stand; a floor is a rule with no instance to govern yet, and is
  filed the first time a cite wants one. The case at
  `tests/commentCitations.test.ts:901` titled with the old enumeration wants
  its title widened beside it — a build entry.
