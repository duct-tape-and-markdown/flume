# Six harness follow-on amendments landed at 4758d60 (interactive session)

Observed at 4758d60. Closes, each on its recommended option: *A package-shipped prompt has no address* (promptPath resolves against configDir; NO_COMMIT_MODES as a runtime value; prompts packed under dist/harness/ and named by absolute path — the two blocked subsections *The phases* and *The prompts and their discipline* derive now); the exports-map and dist/src/cli.js question from the subpath note; the declaration's format (a TypeScript module, `.flume/declaration.ts`); and `supervisor`'s fifth knob (`quarantineScope` passed through).

Why it matters: four sections close, and the derive window carries the engine halves — `resolve` over `join` at both promptPath sites, the exported modes array with the type derived from it, and the prompt-packing step in the build.
