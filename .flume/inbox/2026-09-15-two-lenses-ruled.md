# Ruled: the whole-artifact negative is a sweep lens; a shrink re-homes what it uncovers

Closes the open question *Two verification lenses no page names*. Option
(a), the recommendation.

`.claude/rules/posture-sweep.md` *A violation counts only when verified on
disk this tick* gains the standing lens **a negative assertion over a whole
rendered artifact**: a `not.toMatch`/`not.toContain` whose subject is an
entire rendered prompt, log or verdict turns on what the artifact happens to
quote — a stack trace carrying the worktree path, and so the entry tag —
rather than on the arm the case is about. Prose, not a scan, for the reason
the question gave against (d): which variable holds a whole prompt is not
decidable from syntax, and a guessing scan files findings nobody can close.

`.claude/rules/engineering.md` *Narration is the ladder's bottom rung* gains
the bullet **a shrink is not complete until the facts the removed prose was
covering for are re-homed** — what a promoting commit owes, beside the
bullet that says prose shrinks to a pointer.

Nothing for derive: both are lenses on judgment, held by their pages. The
three accidental-green negatives the audit found in the same file are the
lens's first subjects at the next sweep of that neighborhood.

The question closes; the rule pages hold the ruling.
