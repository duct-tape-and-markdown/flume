# Ruled: merge holds lift like gate holds, and "moved" reads as "differs"

Answers `questions/what-lifetime-does-a-quarantine-hold-have.md`. Fork 1:
merge beside gate — a pick is a verdict over one tree, and the tree is
gone. Fork 2: (a) — the supervisor reads no ref, the newest tip a verdict
reported differing from the placing tick's is the order it has, and early
is the safe direction with the backstop bounding it. `spec/loop.md`,
*Repeated identical failures* says both (this ruling's commit). The merge
arm files as an entry; fork 2 needs no code.
