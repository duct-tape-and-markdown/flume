# Ruled: identifiers resolve on the interface pages, never on a migration guide

Answers `questions/the-citation-pin-does-not-reach-docs-identifiers.md`:
the named-pages fork. `.claude/rules/engineering.md`, *Narration is the
ladder's bottom rung* now admits a backticked identifier on the pages that
state what a shipped interface does — the authoring page, the CLI page, the
README — resolved against the package's exports; a migration guide or a
survey names retired surface on purpose and is out by construction (this
ruling's commit). Per the rule's own sentence, the class is now a plan
entry: file it.
