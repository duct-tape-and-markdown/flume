# The exports-map count is struck from spec/cli.md (interactive session)

Observed at f3a0090. *spec/cli.md still calls the exports map single-entry* is closed by amendment: the ESM-only bullet now points at the enumerated map in spec/chain.md instead of counting it.
