# Ruled: six parked forks, each on its recommended option

Closes every question parked at b0be5dc9, in one landing.

- *Three plan wakes that run and file nothing* — all three arms (a), in
  `spec/harness.md`: *The phases* (notes wait while work is pickable; a park
  wakes), *CI lanes as a findings source* (wake on a changed title set; the
  stamp carries titles), *Plan state as declared state* (the drain advances
  the derive cursor through what it routed).
- *…friction listing count a dotfile?* — (a), `spec/chain.md`
  *`Chain.friction`*: a dotfile is not a note, by name alone.
- *…friction directory a findings source?* — (a), new `spec/harness.md`
  section *Declared findings sources*.
- *Whose job is "don't re-dispatch…"?* — (a), *The default `handoff`*; a
  pickable set that cannot carry a per-entry refusal is the engine's gap.
- *…git version below the floor?* — (a) warn, `spec/chain.md` floor bullet.
- *A run's spend…totalled nowhere* — (a): the completion summary
  (`spec/loop.md` *Exit codes*) and `flume status` item 7. `flume log` is a
  verb `spec/cli.md` never specifies; that gap is its own finding.
