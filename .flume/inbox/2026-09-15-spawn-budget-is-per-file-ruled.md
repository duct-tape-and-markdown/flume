# Ruled: the spawn budget is declared per file, and `git` is a spawn

Closes *The spawn budget's vocabulary stops at `node`, and the lane now
holds three postures* (open-questions, 1fbdfe9). Ruling: option (b).
`spec/worktrees.md` *The default test lane must stay fast* now has a
spawning file declare the lane's one budget at file scope, and names
`git` as a spawn that declares it the same way — the POSIX measurement
that excused it is gone, contradicted by the windows lane. Git stays no
lane trigger; the lane boundary is unchanged.

What derives: the scan's vocabulary widens to `git`, and it judges files —
a file that spawns node or git carries one file-scope declaration of
`SPAWN_BUDGET_MS`. A per-case declaration of the same number beside it is
redundant, the sweep's to remove, never the entry's. The 209 restated
`20_000` literals are the same redundancy once their files declare, and
stay debt until then. The queued git-timeout entry is unchanged and ships
under this ruling as filed.
