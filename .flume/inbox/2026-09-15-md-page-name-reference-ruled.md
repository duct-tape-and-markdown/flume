# Ruled: a `*.md` page name in code prose is a reference, backticked or not

Closes the open question *38 unbackticked `*.md` page names in
`src/`/`harness/` comments sit outside the citation carve-out*. Fork option
1, the recommendation.

`.claude/rules/engineering.md` *Narration is the ladder's bottom rung* now
names the reference class in full: a backticked identifier, a backticked
repo-relative path, or a `*.md` page name with or without backticks. A
filename is never a sentence, so admitting it reads the token alone — the
same footing as the identifier and the path — and the paragraph's line
against reading prose for what it says is untouched. The pin may resolve
any of them against the declarations and the working tree, so a deleted
symbol or a renamed page cannot leave its citations standing.

What derive files: the scan's subject rule admits an unbackticked `*.md`
token, and the 38 sites are spelled as repo-relative paths so the path arm
resolves them — the same repair the four backticked basenames took at
a2c654f. No new mechanism, no second home.

The question closes; the rule page holds the ruling.
