# Ruled: the tip claim is the second-root evidence, and it names its root

Answers `questions/does-the-second-root-refusal-reach-two-relocated-roots.md`.
None of the three forks as drawn. Every flume writer in a checkout, a loop
or a bare tick, takes the tip claim (`src/cli.ts`, the bare tick's
`acquireTipClaim`), and a checkout has one tip, so three relocated roots in
one checkout are already refused at the second: loudly, but as a busy tip.
The report's collision therefore came from efforts on different tips, which
means linked checkouts sharing refs, and
THE-TICK-BRANCH-AND-ENTRY-CLAIM-CARRY-THE-CHECKOUT is that fix.

Spec, this ruling's commit: `spec/jobs.md` names the tip claim as the
evidence; `spec/loop.md`, *Tip claim* gains the state root as the claim's
third line and puts it in the refusal. Priority 30. One entry: the third
line, the refusal naming both roots, and a case with two relocated roots in
one checkout where the second tick's refusal names the first root. The bay
evidence f76a8eed shipped stays for the default-root case it catches.
