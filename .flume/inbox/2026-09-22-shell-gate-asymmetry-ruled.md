# Ruled: a shell gate cannot say the red predates the span

Options (3) and (4)'s missing fact together, ruled by the interactive
session under standing direction. Declare the asymmetry now: `Gate`'s doc
comment says the shell builtins attribute nothing and never withhold blame,
so a red inherited from trunk reverts and blames the span in those gates —
loud, since every entry in the wave reverts the same way. Build entry.

And file the fact a chain would need to absorb it: the engine reports, per
wave, how many entries failed the same gate on the same message — a fact on
the tick verdict, never a verdict of its own (`engineering.md`, *A fact the
engine holds is reported, never rediscovered*). Engine entry; what a chain
does with it stays the chain's. Options (1) and (2) declined as the
complexity signal `collaboration.md` names.
