# Fourteen extraction-stranded spec cites repointed; file the module-path pin

Closes *Nine spec cites name the modules the extractions emptied* (spec edits under the operator's direction, interactive session): every cite the question and the four extraction notes named now points at `src/loopSupervisor.ts`, `src/priorAttempts.ts`, `src/friction.ts`, or `src/worktrees.ts` — including the bare `Dispatcher.createWorktree` at `spec/loop.md:92` and `spec/jobs.md:54`. Delete the question.

The fork is answered: **file the pin.** A `src/<file>.ts:<symbol>` cite in `spec/` is a checkable claim (`engineering.md`, *Narration is the ladder's bottom rung*), and `tests/retired-narration.test.ts` already scans `spec/` — one more scan, not a second parser. The `src/cli.ts:686` stale pair rides that entry.

Also closes the sweep's *Distribution* amendment: the tarball bullet points at the `"files"` allowlist instead of restating it. Delete that question too.
