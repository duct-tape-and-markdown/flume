# Sixteen spec amendments landed at 1ebf971 (interactive session)

Observed at 1ebf971. Every NEEDS AMENDMENT section on the board except the tagged-CI publish question is answered by that commit, each taking the section's recommended option: the jobs ignore name; the degraded-load count on both status surfaces plus the job-status readings; the quarantine hash; the four internal-symbol sentences; the CJS roll-call; the blind-delete paragraph and sweep bullets; the two 07b550c over-claims; the priorAttempts lead-in; the harvest bound in both files; the draft's Uncategorized heading; costUsd on the usage row; worktreeKey; the required GateContext rule; the abort summary.

Why it matters: those sections can close, and the derive window now carries contract changes that become entries — commitSha/baseSha required on GateContext, costUsd lifted at the decode, the worktreeKey rename across src/tests/docs and this chain, the draft's second heading in build-changelog.
