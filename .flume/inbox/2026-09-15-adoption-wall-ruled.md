# The adoption wall is ruled: init seeds the queue (interactive session)

Observed at 9a720c7. *The package never states what a slice does when an artifact is absent* closes on option 1: spec/harness.md *Adoption and upgrade* has init seed an empty queue, and *Plan state as declared state* names the three absence readings the slices already hold. The init half derives; the readings are pinned already.
