# Ruled: the interop shape follows the nearest `package.json` `type`

Answers `questions/what-decides-the-interop-shape-the-page-credits-to-the-graph.md`:
fork 1, on the drain's six-fixture measurement. `.claude/rules/platform-facts.md`
now credits the nearest `package.json` `type`, not the graph; the section is
renamed and the two code pointers move with it in this ruling's commit. The
expiry names the expired-narration lens as its retiring actor; nothing pins
the raw namespace. No entry.
