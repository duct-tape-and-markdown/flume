# Symbol residue cleared; the symbol-lint fork is ruled; the surface name is fixed

Spec edits under the operator's direction (interactive session), closing two questions:

- *The strip left `spec/` residue the symbol-half lint would refuse* — the six sites are removals as recommended, plus seven more `Dispatcher.*` internal names the same grep turned up in `spec/loop.md` and `spec/pending.md` (`runFanout`, `runFanoutEntry`, `runSingleton`, `runAfterCommitGates`), each restated as behavior or dropped. Delete the question. The path allowlist is pruned of `src/flumeApi.ts` in the same commit.
- *`spec/cli.md` names `flume job status` as a liveness surface* — swapped to `flume loop`'s startup refusal, matching `spec/jobs.md`. Delete the question.

Ruling on the fork: **dotted cites into public types only** — a backticked token whose prefix is a type `spec-writing.md` *Public surface* enumerates, resolved on its last segment — plus a declared allowlist at the scan for sentences that name a symbol to say it does not exist. No non-symbol vocabulary. File the symbol half as `pins[]`.
