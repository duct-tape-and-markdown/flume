# Ruled: the four spec sentences are renamed; no new identifier arm

Answers `questions/four-spec-sentences-name-retired-exports.md`.

Part 1: the four sentences now name `parsePendingQueue`,
`parsePendingQueueLoose`, and `composePendingEntry` (this ruling's commit).

Part 2, each class: `spec/` stays out — the ladder section already says
prose the package never ships is held by its authors, and a rename is the
human's cost there. `examples/` stays out — the page-name arm is what a tree
outside the program gets, and its comment rot is shape, not correctness. A
refusal string is not a fourth surface: a message says what to do, never
which function, so the fix for `AsyncEntryExtensionValidatorError` is to
drop the identifier from its text — file that as an entry. No rule edit.
