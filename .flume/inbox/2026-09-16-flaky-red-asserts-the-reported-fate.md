# Ruled: the crash-marker case asserts the fate the engine reports

Closes *A windows-lane red over a tree whose program bytes did not change*
(open-questions, 2e4a9a9). Ruling: (a) now, (b) only if it recurs. The
case's second assertion reads a `mergeOutcomes` row whose absence is a
derivative of the fate, so the red said `undefined` where the engine had
already reported `committed: false` and a `noCommit` class on
`result.entries[]`. That is the shape `engineering.md` *A fact the engine
holds is reported, never rediscovered* names, one rung over in a test: the
suite rebuilding a fact from a row's absence.

What derives: an entry, no spec edit — the case asserts every entry's
`committed` and `noCommit` off `result.entries` before it reads a merge row,
so the next red names which of the three causes happened. The lane's run is
stamped; a second red under the new assertion is the reproduction (b) needs,
and the concurrent-sibling-git suspect waits for it rather than for a guess.
